---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:40'
updated: '2026-06-08T00:38:40'
---

# agent/auxiliary_client.py

Symbols in `agent/auxiliary_client.py`.

- L72 `_load_openai_cls()` (function) — Import and cache ``openai.OpenAI``.
- L81 `_OpenAIProxy` (class) — Module-level proxy that looks like the ``openai.OpenAI`` class.
- L90 `__call__(self, *args, **kwargs)` (method)
- L93 `__instancecheck__(self, obj)` (method)
- L96 `__repr__(self)` (method)
- L110 `_safe_isinstance(obj: Any, maybe_type: Any)` (function) — Return False instead of raising when a patched symbol is not a type.
- L118 `_extract_url_query_params(url: str)` (function) — Extract query params from URL, return (clean_url, default_query dict or None).
- L165 `_normalize_aux_provider(provider: Optional[str])` (function)
- L193 `_is_kimi_model(model: Optional[str])` (function) — True for any Kimi / Moonshot model that manages temperature server-side.
- L199 `_is_arcee_trinity_thinking(model: Optional[str])` (function) — True for Arcee Trinity Large Thinking (direct or via OpenRouter).
- L217 `_is_codex_gpt55(model: Optional[str], provider: Optional[str]=None)` (function) — True for gpt-5.5 accessed through the ChatGPT Codex OAuth backend.
- L234 `_fixed_temperature_for_model(model: Optional[str], base_url: Optional[str]=None)` (function) — Return a temperature directive for models with strict contracts.
- L256 `_compression_threshold_for_model(model: Optional[str], provider: Optional[str]=None, *, allow_codex_gpt55_autoraise: bool=True)` (function) — Return a context-compression threshold override for specific models.
- L285 `_get_aux_model_for_provider(provider_id: str)` (function) — Return the cheap auxiliary model for a provider.
- L360 `_apply_user_default_headers(headers: dict | None)` (function) — Merge user-configured ``model.default_headers`` onto resolved headers.
- L389 `build_or_headers(or_config: dict | None=None)` (function) — Build OpenRouter headers, optionally including response-cache headers.
- L448 `build_nvidia_nim_headers(base_url: str | None)` (function) — Return NVIDIA NIM cloud attribution headers for build.nvidia.com traffic.
- L467 `_nous_extra_body()` (function) — Return a fresh Nous Portal ``extra_body`` dict.
- L503 `_codex_cloudflare_headers(access_token: str)` (function) — Headers required to avoid Cloudflare 403s on chatgpt.com/backend-api/codex.
- L542 `_to_openai_base_url(base_url: str)` (function) — Normalize an Anthropic-style base URL to OpenAI-compatible format.
- L572 `_select_pool_entry(provider: str)` (function) — Return (pool_exists_for_provider, selected_entry).
- L588 `_peek_pool_entry(provider: str)` (function) — Best-effort current/next pool entry without mutating selection order.
- L611 `_pool_runtime_api_key(entry: Any)` (function)
- L620 `_pool_runtime_base_url(entry: Any, fallback: str='')` (function)
- L640 `_convert_content_for_responses(content: Any)` (function) — Convert chat.completions content to Responses API format.
- L688 `_CodexCompletionsAdapter` (class) — Drop-in shim that accepts chat.completions.create() kwargs and
- L692 `__init__(self, real_client: OpenAI, model: str)` (method)
- L696 `create(self, **kwargs)` (method)
- L964 `_CodexChatShim` (class) — Wraps the adapter to provide client.chat.completions.create().
- L967 `__init__(self, adapter: _CodexCompletionsAdapter)` (method)
- L971 `CodexAuxiliaryClient` (class) — OpenAI-client-compatible wrapper that routes through Codex Responses API.
- L978 `__init__(self, real_client: OpenAI, model: str)` (method)
- L985 `close(self)` (method)
- L989 `_AsyncCodexCompletionsAdapter` (class) — Async version of the Codex Responses adapter.
- L996 `__init__(self, sync_adapter: _CodexCompletionsAdapter)` (method)
- L999 `create(self, **kwargs)` (method)
- L1004 `_AsyncCodexChatShim` (class)
- L1005 `__init__(self, adapter: _AsyncCodexCompletionsAdapter)` (method)
- L1009 `AsyncCodexAuxiliaryClient` (class) — Async-compatible wrapper matching AsyncOpenAI.chat.completions.create().
- L1012 `__init__(self, sync_wrapper: 'CodexAuxiliaryClient')` (method)
- L1028 `_AnthropicCompletionsAdapter` (class) — OpenAI-client-compatible adapter for Anthropic Messages API.
- L1031 `__init__(self, real_client: Any, model: str, is_oauth: bool=False)` (method)
- L1036 `create(self, **kwargs)` (method)
- L1119 `_AnthropicChatShim` (class)
- L1120 `__init__(self, adapter: _AnthropicCompletionsAdapter)` (method)
- L1124 `AnthropicAuxiliaryClient` (class) — OpenAI-client-compatible wrapper over a native Anthropic client.
- L1127 `__init__(self, real_client: Any, model: str, api_key: str, base_url: str, is_oauth: bool=False)` (method)
- L1134 `close(self)` (method)
- L1140 `_AsyncAnthropicCompletionsAdapter` (class)
- L1141 `__init__(self, sync_adapter: _AnthropicCompletionsAdapter)` (method)
- L1144 `create(self, **kwargs)` (method)
- L1149 `_AsyncAnthropicChatShim` (class)
- L1150 `__init__(self, adapter: _AsyncAnthropicCompletionsAdapter)` (method)
- L1154 `AsyncAnthropicAuxiliaryClient` (class)
- L1155 `__init__(self, sync_wrapper: 'AnthropicAuxiliaryClient')` (method)
- L1166 `_endpoint_speaks_anthropic_messages(base_url: str)` (function) — True if the endpoint at ``base_url`` speaks the Anthropic Messages
- L1194 `_maybe_wrap_anthropic(client_obj: Any, model: str, api_key: str, base_url: str, api_mode: Optional[str]=None)` (function) — Rewrap a plain OpenAI client in ``AnthropicAuxiliaryClient`` when
- L1276 `_read_nous_auth()` (function) — Read and validate ~/.hermes/auth.json for an active Nous provider.
- L1314 `_nous_api_key(provider: dict)` (function) — Extract a usable Nous inference JWT from stored auth state.
- L1334 `_nous_base_url()` (function) — Resolve the Nous inference base URL from env or default.
- L1339 `_resolve_nous_runtime_api(*, force_refresh: bool=False)` (function) — Return fresh Nous runtime credentials when available.
- L1365 `_resolve_xai_oauth_for_aux()` (function) — Resolve a fresh xAI OAuth (api_key, base_url) for auxiliary clients.
- L1420 `_read_codex_access_token()` (function) — Read a valid, non-expired Codex OAuth access token from Hermes auth store.
- L1463 `_resolve_api_key_provider()` (function) — Try each API-key provider in PROVIDER_REGISTRY order.
- L1581 `_try_openrouter(explicit_api_key: str=None, model: str=None)` (function)
- L1602 `_describe_openrouter_unavailable()` (function) — Return a more precise OpenRouter auth failure reason for logs.
- L1615 `_try_nous(vision: bool=False)` (function)
- L1699 `_refresh_nous_recommended_model(*, vision: bool, stale_model: Optional[str])` (function) — Re-fetch the Nous Portal's recommended model after a stale-model 404.
- L1740 `_read_main_model()` (function) — Read the user's configured main model from config.yaml.
- L1770 `_read_main_provider()` (function) — Read the user's configured main provider from config.yaml.
- L1804 `set_runtime_main(provider: str, model: str, *, base_url: str='', api_key: str='', api_mode: str='')` (function) — Record the live runtime provider/model/credentials for the current AIAgent.
- L1832 `clear_runtime_main()` (function) — Clear the runtime override (e.g. on session end).
- L1843 `_resolve_custom_runtime()` (function) — Resolve the active custom/main endpoint the same way the main CLI does.
- L1893 `_current_custom_base_url()` (function)
- L1898 `_validate_proxy_env_urls()` (function) — Fail fast with a clear error when proxy env vars have malformed URLs.
- L1927 `_validate_base_url(base_url: str)` (function) — Reject obviously broken custom endpoint URLs before they reach httpx.
- L1945 `_try_custom_endpoint()` (function)
- L1996 `_build_xai_oauth_aux_client(model: str)` (function) — Build a CodexAuxiliaryClient for an xAI Grok OAuth-authenticated session.
- L2022 `_build_codex_client(model: str)` (function) — Build a CodexAuxiliaryClient for an explicitly-requested model.
- L2063 `_try_azure_foundry(*, model: Optional[str]=None, explicit_api_key: Optional[str]=None, explicit_base_url: Optional[str]=None, api_mode: Optional[str]=None)` (function) — Resolve an Azure Foundry auxiliary client via the runtime resolver.
- L2177 `_try_anthropic(explicit_api_key: str=None)` (function)
- L2235 `_normalize_main_runtime(main_runtime: Optional[Dict[str, Any]])` (function) — Return a sanitized copy of a live main-runtime override.
- L2261 `_get_provider_chain()` (function) — Return the ordered provider detection chain.
- L2319 `_normalize_chain_label(provider: str)` (function) — Normalize a resolved_provider value to a chain label used by
- L2331 `_mark_provider_unhealthy(provider: str, ttl: Optional[float]=None)` (function) — Mark ``provider`` as recently-402'd, hidden from chain iteration
- L2350 `_is_provider_unhealthy(label: str)` (function) — True iff ``label`` is in the unhealthy cache and the TTL hasn't expired.
- L2366 `_log_skip_unhealthy(label: str, task: Optional[str]=None)` (function) — Emit a single info-level log per minute when we skip an unhealthy
- L2382 `_reset_aux_unhealthy_cache()` (function) — Clear the unhealthy cache. Used by tests and by a future explicit
- L2389 `_is_payment_error(exc: Exception)` (function) — Detect payment/credit/quota exhaustion errors.
- L2429 `_nous_portal_account_has_fresh_paid_access()` (function) — Return True only when the fresh Nous account API says paid access is allowed.
- L2441 `_is_rate_limit_error(exc: Exception)` (function) — Detect rate-limit errors that warrant provider fallback.
- L2478 `_is_connection_error(exc: Exception)` (function) — Detect connection/network errors that warrant provider fallback.
- L2516 `_is_auth_error(exc: Exception)` (function) — Detect auth failures that should trigger provider-specific refresh.
- L2534 `_is_unsupported_parameter_error(exc: Exception, param: str)` (function) — Detect provider 400s for an unsupported request parameter.
- L2568 `_is_unsupported_temperature_error(exc: Exception)` (function) — Back-compat wrapper: detect API errors where the model rejects ``temperature``.
- L2577 `_is_model_not_found_error(exc: Exception)` (function) — Detect "the requested model doesn't exist" errors (404 / invalid model).
- L2617 `_evict_cached_clients(provider: str)` (function) — Drop cached auxiliary clients for a provider so fresh creds are used.
- L2638 `_evict_cached_client_instance(target: Any)` (function) — Drop the cache entry whose stored client is *target*.
- L2673 `_pool_cache_hint(provider: str, *, main_runtime: Optional[Dict[str, Any]]=None)` (function) — Return a stable cache discriminator for pooled providers.
- L2694 `_pool_error_context(exc: Exception)` (function)
- L2702 `_recoverable_pool_provider(resolved_provider: str, client: Any, main_runtime: Optional[Dict[str, Any]]=None)` (function) — Infer which provider pool can recover the current auxiliary client.
- L2745 `_recover_provider_pool(provider: str, exc: Exception, *, failed_api_key: str='')` (function) — Try same-provider credential-pool recovery for auxiliary calls.
- L2794 `_retry_same_provider_sync(*, task: Optional[str], resolved_provider: str, resolved_model: Optional[str], resolved_base_url: Optional[str], resolved_api_key: Optional[str], resolved_api_mode: Optional[str], main_runtime: Optional[Dict[str, Any]], final_model: Optional[str], messages: list, temperature: Optional[float], max_tokens: Optional[int], tools: Optional[list], effective_timeout: float, effective_extra_body: dict)` (function)
- L2852 `_retry_same_provider_async(*, task: Optional[str], resolved_provider: str, resolved_model: Optional[str], resolved_base_url: Optional[str], resolved_api_key: Optional[str], resolved_api_mode: Optional[str], final_model: Optional[str], messages: list, temperature: Optional[float], max_tokens: Optional[int], tools: Optional[list], effective_timeout: float, effective_extra_body: dict)` (function)
- L2909 `_refresh_provider_credentials(provider: str)` (function) — Refresh short-lived credentials for OAuth-backed auxiliary providers.
- L2967 `_try_payment_fallback(failed_provider: str, task: str=None, reason: str='payment error')` (function) — Try alternative providers after a payment/credit or connection error.
- L3018 `_try_main_agent_model_fallback(failed_provider: str, task: str=None, reason: str='error')` (function) — Last-resort fallback to the user's main agent provider + model.
- L3067 `_try_configured_fallback_chain(task: str, failed_provider: str, reason: str='error')` (function) — Try user-configured fallback_chain for a specific auxiliary task.
- L3126 `_resolve_single_provider(provider: str, model: Optional[str]=None, base_url: Optional[str]=None, api_key: Optional[str]=None)` (function) — Resolve a single provider entry from fallback_chain to an OpenAI client.
- L3145 `_resolve_auto(main_runtime: Optional[Dict[str, Any]]=None)` (function) — Full auto-detection chain.
- L3277 `_to_async_client(sync_client, model: str, is_vision: bool=False)` (function) — Convert a sync client to its async counterpart, preserving Codex routing.
- L3342 `_normalize_resolved_model(model_name: Optional[str], provider: str)` (function) — Normalize a resolved model for the provider that will receive it.
- L3354 `resolve_provider_client(provider: str, model: str=None, async_mode: bool=False, raw_codex: bool=False, explicit_base_url: str=None, explicit_api_key: str=None, api_mode: str=None, main_runtime: Optional[Dict[str, Any]]=None, is_vision: bool=False)` (function) — Central router: given a provider name and optional model, return a
- L4008 `get_text_auxiliary_client(task: str='', *, main_runtime: Optional[Dict[str, Any]]=None)` (function) — Return (client, default_model_slug) for text-only auxiliary tasks.
- L4033 `get_async_text_auxiliary_client(task: str='', *, main_runtime: Optional[Dict[str, Any]]=None)` (function) — Return (async_client, model_slug) for async consumers.
- L4058 `_main_model_supports_vision(provider: str, model: Optional[str])` (function) — Return True when ``provider``/``model`` is known to accept image input.
- L4089 `_normalize_vision_provider(provider: Optional[str])` (function)
- L4093 `_resolve_strict_vision_backend(provider: str, model: Optional[str]=None)` (function)
- L4116 `_strict_vision_backend_available(provider: str)` (function)
- L4120 `get_available_vision_backends()` (function) — Return the currently available vision backends in auto-selection order.
- L4145 `resolve_vision_provider_client(provider: Optional[str]=None, model: Optional[str]=None, *, base_url: Optional[str]=None, api_key: Optional[str]=None, async_mode: bool=False)` (function) — Resolve the client actually used for vision tasks.
- L4312 `get_auxiliary_extra_body()` (function) — Return extra_body kwargs for auxiliary API calls.
- L4321 `auxiliary_max_tokens_param(value: int)` (function) — Return the correct max tokens kwarg for the auxiliary client's provider.
- L4363 `_client_cache_key(provider: str, *, async_mode: bool, base_url: Optional[str]=None, api_key: Optional[str]=None, api_mode: Optional[str]=None, main_runtime: Optional[Dict[str, Any]]=None, is_vision: bool=False)` (function)
- L4379 `_store_cached_client(cache_key: tuple, client: Any, default_model: Optional[str], *, bound_loop: Any=None)` (function)
- L4393 `_refresh_nous_auxiliary_client(*, cache_provider: str, model: Optional[str], async_mode: bool, base_url: Optional[str]=None, api_key: Optional[str]=None, api_mode: Optional[str]=None, main_runtime: Optional[Dict[str, Any]]=None, is_vision: bool=False)` (function) — Refresh Nous runtime creds, rebuild the client, and replace the cache entry.
- L4437 `neuter_async_httpx_del()` (function) — Monkey-patch ``AsyncHttpxClientWrapper.__del__`` to be a no-op.
- L4470 `_force_close_async_httpx(client: Any)` (function) — Mark the httpx AsyncClient inside an AsyncOpenAI client as closed.
- L4490 `shutdown_cached_clients()` (function) — Close all cached clients (sync and async) to prevent event-loop errors.
- L4517 `cleanup_stale_async_clients()` (function) — Force-close cached async clients whose event loop is closed.
- L4536 `_is_openrouter_client(client: Any)` (function)
- L4543 `_cached_client_accepts_slash_models(client: Any, cached_default: Optional[str])` (function) — Best-effort check for cached clients that accept ``vendor/model`` IDs.
- L4550 `_compat_model(client: Any, model: Optional[str], cached_default: Optional[str])` (function) — Keep slash-bearing model IDs only for cached clients that support them.
- L4560 `_get_cached_client(provider: str, model: str=None, async_mode: bool=False, base_url: str=None, api_key: str=None, api_mode: str=None, main_runtime: Optional[Dict[str, Any]]=None, is_vision: bool=False)` (function) — Get or create a cached client for the given provider.
- L4686 `_resolve_task_provider_model(task: str=None, provider: str=None, model: str=None, base_url: str=None, api_key: str=None)` (function) — Determine provider + model for a call.
- L4767 `_get_auxiliary_task_config(task: str)` (function) — Return the config dict for auxiliary.<task>, or {} when unavailable.
- L4811 `_get_task_timeout(task: str, default: float=_DEFAULT_AUX_TIMEOUT)` (function) — Read timeout from auxiliary.{task}.timeout in config, falling back to *default*.
- L4825 `_get_task_extra_body(task: str)` (function) — Read auxiliary.<task>.extra_body and return a shallow copy when valid.
- L4843 `_is_anthropic_compat_endpoint(provider: str, base_url: str)` (function) — Detect if an endpoint expects Anthropic-format content blocks.
- L4855 `_convert_openai_images_to_anthropic(messages: list)` (function) — Convert OpenAI ``image_url``/``video_url`` blocks to Anthropic format.
- L4940 `_build_call_kwargs(provider: str, model: str, messages: list, temperature: Optional[float]=None, max_tokens: Optional[int]=None, tools: Optional[list]=None, timeout: float=30.0, extra_body: Optional[dict]=None, base_url: Optional[str]=None)` (function) — Build kwargs for .chat.completions.create() with model/provider adjustments.
- L5028 `_validate_llm_response(response: Any, task: str=None)` (function) — Validate that an LLM response has the expected .choices[0].message shape.
- L5059 `call_llm(task: str=None, *, provider: str=None, model: str=None, base_url: str=None, api_key: str=None, main_runtime: Optional[Dict[str, Any]]=None, messages: list, temperature: float=None, max_tokens: int=None, tools: list=None, timeout: float=None, extra_body: dict=None)` (function) — Centralized synchronous LLM call.
- L5507 `extract_content_or_reasoning(response)` (function) — Extract content from an LLM response, falling back to reasoning fields.
- L5563 `async_call_llm(task: str=None, *, provider: str=None, model: str=None, base_url: str=None, api_key: str=None, main_runtime: Optional[Dict[str, Any]]=None, messages: list, temperature: float=None, max_tokens: int=None, tools: list=None, timeout: float=None, extra_body: dict=None)` (function) — Centralized asynchronous LLM call.
