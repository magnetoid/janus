---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:44'
updated: '2026-06-08T00:38:44'
---

# tests/tools/test_skills_hub.py

Symbols in `tests/tools/test_skills_hub.py`.

- L36 `TestParseFrontmatterQuick` (class)
- L37 `test_valid_frontmatter(self)` (method)
- L43 `test_no_frontmatter(self)` (method)
- L48 `test_no_closing_delimiter(self)` (method)
- L53 `test_empty_content(self)` (method)
- L57 `test_nested_yaml(self)` (method)
- L62 `test_invalid_yaml_returns_empty(self)` (method)
- L67 `test_non_dict_yaml_returns_empty(self)` (method)
- L78 `TestSkillsShGroupings` (class) — Parsing + stamping of the skills.sh.json grouping sidecar.
- L87 `test_parse_basic_groupings(self)` (method)
- L102 `test_parse_invalid_json_returns_none(self)` (method)
- L105 `test_parse_non_dict_returns_none(self)` (method)
- L108 `test_parse_missing_groupings_returns_none(self)` (method)
- L111 `test_parse_empty_groupings_returns_empty_map(self)` (method)
- L114 `test_parse_tolerates_malformed_group(self)` (method)
- L123 `test_parse_first_grouping_wins_on_duplicate(self)` (method)
- L130 `test_get_groupings_caches_per_repo(self)` (method)
- L142 `test_get_groupings_no_sidecar_returns_none_and_caches(self)` (method)
- L150 `test_list_skills_stamps_category_from_sidecar(self)` (method)
- L175 `test_list_skills_no_sidecar_leaves_extra_empty(self)` (method)
- L197 `test_meta_to_dict_roundtrip_preserves_extra(self)` (method)
- L215 `TestTrustLevelFor` (class)
- L216 `_source(self)` (method)
- L220 `test_trusted_repo(self)` (method)
- L228 `test_community_repo(self)` (method)
- L232 `test_short_identifier(self)` (method)
- L236 `test_two_part_identifier(self)` (method)
- L242 `test_nvidia_skills_tap_is_registered_and_trusted(self)` (method)
- L257 `test_browseable_trusted_repos_have_taps(self)` (method)
- L278 `TestSkillsShSource` (class)
- L279 `_source(self)` (method)
- L286 `test_search_maps_skills_sh_results_to_prefixed_identifiers(self, mock_get, _mock_read_cache, _mock_write_cache)` (method)
- L315 `test_empty_search_uses_featured_homepage_links(self, mock_get, _mock_read_cache, _mock_write_cache)` (method)
- L334 `test_fetch_delegates_to_github_source_and_relabels_bundle(self, mock_fetch)` (method)
- L351 `test_fetch_accepts_common_skills_sh_prefix_typo(self, mock_fetch)` (method)
- L372 `test_inspect_delegates_to_github_source_and_relabels_meta(self, mock_inspect, mock_get, _mock_read_cache, _mock_write_cache)` (method)
- L403 `test_inspect_accepts_common_skills_sh_prefix_typo(self, mock_inspect)` (method)
- L424 `test_inspect_falls_back_to_repo_skill_catalog_when_slug_differs(self, mock_inspect, mock_list_skills)` (method)
- L448 `test_inspect_uses_detail_page_to_resolve_alias_skill(self, mock_inspect, mock_list_skills, mock_get, _mock_read_cache, _mock_write_cache)` (method)
- L481 `test_fetch_uses_detail_page_to_resolve_alias_skill(self, mock_fetch, mock_list_skills, mock_get, _mock_read_cache, _mock_write_cache)` (method)
- L521 `test_fetch_downloads_only_the_resolved_identifier(self, mock_fetch, mock_detail, mock_discover, _mock_read_cache, _mock_write_cache)` (method)
- L553 `test_fetch_falls_back_to_tree_search_for_deeply_nested_skills(self, mock_fetch, mock_get, _mock_read_cache, _mock_write_cache)` (method) — Skills in deeply nested dirs (e.g. cli-tool/components/skills/dev/my-skill/)
- L614 `test_discover_identifier_uses_tree_search_before_root_scan(self, mock_get, mock_list_skills, mock_find_in_tree)` (method)
- L644 `test_empty_query_walks_sitemap_not_homepage(self, mock_get, _mock_read_cache, _mock_write_cache)` (method) — Empty query must walk the full sitemap.
- L704 `TestFindSkillInRepoTree` (class) — Tests for GitHubSource._find_skill_in_repo_tree.
- L707 `_source(self)` (method)
- L713 `test_finds_deeply_nested_skill(self, mock_get)` (method)
- L738 `test_finds_root_level_skill(self, mock_get)` (method)
- L761 `test_returns_none_when_skill_not_found(self, mock_get)` (method)
- L784 `test_returns_none_when_repo_api_fails(self, mock_get)` (method)
- L790 `TestWellKnownSkillSource` (class)
- L792 `_allow_public_skill_fetches(self, monkeypatch)` (method)
- L796 `_source(self)` (method)
- L802 `test_search_reads_index_from_well_known_url(self, mock_get, _mock_read_cache, _mock_write_cache)` (method)
- L824 `test_search_accepts_domain_root_and_resolves_index(self, mock_get, _mock_read_cache, _mock_write_cache)` (method)
- L839 `test_inspect_fetches_skill_md_from_well_known_endpoint(self, mock_get, _mock_read_cache, _mock_write_cache)` (method)
- L861 `test_fetch_downloads_skill_files_from_well_known_endpoint(self, mock_get, _mock_read_cache, _mock_write_cache)` (method)
- L889 `test_fetch_rejects_unsafe_file_paths_from_well_known_endpoint(self, mock_get, _mock_read_cache, _mock_write_cache)` (method)
- L910 `TestUrlSource` (class)
- L912 `_allow_public_skill_fetches(self, monkeypatch)` (method)
- L916 `_source(self)` (method)
- L920 `test_matches_bare_md_url(self)` (method)
- L923 `test_matches_http_scheme(self)` (method)
- L926 `test_rejects_non_md_url(self)` (method)
- L930 `test_rejects_well_known_url(self)` (method)
- L939 `test_rejects_wrapped_identifiers(self)` (method)
- L944 `test_rejects_non_string(self)` (method)
- L948 `test_search_returns_empty(self)` (method)
- L954 `test_inspect_reads_frontmatter_from_url(self, mock_get)` (method)
- L978 `test_inspect_returns_none_when_url_not_md(self, mock_get)` (method)
- L985 `test_inspect_returns_none_on_404(self, mock_get)` (method)
- L990 `test_inspect_returns_none_on_http_error(self, mock_get)` (method)
- L997 `test_inspect_blocks_private_url(self, _mock_safe, _mock_policy, mock_get)` (method)
- L1002 `test_inspect_flags_awaiting_name_when_unresolvable(self, mock_get)` (method)
- L1016 `test_fetch_builds_single_file_bundle(self, mock_get)` (method)
- L1037 `test_fetch_falls_back_to_url_directory_name(self, mock_get)` (method)
- L1049 `test_fetch_falls_back_to_filename_when_no_parent_dir(self, mock_get)` (method)
- L1060 `test_fetch_awaiting_name_when_unresolvable(self, mock_get)` (method)
- L1074 `test_fetch_awaiting_name_rejects_sentinel_slug(self, mock_get)` (method)
- L1087 `test_fetch_ignores_unsafe_frontmatter_name_and_falls_through_to_slug(self, mock_get)` (method)
- L1099 `test_fetch_returns_none_on_404(self, mock_get)` (method)
- L1106 `test_fetch_blocks_redirect_to_private_url(self, _mock_safe, _mock_policy, mock_get)` (method)
- L1117 `test_fetch_blocks_private_url(self, _mock_safe, _mock_policy, mock_get)` (method)
- L1122 `test_fetch_skips_non_matching_identifier(self, mock_get)` (method)
- L1127 `test_is_valid_skill_name_accepts_identifiers(self)` (method)
- L1132 `test_is_valid_skill_name_rejects_sentinel_and_garbage(self)` (method)
- L1145 `TestCheckForSkillUpdates` (class)
- L1146 `test_bundle_content_hash_matches_installed_content_hash(self, tmp_path)` (method)
- L1167 `test_bundle_content_hash_accepts_binary_files(self)` (method)
- L1183 `test_bundle_content_hash_bytes_matches_str_equivalent(self)` (method) — Bytes content must hash identically to its str-decoded form.
- L1208 `test_bundle_content_hash_mixed_matches_on_disk(self, tmp_path)` (method) — In-memory bundle hash must equal on-disk content_hash for mixed bytes+str.
- L1230 `test_reports_update_when_remote_hash_differs(self)` (method)
- L1256 `test_reports_up_to_date_when_hash_matches(self)` (method)
- L1281 `TestCreateSourceRouter` (class)
- L1282 `test_includes_skills_sh_source(self)` (method)
- L1286 `test_includes_well_known_source(self)` (method)
- L1290 `test_includes_url_source(self)` (method)
- L1294 `test_url_source_runs_before_github_source(self)` (method)
- L1307 `TestHubLockFile` (class)
- L1308 `test_load_missing_file(self, tmp_path)` (method)
- L1313 `test_load_valid_file(self, tmp_path)` (method)
- L1323 `test_load_corrupt_json(self, tmp_path)` (method)
- L1330 `test_save_creates_parent_dir(self, tmp_path)` (method)
- L1336 `test_record_install(self, tmp_path)` (method)
- L1356 `test_record_uninstall(self, tmp_path)` (method)
- L1367 `test_record_uninstall_nonexistent(self, tmp_path)` (method)
- L1373 `test_get_installed(self, tmp_path)` (method)
- L1383 `test_list_installed(self, tmp_path)` (method)
- L1406 `TestTapsManager` (class)
- L1407 `test_load_missing_file(self, tmp_path)` (method)
- L1411 `test_load_valid_file(self, tmp_path)` (method)
- L1419 `test_load_corrupt_json(self, tmp_path)` (method)
- L1425 `test_add_new_tap(self, tmp_path)` (method)
- L1432 `test_add_duplicate_tap(self, tmp_path)` (method)
- L1438 `test_remove_existing_tap(self, tmp_path)` (method)
- L1444 `test_remove_nonexistent_tap(self, tmp_path)` (method)
- L1448 `test_list_taps(self, tmp_path)` (method)
- L1461 `TestConvertToSkillMd` (class)
- L1462 `test_basic_conversion(self)` (method)
- L1482 `test_missing_system_role(self)` (method)
- L1490 `test_missing_meta(self)` (method)
- L1501 `TestUnifiedSearchDedup` (class)
- L1502 `_make_source(self, source_id, results)` (method) — Create a mock SkillSource that returns fixed results.
- L1509 `test_dedup_keeps_first_seen(self)` (method)
- L1521 `test_dedup_prefers_trusted_over_community(self)` (method)
- L1533 `test_dedup_prefers_builtin_over_trusted(self)` (method) — Regression: builtin must not be overwritten by trusted.
- L1545 `test_dedup_trusted_not_overwritten_by_community(self)` (method)
- L1555 `test_browse_sh_same_name_different_site_not_deduped(self)` (method)
- L1572 `test_source_filter(self)` (method)
- L1583 `test_limit_respected(self)` (method)
- L1593 `test_source_error_handled(self)` (method)
- L1610 `TestAppendAuditLog` (class)
- L1611 `test_creates_log_entry(self, tmp_path)` (method)
- L1621 `test_appends_multiple_entries(self, tmp_path)` (method)
- L1629 `test_extra_field_included(self, tmp_path)` (method)
- L1642 `TestSkillMetaToDict` (class)
- L1643 `test_roundtrip(self)` (method)
- L1663 `TestOptionalSkillSourceBinaryAssets` (class)
- L1664 `test_fetch_preserves_binary_assets(self, tmp_path)` (method)
- L1694 `TestQuarantineBundleBinaryAssets` (class)
- L1695 `test_quarantine_bundle_writes_binary_files(self, tmp_path)` (method)
- L1722 `test_quarantine_bundle_rejects_traversal_file_paths(self, tmp_path)` (method)
- L1749 `test_quarantine_bundle_rejects_absolute_file_paths(self, tmp_path)` (method)
- L1783 `TestDownloadDirectoryViaTree` (class) — Tests for the Git Trees API path in _download_directory.
- L1786 `_source(self)` (method)
- L1793 `test_tree_api_downloads_subdirectories(self, mock_get, mock_fetch)` (method) — Tree API returns files from nested subdirectories.
- L1820 `test_falls_back_on_truncated_tree(self, mock_get, mock_fallback)` (method) — When tree is truncated, fall back to recursive Contents API.
- L1834 `test_falls_back_on_repo_api_failure(self, mock_get, mock_fallback)` (method) — When the repo endpoint returns non-200, fall back to Contents API.
- L1846 `test_tree_api_skips_failed_file_fetches(self, mock_get, mock_fetch)` (method) — Files that fail to fetch are skipped, not fatal.
- L1869 `test_falls_back_on_network_error(self, mock_get, mock_fallback)` (method) — Network errors in tree API trigger fallback.
- L1879 `TestDownloadDirectoryRecursive` (class) — Tests for the Contents API fallback path.
- L1882 `_source(self)` (method)
- L1889 `test_recursive_downloads_subdirectories(self, mock_get, mock_fetch)` (method) — Contents API recursion includes subdirectories.
- L1909 `test_recursive_handles_subdir_failure(self, mock_get, mock_fetch)` (method) — Subdirectory 403/rate-limit returns empty but doesn't crash.
- L1931 `TestInstallPathSafety` (class) — Guard the lock-file → ``uninstall_skill`` rmtree path.
- L1941 `isolated_skills_dir(self, tmp_path, monkeypatch)` (method)
- L1948 `patch_lock_file(self, monkeypatch)` (method) — Redirect HubLockFile's default path to a test-controlled file.
- L1971 `test_record_install_rejects_unsafe_paths(self, tmp_path, bad_install_path)` (method) — record_install must reject malformed install_path values at write time.
- L1986 `test_record_install_rejects_mismatched_last_component(self, tmp_path)` (method) — The final component of install_path MUST equal the skill name.
- L2001 `test_record_install_accepts_bare_name(self, tmp_path)` (method)
- L2010 `test_record_install_accepts_category_and_name(self, tmp_path)` (method)
- L2019 `test_record_install_accepts_nested_official_skill_path(self, tmp_path)` (method)
- L2032 `test_uninstall_rejects_poisoned_absolute_path(self, tmp_path, isolated_skills_dir, patch_lock_file)` (method) — Hand-edited lock.json with absolute install_path must not delete anything.
- L2066 `test_uninstall_rejects_traversal(self, tmp_path, isolated_skills_dir, patch_lock_file)` (method)
- L2093 `test_uninstall_rejects_empty_install_path(self, tmp_path, isolated_skills_dir, patch_lock_file)` (method) — Empty install_path resolves to SKILLS_DIR itself — must be refused.
- L2120 `test_uninstall_rejects_symlink_redirect_inside_skills(self, tmp_path, isolated_skills_dir, patch_lock_file)` (method) — A symlinked skill dir that points outside skills/ must not be followed.
- L2158 `test_install_from_quarantine_rejects_symlinks(self, tmp_path)` (method) — Skill install must not follow symlinks that leak file contents
