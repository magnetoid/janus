---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:41'
updated: '2026-06-08T00:38:41'
---

# tests/agent/test_error_classifier.py

Symbols in `tests/agent/test_error_classifier.py`.

- L17 `MockAPIError` (class) — Simulates an OpenAI SDK APIStatusError.
- L19 `__init__(self, message, status_code=None, body=None)` (method)
- L25 `MockTransportError` (class) — Simulates a transport-level error with a specific type name.
- L30 `ReadTimeout` (class)
- L34 `ConnectError` (class)
- L38 `RemoteProtocolError` (class)
- L42 `ServerDisconnectedError` (class)
- L48 `TestFailoverReason` (class)
- L49 `test_all_reasons_have_string_values(self)` (method)
- L53 `test_enum_members_exist(self)` (method)
- L74 `TestClassifiedError` (class)
- L75 `test_is_auth_property(self)` (method)
- L85 `test_defaults(self)` (method)
- L97 `TestExtractStatusCode` (class)
- L98 `test_from_status_code_attr(self)` (method)
- L102 `test_from_status_attr(self)` (method)
- L107 `test_from_cause_chain(self)` (method)
- L113 `test_none_when_missing(self)` (method)
- L116 `test_rejects_non_http_status(self)` (method) — Integers outside 100-599 on .status should be ignored.
- L125 `TestExtractErrorBody` (class)
- L126 `test_from_body_attr(self)` (method)
- L130 `test_empty_when_no_body(self)` (method)
- L136 `TestExtractErrorCode` (class)
- L137 `test_from_nested_error_code(self)` (method)
- L141 `test_from_nested_error_type(self)` (method)
- L145 `test_from_top_level_code(self)` (method)
- L149 `test_from_wrapped_json_message(self)` (method)
- L162 `test_empty_when_no_code(self)` (method)
- L169 `TestClassify402` (class) — The critical 402 billing vs rate_limit disambiguation.
- L172 `test_billing_exhaustion(self)` (method) — Plain 402 = billing.
- L181 `test_transient_usage_limit(self)` (method) — 402 with 'usage limit' + 'try again' = rate limit, not billing.
- L190 `test_quota_with_retry(self)` (method) — 402 with 'quota' + 'retry' = rate limit.
- L198 `test_quota_without_retry(self)` (method) — 402 with just 'quota' but no transient signal = billing.
- L206 `test_insufficient_credits(self)` (method)
- L216 `TestClassifyApiError` (class) — End-to-end classification tests.
- L221 `test_401_classified_as_auth(self)` (method)
- L231 `test_403_classified_as_auth(self)` (method)
- L237 `test_403_key_limit_classified_as_billing(self)` (method) — OpenRouter 403 'key limit exceeded' is billing, not auth.
- L245 `test_403_spending_limit_classified_as_billing(self)` (method)
- L252 `test_402_plain_billing(self)` (method)
- L258 `test_402_out_of_funds_billing(self)` (method)
- L274 `test_402_transient_usage_limit(self)` (method)
- L280 `test_403_plan_entitlement_billing(self)` (method)
- L286 `test_404_free_tier_model_block_is_billing(self)` (method)
- L305 `test_429_rate_limit(self)` (method)
- L311 `test_alibaba_rate_increased_too_quickly(self)` (method) — Alibaba/DashScope returns a unique throttling message.
- L329 `test_500_server_error(self)` (method)
- L335 `test_502_server_error(self)` (method)
- L340 `test_503_overloaded(self)` (method)
- L345 `test_529_anthropic_overloaded(self)` (method)
- L356 `test_502_with_unknown_parameter_is_non_retryable(self)` (method)
- L376 `test_502_with_unsupported_parameter_is_non_retryable(self)` (method)
- L391 `test_500_with_invalid_request_error_type_is_non_retryable(self)` (method)
- L401 `test_502_plain_bad_gateway_still_retryable(self)` (method) — A genuine 502 with no request-validation signal stays retryable.
- L410 `test_404_model_not_found(self)` (method)
- L417 `test_404_generic(self)` (method)
- L430 `test_404_openrouter_policy_blocked(self)` (method)
- L447 `test_400_openrouter_policy_blocked(self)` (method)
- L460 `test_message_only_openrouter_policy_blocked(self)` (method)
- L478 `test_message_only_cyber_content_policy_blocked(self)` (method)
- L494 `test_400_cyber_content_policy_blocked(self)` (method)
- L506 `test_openai_usage_policy_violation_content_policy_blocked(self)` (method)
- L518 `test_anthropic_safety_system_content_policy_blocked(self)` (method)
- L529 `test_azure_content_filter_content_policy_blocked(self)` (method)
- L542 `test_404_model_not_found_still_works(self)` (method)
- L555 `test_413_payload_too_large(self)` (method)
- L563 `test_400_context_length(self)` (method)
- L569 `test_400_too_many_tokens(self)` (method)
- L574 `test_400_prompt_too_long(self)` (method)
- L579 `test_400_generic_large_session(self)` (method) — Generic 400 with large session → context overflow heuristic.
- L589 `test_400_generic_small_session_is_format_error(self)` (method) — Generic 400 with small session → format error, not context overflow.
- L599 `test_400_generic_many_messages_below_large_context_pressure_is_format_error(self)` (method) — Large-context sessions should not overflow solely due to message count.
- L619 `test_disconnect_large_session_context_overflow(self)` (method) — Server disconnect with large session → context overflow.
- L626 `test_disconnect_small_session_timeout(self)` (method) — Server disconnect with small session → timeout.
- L632 `test_disconnect_many_messages_below_large_context_pressure_is_timeout(self)` (method) — Large-context disconnects should not overflow solely due to message count.
- L648 `test_anthropic_thinking_signature(self)` (method)
- L657 `test_non_anthropic_400_with_signature_not_classified_as_thinking(self)` (method) — 400 with 'signature' but from non-Anthropic → format error.
- L664 `test_invalid_encrypted_content_classified_as_retryable_replay_failure(self)` (method)
- L685 `test_invalid_encrypted_content_broad_message_match_does_not_catch_generic_parse_error(self)` (method)
- L698 `test_invalid_encrypted_content_code_is_case_insensitive_for_400(self, error_code)` (method)
- L711 `test_llama_cpp_grammar_parse_error(self)` (method) — llama.cpp rejects regex escapes in JSON Schema `pattern`.
- L722 `test_llama_cpp_unable_to_generate_parser(self)` (method) — Older llama.cpp builds surface the error as 'unable to generate parser'.
- L731 `test_llama_cpp_json_schema_to_grammar_phrase(self)` (method) — Some builds mention the module name explicitly.
- L740 `test_llama_cpp_grammar_requires_400(self)` (method) — A 500 with the same phrase isn't the llama.cpp grammar case.
- L748 `test_anthropic_long_context_tier(self)` (method)
- L757 `test_normal_429_not_long_context(self)` (method) — Normal 429 without 'extra usage' + 'long context' → rate_limit.
- L765 `test_anthropic_oauth_1m_beta_forbidden(self)` (method) — 400 + 'long context beta is not yet available for this subscription'
- L777 `test_anthropic_oauth_1m_beta_forbidden_does_not_collide_with_tier_gate(self)` (method) — The 429 'extra usage' + 'long context' tier gate keeps its own
- L787 `test_400_without_beta_phrase_is_not_1m_beta_forbidden(self)` (method) — A generic 400 that happens to mention 'long context' but not the
- L799 `test_read_timeout(self)` (method)
- L805 `test_connect_error(self)` (method)
- L810 `test_connection_error_builtin(self)` (method)
- L815 `test_timeout_error_builtin(self)` (method)
- L820 `test_runtime_error_cli_turn_timed_out_classifies_as_timeout(self)` (method)
- L830 `test_runtime_error_request_timed_out_classifies_as_timeout(self)` (method)
- L836 `test_runtime_error_deadline_exceeded_classifies_as_timeout(self)` (method)
- L844 `test_error_code_resource_exhausted(self)` (method)
- L852 `test_error_code_model_not_found(self)` (method)
- L860 `test_error_code_context_length_exceeded(self)` (method)
- L868 `test_error_code_model_not_supported_on_free_tier_is_billing(self)` (method)
- L883 `test_message_billing_pattern(self)` (method)
- L888 `test_message_free_tier_model_block_is_billing(self)` (method)
- L893 `test_message_rate_limit_pattern(self)` (method)
- L898 `test_message_auth_pattern(self)` (method)
- L903 `test_message_model_not_found_pattern(self)` (method)
- L908 `test_message_context_overflow_pattern(self)` (method)
- L915 `test_message_usage_limit_transient_is_rate_limit(self)` (method) — 'usage limit' + 'try again' with no status code → rate_limit, not billing.
- L924 `test_message_usage_limit_no_retry_signal_is_billing(self)` (method) — 'usage limit' with no transient signal and no status code → billing.
- L932 `test_message_quota_with_reset_window_is_rate_limit(self)` (method) — 'quota' + 'resets at' with no status code → rate_limit.
- L939 `test_message_limit_exceeded_with_wait_is_rate_limit(self)` (method) — 'limit exceeded' + 'wait' with no status code → rate_limit.
- L948 `test_generic_exception_is_unknown(self)` (method)
- L956 `test_400_descriptive_format_error(self)` (method) — 400 with descriptive message (not context overflow) → format error.
- L967 `test_422_format_error(self)` (method)
- L973 `test_400_flat_body_descriptive_not_context_overflow(self)` (method) — Responses API flat body with descriptive error + large session → format error.
- L991 `test_400_flat_body_generic_large_session_still_context_overflow(self)` (method) — Flat body with generic 'Error' message + large session → context overflow.
- L1007 `test_peer_closed_large_session(self)` (method)
- L1014 `test_chinese_context_overflow(self)` (method)
- L1021 `test_vllm_max_model_len_overflow(self)` (method) — vLLM's 'exceeds the max_model_len' error → context_overflow.
- L1031 `test_vllm_prompt_length_exceeds(self)` (method) — vLLM prompt length error → context_overflow.
- L1040 `test_vllm_input_too_long(self)` (method) — vLLM 'input is too long' error → context_overflow.
- L1046 `test_ollama_context_length_exceeded(self)` (method) — Ollama 'context length exceeded' error → context_overflow.
- L1052 `test_llamacpp_slot_context(self)` (method) — llama.cpp / llama-server 'slot context' error → context_overflow.
- L1063 `test_provider_and_model_in_result(self)` (method)
- L1070 `test_message_extracted(self)` (method)
- L1082 `TestAdversarialEdgeCases` (class) — Edge cases discovered during live testing with real SDK objects.
- L1085 `test_empty_exception_message(self)` (method)
- L1090 `test_500_with_none_body(self)` (method)
- L1095 `test_non_dict_body(self)` (method) — Some providers return strings instead of JSON.
- L1103 `test_list_body(self)` (method)
- L1110 `test_circular_cause_chain(self)` (method) — Must not infinite-loop on circular __cause__.
- L1117 `test_three_level_cause_chain(self)` (method)
- L1127 `test_400_with_rate_limit_text(self)` (method) — Some providers send rate limits as 400 instead of 429.
- L1137 `test_400_with_billing_text(self)` (method) — Some providers send billing errors as 400.
- L1147 `test_200_with_error_body(self)` (method) — 200 status with error in body — should be unknown, not crash.
- L1155 `test_ollama_context_size_exceeded(self)` (method)
- L1164 `test_connection_refused_error(self)` (method)
- L1169 `test_body_message_enrichment(self)` (method) — Body message must be included in pattern matching even when
- L1181 `test_disconnect_pattern_ordering(self)` (method) — Disconnect + large session must beat generic transport catch.
- L1191 `test_credit_balance_too_low(self)` (method)
- L1200 `test_deepseek_402_chinese(self)` (method) — Chinese billing message should still match billing patterns.
- L1207 `test_openrouter_wrapped_context_overflow_in_metadata_raw(self)` (method) — OpenRouter wraps provider errors in metadata.raw JSON string.
- L1226 `test_openrouter_wrapped_rate_limit_in_metadata_raw(self)` (method)
- L1242 `test_thinking_signature_via_openrouter(self)` (method) — Thinking signature errors proxied through OpenRouter must be caught.
- L1252 `test_generic_400_large_by_message_count(self)` (method) — Many small messages (>80) should trigger context overflow heuristic.
- L1265 `test_disconnect_large_by_message_count(self)` (method) — Server disconnect with 200+ messages should trigger context overflow.
- L1273 `test_openrouter_wrapped_model_not_found_in_metadata_raw(self)` (method)
- L1291 `test_pydantic_dict_message_no_crash(self)` (method) — Pydantic validation errors return message as dict, not string.
- L1320 `test_nested_error_dict_message_no_crash(self)` (method) — Nested body['error']['message'] as dict must not crash.
- L1342 `test_metadata_raw_dict_message_no_crash(self)` (method) — OpenRouter metadata.raw with dict message must not crash.
- L1361 `test_list_message_no_crash(self)` (method) — Some providers return message as a list of error entries.
- L1371 `test_int_message_no_crash(self)` (method) — Any non-string type must be coerced safely.
- L1377 `test_none_message_still_works(self)` (method) — Regression: None fallback (the 'or ""' path) must still work.
- L1386 `TestSSLTransientPatterns` (class) — SSL/TLS alerts mid-stream should retry as timeout, not unknown, and
- L1396 `test_bad_record_mac_classifies_as_timeout(self)` (method) — OpenSSL 3.x mid-stream bad record mac alert.
- L1404 `test_openssl_3x_format_classifies_as_timeout(self)` (method) — New format `ERR_SSL_SSL/TLS_ALERT_BAD_RECORD_MAC` still matches
- L1414 `test_tls_alert_internal_error_classifies_as_timeout(self)` (method)
- L1421 `test_ssl_handshake_failure_classifies_as_timeout(self)` (method)
- L1427 `test_ssl_prefix_classifies_as_timeout(self)` (method) — Python's generic '[SSL: XYZ]' prefix from the ssl module.
- L1434 `test_ssl_alert_on_large_session_does_not_compress(self)` (method) — Critical: SSL alerts on big contexts must NOT trigger context
- L1450 `test_plain_disconnect_on_large_session_still_compresses(self)` (method) — Regression guard: the context-overflow-via-disconnect path
- L1465 `test_real_ssl_error_type_classifies_as_timeout(self)` (method) — Real ssl.SSLError instance — the type name alone (not message)
- L1476 `TestRateLimitErrorWithoutStatusCode` (class) — Regression tests for the Copilot/GitHub Models edge case where the
- L1480 `_make_rate_limit_error(self, status_code=None)` (method) — Create an exception whose class name is 'RateLimitError' with
- L1488 `test_rate_limit_error_without_status_code_classified_as_rate_limit(self)` (method) — RateLimitError with status_code=None must classify as rate_limit.
- L1494 `test_rate_limit_error_with_status_code_429_classified_as_rate_limit(self)` (method) — RateLimitError that does set status_code=429 still classifies correctly.
- L1500 `test_other_error_without_status_code_not_forced_to_rate_limit(self)` (method) — A non-RateLimitError with missing status_code must NOT be forced to 429.
- L1512 `TestMultimodalToolContentUnsupported` (class) — Issue #27344 — providers that reject list-type tool message content
- L1518 `test_xiaomi_mimo_text_is_not_set_pattern(self)` (method) — The actual Xiaomi MiMo 400 wording from the bug report.
- L1528 `test_generic_tool_message_must_be_string(self)` (method)
- L1536 `test_expected_string_got_list(self)` (method)
- L1544 `test_multimodal_tool_content_takes_priority_over_context_overflow(self)` (method) — Some providers return a 400 whose message contains BOTH
- L1556 `test_no_status_code_path_also_classifies(self)` (method) — When the error reaches us without a status code (transport
- L1565 `test_unrelated_400_is_not_misclassified(self)` (method) — Make sure the patterns don't false-positive on normal 400s.
