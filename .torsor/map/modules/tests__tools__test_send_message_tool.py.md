---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:44'
updated: '2026-06-08T00:38:44'
---

# tests/tools/test_send_message_tool.py

Symbols in `tests/tools/test_send_message_tool.py`.

- L20 `_reset_signal_scheduler()` (function) — Drop the process-wide attachment scheduler so each test gets a
- L49 `_send_discord(token, chat_id, message, *, thread_id=None, media_files=None)` (function) — Pre-migration ``(token, chat_id, message, …)`` adapter around the
- L71 `_discord_entry()` (function) — Return the live Discord PlatformEntry, importing lazily so plugin
- L80 `_patch_discord_sender` (class) — Patch the Discord registry entry's ``standalone_sender_fn`` with the
- L94 `__init__(self, mock)` (method)
- L99 `_adapter(self, pconfig, chat_id, message, *, thread_id=None, media_files=None)` (method)
- L106 `__enter__(self)` (method)
- L112 `__exit__(self, exc_type, exc, tb)` (method)
- L118 `_run_async_immediately(coro)` (function)
- L122 `_make_config()` (function)
- L130 `_install_telegram_mock(monkeypatch, bot)` (function)
- L141 `_ensure_slack_mock(monkeypatch)` (function)
- L165 `TestSendMessageTool` (class)
- L166 `test_ntfy_topic_target_is_explicit(self)` (method)
- L173 `test_ntfy_topic_target_bypasses_channel_directory(self)` (method)
- L208 `test_cron_duplicate_target_is_skipped_and_explained(self)` (method)
- L243 `test_resolved_telegram_topic_name_preserves_thread_id(self)` (method)
- L273 `test_display_label_target_resolves_via_channel_directory(self, tmp_path)` (method)
- L312 `test_resolved_slack_thread_name_preserves_thread_id(self)` (method)
- L346 `test_resolved_matrix_thread_name_preserves_thread_id(self)` (method)
- L387 `test_mirror_receives_current_session_user_id(self)` (method)
- L420 `test_media_tag_outside_allowed_roots_is_not_sent(self, tmp_path, monkeypatch)` (method)
- L458 `test_top_level_send_failure_redacts_query_token(self)` (method)
- L486 `TestSendTelegramMediaDelivery` (class)
- L487 `test_sends_text_then_photo_for_media_tag(self, tmp_path, monkeypatch)` (method)
- L517 `test_sends_voice_for_ogg_with_voice_directive(self, tmp_path, monkeypatch)` (method)
- L544 `test_sends_audio_for_mp3(self, tmp_path, monkeypatch)` (method)
- L570 `test_missing_media_returns_error_without_leaking_raw_tag(self, monkeypatch)` (method)
- L599 `TestSendToPlatformChunking` (class)
- L600 `test_long_message_is_chunked(self)` (method) — Messages exceeding the platform limit are split into multiple sends.
- L617 `test_slack_messages_are_formatted_before_send(self, monkeypatch)` (method)
- L643 `test_slack_bold_italic_formatted_before_send(self, monkeypatch)` (method) — Bold+italic ***text*** survives tool-layer formatting.
- L663 `test_slack_blockquote_formatted_before_send(self, monkeypatch)` (method) — Blockquote '>' markers must survive formatting (not escaped to '&gt;').
- L685 `test_slack_pre_escaped_entities_not_double_escaped(self, monkeypatch)` (method) — Pre-escaped HTML entities survive tool-layer formatting without double-escaping.
- L706 `test_slack_url_with_parens_formatted_before_send(self, monkeypatch)` (method) — Wikipedia-style URL with parens survives tool-layer formatting.
- L725 `test_telegram_media_attaches_to_last_chunk(self)` (method)
- L747 `test_matrix_media_uses_native_adapter_helper(self, tmp_path)` (method)
- L773 `test_matrix_text_only_uses_lightweight_path(self)` (method) — Text-only Matrix sends should NOT go through the heavy adapter path.
- L792 `test_send_matrix_via_adapter_sends_document(self, tmp_path)` (method)
- L849 `TestSendToPlatformWhatsapp` (class)
- L850 `test_whatsapp_routes_via_local_bridge_sender(self)` (method)
- L868 `TestSendTelegramHtmlDetection` (class) — Verify that messages containing HTML tags are sent with parse_mode=HTML
- L872 `_make_bot(self)` (method)
- L882 `test_html_message_uses_html_parse_mode(self, monkeypatch)` (method)
- L895 `test_plain_text_uses_markdown_v2(self, monkeypatch)` (method)
- L907 `test_disable_link_previews_sets_disable_web_page_preview(self, monkeypatch)` (method)
- L918 `test_html_with_code_and_pre_tags(self, monkeypatch)` (method)
- L928 `test_closing_tag_detected(self, monkeypatch)` (method)
- L937 `test_angle_brackets_in_math_not_detected(self, monkeypatch)` (method) — Expressions like 'x < 5' or '3 > 2' should not trigger HTML mode.
- L947 `test_html_parse_failure_falls_back_to_plain(self, monkeypatch)` (method) — If Telegram rejects the HTML, fall back to plain text.
- L967 `test_transient_bad_gateway_retries_text_send(self, monkeypatch)` (method)
- L985 `TestSendTelegramThreadIdMapping` (class) — General-topic mapping in _send_telegram (issue #22267).
- L996 `_make_bot(self)` (method)
- L1001 `test_general_topic_thread_id_omitted(self, monkeypatch)` (method) — thread_id="1" must be dropped before calling the Bot API.
- L1012 `test_non_general_topic_thread_id_preserved(self, monkeypatch)` (method) — Real forum-topic thread ids (>1) still pass through as ints.
- L1022 `test_no_thread_id_no_kwarg(self, monkeypatch)` (method) — With no thread_id, message_thread_id must not appear in kwargs.
- L1032 `test_general_topic_thread_id_int_input_also_dropped(self, monkeypatch)` (method) — thread_id passed as the int 1 (not str) must still be dropped.
- L1042 `test_thread_not_found_retries_without_message_thread_id(self, monkeypatch)` (method) — When send_message raises "thread not found", retry without thread_id (#27012).
- L1065 `test_thread_not_found_for_media_retries_without_message_thread_id(self, monkeypatch, tmp_path)` (method) — Media send with stale thread_id retries without it (#27012).
- L1101 `TestParseTargetRefDiscord` (class) — _parse_target_ref correctly extracts chat_id and thread_id for Discord.
- L1104 `test_discord_chat_id_with_thread_id(self)` (method) — discord:chat_id:thread_id returns both values.
- L1111 `test_discord_chat_id_without_thread_id(self)` (method) — discord:chat_id returns None for thread_id.
- L1118 `test_discord_large_snowflake_without_thread(self)` (method) — Large Discord snowflake IDs work without thread.
- L1125 `test_discord_channel_with_thread(self)` (method) — Full Discord format: channel:thread.
- L1132 `test_discord_whitespace_is_stripped(self)` (method) — Whitespace around Discord targets is stripped.
- L1140 `TestParseTargetRefMatrix` (class) — _parse_target_ref correctly handles Matrix room IDs and user MXIDs.
- L1143 `test_matrix_thread_target_is_explicit(self)` (method) — Session-derived Matrix thread targets round-trip as room + event id.
- L1153 `test_matrix_room_id_is_explicit(self)` (method) — Matrix room IDs (!) are recognized as explicit targets.
- L1160 `test_matrix_user_mxid_is_explicit(self)` (method) — Matrix user MXIDs (@) are recognized as explicit targets.
- L1167 `test_matrix_alias_is_not_explicit(self)` (method) — Matrix room aliases (#) are NOT explicit — they need resolution.
- L1173 `test_matrix_prefix_only_matches_matrix_platform(self)` (method) — ! and @ prefixes are only treated as explicit for the matrix platform.
- L1182 `TestParseTargetRefE164` (class) — _parse_target_ref accepts E.164 phone numbers for phone-based platforms.
- L1185 `test_signal_e164_preserves_plus_prefix(self)` (method) — signal:+E164 is explicit and preserves the leading '+' for signal-cli.
- L1192 `test_sms_e164_is_explicit(self)` (method)
- L1197 `test_whatsapp_e164_is_explicit(self)` (method)
- L1202 `test_signal_bare_digits_still_work(self)` (method) — Bare digit strings continue to match the generic numeric branch.
- L1208 `test_signal_invalid_e164_rejected(self)` (method) — Too-short, too-long, and non-numeric E.164 strings are not explicit.
- L1215 `test_e164_prefix_only_matches_phone_platforms(self)` (method) — '+' prefix must NOT be treated as explicit for non-phone platforms.
- L1222 `TestParseTargetRefSlack` (class) — _parse_target_ref recognizes Slack channel/user IDs as explicit.
- L1225 `test_thread_target_is_explicit(self)` (method)
- L1231 `test_public_channel_id_is_explicit(self)` (method)
- L1237 `test_private_channel_id_is_explicit(self)` (method)
- L1240 `test_dm_id_is_explicit(self)` (method)
- L1243 `test_user_id_is_not_explicit(self)` (method) — Slack user IDs (U...) and workspace IDs (W...) are NOT explicit send
- L1251 `test_whitespace_is_stripped(self)` (method)
- L1256 `test_lowercase_or_short_id_is_not_explicit(self)` (method)
- L1261 `test_slack_id_not_explicit_for_other_platforms(self)` (method)
- L1266 `TestParseTargetRefEmail` (class) — _parse_target_ref recognizes email addresses as explicit for the email platform.
- L1269 `test_standard_email_is_explicit(self)` (method)
- L1275 `test_email_with_dots_in_local_part(self)` (method)
- L1280 `test_email_with_plus_tag(self)` (method)
- L1285 `test_email_strips_whitespace(self)` (method)
- L1290 `test_invalid_email_not_explicit(self)` (method)
- L1296 `test_email_not_explicit_for_other_platforms(self)` (method)
- L1302 `TestEmailHomeChannelErrorHint` (class) — The no-home-channel error for email points at the real env var.
- L1310 `test_email_error_names_email_home_address(self)` (method)
- L1330 `test_non_email_platform_keeps_generic_home_channel_hint(self)` (method)
- L1350 `TestSendDiscordThreadId` (class) — _send_discord uses thread_id when provided.
- L1354 `_build_mock(response_status, response_data=None, response_text='error body')` (method) — Build a properly-structured aiohttp mock chain.
- L1375 `_run(self, token, chat_id, message, thread_id=None)` (method)
- L1378 `test_without_thread_id_uses_chat_id_endpoint(self)` (method) — When no thread_id, sends to /channels/{chat_id}/messages.
- L1386 `test_with_thread_id_uses_thread_endpoint(self)` (method) — When thread_id is provided, sends to /channels/{thread_id}/messages.
- L1394 `test_success_returns_message_id(self)` (method) — Successful send returns the Discord message ID.
- L1403 `test_error_status_returns_error_dict(self)` (method) — Non-200/201 responses return an error dict.
- L1412 `TestSendToPlatformDiscordThread` (class) — _send_to_platform passes thread_id through to _send_discord.
- L1415 `test_discord_thread_id_passed_to_send_discord(self)` (method) — Discord platform with thread_id passes it to _send_discord.
- L1435 `test_discord_no_thread_id_when_not_provided(self)` (method) — Discord platform without thread_id passes None.
- L1459 `TestSendDiscordMedia` (class) — _send_discord uploads media files via multipart/form-data.
- L1463 `_build_mock(response_status, response_data=None, response_text='error body')` (method) — Build a properly-structured aiohttp mock chain.
- L1479 `test_text_and_media_sends_both(self, tmp_path)` (method) — Text message is sent first, then each media file as multipart.
- L1495 `test_media_only_skips_text_post(self, tmp_path)` (method) — When message is empty and media is present, text POST is skipped.
- L1510 `test_missing_media_file_collected_as_warning(self)` (method) — Non-existent media paths produce warnings but don't fail.
- L1524 `test_media_upload_failure_collected_as_warning(self, tmp_path)` (method) — Failed media upload becomes a warning, text still succeeds.
- L1557 `test_no_text_no_media_returns_error(self)` (method) — Empty text with no media returns error dict.
- L1569 `test_multiple_media_files_uploaded_separately(self, tmp_path)` (method) — Each media file gets its own multipart POST.
- L1589 `TestSendToPlatformDiscordMedia` (class) — _send_to_platform routes Discord media correctly.
- L1592 `test_media_files_passed_on_last_chunk_only(self)` (method) — Discord media_files are only passed on the final chunk.
- L1619 `test_single_chunk_gets_media(self)` (method) — Short message (single chunk) gets media_files directly.
- L1640 `TestSendMatrixUrlEncoding` (class) — _send_matrix URL-encodes Matrix room IDs in the API path.
- L1643 `test_room_id_is_percent_encoded_in_url(self)` (method) — Matrix room IDs with ! and : are percent-encoded in the PUT URL.
- L1680 `TestDeriveForumThreadName` (class)
- L1681 `test_single_line_message(self)` (method)
- L1684 `test_multi_line_uses_first_line(self)` (method)
- L1687 `test_strips_markdown_heading(self)` (method)
- L1690 `test_strips_multiple_hash_levels(self)` (method)
- L1693 `test_empty_message_falls_back_to_default(self)` (method)
- L1696 `test_whitespace_only_falls_back(self)` (method)
- L1699 `test_hash_only_falls_back(self)` (method)
- L1702 `test_truncates_to_100_chars(self)` (method)
- L1707 `test_strips_whitespace_around_first_line(self)` (method)
- L1716 `TestSendDiscordForum` (class) — _send_discord creates thread posts for forum channels.
- L1720 `_build_mock(response_status, response_data=None, response_text='error body')` (method)
- L1736 `test_directory_forum_creates_thread(self)` (method) — Directory says 'forum' — creates a thread post.
- L1758 `test_directory_forum_skips_probe(self)` (method) — When directory says 'forum', no GET probe is made.
- L1772 `test_directory_channel_skips_forum(self)` (method) — When directory says 'channel', sends via normal messages endpoint.
- L1787 `test_directory_none_probes_and_detects_forum(self)` (method) — When directory has no entry, probes GET /channels/{id} and detects type 15.
- L1824 `test_directory_lookup_exception_falls_through_to_probe(self)` (method) — When lookup_channel_type raises, falls through to API probe.
- L1838 `test_forum_thread_creation_error(self)` (method) — Forum thread creation returning non-200/201 returns an error dict.
- L1853 `TestSendToPlatformDiscordForum` (class) — _send_to_platform delegates forum detection to _send_discord.
- L1856 `test_send_to_platform_discord_delegates_to_send_discord(self)` (method) — Discord messages are routed through _send_discord, which handles forum detection.
- L1875 `test_send_to_platform_discord_with_thread_id(self)` (method) — Thread ID is still passed through when sending to Discord.
- L1900 `TestSendDiscordForumMedia` (class) — _send_discord uploads media as part of the starter message when the target is a forum.
- L1904 `_build_thread_resp(thread_id='th_999', msg_id='msg_500')` (method)
- L1913 `test_forum_with_media_uses_multipart(self, tmp_path, monkeypatch)` (method) — Forum + media → single multipart POST to /threads carrying the starter + files.
- L1955 `test_forum_without_media_still_json_only(self, tmp_path, monkeypatch)` (method) — Forum + no media → JSON POST (no multipart overhead).
- L1983 `test_forum_missing_media_file_collected_as_warning(self, tmp_path, monkeypatch)` (method) — Missing media files produce warnings but the thread is still created.
- L2013 `TestForumProbeCache` (class) — _DISCORD_CHANNEL_TYPE_PROBE_CACHE memoizes forum detection results.
- L2016 `setup_method(self)` (method)
- L2020 `test_cache_round_trip(self)` (method)
- L2027 `test_probe_result_is_memoized(self, monkeypatch)` (method) — An API-probed channel type is cached so subsequent sends skip the probe.
- L2092 `_FakeSignalHttp` (class) — Stand-in for httpx.AsyncClient used as an async context manager.
- L2100 `__init__(self, responses)` (method)
- L2104 `__call__(self, *_a, **_kw)` (method)
- L2107 `__aenter__(self)` (method)
- L2110 `__aexit__(self, *_a)` (method)
- L2113 `post(self, url, json=None)` (method)
- L2127 `_install_signal_http(monkeypatch, fake)` (function) — Patch httpx.AsyncClient at the module level so the lazy import in
- L2135 `_patch_sendmsg_sleep_and_time(monkeypatch, capture: list)` (function) — Mock asyncio.sleep + time.monotonic in the signal_rate_limit
- L2163 `TestSendSignalChunking` (class)
- L2164 `test_text_only_single_rpc(self, monkeypatch)` (method)
- L2182 `test_chunks_attachments_above_max(self, tmp_path, monkeypatch)` (method) — 33 attachments → 2 batches; text only on first batch. Batch 1
- L2225 `test_full_followup_batch_emits_pacing_notice(self, tmp_path, monkeypatch)` (method) — 64 attachments → 2 full batches. Batch 1 needs 14 more tokens
- L2272 `test_429_with_retry_after_drives_exact_backoff(self, tmp_path, monkeypatch)` (method) — signal-cli ≥ v0.14.3 surfaces Retry-After under
- L2317 `test_429_without_retry_after_falls_back_to_default(self, tmp_path, monkeypatch)` (method) — Older signal-cli (< v0.14.3) doesn't surface Retry-After.
- L2346 `test_429_retry_exhaust_continues_to_next_batch(self, tmp_path, monkeypatch)` (method) — Both attempts on batch 0 fail; batch 1 still gets a chance.
- L2399 `test_non_rate_limit_error_returns_immediately(self, tmp_path, monkeypatch)` (method) — A non-429 RPC error should not retry — it returns an error result.
- L2422 `test_skipped_missing_files_reported_in_warnings(self, tmp_path, monkeypatch)` (method)
- L2448 `_FakePlatform` (class) — Stand-in for the gateway.config.Platform enum.  Holds the .value
- L2452 `__init__(self, value)` (method)
- L2456 `TestSendViaAdapterStandaloneFallback` (class) — Coverage for the out-of-process plugin-platform send path.
- L2467 `_make_entry(send_fn)` (method)
- L2479 `test_live_ntfy_adapter_receives_explicit_publish_topic(self, monkeypatch)` (method)
- L2510 `test_standalone_sender_fn_called_when_no_adapter(self, monkeypatch)` (method) — Registry has hook, runner ref returns None: the hook is awaited.
- L2544 `test_standalone_sender_fn_kwargs_forwarded(self, monkeypatch)` (method) — thread_id, media_files, and force_document all reach the hook.
- L2579 `test_standalone_sender_fn_absent_returns_helpful_error(self, monkeypatch)` (method) — Registry entry has no hook: the fall-through error explains both
- L2603 `test_standalone_sender_fn_raises_is_caught_and_formatted(self, monkeypatch)` (method) — Hook raises: error dict has 'Plugin standalone send failed: ...'
- L2627 `test_standalone_sender_fn_return_shape_passed_through(self, monkeypatch)` (method) — Hook returns success dict: passed through unchanged.
- L2657 `TestCheckSendMessage` (class) — The tool's check_fn governs whether the model sees ``send_message`` as
- L2671 `test_kanban_task_env_grants_access(self, monkeypatch)` (method) — Workers spawned by the dispatcher (HERMES_KANBAN_TASK set) must be
- L2683 `test_kanban_task_env_short_circuits_before_gateway_check(self, monkeypatch)` (method) — Honoring HERMES_KANBAN_TASK must not depend on importing or calling
- L2699 `test_messaging_platform_session_grants_access(self, monkeypatch)` (method) — Telegram/Discord/etc. sessions pass via the platform branch even
- L2710 `test_local_platform_falls_through_to_gateway_check(self, monkeypatch)` (method) — ``HERMES_SESSION_PLATFORM=local`` means CLI-style — must defer to
- L2722 `test_running_gateway_grants_access(self, monkeypatch)` (method) — Plain CLI session (no kanban task, empty platform) with a live
- L2733 `test_no_signals_means_unavailable(self, monkeypatch)` (method) — No kanban task, no platform, no gateway: tool is hidden.
- L2743 `test_gateway_status_import_error_is_swallowed(self, monkeypatch)` (method) — If gateway.status can't be imported (unusual deployment / partial
- L2756 `TestSendTelegramThreadNotFoundRetry` (class) — Tests for thread-not-found retry behaviour in _send_telegram (#27012).
- L2759 `test_is_thread_not_found_matches_expected_errors(self)` (method) — _is_telegram_thread_not_found should detect thread-not-found errors.
- L2771 `test_text_send_retries_without_thread_id_on_thread_not_found(self)` (method) — When thread is not found, the text send should retry without
- L2805 `test_disable_web_page_preview_not_leaked_to_media_sends(self)` (method) — disable_web_page_preview should only appear in text send, not media sends.
