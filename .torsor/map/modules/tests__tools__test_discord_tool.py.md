---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:43'
updated: '2026-06-08T00:38:43'
---

# tests/tools/test_discord_tool.py

Symbols in `tests/tools/test_discord_tool.py`.

- L36 `_mock_urlopen(response_data, status=200)` (function) — Create a mock for urllib.request.urlopen.
- L50 `TestCheckRequirements` (class)
- L51 `test_no_token(self, monkeypatch)` (method)
- L55 `test_empty_token(self, monkeypatch)` (method)
- L59 `test_valid_token(self, monkeypatch)` (method)
- L63 `test_get_bot_token(self, monkeypatch)` (method)
- L67 `test_get_bot_token_missing(self, monkeypatch)` (method)
- L76 `TestChannelTypeNames` (class)
- L77 `test_known_types(self)` (method)
- L85 `test_unknown_type(self)` (method)
- L93 `TestDiscordRequest` (class)
- L95 `test_get_request(self, mock_urlopen_fn)` (method)
- L108 `test_get_with_params(self, mock_urlopen_fn)` (method)
- L115 `test_post_with_body(self, mock_urlopen_fn)` (method)
- L123 `test_204_returns_none(self, mock_urlopen_fn)` (method)
- L130 `test_http_error(self, mock_urlopen_fn)` (method)
- L150 `TestDiscordServerValidation` (class)
- L151 `test_no_token(self, monkeypatch)` (method)
- L157 `test_unknown_action(self, monkeypatch)` (method)
- L164 `test_missing_required_guild_id(self, monkeypatch)` (method)
- L170 `test_missing_required_channel_id(self, monkeypatch)` (method)
- L176 `test_missing_required_message_id_for_delete(self, monkeypatch)` (method)
- L182 `test_missing_multiple_params(self, monkeypatch)` (method)
- L195 `TestListGuilds` (class)
- L197 `test_list_guilds(self, mock_req, monkeypatch)` (method)
- L214 `TestServerInfo` (class)
- L216 `test_server_info(self, mock_req, monkeypatch)` (method)
- L244 `TestListChannels` (class)
- L246 `test_list_channels_organized(self, mock_req, monkeypatch)` (method)
- L266 `test_empty_guild(self, mock_req, monkeypatch)` (method)
- L277 `TestChannelInfo` (class)
- L279 `test_channel_info(self, mock_req, monkeypatch)` (method)
- L296 `TestListRoles` (class)
- L298 `test_list_roles_sorted(self, mock_req, monkeypatch)` (method)
- L318 `TestMemberInfo` (class)
- L320 `test_member_info(self, mock_req, monkeypatch)` (method)
- L339 `TestSearchMembers` (class)
- L341 `test_search_members(self, mock_req, monkeypatch)` (method)
- L355 `test_search_members_limit_capped(self, mock_req, monkeypatch)` (method)
- L367 `TestFetchMessages` (class)
- L369 `test_fetch_messages(self, mock_req, monkeypatch)` (method)
- L388 `test_fetch_messages_with_pagination(self, mock_req, monkeypatch)` (method)
- L401 `TestListPins` (class)
- L403 `test_list_pins(self, mock_req, monkeypatch)` (method)
- L417 `TestPinUnpinDelete` (class)
- L419 `test_pin_message(self, mock_req, monkeypatch)` (method)
- L427 `test_unpin_message(self, mock_req, monkeypatch)` (method)
- L435 `test_delete_message(self, mock_req, monkeypatch)` (method)
- L448 `TestCreateThread` (class)
- L450 `test_create_standalone_thread(self, mock_req, monkeypatch)` (method)
- L463 `test_create_thread_from_message(self, mock_req, monkeypatch)` (method)
- L480 `TestRoleManagement` (class)
- L482 `test_add_role(self, mock_req, monkeypatch)` (method)
- L494 `test_remove_role(self, mock_req, monkeypatch)` (method)
- L507 `TestErrorHandling` (class)
- L509 `test_api_error_handled(self, mock_req, monkeypatch)` (method)
- L517 `test_unexpected_error_handled_admin(self, mock_req, monkeypatch)` (method)
- L525 `test_unexpected_error_handled_core(self, mock_req, monkeypatch)` (method)
- L537 `TestRegistration` (class)
- L538 `test_core_tool_registered(self)` (method)
- L547 `test_admin_tool_registered(self)` (method)
- L556 `test_core_schema_actions(self)` (method) — Core static schema should list only core actions.
- L563 `test_admin_schema_actions(self)` (method) — Admin static schema should list only admin actions.
- L571 `test_all_actions_covered(self)` (method) — Core + admin actions should cover all known actions.
- L576 `test_schema_parameter_bounds(self)` (method)
- L584 `test_core_schema_description(self)` (method) — Core schema description should mention core actions.
- L596 `test_admin_schema_description(self)` (method) — Admin schema description should mention admin actions.
- L608 `test_handler_callable(self)` (method)
- L620 `TestToolsetInclusion` (class)
- L621 `test_discord_tools_in_hermes_discord_toolset(self)` (method)
- L626 `test_discord_tools_not_in_core_tools(self)` (method)
- L631 `test_discord_tools_not_in_other_toolsets(self)` (method)
- L649 `TestCapabilityDetection` (class)
- L650 `setup_method(self)` (method)
- L653 `teardown_method(self)` (method)
- L657 `test_both_intents_enabled(self, mock_req)` (method)
- L666 `test_no_intents(self, mock_req)` (method)
- L674 `test_limited_intent_variants_counted(self, mock_req)` (method)
- L682 `test_only_members_intent(self, mock_req)` (method)
- L689 `test_detection_failure_is_permissive(self, mock_req)` (method) — If detection fails (network/401/revoked token), expose everything
- L700 `test_detection_is_cached(self, mock_req)` (method)
- L708 `test_force_refresh(self, mock_req)` (method)
- L715 `test_cache_is_keyed_by_token(self, mock_req)` (method) — Regression: token A's capabilities must not leak to token B.
- L751 `TestConfigAllowlist` (class)
- L753 `_reset_tools_logger(self)` (method) — Restore the ``tools`` logger level after cross-test pollution.
- L774 `test_empty_string_returns_none(self, monkeypatch)` (method) — Empty config means no allowlist — all actions visible.
- L782 `test_missing_key_returns_none(self, monkeypatch)` (method)
- L789 `test_comma_separated_string(self, monkeypatch)` (method)
- L797 `test_yaml_list(self, monkeypatch)` (method)
- L805 `test_unknown_names_dropped(self, monkeypatch, caplog)` (method)
- L815 `test_config_load_failure_is_permissive(self, monkeypatch)` (method) — If config can't be loaded at all, fall back to None (all allowed).
- L822 `test_unexpected_type_ignored(self, monkeypatch, caplog)` (method)
- L837 `TestAvailableActions` (class)
- L838 `test_all_available_when_unrestricted(self)` (method)
- L842 `test_no_members_intent_hides_member_actions(self)` (method)
- L850 `test_no_message_content_keeps_fetch_messages(self)` (method) — MESSAGE_CONTENT affects the content field, not the action.
- L858 `test_allowlist_intersects_with_intents(self)` (method) — Allowlist can only narrow — not re-enable intent-gated actions.
- L866 `test_empty_allowlist_yields_empty(self)` (method)
- L870 `test_allowlist_preserves_canonical_order(self)` (method)
- L881 `TestDynamicSchema` (class)
- L882 `setup_method(self)` (method)
- L885 `teardown_method(self)` (method)
- L889 `test_no_token_returns_none(self, mock_req, monkeypatch)` (method)
- L896 `test_full_intents_core_schema(self, mock_req, monkeypatch)` (method)
- L909 `test_full_intents_admin_schema(self, mock_req, monkeypatch)` (method)
- L924 `test_no_members_intent_removes_member_actions_from_admin_schema(self, mock_req, monkeypatch)` (method) — member_info is an admin action; it should be hidden when
- L941 `test_no_members_intent_hides_search_members_from_core(self, mock_req, monkeypatch)` (method) — search_members is a core action gated by GUILD_MEMBERS intent.
- L956 `test_no_message_content_adds_warning_note(self, mock_req, monkeypatch)` (method)
- L970 `test_config_allowlist_narrows_admin_schema(self, mock_req, monkeypatch)` (method)
- L984 `test_empty_allowlist_with_valid_values_hides_tools(self, mock_req, monkeypatch)` (method) — If the allowlist resolves to zero valid actions (e.g. all names
- L997 `test_backward_compat_wrapper(self, mock_req, monkeypatch)` (method) — get_dynamic_schema() should delegate to get_dynamic_schema_core().
- L1016 `TestRuntimeAllowlistEnforcement` (class)
- L1018 `test_denied_action_blocked_at_runtime(self, mock_req, monkeypatch)` (method)
- L1030 `test_allowed_action_proceeds(self, mock_req, monkeypatch)` (method)
- L1045 `Test403Enrichment` (class)
- L1046 `test_enrich_known_action(self)` (method)
- L1051 `test_enrich_unknown_action_includes_body(self)` (method)
- L1057 `test_403_in_runtime_is_enriched(self, mock_req, monkeypatch)` (method)
- L1071 `test_non_403_errors_are_not_enriched(self, mock_req, monkeypatch)` (method)
- L1087 `TestModelToolsIntegration` (class)
- L1088 `setup_method(self)` (method)
- L1095 `teardown_method(self)` (method)
- L1103 `test_discord_admin_schema_rebuilt_by_get_tool_definitions(self, mock_req, monkeypatch)` (method) — When model_tools.get_tool_definitions runs with discord_admin
- L1127 `test_discord_tools_dropped_when_allowlist_empties_them(self, mock_req, monkeypatch)` (method)
