---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/hermes_cli/test_profiles.py

Symbols in `tests/hermes_cli/test_profiles.py`.

- L44 `profile_env(tmp_path, monkeypatch)` (function) — Set up an isolated environment for profile tests.
- L62 `TestNormalizeProfileName` (class) — Tests for normalize_profile_name().
- L65 `test_title_case_normalized(self)` (method)
- L69 `test_default_case_insensitive(self)` (method)
- L73 `test_empty_raises(self)` (method)
- L80 `TestValidateProfileName` (class) — Tests for validate_profile_name().
- L84 `test_valid_names_accepted(self, name)` (method)
- L88 `test_uppercase_rejected(self)` (method)
- L94 `test_invalid_names_rejected(self, name)` (method)
- L98 `test_too_long_rejected(self)` (method)
- L103 `test_max_length_accepted(self)` (method)
- L108 `test_default_accepted(self)` (method)
- L112 `test_empty_string_rejected(self)` (method)
- L117 `test_reserved_names_rejected(self, name)` (method) — Reserved names collide with the Hermes install itself or with
- L129 `TestGetProfileDir` (class) — Tests for get_profile_dir().
- L132 `test_default_returns_hermes_home(self, profile_env)` (method)
- L137 `test_named_profile_returns_profiles_subdir(self, profile_env)` (method)
- L142 `test_named_profile_matching_is_case_insensitive(self, profile_env)` (method)
- L151 `TestCreateProfile` (class) — Tests for create_profile().
- L154 `test_creates_directory_with_subdirs(self, profile_env)` (method)
- L161 `test_duplicate_raises_file_exists(self, profile_env)` (method)
- L166 `test_default_raises_value_error(self, profile_env)` (method)
- L170 `test_invalid_name_raises_value_error(self, profile_env)` (method)
- L174 `test_clone_config_copies_files(self, profile_env)` (method)
- L188 `test_clone_config_copies_source_skills(self, profile_env)` (method)
- L205 `test_clone_all_copies_entire_tree(self, profile_env)` (method)
- L227 `test_clone_all_excludes_sibling_profiles_tree(self, profile_env)` (method) — --clone-all from default ~/.hermes must not copy profiles/* (nested explosion).
- L244 `test_clone_all_excludes_default_infrastructure(self, profile_env)` (method) — --clone-all from default profile excludes hermes-agent, .worktrees,
- L302 `test_clone_config_missing_files_skipped(self, profile_env)` (method) — Clone config gracefully skips files that don't exist in source.
- L316 `TestNoSkillsOptOut` (class) — Tests for `hermes profile create --no-skills` and the opt-out marker.
- L319 `test_no_skills_writes_marker_and_skips_seeding(self, profile_env)` (method)
- L335 `test_no_skills_conflicts_with_clone(self, profile_env)` (method)
- L344 `test_no_skills_conflicts_with_clone_all(self, profile_env)` (method)
- L353 `test_seed_profile_skills_respects_marker(self, profile_env)` (method) — seed_profile_skills() must no-op on opted-out profiles even when
- L368 `test_default_profile_gets_skills_seeded(self, profile_env, monkeypatch)` (method) — Sanity: without --no-skills, seed_profile_skills() runs the real
- L395 `test_delete_marker_re_enables_seeding(self, profile_env, monkeypatch)` (method) — Deleting .no-bundled-skills opts the profile back in.
- L426 `TestDeleteProfile` (class) — Tests for delete_profile().
- L429 `test_removes_directory(self, profile_env)` (method)
- L437 `test_default_raises_value_error(self, profile_env)` (method)
- L441 `test_nonexistent_raises_file_not_found(self, profile_env)` (method)
- L450 `TestListProfiles` (class) — Tests for list_profiles().
- L453 `test_returns_default_when_no_named_profiles(self, profile_env)` (method)
- L458 `test_includes_named_profiles(self, profile_env)` (method)
- L466 `test_sorted_alphabetically(self, profile_env)` (method)
- L474 `test_default_is_first(self, profile_env)` (method)
- L485 `TestActiveProfile` (class) — Tests for set_active_profile() / get_active_profile().
- L488 `test_set_and_get_roundtrip(self, profile_env)` (method)
- L493 `test_no_file_returns_default(self, profile_env)` (method)
- L496 `test_empty_file_returns_default(self, profile_env)` (method)
- L502 `test_set_to_default_removes_file(self, profile_env)` (method)
- L512 `test_set_nonexistent_raises(self, profile_env)` (method)
- L521 `TestGetActiveProfileName` (class) — Tests for get_active_profile_name().
- L524 `test_default_hermes_home_returns_default(self, profile_env)` (method)
- L528 `test_profile_path_returns_profile_name(self, profile_env, monkeypatch)` (method)
- L535 `test_custom_path_returns_default(self, profile_env, monkeypatch)` (method) — A custom HERMES_HOME (Docker, etc.) IS the default root.
- L551 `TestResolveProfileEnv` (class) — Tests for resolve_profile_env().
- L554 `test_existing_profile_returns_path(self, profile_env)` (method)
- L560 `test_default_returns_default_home(self, profile_env)` (method)
- L565 `test_nonexistent_raises_file_not_found(self, profile_env)` (method)
- L569 `test_invalid_name_raises_value_error(self, profile_env)` (method)
- L578 `TestAliasCollision` (class) — Tests for check_alias_collision().
- L581 `test_normal_name_returns_none(self, profile_env)` (method)
- L588 `test_reserved_name_returns_message(self, profile_env)` (method)
- L593 `test_subcommand_returns_message(self, profile_env)` (method)
- L598 `test_default_is_reserved(self, profile_env)` (method)
- L603 `test_uses_where_on_windows(self, profile_env, monkeypatch)` (method)
- L611 `test_uses_which_on_posix(self, profile_env, monkeypatch)` (method)
- L619 `test_windows_checks_bat_extension(self, profile_env, monkeypatch)` (method)
- L637 `TestWrapperScript` (class) — Tests for create_wrapper_script() and remove_wrapper_script().
- L640 `test_creates_sh_on_posix(self, profile_env, monkeypatch)` (method)
- L650 `test_creates_bat_on_windows(self, profile_env, monkeypatch)` (method)
- L661 `test_remove_finds_bat_on_windows(self, profile_env, monkeypatch)` (method)
- L671 `test_remove_finds_sh_on_posix(self, profile_env, monkeypatch)` (method)
- L681 `test_remove_returns_false_when_absent(self, profile_env)` (method)
- L685 `test_custom_alias_target_on_posix(self, profile_env, monkeypatch)` (method)
- L697 `test_custom_alias_target_on_windows(self, profile_env, monkeypatch)` (method)
- L716 `TestFindAliasForProfile` (class) — Tests for find_alias_for_profile() and alias display in list/show.
- L719 `test_profile_named_alias(self, profile_env, monkeypatch)` (method)
- L725 `test_custom_alias_name_preferred(self, profile_env, monkeypatch)` (method)
- L733 `test_no_alias_returns_none(self, profile_env, monkeypatch)` (method)
- L738 `test_ignores_unrelated_files(self, profile_env, monkeypatch)` (method)
- L747 `test_custom_alias_on_windows(self, profile_env, monkeypatch)` (method)
- L754 `test_list_profiles_surfaces_custom_alias(self, profile_env, monkeypatch)` (method)
- L773 `TestRenameProfile` (class) — Tests for rename_profile().
- L776 `test_renames_directory(self, profile_env)` (method)
- L790 `test_renames_root_honcho_host_without_changing_ai_peer(self, profile_env)` (method)
- L817 `test_pins_ai_peer_when_absent_on_honcho_host_rename(self, profile_env)` (method)
- L835 `test_does_not_overwrite_existing_honcho_host_on_rename(self, profile_env)` (method)
- L853 `test_default_raises_value_error(self, profile_env)` (method)
- L857 `test_rename_to_default_raises_value_error(self, profile_env)` (method)
- L862 `test_nonexistent_raises_file_not_found(self, profile_env)` (method)
- L866 `test_target_exists_raises_file_exists(self, profile_env)` (method)
- L877 `TestExportImport` (class) — Tests for export_profile() / import_profile().
- L880 `test_export_creates_tar_gz(self, profile_env, tmp_path)` (method)
- L893 `test_import_restores_from_archive(self, profile_env, tmp_path)` (method)
- L912 `test_import_to_existing_name_raises(self, profile_env, tmp_path)` (method)
- L924 `test_import_with_explicit_name_does_not_mutate_existing_archive_root_profile(self, profile_env, tmp_path)` (method)
- L945 `test_import_rejects_archive_with_multiple_top_level_directories(self, profile_env, tmp_path)` (method)
- L965 `test_import_rejects_traversal_archive_member(self, profile_env, tmp_path)` (method)
- L982 `test_import_rejects_absolute_archive_member(self, profile_env, tmp_path)` (method)
- L999 `test_export_nonexistent_raises(self, profile_env, tmp_path)` (method)
- L1007 `test_export_default_creates_valid_archive(self, profile_env, tmp_path)` (method) — Exporting the default profile produces a valid tar.gz.
- L1019 `test_export_default_includes_profile_data(self, profile_env, tmp_path)` (method) — Profile data files end up in the archive (credentials excluded).
- L1041 `test_export_default_excludes_infrastructure(self, profile_env, tmp_path)` (method) — Repo checkout, worktrees, profiles, databases are excluded.
- L1088 `test_export_default_excludes_pycache_at_any_depth(self, profile_env, tmp_path)` (method) — __pycache__ dirs are excluded even inside nested directories.
- L1105 `test_import_default_without_name_raises(self, profile_env, tmp_path)` (method) — Importing a default export without --name gives clear guidance.
- L1117 `test_import_default_with_explicit_default_name_raises(self, profile_env, tmp_path)` (method) — Explicitly importing as 'default' is also rejected.
- L1129 `test_import_default_export_with_new_name_roundtrip(self, profile_env, tmp_path)` (method) — Export default → import under a different name → data preserved.
- L1151 `TestProfileIsolation` (class) — Verify that two profiles have completely separate paths.
- L1154 `test_separate_config_paths(self, profile_env)` (method)
- L1162 `test_separate_state_db_paths(self, profile_env)` (method)
- L1167 `test_separate_skills_paths(self, profile_env)` (method)
- L1182 `TestInternalHelpers` (class) — Tests for _get_profiles_root() and _get_default_hermes_home().
- L1185 `test_profiles_root_under_home(self, profile_env)` (method)
- L1190 `test_default_hermes_home(self, profile_env)` (method)
- L1195 `test_profiles_root_docker_deployment(self, tmp_path, monkeypatch)` (method) — In Docker (HERMES_HOME outside ~/.hermes), profiles go under HERMES_HOME.
- L1204 `test_default_hermes_home_docker(self, tmp_path, monkeypatch)` (method) — In Docker, _get_default_hermes_home() returns HERMES_HOME itself.
- L1213 `test_profiles_root_profile_mode(self, tmp_path, monkeypatch)` (method) — In profile mode (HERMES_HOME under ~/.hermes), profiles root is still ~/.hermes/profiles.
- L1223 `test_active_profile_path_docker(self, tmp_path, monkeypatch)` (method) — In Docker, active_profile file lives under HERMES_HOME.
- L1233 `test_create_profile_docker(self, tmp_path, monkeypatch)` (method) — Profile created in Docker lands under HERMES_HOME/profiles/.
- L1244 `test_active_profile_name_docker_default(self, tmp_path, monkeypatch)` (method) — In Docker (no profile active), get_active_profile_name() returns 'default'.
- L1252 `test_active_profile_name_docker_profile(self, tmp_path, monkeypatch)` (method) — In Docker with a profile active, get_active_profile_name() returns the profile name.
- L1266 `TestEdgeCases` (class) — Additional edge-case tests.
- L1269 `test_create_profile_returns_correct_path(self, profile_env)` (method)
- L1275 `test_list_profiles_default_info_fields(self, profile_env)` (method)
- L1282 `test_gateway_running_check_with_pid_file(self, profile_env)` (method) — Verify _check_gateway_running uses the shared gateway PID validator.
- L1295 `test_gateway_running_check_plain_pid(self, profile_env)` (method) — Shared PID validator returning None means the profile is not running.
- L1308 `test_profile_name_boundary_single_char(self)` (method) — Single alphanumeric character is valid.
- L1313 `test_profile_name_boundary_all_hyphens(self)` (method) — Name starting with hyphen is invalid.
- L1318 `test_profile_name_underscore_start(self)` (method) — Name starting with underscore is invalid (must start with [a-z0-9]).
- L1323 `test_clone_from_named_profile(self, profile_env)` (method) — Clone config from a named (non-default) profile.
- L1337 `test_delete_clears_active_profile(self, profile_env)` (method) — Deleting the active profile resets active to default.
