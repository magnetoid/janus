---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/hermes_cli/test_default_interface_resolution.py

Symbols in `tests/hermes_cli/test_default_interface_resolution.py`.

- L34 `_reset_early_cache(monkeypatch)` (function)
- L43 `_args(**kw)` (function)
- L49 `_patch_config(monkeypatch, interface)` (function)
- L60 `TestResolveUseTui` (class)
- L61 `test_cli_flag_beats_config_tui(self, monkeypatch)` (method)
- L65 `test_cli_flag_beats_tui_flag_and_env(self, monkeypatch)` (method)
- L70 `test_tui_flag_beats_config_cli(self, monkeypatch)` (method)
- L74 `test_env_beats_config_cli(self, monkeypatch)` (method)
- L79 `test_config_tui_with_no_flags(self, monkeypatch)` (method)
- L83 `test_config_cli_is_default(self, monkeypatch)` (method)
- L87 `test_interface_value_is_case_insensitive(self, monkeypatch)` (method)
- L91 `test_load_config_failure_falls_back_to_cli(self, monkeypatch)` (method)
- L104 `TestWantsTuiEarly` (class)
- L106 `home_with_interface(self, tmp_path, monkeypatch)` (method)
- L116 `test_config_tui_bare_argv(self, home_with_interface)` (method)
- L120 `test_cli_flag_overrides_config_tui(self, home_with_interface)` (method)
- L124 `test_tui_flag_with_config_cli(self, home_with_interface)` (method)
- L128 `test_env_with_config_cli(self, home_with_interface, monkeypatch)` (method)
- L133 `test_config_cli_bare_argv(self, home_with_interface)` (method)
- L137 `test_missing_config_defaults_to_cli(self, tmp_path, monkeypatch)` (method)
- L143 `test_unreadable_config_defaults_to_cli(self, tmp_path, monkeypatch)` (method)
- L154 `TestParserFlags` (class)
- L155 `_parser(self)` (method)
- L161 `test_top_level_cli_flag(self)` (method)
- L165 `test_top_level_tui_flag(self)` (method)
- L169 `test_chat_subcommand_cli_flag(self)` (method)
- L173 `test_chat_subcommand_tui_flag(self)` (method)
- L177 `test_cli_and_tui_are_relaunch_inherited(self)` (method)
- L188 `test_default_config_interface_is_cli()` (function)
