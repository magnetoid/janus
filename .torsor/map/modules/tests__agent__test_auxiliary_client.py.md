---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:41'
updated: '2026-06-08T00:38:41'
---

# tests/agent/test_auxiliary_client.py

Symbols in `tests/agent/test_auxiliary_client.py`.

- L35 `_jwt_with_claims(claims: dict)` (function)
- L42 `_clean_env(monkeypatch)` (function) — Strip provider env vars so each test starts clean.
- L63 `codex_auth_dir(tmp_path, monkeypatch)` (function) — Provide a writable ~/.codex/ directory with a valid auth.json.
- L81 `TestAuxiliaryMaxTokensParam` (class)
- L82 `test_uses_max_completion_tokens_for_github_copilot_custom_base(self)` (method)
- L87 `test_uses_max_completion_tokens_for_github_copilot_custom_base_path(self)` (method)
- L93 `TestBuildCallKwargsMaxTokens` (class) — _build_call_kwargs should not cap output by default (#34530).
- L115 `test_omits_max_tokens_for_openai_compatible(self, provider, model, base_url)` (method)
- L135 `test_keeps_max_tokens_on_anthropic_wire(self, provider, model, base_url)` (method)
- L149 `TestNormalizeAuxProvider` (class)
- L150 `test_maps_github_copilot_aliases(self)` (method)
- L155 `test_maps_github_copilot_acp_aliases(self)` (method)
- L160 `TestReadCodexAccessToken` (class)
- L161 `test_valid_auth_store(self, tmp_path, monkeypatch)` (method)
- L176 `test_pool_without_selected_entry_falls_back_to_auth_store(self, tmp_path, monkeypatch)` (method)
- L190 `test_missing_returns_none(self, tmp_path, monkeypatch)` (method)
- L199 `test_empty_token_returns_none(self, tmp_path, monkeypatch)` (method)
- L214 `test_malformed_json_returns_none(self, tmp_path)` (method)
- L222 `test_missing_tokens_key_returns_none(self, tmp_path)` (method)
- L231 `test_expired_jwt_returns_none(self, tmp_path, monkeypatch)` (method) — Expired JWT tokens should be skipped so auto chain continues.
- L257 `test_valid_jwt_returns_token(self, tmp_path, monkeypatch)` (method) — Non-expired JWT tokens should be returned.
- L281 `test_non_jwt_token_passes_through(self, tmp_path, monkeypatch)` (method) — Non-JWT tokens (no dots) should be returned as-is.
- L298 `TestResolveXaiOAuthForAux` (class)
- L299 `test_uses_pool_backed_credentials_without_singleton(self, tmp_path, monkeypatch)` (method) — Auxiliary xAI OAuth must see pool-only credentials.
- L337 `test_pool_backed_credentials_honor_base_url_env_override(self, tmp_path, monkeypatch)` (method)
- L369 `TestAnthropicOAuthFlag` (class) — Test that OAuth tokens get is_oauth=True in auxiliary Anthropic client.
- L372 `test_oauth_token_sets_flag(self, monkeypatch)` (method) — OAuth tokens (sk-ant-oat01-*) should create client with is_oauth=True.
- L385 `test_api_key_no_oauth_flag(self, monkeypatch)` (method) — Regular API keys (sk-ant-api-*) should create client with is_oauth=False.
- L398 `test_pool_entry_takes_priority_over_legacy_resolution(self)` (method)
- L424 `TestBuildCodexClient` (class)
- L425 `test_pool_without_selected_entry_falls_back_to_auth_store(self)` (method)
- L441 `test_rejects_missing_model(self)` (method) — Callers must pass an explicit model; no hardcoded default.
- L449 `test_cached_codex_client_rebuilds_when_pool_entry_changes(self)` (method)
- L496 `TestResolveProviderClientUniversalModelFallback` (class) — resolve_provider_client() picks a sensible model when callers pass none (#31845).
- L518 `test_empty_model_for_oauth_provider_falls_back_to_main_model(self)` (method) — xai-oauth: no catalog default → uses main model.
- L546 `test_empty_model_for_codex_also_uses_main_model(self)` (method) — openai-codex: symmetric with xai-oauth — same universal fallback.
- L574 `test_empty_model_for_catalog_provider_uses_catalog_default(self)` (method) — anthropic / nous / openrouter / etc.: catalog default wins
- L616 `test_explicit_model_takes_precedence_over_fallbacks(self)` (method) — Step 1: caller-passed model wins.  Per-task config
- L645 `TestExpiredCodexFallback` (class) — Test that expired Codex tokens don't block the auto chain.
- L648 `test_expired_codex_falls_through_to_next(self, tmp_path, monkeypatch)` (method) — When Codex token is expired, auto chain should skip it and try next provider.
- L681 `test_expired_codex_openrouter_wins(self, tmp_path, monkeypatch)` (method) — With expired Codex + OpenRouter key, OpenRouter should win (1st in chain).
- L723 `test_expired_codex_custom_endpoint_wins(self, tmp_path, monkeypatch)` (method) — With expired Codex + custom endpoint (Ollama), custom should win (3rd in chain).
- L755 `test_hermes_oauth_file_sets_oauth_flag(self, monkeypatch)` (method) — OAuth-style tokens should get is_oauth=*** (token is not sk-ant-api-*).
- L768 `test_jwt_missing_exp_passes_through(self, tmp_path, monkeypatch)` (method) — JWT with valid JSON but no exp claim should pass through.
- L790 `test_jwt_invalid_json_payload_passes_through(self, tmp_path, monkeypatch)` (method) — JWT with valid base64 but invalid JSON payload should pass through.
- L811 `test_claude_code_oauth_env_sets_flag(self, monkeypatch)` (method) — CLAUDE_CODE_OAUTH_TOKEN env var should get is_oauth=True.
- L824 `TestExplicitProviderRouting` (class) — Test explicit provider selection bypasses auto chain correctly.
- L827 `test_explicit_anthropic_api_key(self, monkeypatch)` (method) — provider='anthropic' + regular API key should work with is_oauth=False.
- L838 `test_explicit_openrouter_pool_exhausted_logs_precise_warning(self, monkeypatch, caplog)` (method)
- L854 `test_explicit_openrouter_missing_env_keeps_not_set_warning(self, monkeypatch, caplog)` (method)
- L866 `TestGetTextAuxiliaryClient` (class) — Test the full resolution chain for get_text_auxiliary_client.
- L869 `test_codex_pool_entry_takes_priority_over_auth_store(self)` (method)
- L895 `test_returns_none_when_nothing_available(self, monkeypatch)` (method)
- L906 `test_custom_endpoint_uses_codex_wrapper_when_runtime_requests_responses_api(self)` (method)
- L922 `TestVisionClientFallback` (class) — Vision client auto mode resolves known-good multimodal backends.
- L925 `test_vision_auto_includes_active_provider_when_configured(self, monkeypatch)` (method) — Active provider appears in available backends when credentials exist.
- L939 `test_resolve_provider_client_returns_native_anthropic_wrapper(self, monkeypatch)` (method)
- L953 `TestAuxiliaryPoolAwareness` (class)
- L954 `test_try_nous_uses_pool_entry(self)` (method)
- L988 `test_try_nous_uses_portal_recommendation_for_text(self)` (method) — When the Portal recommends a compaction model, _try_nous honors it.
- L1006 `test_try_nous_uses_portal_recommendation_for_vision(self)` (method) — Vision tasks should ask for the vision-specific recommendation.
- L1022 `test_try_nous_falls_back_when_recommendation_lookup_raises(self)` (method) — If the Portal lookup throws, we must still return a usable model.
- L1037 `test_call_llm_retries_nous_after_401(self)` (method)
- L1065 `test_call_llm_refreshes_nous_after_free_tier_block_when_account_paid(self)` (method)
- L1107 `test_async_call_llm_retries_nous_after_401(self)` (method)
- L1136 `test_async_call_llm_refreshes_nous_after_free_tier_block_when_account_paid(self)` (method)
- L1177 `test_cached_gmi_client_keeps_explicit_slash_model_override(self)` (method)
- L1214 `TestIsPaymentError` (class) — _is_payment_error detects 402 and credit-related errors.
- L1217 `test_402_status_code(self)` (method)
- L1222 `test_402_with_credits_message(self)` (method)
- L1227 `test_429_with_credits_message(self)` (method)
- L1232 `test_404_free_tier_model_block_is_payment(self)` (method)
- L1240 `test_404_generic_not_found_is_not_payment(self)` (method)
- L1245 `test_429_without_credits_message_is_not_payment(self)` (method) — Normal rate limits should NOT be treated as payment errors.
- L1251 `test_generic_500_is_not_payment(self)` (method)
- L1256 `test_no_status_code_with_billing_message(self)` (method)
- L1260 `test_no_status_code_no_message(self)` (method)
- L1266 `test_429_quota_exceeded(self)` (method) — Cloud provider quota exhaustion (e.g. Vertex AI) is a payment error.
- L1272 `test_429_too_many_tokens_per_day(self)` (method) — Bedrock / LiteLLM daily token limit is a payment error.
- L1278 `test_429_daily_limit_phrase(self)` (method) — Generic 'daily limit' phrasing is a payment error.
- L1284 `test_429_resource_exhausted_grpc(self)` (method) — Vertex AI gRPC RESOURCE_EXHAUSTED maps to payment error.
- L1290 `test_429_daily_quota_phrase(self)` (method) — 'daily quota' phrasing is a payment error.
- L1296 `test_429_transient_rate_limit_not_quota(self)` (method) — Transient 429 rate limit without quota keywords is NOT a payment error.
- L1303 `TestIsModelNotFoundError` (class) — _is_model_not_found_error detects stale/invalid model 404s, distinct
- L1307 `test_nous_openrouter_catalog_404(self)` (method) — The exact incident error: a Portal-recommended model dropped from
- L1317 `test_openai_style_model_does_not_exist(self)` (method)
- L1322 `test_invalid_model_id_400(self)` (method)
- L1327 `test_no_such_model(self)` (method)
- L1332 `test_billing_404_is_not_model_not_found(self)` (method) — Free-tier / credit 404s belong to _is_payment_error, not here —
- L1342 `test_out_of_funds_404_is_not_model_not_found(self)` (method)
- L1350 `test_rate_limit_is_not_model_not_found(self)` (method)
- L1355 `test_500_is_not_model_not_found(self)` (method)
- L1361 `TestRefreshNousRecommendedModel` (class) — _refresh_nous_recommended_model picks a fresh model after a stale 404.
- L1364 `test_returns_fresh_portal_recommendation(self, monkeypatch)` (method)
- L1373 `test_falls_back_to_default_when_portal_matches_stale(self, monkeypatch)` (method) — If the Portal still recommends the model that just 404'd, fall back
- L1384 `test_falls_back_to_default_when_portal_unavailable(self, monkeypatch)` (method)
- L1393 `test_returns_none_when_no_distinct_alternative(self, monkeypatch)` (method) — When the failed model IS the default and the Portal has nothing
- L1405 `TestIsRateLimitError` (class) — _is_rate_limit_error detects 429 rate-limit errors warranting fallback.
- L1408 `test_429_with_rate_limit_message(self)` (method)
- L1413 `test_429_with_resets_in_message(self)` (method) — Nous-style 429: 'resets in 3508s'.
- L1419 `test_429_with_too_many_requests(self)` (method)
- L1424 `test_429_without_billing_keywords_is_rate_limit(self)` (method) — Generic 429 without billing keywords = likely a rate limit.
- L1430 `test_429_with_credits_message_is_not_rate_limit(self)` (method) — Billing-related 429 should NOT be classified as rate limit.
- L1436 `test_429_with_billing_message_is_not_rate_limit(self)` (method)
- L1441 `test_402_is_not_rate_limit(self)` (method)
- L1446 `test_500_is_not_rate_limit(self)` (method)
- L1451 `test_openai_ratelimiterror_classname(self)` (method) — OpenAI SDK RateLimitError may omit .status_code — detect by class name.
- L1459 `test_no_status_code_no_keywords_is_not_rate_limit(self)` (method)
- L1464 `TestGetProviderChain` (class) — _get_provider_chain() resolves functions at call time (testable).
- L1467 `test_returns_four_entries(self)` (method)
- L1477 `test_picks_up_patched_functions(self)` (method) — Patches on _try_* functions must be visible in the chain.
- L1485 `TestTryPaymentFallback` (class) — _try_payment_fallback skips the failed provider and tries alternatives.
- L1489 `_clear_unhealthy_cache(self)` (method) — Earlier tests in this file call _mark_provider_unhealthy() which
- L1502 `test_skips_failed_provider(self)` (method)
- L1512 `test_returns_none_when_no_fallback(self)` (method)
- L1522 `test_codex_alias_maps_to_chain_label(self)` (method) — 'codex' should map to 'openai-codex' in the skip set.
- L1531 `test_codex_not_in_fallback_chain(self)` (method) — Codex is deliberately NOT a fallback rung (shifting model allow-list).
- L1548 `TestCallLlmPaymentFallback` (class) — call_llm() retries with a different provider on 402 / payment / rate-limit errors.
- L1551 `_make_402_error(self, msg='Payment Required: insufficient credits')` (method)
- L1556 `_make_429_rate_limit_error(self, msg='Rate limit exceeded, try again in 60 seconds')` (method)
- L1561 `test_non_payment_error_not_caught(self, monkeypatch)` (method) — Non-payment/non-connection errors (500) should NOT trigger fallback.
- L1580 `test_429_rate_limit_triggers_fallback(self, monkeypatch)` (method) — 429 rate-limit errors should trigger fallback to next provider.
- L1607 `TestAuxiliaryFallbackLayering` (class) — Explicit-provider users get layered fallback: configured_chain → main agent → warn.
- L1610 `_make_payment_err(self)` (method)
- L1615 `test_explicit_provider_uses_configured_chain_first(self, monkeypatch, caplog)` (method) — When a user has fallback_chain configured, it's tried BEFORE the main agent model.
- L1646 `test_explicit_provider_falls_back_to_main_when_chain_exhausted(self, monkeypatch)` (method) — If configured fallback_chain returns nothing, main agent model is tried next.
- L1673 `test_warning_emitted_when_all_fallbacks_exhausted(self, monkeypatch, caplog)` (method) — When chain AND main model both fail, a user-visible warning fires before re-raise.
- L1700 `TestTryMainAgentModelFallback` (class) — _try_main_agent_model_fallback resolves the user's main provider+model as a safety net.
- L1703 `test_returns_none_when_main_provider_is_auto(self)` (method)
- L1710 `test_returns_none_when_failed_provider_equals_main(self)` (method) — If the thing that failed IS the main model, no point retrying it.
- L1718 `test_resolves_main_provider_client(self)` (method)
- L1731 `test_skips_when_main_provider_is_unhealthy(self)` (method)
- L1745 `test_resolve_api_key_provider_skips_unconfigured_anthropic(monkeypatch)` (function) — _resolve_api_key_provider must not try anthropic when user never configured it.
- L1797 `TestIsConnectionError` (class) — Tests for _is_connection_error detection.
- L1800 `test_connection_refused(self)` (method)
- L1805 `test_timeout(self)` (method)
- L1810 `test_dns_failure(self)` (method)
- L1815 `test_normal_api_error_not_connection(self)` (method)
- L1821 `test_500_not_connection(self)` (method)
- L1828 `TestKimiTemperatureOmitted` (class) — Kimi/Moonshot models should have temperature OMITTED from API kwargs.
- L1853 `test_kimi_models_omit_temperature(self, model)` (method) — No kimi model should have a temperature key in kwargs.
- L1866 `test_kimi_for_coding_no_temperature_when_none(self)` (method) — When caller passes temperature=None, still no temperature key.
- L1879 `test_sync_call_omits_temperature(self)` (method)
- L1904 `test_async_call_omits_temperature(self)` (method)
- L1936 `test_non_kimi_models_preserve_temperature(self, model)` (method)
- L1956 `test_kimi_k2_5_omits_temperature_regardless_of_endpoint(self, base_url)` (method) — Temperature is omitted regardless of which Kimi endpoint is used.
- L1976 `TestStaleBaseUrlWarning` (class) — _resolve_auto() warns when OPENAI_BASE_URL conflicts with config provider (#5161).
- L1979 `test_warns_when_openai_base_url_set_with_named_provider(self, monkeypatch, caplog)` (method) — Warning fires when OPENAI_BASE_URL is set but provider is a named provider.
- L1997 `TestAuxiliaryTaskExtraBody` (class)
- L1998 `test_sync_call_merges_task_extra_body_from_config(self)` (method)
- L2032 `test_async_call_explicit_extra_body_overrides_task_config(self)` (method)
- L2060 `test_no_warning_when_provider_is_custom(self, monkeypatch, caplog)` (method) — No warning when the provider is 'custom' — OPENAI_BASE_URL is expected.
- L2079 `test_no_warning_when_provider_is_named_custom(self, monkeypatch, caplog)` (method) — No warning when the provider is 'custom:myname' — base_url comes from config.
- L2096 `test_no_warning_when_openai_base_url_not_set(self, monkeypatch, caplog)` (method) — No warning when OPENAI_BASE_URL is absent.
- L2115 `TestAnthropicCompatImageConversion` (class) — Tests for _is_anthropic_compat_endpoint and _convert_openai_images_to_anthropic.
- L2118 `test_known_providers_detected(self)` (method)
- L2123 `test_openrouter_not_detected(self)` (method)
- L2128 `test_url_based_detection(self)` (method)
- L2134 `test_base64_image_converted(self)` (method)
- L2150 `test_url_image_converted(self)` (method)
- L2164 `test_text_only_messages_unchanged(self)` (method)
- L2170 `test_jpeg_media_type_parsed(self)` (method)
- L2181 `test_base64_video_converted_to_video_block(self)` (method)
- L2199 `test_video_media_type_parsed_from_data_uri(self)` (method)
- L2210 `test_url_video_converted_to_video_block(self)` (method)
- L2223 `test_mixed_image_and_video_both_converted(self)` (method)
- L2237 `_AuxAuth401` (class)
- L2240 `__init__(self, message='Provided authentication token is expired')` (method)
- L2244 `_DummyResponse` (class)
- L2245 `__init__(self, text='ok')` (method)
- L2249 `_FailingThenSuccessCompletions` (class)
- L2250 `__init__(self)` (method)
- L2253 `create(self, **kwargs)` (method)
- L2260 `_AsyncFailingThenSuccessCompletions` (class)
- L2261 `__init__(self)` (method)
- L2264 `create(self, **kwargs)` (method)
- L2271 `TestAuxiliaryAuthRefreshRetry` (class)
- L2272 `test_call_llm_refreshes_codex_on_401_for_vision(self)` (method)
- L2298 `test_call_llm_refreshes_codex_on_401_for_non_vision(self)` (method)
- L2324 `test_call_llm_refreshes_anthropic_on_401_for_non_vision(self)` (method)
- L2351 `test_async_call_llm_refreshes_codex_on_401_for_vision(self)` (method)
- L2377 `test_refresh_provider_credentials_force_refreshes_anthropic_oauth_and_evicts_cache(self, monkeypatch)` (method)
- L2408 `test_async_call_llm_refreshes_anthropic_on_401_for_non_vision(self)` (method)
- L2435 `TestAuxiliaryPoolRotationRetry` (class)
- L2436 `test_call_llm_rotates_explicit_codex_pool_on_429(self)` (method)
- L2486 `test_async_call_llm_rotates_explicit_codex_pool_on_429(self)` (method)
- L2536 `TestCodexAdapterReasoningTranslation` (class) — Verify _CodexCompletionsAdapter translates extra_body.reasoning
- L2547 `_build_adapter()` (method) — Build a _CodexCompletionsAdapter with a mocked responses.create().
- L2590 `test_reasoning_effort_medium_translated_to_top_level(self)` (method)
- L2599 `test_reasoning_effort_minimal_clamped_to_low(self)` (method) — Codex backend rejects 'minimal'; adapter clamps to 'low' per main transport.
- L2609 `test_reasoning_effort_low_passed_through(self)` (method)
- L2617 `test_reasoning_effort_high_passed_through(self)` (method)
- L2625 `test_reasoning_disabled_omits_reasoning_and_include(self)` (method)
- L2634 `test_reasoning_default_effort_when_only_enabled_flag(self)` (method) — extra_body={"reasoning": {}} (truthy enabled by omission) → default 'medium'.
- L2644 `test_no_extra_body_means_no_reasoning_keys(self)` (method) — Baseline: without extra_body, no reasoning/include is sent (preserves
- L2652 `test_extra_body_without_reasoning_key_is_noop(self)` (method)
- L2661 `test_non_dict_reasoning_value_is_ignored_gracefully(self)` (method) — Defensive: if a caller accidentally passes a string/None, we
- L2671 `test_reasoning_effort_null_falls_back_to_medium(self)` (method) — Parity with agent/transports/codex.py::build_kwargs() — falsy
- L2684 `test_reasoning_effort_empty_string_falls_back_to_medium(self)` (method) — Empty-string effort (e.g. ``effort: ""`` in YAML) is falsy in
- L2696 `test_reasoning_effort_zero_falls_back_to_medium(self)` (method) — Numeric ``0`` is also falsy — the docstring lists it explicitly,
- L2709 `TestVisionAutoSkipsKimiCoding` (class) — _resolve_auto vision branch skips providers that have no vision on
- L2716 `test_kimi_coding_skipped_falls_through_to_openrouter(self, monkeypatch)` (method) — kimi-coding as main + vision auto → OpenRouter (not kimi).
- L2755 `test_kimi_coding_cn_skipped_too(self, monkeypatch)` (method) — Same skip applies to the CN variant.
- L2781 `test_explicit_override_to_kimi_coding_still_honored(self, monkeypatch)` (method) — When a user *explicitly* requests kimi-coding for vision (e.g.
- L2803 `test_skip_set_covers_exactly_known_entries(self)` (method) — Guard against accidental widening of the skip list.
- L2812 `TestCodexAuxiliaryAdapterTimeout` (class)
- L2813 `test_forwards_timeout_to_responses_create(self)` (method)
- L2849 `test_enforces_total_timeout_while_stream_keeps_emitting_events(self)` (method)
- L2875 `TestCodexAuxiliaryAdapterNullOutputRecovery` (class)
- L2876 `test_recovers_output_item_when_terminal_event_has_null_output(self)` (method) — Regression for #11179 in auxiliary calls.
- L2917 `test_handles_final_output_is_none_after_consumer(self)` (method) — Regression for #33368 — defense against ``final.output`` being ``None``.
- L2979 `TestAuxiliaryClientPoisonedCacheEviction` (class) — Connection/timeout errors must evict the cached aux client.
- L2988 `test_evict_cached_client_instance_drops_direct_match(self)` (method)
- L3007 `test_evict_cached_client_instance_walks_codex_wrapper(self)` (method) — Closing the underlying OpenAI client must evict the Codex shim.
- L3029 `test_evict_cached_client_instance_handles_none_and_misses(self)` (method)
- L3035 `test_evict_cached_client_instance_walks_async_wrapper(self)` (method) — async_mode is part of the cache key so sync and async share the same
- L3071 `test_codex_timeout_evicts_cached_wrapper(self)` (method) — The timeout closer evicts the cache entry that wraps the closed client.
- L3118 `test_call_llm_evicts_on_connection_error_with_explicit_provider(self)` (method) — Connection error on an explicit provider must drop the cached client.
- L3164 `test_async_call_llm_evicts_on_connection_error_with_explicit_provider(self)` (method)
- L3202 `TestBuildCallKwargsToolDedup` (class) — _build_call_kwargs must deduplicate tool names before passing to API.
- L3211 `_make_tool(self, name: str)` (method)
- L3221 `test_unique_tools_pass_through_unchanged(self)` (method)
- L3230 `test_duplicate_tool_names_are_deduplicated(self)` (method) — RED test — must fail until dedup guard is added.
- L3250 `test_empty_tools_unchanged(self)` (method)
- L3256 `test_none_tools_unchanged(self)` (method)
- L3264 `_clean_env(monkeypatch)` (function) — Strip provider env vars so each test starts clean.
- L3273 `TestNvidiaBillingHeaders` (class) — NVIDIA NIM billing-origin headers are scoped to NVIDIA cloud.
- L3276 `test_resolve_provider_client_cloud_adds_billing_origin_header(self, monkeypatch)` (method)
- L3294 `test_resolve_provider_client_local_nim_skips_billing_origin_header(self, monkeypatch)` (method)
- L3313 `TestOpenRouterExplicitApiKey` (class) — Test that explicit_api_key is correctly propagated to _try_openrouter().
- L3316 `test_resolve_provider_client_passes_explicit_api_key_to_openrouter(self, monkeypatch)` (method) — When resolve_provider_client() is called with explicit_api_key for OpenRouter,
- L3349 `test_resolve_provider_client_without_explicit_api_key_falls_back_to_env(self, monkeypatch)` (method) — When resolve_provider_client() is called WITHOUT explicit_api_key for OpenRouter,
- L3379 `TestAnthropicExplicitApiKey` (class) — Test that explicit_api_key is correctly propagated to _try_anthropic().
- L3388 `test_try_anthropic_uses_explicit_api_key_over_env(self)` (method) — _try_anthropic(explicit_api_key) must use the supplied key, not the env fallback.
- L3402 `test_try_anthropic_without_explicit_key_falls_back_to_resolve(self)` (method) — Without explicit_api_key, _try_anthropic falls back to resolve_anthropic_token.
- L3413 `test_resolve_provider_client_passes_explicit_api_key_to_anthropic(self)` (method) — resolve_provider_client(provider='anthropic', explicit_api_key=...) must propagate the key.
- L3432 `TestAuxUnhealthyCache` (class) — Recently-402'd providers are skipped on subsequent aux calls.
- L3441 `setup_method(self)` (method)
- L3445 `teardown_method(self)` (method)
- L3449 `test_mark_then_skip(self)` (method)
- L3458 `test_ttl_expiry_evicts(self)` (method)
- L3472 `test_alias_normalization(self)` (method) — 'codex' should normalize to 'openai-codex' so the cache lookup
- L3482 `test_resolve_auto_skips_unhealthy_step2(self)` (method) — _resolve_auto Step-2 chain skips unhealthy providers.
- L3503 `test_resolve_auto_skips_unhealthy_main_in_step1(self)` (method) — Step-1 also consults the unhealthy cache so a depleted main
- L3527 `test_payment_fallback_skips_unhealthy(self)` (method) — _try_payment_fallback also consults the unhealthy cache so a 402
- L3551 `test_call_llm_marks_provider_unhealthy_on_402(self, monkeypatch)` (method) — A 402 from call_llm causes the provider to be marked unhealthy
