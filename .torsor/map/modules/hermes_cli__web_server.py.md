---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:40'
updated: '2026-06-08T00:38:40'
---

# hermes_cli/web_server.py

Symbols in `hermes_cli/web_server.py`.

- L105 `_start_desktop_cron_ticker(stop_event: 'threading.Event', interval: int=60)` (function) — Tick the cron scheduler from inside the desktop dashboard backend.
- L130 `_lifespan(app: 'FastAPI')` (function)
- L156 `_get_event_state(app: 'FastAPI')` (function) — Return (event_channels, event_lock) from app.state.
- L228 `_has_valid_session_token(request: Request)` (function) — True if the request carries a valid dashboard session token.
- L248 `_require_token(request: Request)` (function) — Validate the ephemeral session token.  Raises 401 on mismatch.
- L265 `should_require_auth(host: str, allow_public: bool)` (function) — Return True iff the dashboard OAuth auth gate must be active.
- L281 `_is_accepted_host(host_header: str, bound_host: str)` (function) — True if the Host header targets the interface we bound to.
- L326 `host_header_middleware(request: Request, call_next)` (function) — Reject requests whose Host header doesn't match the bound interface.
- L365 `_dashboard_auth_gate(request: Request, call_next)` (function)
- L371 `auth_middleware(request: Request, call_next)` (function) — Require the session token on all /api/ routes except the public list.
- L531 `_infer_type(value: Any)` (function) — Infer a UI field type from a Python value.
- L546 `_build_schema_from_config(config: Dict[str, Any], prefix: str='')` (function) — Walk DEFAULT_CONFIG and produce a flat dot-path → field schema dict.
- L600 `ConfigUpdate` (class)
- L604 `EnvVarUpdate` (class)
- L609 `EnvVarDelete` (class)
- L613 `EnvVarReveal` (class)
- L617 `MessagingPlatformUpdate` (class)
- L623 `TelegramOnboardingStart` (class)
- L627 `TelegramOnboardingApply` (class)
- L631 `AudioTranscriptionRequest` (class)
- L636 `ModelAssignment` (class) — Payload for POST /api/model/set — assign a provider/model to a slot.
- L669 `_audio_extension_for_mime(mime_type: str)` (function)
- L674 `ModelAssignment` (class) — Payload for POST /api/model/set — assign a provider/model to a slot.
- L694 `_apply_main_model_assignment(model_cfg: 'Any', provider: str, model: str, base_url: str='')` (function) — Apply a main-slot model assignment to a ``model`` config dict in place.
- L757 `_probe_gateway_health()` (function) — Probe the gateway via its HTTP health endpoint (cross-container).
- L814 `_media_serve_roots()` (function) — Directories ``GET /api/media`` is allowed to read from.
- L835 `get_media(path: str)` (function) — Return a gateway-local image file as a base64 data URL.
- L868 `get_status()` (function)
- L989 `get_system_stats()` (function) — Host + process system stats for the System page.
- L1077 `get_curator_status()` (function)
- L1097 `CuratorPause` (class)
- L1102 `set_curator_paused(body: CuratorPause)` (function)
- L1110 `run_curator()` (function) — Trigger a curator review now (backgrounded; tail via action status).
- L1119 `_safe_call(mod, fn_name: str, default)` (function)
- L1133 `get_portal_status()` (function)
- L1181 `run_prompt_size()` (function)
- L1190 `run_dump()` (function)
- L1199 `run_config_migrate()` (function)
- L1207 `DebugShareRequest` (class)
- L1217 `run_debug_share_endpoint(body: DebugShareRequest | None=None)` (function) — Upload a redacted debug report + full logs and return the paste URLs.
- L1294 `_record_completed_action(name: str, message: str, exit_code: int=1)` (function) — Record a non-spawned action result and write it to the action log.
- L1310 `_spawn_hermes_action(subcommand: List[str], name: str)` (function) — Spawn ``hermes <subcommand>`` detached and record the Popen handle.
- L1351 `_tail_lines(path: Path, n: int)` (function) — Return the last ``n`` lines of ``path``.  Reads the whole file — fine
- L1366 `restart_gateway()` (function) — Kick off a ``hermes gateway restart`` in the background.
- L1381 `update_hermes()` (function) — Kick off ``hermes update`` in the background.
- L1409 `check_hermes_update(force: bool=False)` (function) — Report whether a Hermes update is available, without applying it.
- L1475 `transcribe_audio_upload(payload: AudioTranscriptionRequest)` (function)
- L1548 `TTSSpeakRequest` (class)
- L1552 `_elevenlabs_voice_label(voice: Dict[str, Any])` (function)
- L1560 `get_elevenlabs_voices()` (function) — Return ElevenLabs voices when an API key is configured.
- L1610 `speak_text(payload: TTSSpeakRequest)` (function) — Synthesize speech and return audio as base64 data URL.
- L1675 `get_action_status(name: str, lines: int=200)` (function) — Tail an action log and report whether the process is still running.
- L1705 `get_sessions(limit: int=20, offset: int=0, min_messages: int=0, archived: str='exclude', order: str='created', source: str=None, exclude_sources: str=None)` (function) — List sessions.
- L1783 `get_profiles_sessions(limit: int=20, offset: int=0, min_messages: int=0, archived: str='exclude', order: str='recent', profile: str='all', source: str=None, exclude_sources: str=None)` (function) — Unified, read-only session list aggregated across ALL profiles.
- L1902 `search_sessions(q: str='', limit: int=20)` (function) — Search sessions by ID plus full-text message content using FTS5.
- L2064 `_normalize_config_for_web(config: Dict[str, Any])` (function) — Normalize config for the web UI.
- L2088 `get_config()` (function)
- L2095 `get_defaults()` (function)
- L2100 `get_schema()` (function)
- L2115 `get_model_info()` (function) — Return resolved model metadata for the currently configured model.
- L2215 `get_model_options()` (function) — Return authenticated providers + their curated model lists.
- L2249 `get_recommended_default_model(provider: str='')` (function) — Return the recommended default model for a freshly-authenticated provider.
- L2321 `get_auxiliary_models()` (function) — Return current auxiliary task assignments.
- L2365 `set_model_assignment(body: ModelAssignment)` (function) — Assign a model to the main slot or an auxiliary task slot.
- L2512 `_denormalize_config_from_web(config: Dict[str, Any])` (function) — Reverse _normalize_config_for_web before saving.
- L2565 `update_config(body: ConfigUpdate)` (function)
- L2575 `get_env_vars()` (function)
- L2599 `set_env_var(body: EnvVarUpdate)` (function)
- L2627 `_parse_model_ids(resp: 'Any')` (function) — Extract model ids from an OpenAI-compatible ``/v1/models`` response.
- L2655 `validate_provider_credential(body: EnvVarUpdate, request: Request)` (function) — Live-probe a provider credential before it's saved.
- L2712 `remove_env_var(body: EnvVarDelete)` (function)
- L2731 `reveal_env_var(body: EnvVarReveal, request: Request)` (function) — Return the real (unredacted) value of a single env var.
- L3110 `_messaging_platform_catalog()` (function) — Build the messaging catalog from the gateway's Platform enum + plugin registry.
- L3151 `_channel_managed_env_keys()` (function) — Env-var keys owned by a Channels page platform card.
- L3180 `_platform_env_prefixes(platform_id: str)` (function) — Env-var prefixes owned by a messaging platform card.
- L3195 `_discover_platform_env_vars(platform_id: str)` (function) — All messaging-category env vars for a platform (override + plugin + prefix).
- L3210 `_merge_platform_env_vars(platform_id: str, override: dict[str, Any], plugin_entry: Any | None)` (function) — Canonical env-var list for a messaging platform card.
- L3224 `_build_catalog_entry(platform_id: str, plugin_entry: Any | None=None)` (function)
- L3259 `_catalog_lookup(platform_id: str)` (function)
- L3266 `_messaging_env_info(key: str)` (function)
- L3277 `_gateway_platform_config(platform_id: str)` (function)
- L3286 `_messaging_platform_payload(entry: dict[str, Any], env_on_disk: dict[str, str], runtime: dict | None)` (function)
- L3373 `_write_platform_enabled(platform_id: str, enabled: bool)` (function)
- L3392 `_TelegramOnboardingPairing` (class)
- L3405 `_telegram_onboarding_base_url()` (function)
- L3413 `_parse_expiry_ts(value: str)` (function)
- L3424 `_prune_telegram_onboarding_pairings()` (function)
- L3435 `_normalize_telegram_user_id(value: Any)` (function)
- L3442 `_telegram_onboarding_error_message(error: str, fallback: str)` (function)
- L3453 `_telegram_onboarding_request_sync(method: str, path: str, *, body: dict[str, Any] | None=None, bearer_token: str | None=None)` (function)
- L3513 `_telegram_onboarding_request(method: str, path: str, *, body: dict[str, Any] | None=None, bearer_token: str | None=None)` (function)
- L3530 `start_telegram_onboarding(body: TelegramOnboardingStart)` (function)
- L3568 `get_telegram_onboarding_status(pairing_id: str)` (function)
- L3642 `apply_telegram_onboarding(pairing_id: str, body: TelegramOnboardingApply)` (function)
- L3704 `cancel_telegram_onboarding(pairing_id: str)` (function)
- L3711 `get_messaging_platforms()` (function)
- L3723 `update_messaging_platform(platform_id: str, body: MessagingPlatformUpdate)` (function)
- L3762 `test_messaging_platform(platform_id: str)` (function)
- L3823 `_truncate_token(value: Optional[str], visible: int=6)` (function) — Return ``...XXXXXX`` (last N chars) for safe display in the UI.
- L3848 `_anthropic_oauth_status()` (function) — Combined status across the three Anthropic credential sources we read.
- L3913 `_claude_code_only_status()` (function) — Surface Claude Code CLI credentials as their own provider entry.
- L4015 `_resolve_provider_status(provider_id: str, status_fn)` (function) — Dispatch to the right status helper for an OAuth provider entry.
- L4085 `list_oauth_providers()` (function) — Enumerate every OAuth-capable LLM provider with current status.
- L4117 `disconnect_oauth_provider(provider_id: str, request: Request)` (function) — Disconnect an OAuth provider. Token-protected (matches /env/reveal).
- L4229 `_gc_oauth_sessions()` (function) — Drop expired sessions. Called opportunistically on /start.
- L4238 `_new_oauth_session(provider_id: str, flow: str)` (function) — Create + register a new OAuth session, return (session_id, session_dict).
- L4254 `_save_anthropic_oauth_creds(access_token: str, refresh_token: str, expires_at_ms: int)` (function) — Persist Anthropic PKCE creds to both Hermes file AND credential pool.
- L4321 `_start_anthropic_pkce()` (function) — Begin PKCE flow. Returns the auth URL the UI should open.
- L4348 `_submit_anthropic_pkce(session_id: str, code_input: str)` (function) — Exchange authorization code for tokens. Persists on success.
- L4414 `_start_device_code_flow(provider_id: str)` (function) — Initiate a device-code flow (Nous, OpenAI Codex, or MiniMax).
- L4596 `_start_xai_loopback_flow()` (function) — Begin the xAI loopback PKCE flow.
- L4658 `_xai_loopback_worker(session_id: str)` (function) — Wait for the xAI loopback callback, exchange the code, persist tokens.
- L4756 `_add_xai_oauth_pool_entry(access_token: str, refresh_token: str, base_url: str, last_refresh: str)` (function) — Mirror `hermes auth add xai-oauth`'s credential-pool insert.
- L4801 `_nous_poller(session_id: str)` (function) — Background poller that drives a Nous device-code flow to completion.
- L4864 `_minimax_poller(session_id: str)` (function) — Background poller that drives a MiniMax OAuth flow to completion.
- L4947 `_codex_full_login_worker(session_id: str)` (function) — Run the complete OpenAI Codex device-code flow.
- L5066 `start_oauth_login(provider_id: str, request: Request)` (function) — Initiate an OAuth login flow. Token-protected.
- L5102 `OAuthSubmitBody` (class)
- L5108 `submit_oauth_code(provider_id: str, body: OAuthSubmitBody, request: Request)` (function) — Submit the auth code for PKCE flows. Token-protected.
- L5119 `poll_oauth_session(provider_id: str, session_id: str)` (function) — Poll a session's status (no auth — read-only state).
- L5142 `cancel_oauth_session(session_id: str, request: Request)` (function) — Cancel a pending OAuth session. Token-protected.
- L5185 `_session_latest_descendant(session_id: str)` (function) — Resolve a session id to the newest child leaf session.
- L5272 `BulkDeleteSessions` (class)
- L5277 `bulk_delete_sessions_endpoint(body: BulkDeleteSessions)` (function) — Delete every session in ``body.ids`` in a single DB transaction.
- L5328 `count_empty_sessions_endpoint()` (function) — Return the number of empty, ended, non-archived sessions.
- L5344 `delete_empty_sessions_endpoint()` (function) — Delete every empty (``message_count == 0``), ended,
- L5373 `get_session_stats()` (function) — Session-store statistics for the Sessions page (mirrors `hermes sessions stats`).
- L5405 `_open_session_db_for_profile(profile: Optional[str])` (function) — Open a SessionDB for read paths, optionally for another profile.
- L5421 `get_session_detail(session_id: str, profile: Optional[str]=None)` (function)
- L5437 `get_session_latest_descendant(session_id: str)` (function)
- L5449 `get_session_messages(session_id: str, profile: Optional[str]=None)` (function)
- L5462 `delete_session_endpoint(session_id: str, profile: Optional[str]=None)` (function)
- L5475 `SessionRename` (class)
- L5484 `rename_session_endpoint(session_id: str, body: SessionRename)` (function) — Update a session: rename (or clear its title) and/or archive it.
- L5518 `export_session_endpoint(session_id: str)` (function) — Export a single session (metadata + messages) as JSON.
- L5535 `SessionPrune` (class)
- L5541 `prune_sessions_endpoint(body: SessionPrune)` (function) — Delete ended sessions older than N days (mirrors `hermes sessions prune`).
- L5566 `get_logs(file: str='agent', lines: int=100, level: Optional[str]=None, component: Optional[str]=None, search: Optional[str]=None)` (function)
- L5623 `CronJobCreate` (class)
- L5630 `CronJobUpdate` (class)
- L5637 `_cron_profile_dicts()` (function) — Return dashboard profile records, falling back to a directory scan.
- L5647 `_cron_profile_home(profile: Optional[str])` (function) — Resolve a profile query value to (profile_name, HERMES_HOME).
- L5662 `_annotate_cron_job(job: Dict[str, Any], profile: str, home: Path)` (function)
- L5671 `_call_cron_for_profile(profile: Optional[str], func_name: str, *args, **kwargs)` (function) — Run cron.jobs helpers against the selected profile's cron directory.
- L5703 `_find_cron_job_profile(job_id: str)` (function)
- L5715 `list_cron_jobs(profile: str='all')` (function)
- L5733 `get_cron_job(job_id: str, profile: Optional[str]=None)` (function)
- L5744 `list_cron_job_runs(job_id: str, profile: Optional[str]=None, limit: int=20)` (function) — Run sessions produced by a cron job, newest first.
- L5790 `create_cron_job(body: CronJobCreate, profile: str='default')` (function)
- L5806 `get_cron_delivery_targets()` (function) — Delivery targets the cron dropdown should offer.
- L5834 `update_cron_job(job_id: str, body: CronJobUpdate, profile: Optional[str]=None)` (function)
- L5848 `pause_cron_job(job_id: str, profile: Optional[str]=None)` (function)
- L5859 `resume_cron_job(job_id: str, profile: Optional[str]=None)` (function)
- L5870 `trigger_cron_job(job_id: str, profile: Optional[str]=None)` (function)
- L5881 `delete_cron_job(job_id: str, profile: Optional[str]=None)` (function)
- L5904 `MCPServerCreate` (class)
- L5915 `_redact_mcp_env(env: Dict[str, Any])` (function) — Mask secret-shaped MCP env values for read responses.
- L5926 `_mcp_server_summary(name: str, cfg: Dict[str, Any])` (function)
- L5943 `list_mcp_servers()` (function)
- L5955 `add_mcp_server(body: MCPServerCreate)` (function)
- L5991 `remove_mcp_server(name: str)` (function)
- L6000 `test_mcp_server(name: str)` (function) — Connect to the server, list its tools, disconnect.  Returns tool list.
- L6024 `MCPEnabledToggle` (class)
- L6029 `set_mcp_server_enabled(name: str, body: MCPEnabledToggle)` (function) — Enable or disable an MCP server (takes effect on next session/gateway).
- L6048 `list_mcp_catalog()` (function) — Browse the Nous-approved MCP catalog (the optional-mcps/ manifests).
- L6095 `MCPCatalogInstall` (class)
- L6103 `install_mcp_catalog_entry(body: MCPCatalogInstall)` (function) — Install a catalog MCP into config.yaml.
- L6155 `PairingApprove` (class)
- L6160 `PairingRevoke` (class)
- L6165 `_pairing_store()` (function)
- L6172 `list_pairing()` (function)
- L6181 `approve_pairing(body: PairingApprove)` (function)
- L6203 `revoke_pairing(body: PairingRevoke)` (function)
- L6217 `clear_pending_pairing()` (function)
- L6232 `WebhookCreate` (class)
- L6245 `_webhook_route_summary(name: str, route: Dict[str, Any], base_url: str)` (function)
- L6264 `list_webhooks()` (function)
- L6280 `create_webhook(body: WebhookCreate)` (function)
- L6332 `delete_webhook(name: str)` (function)
- L6344 `WebhookEnabledToggle` (class)
- L6349 `set_webhook_enabled(name: str, body: WebhookEnabledToggle)` (function) — Enable or disable a webhook route.
- L6379 `start_gateway()` (function)
- L6389 `stop_gateway()` (function)
- L6407 `CredentialPoolAdd` (class)
- L6415 `_pool_entry_summary(entry: Any, index: int)` (function) — Redacted, display-safe view of one PooledCredential.
- L6436 `list_credential_pool()` (function)
- L6463 `add_credential_pool_entry(body: CredentialPoolAdd)` (function)
- L6497 `remove_credential_pool_entry(provider: str, index: int)` (function) — Remove a pool entry.  ``index`` is 1-based (matches the list response).
- L6524 `MemoryProviderSelect` (class)
- L6529 `MemoryReset` (class)
- L6535 `get_memory_status()` (function)
- L6570 `set_memory_provider(body: MemoryProviderSelect)` (function)
- L6594 `reset_memory(body: MemoryReset)` (function)
- L6631 `run_doctor()` (function)
- L6641 `run_security_audit()` (function)
- L6650 `BackupRequest` (class)
- L6656 `run_backup(body: BackupRequest)` (function)
- L6668 `ImportRequest` (class)
- L6673 `run_import(body: ImportRequest)` (function)
- L6688 `list_hooks()` (function) — List configured shell hooks from config.yaml with consent + health.
- L6735 `HookCreate` (class)
- L6747 `create_hook(body: HookCreate)` (function) — Add a shell hook to config.yaml (and optionally approve it).
- L6803 `HookDelete` (class)
- L6809 `delete_hook(body: HookDelete)` (function) — Remove a hook from config.yaml and revoke its consent allowlist entry.
- L6846 `list_checkpoints()` (function) — List the /rollback shadow store checkpoints (read-only).
- L6878 `prune_checkpoints()` (function)
- L6897 `SkillInstallRequest` (class)
- L6902 `install_skill_hub(body: SkillInstallRequest)` (function)
- L6914 `SkillUninstallRequest` (class)
- L6919 `uninstall_skill_hub(body: SkillUninstallRequest)` (function)
- L6932 `update_skills_hub()` (function)
- L6957 `_skill_meta_to_payload(m)` (function)
- L6969 `_installed_hub_identifiers()` (function) — Map identifier -> installed lock entry for hub-installed skills.
- L6993 `list_skills_hub_sources()` (function) — List the configured skill-hub sources and installed-skill provenance.
- L7051 `search_skills_hub(q: str='', source: str='all', limit: int=20)` (function) — Search the skill hub across all configured sources.
- L7097 `preview_skill_hub(identifier: str='')` (function) — Fetch a hub skill's SKILL.md content + metadata for in-dashboard reading.
- L7158 `scan_skill_hub(identifier: str='')` (function) — Run the install-time security scan on a hub skill WITHOUT installing it.
- L7253 `ProfileCreate` (class)
- L7268 `ProfileRename` (class)
- L7272 `ProfileSoulUpdate` (class)
- L7276 `ProfileActiveUpdate` (class)
- L7280 `ProfileDescriptionUpdate` (class)
- L7284 `ProfileModelUpdate` (class)
- L7289 `ProfileDescribeAuto` (class)
- L7293 `_profile_attr(info, name: str, default: Any=None)` (function)
- L7300 `_profile_to_dict(info)` (function)
- L7319 `_fallback_profile_dicts(profiles_mod)` (function)
- L7373 `_resolve_profile_dir(name: str)` (function) — Validate ``name`` and resolve to its directory or raise an HTTPException.
- L7385 `_profile_setup_command(name: str)` (function) — Return the shell command used to configure a profile in the CLI.
- L7391 `_write_profile_model(profile_dir: Path, provider: str, model: str)` (function) — Write the main model assignment into a specific profile's config.yaml.
- L7412 `list_profiles_endpoint()` (function)
- L7422 `create_profile_endpoint(body: ProfileCreate)` (function)
- L7481 `get_active_profile_endpoint()` (function) — Return the sticky active profile and the profile this dashboard
- L7502 `set_active_profile_endpoint(body: ProfileActiveUpdate)` (function) — Set the sticky active profile (mirrors ``hermes profile use``).
- L7522 `get_profile_setup_command(name: str)` (function)
- L7527 `open_profile_terminal_endpoint(name: str)` (function)
- L7581 `rename_profile_endpoint(name: str, body: ProfileRename)` (function)
- L7596 `delete_profile_endpoint(name: str)` (function) — Delete a profile. The dashboard collects the user's confirmation in
- L7614 `get_profile_soul(name: str)` (function)
- L7625 `update_profile_soul(name: str, body: ProfileSoulUpdate)` (function)
- L7636 `update_profile_description_endpoint(name: str, body: ProfileDescriptionUpdate)` (function) — Set or clear a profile's role description (kanban routing signal).
- L7659 `update_profile_model_endpoint(name: str, body: ProfileModelUpdate)` (function) — Set the main model (``model.default`` + ``model.provider``) for a
- L7679 `describe_profile_auto_endpoint(name: str, body: ProfileDescribeAuto)` (function) — Auto-generate a profile's description via the auxiliary LLM
- L7711 `SkillToggle` (class)
- L7717 `get_skills()` (function)
- L7729 `toggle_skill(body: SkillToggle)` (function)
- L7742 `get_toolsets()` (function)
- L7776 `ToolsetToggle` (class)
- L7781 `toggle_toolset(name: str, body: ToolsetToggle)` (function) — Enable/disable a configurable toolset for the desktop (cli) platform.
- L7811 `get_toolset_config(name: str)` (function) — Return the provider matrix + key status for a toolset's config panel.
- L7872 `ToolsetProviderSelect` (class)
- L7877 `select_toolset_provider(name: str, body: ToolsetProviderSelect)` (function) — Persist a provider selection for a toolset (no key prompting).
- L7904 `ToolsetEnvUpdate` (class)
- L7909 `save_toolset_env(name: str, body: ToolsetEnvUpdate)` (function) — Persist API keys for a toolset's provider env vars.
- L7962 `ToolsetPostSetup` (class)
- L7967 `run_toolset_post_setup(name: str, body: ToolsetPostSetup)` (function) — Spawn a provider's post-setup install hook as a background action.
- L8009 `RawConfigUpdate` (class)
- L8014 `get_config_raw()` (function)
- L8022 `update_config_raw(body: RawConfigUpdate)` (function)
- L8039 `get_usage_analytics(days: int=30)` (function)
- L8108 `get_models_analytics(days: int=30)` (function) — Rich per-model analytics for the Models dashboard page.
- L8237 `_ws_client_reason(ws: 'WebSocket')` (function) — Return a rejection reason for the client IP, or None when allowed.
- L8259 `_ws_client_is_allowed(ws: 'WebSocket')` (function) — Check if the WebSocket client IP is acceptable.
- L8299 `_ws_host_origin_reason(ws: 'WebSocket')` (function) — Return a Host/Origin rejection reason, or None when allowed.
- L8333 `_ws_host_origin_is_allowed(ws: 'WebSocket')` (function) — Apply the dashboard Host/Origin guard to WebSocket upgrades.
- L8345 `_ws_request_reason(ws: 'WebSocket')` (function) — First Host/Origin or peer-IP rejection reason, or None when allowed.
- L8350 `_ws_request_is_allowed(ws: 'WebSocket')` (function) — Return True when the WebSocket upgrade matches dashboard boundaries.
- L8355 `_ws_auth_mode()` (function) — Short label for the active WS auth mode — logged on every connection.
- L8365 `_ws_auth_reason(ws: 'WebSocket')` (function) — Validate WS-upgrade auth; return ``(reason, credential)``.
- L8449 `_ws_auth_ok(ws: 'WebSocket')` (function) — True when the WS-upgrade credential is accepted. See _ws_auth_reason.
- L8460 `_resolve_chat_argv(resume: Optional[str]=None, sidecar_url: Optional[str]=None)` (function) — Resolve the argv + cwd + env for the chat PTY.
- L8512 `_build_gateway_ws_url()` (function) — ws:// URL the PTY child should attach to for JSON-RPC gateway traffic.
- L8545 `_build_sidecar_url(channel: str)` (function) — ws:// URL the PTY child should publish events to, or None when unbound.
- L8581 `_broadcast_event(app: Any, channel: str, payload: str)` (function) — Fan out one publisher frame to every subscriber on `channel`.
- L8596 `_channel_or_close_code(ws: WebSocket)` (function) — Return the channel id from the query string or None if invalid.
- L8603 `_ws_close_reason(text: str)` (function) — Clamp a WS close reason to the protocol's 123-byte UTF-8 limit.
- L8617 `pty_ws(ws: WebSocket)` (function)
- L8759 `gateway_ws(ws: WebSocket)` (function)
- L8790 `pub_ws(ws: WebSocket)` (function)
- L8818 `events_ws(ws: WebSocket)` (function)
- L8861 `_normalise_prefix(raw: Optional[str])` (function) — Normalise an X-Forwarded-Prefix header value.
- L8873 `mount_spa(application: FastAPI)` (function) — Mount the built SPA. Falls back to index.html for client-side routing.
- L9014 `_parse_theme_layer(value: Any, default_hex: str, default_alpha: float=1.0)` (function) — Normalise a theme layer spec from YAML into `{hex, alpha}` form.
- L9083 `_normalise_theme_definition(data: Dict[str, Any])` (function) — Normalise a user theme YAML into the wire format `ThemeProvider`
- L9228 `_discover_user_themes()` (function) — Scan ~/.hermes/dashboard-themes/*.yaml for user-created themes.
- L9251 `get_dashboard_themes()` (function) — Return available themes and the currently active one.
- L9281 `ThemeSetBody` (class)
- L9286 `set_dashboard_theme(body: ThemeSetBody)` (function) — Set the active dashboard theme (persists to config.yaml).
- L9311 `get_dashboard_font()` (function) — Return the active font override (``"theme"`` = use the theme's font).
- L9320 `FontSetBody` (class)
- L9325 `set_dashboard_font(body: FontSetBody)` (function) — Set the dashboard font override (persists to config.yaml).
- L9346 `_safe_plugin_api_relpath(api_field: Any, *, dashboard_dir: Path)` (function) — Validate the manifest's ``api`` field for the plugin loader.
- L9383 `_discover_dashboard_plugins()` (function) — Scan plugins/*/dashboard/manifest.json for dashboard extensions.
- L9491 `_get_dashboard_plugins(force_rescan: bool=False)` (function)
- L9502 `get_dashboard_plugins()` (function) — Return discovered dashboard plugins (excludes user-hidden ones).
- L9517 `rescan_dashboard_plugins()` (function) — Force re-scan of dashboard plugins.
- L9523 `_AgentPluginInstallBody` (class)
- L9529 `_strip_dashboard_manifest(p: Dict[str, Any])` (function)
- L9533 `_merged_plugins_hub()` (function) — Agent discovery + dashboard manifests + optional provider picker metadata.
- L9649 `get_plugins_hub(request: Request)` (function) — Unified agent plugins + dashboard extension metadata (session protected).
- L9660 `post_agent_plugin_install(request: Request, body: _AgentPluginInstallBody)` (function)
- L9680 `_validate_plugin_name(name: str)` (function) — Reject path-traversal attempts in plugin name URL parameters.
- L9689 `post_agent_plugin_enable(request: Request, name: str)` (function)
- L9701 `post_agent_plugin_disable(request: Request, name: str)` (function)
- L9713 `post_agent_plugin_update(request: Request, name: str)` (function)
- L9726 `delete_agent_plugin(request: Request, name: str)` (function)
- L9738 `_PluginProvidersPutBody` (class)
- L9744 `put_plugin_providers(request: Request, body: _PluginProvidersPutBody)` (function) — Persist memory provider / context engine selection (writes config.yaml).
- L9759 `_PluginVisibilityBody` (class)
- L9764 `post_plugin_visibility(request: Request, name: str, body: _PluginVisibilityBody)` (function) — Toggle a plugin's sidebar visibility (persists to config.yaml dashboard.hidden_plugins).
- L9787 `serve_plugin_asset(plugin_name: str, file_path: str)` (function) — Serve static assets from a dashboard plugin directory.
- L9855 `_mount_plugin_api_routes()` (function) — Import and mount backend API routes from plugins that declare them.
- L9941 `start_server(host: str='127.0.0.1', port: int=9119, open_browser: bool=True, allow_public: bool=False)` (function) — Start the web UI server.
