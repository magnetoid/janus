---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:41'
updated: '2026-06-08T00:38:41'
---

# tests/agent/test_prompt_builder.py

Symbols in `tests/agent/test_prompt_builder.py`.

- L39 `TestGuidanceConstants` (class)
- L40 `test_memory_guidance_discourages_task_logs(self)` (method)
- L47 `test_session_search_guidance_is_simple_cross_session_recall(self)` (method)
- L57 `TestScanContextContent` (class)
- L58 `test_clean_content_passes(self)` (method)
- L63 `test_prompt_injection_blocked(self)` (method)
- L69 `test_disregard_rules_blocked(self)` (method)
- L73 `test_system_prompt_override_blocked(self)` (method)
- L77 `test_html_comment_injection_blocked(self)` (method)
- L81 `test_hidden_div_blocked(self)` (method)
- L87 `test_exfiltration_curl_blocked(self)` (method)
- L91 `test_read_secrets_blocked(self)` (method)
- L95 `test_invisible_unicode_blocked(self)` (method)
- L99 `test_translate_execute_blocked(self)` (method)
- L105 `test_bypass_restrictions_blocked(self)` (method)
- L115 `TestTruncateContent` (class)
- L116 `test_short_content_unchanged(self)` (method)
- L121 `test_long_content_truncated(self)` (method)
- L127 `test_truncation_keeps_head_and_tail(self)` (method)
- L136 `test_exact_limit_unchanged(self)` (method)
- L147 `TestParseSkillFile` (class)
- L148 `test_reads_frontmatter_description(self, tmp_path)` (method)
- L158 `test_missing_description_returns_empty(self, tmp_path)` (method)
- L164 `test_long_description_truncated(self, tmp_path)` (method)
- L172 `test_nonexistent_file_returns_defaults(self, tmp_path)` (method)
- L178 `test_logs_parse_failures_and_returns_defaults(self, tmp_path, monkeypatch, caplog)` (method)
- L195 `test_incompatible_platform_returns_false(self, tmp_path)` (method)
- L207 `test_returns_frontmatter_with_prerequisites(self, tmp_path, monkeypatch)` (method)
- L218 `TestPromptBuilderImports` (class)
- L219 `test_module_import_does_not_eagerly_import_skills_tool(self, monkeypatch)` (method)
- L242 `TestBuildSkillsSystemPrompt` (class)
- L244 `_clear_skills_cache(self)` (method) — Ensure the in-process skills prompt cache doesn't leak between tests.
- L251 `test_empty_when_no_skills_dir(self, monkeypatch, tmp_path)` (method)
- L256 `test_builds_index_with_skills(self, monkeypatch, tmp_path)` (method)
- L268 `test_deduplicates_skills(self, monkeypatch, tmp_path)` (method)
- L279 `test_excludes_incompatible_platform_skills(self, monkeypatch, tmp_path)` (method) — Skills with platforms: [macos] should not appear on Linux.
- L308 `test_includes_matching_platform_skills(self, monkeypatch, tmp_path)` (method) — Skills with platforms: [macos] should appear on macOS.
- L327 `test_excludes_disabled_skills(self, monkeypatch, tmp_path)` (method) — Skills in the user's disabled list should not appear in the system prompt.
- L356 `test_rebuilds_prompt_when_disabled_skills_change(self, monkeypatch, tmp_path)` (method)
- L374 `test_includes_setup_needed_skills(self, monkeypatch, tmp_path)` (method)
- L396 `test_includes_skills_with_met_prerequisites(self, monkeypatch, tmp_path)` (method) — Skills with satisfied prerequisites should appear normally.
- L412 `test_non_local_backend_keeps_skill_visible_without_probe(self, monkeypatch, tmp_path)` (method)
- L431 `TestBuildNousSubscriptionPrompt` (class)
- L432 `test_includes_active_subscription_features(self, monkeypatch)` (method)
- L457 `test_non_subscriber_prompt_includes_relevant_upgrade_guidance(self, monkeypatch)` (method)
- L481 `test_feature_flag_off_returns_empty_prompt(self, monkeypatch)` (method)
- L494 `TestBuildContextFilesPrompt` (class)
- L495 `test_empty_dir_loads_seeded_global_soul(self, tmp_path)` (method)
- L505 `test_loads_agents_md(self, tmp_path)` (method)
- L511 `test_loads_cursorrules(self, tmp_path)` (method)
- L516 `test_loads_soul_md_from_hermes_home_only(self, tmp_path, monkeypatch)` (method)
- L526 `test_soul_md_has_no_wrapper_text(self, tmp_path, monkeypatch)` (method)
- L536 `test_empty_soul_md_adds_nothing(self, tmp_path, monkeypatch)` (method)
- L544 `test_blocks_injection_in_agents_md(self, tmp_path)` (method)
- L551 `test_loads_cursor_rules_mdc(self, tmp_path)` (method)
- L558 `test_agents_md_top_level_only(self, tmp_path)` (method) — AGENTS.md is loaded from cwd only — subdirectory copies are ignored.
- L570 `test_loads_hermes_md(self, tmp_path)` (method)
- L576 `test_loads_hermes_md_uppercase(self, tmp_path)` (method)
- L581 `test_hermes_md_lowercase_takes_priority(self, tmp_path)` (method)
- L588 `test_hermes_md_parent_dir_discovery(self, tmp_path)` (method) — Walks parent dirs up to git root.
- L598 `test_hermes_md_stops_at_git_root(self, tmp_path)` (method) — Should NOT walk past the git root.
- L608 `test_hermes_md_strips_yaml_frontmatter(self, tmp_path)` (method)
- L616 `test_hermes_md_blocks_injection(self, tmp_path)` (method)
- L621 `test_hermes_md_beats_agents_md(self, tmp_path)` (method) — When both exist, .hermes.md wins and AGENTS.md is not loaded.
- L629 `test_agents_md_beats_claude_md(self, tmp_path)` (method)
- L636 `test_claude_md_beats_cursorrules(self, tmp_path)` (method)
- L643 `test_loads_claude_md(self, tmp_path)` (method)
- L650 `test_loads_claude_md_lowercase(self, tmp_path)` (method)
- L659 `test_claude_md_uppercase_takes_priority(self, tmp_path)` (method)
- L670 `test_claude_md_blocks_injection(self, tmp_path)` (method)
- L675 `test_hermes_md_beats_all_others(self, tmp_path)` (method) — When all four types exist, only .hermes.md is loaded.
- L687 `test_cursorrules_loads_when_only_option(self, tmp_path)` (method) — Cursorrules still loads when no higher-priority files exist.
- L699 `TestFindHermesMd` (class)
- L700 `test_finds_in_cwd(self, tmp_path)` (method)
- L704 `test_finds_uppercase(self, tmp_path)` (method)
- L708 `test_prefers_lowercase(self, tmp_path)` (method)
- L713 `test_walks_to_git_root(self, tmp_path)` (method)
- L720 `test_returns_none_when_absent(self, tmp_path)` (method)
- L723 `test_stops_at_git_root(self, tmp_path)` (method) — Does not walk past the git root.
- L732 `TestFindGitRoot` (class)
- L733 `test_finds_git_dir(self, tmp_path)` (method)
- L737 `test_finds_from_subdirectory(self, tmp_path)` (method)
- L743 `test_returns_none_without_git(self, tmp_path)` (method)
- L758 `TestStripYamlFrontmatter` (class)
- L759 `test_strips_frontmatter(self)` (method)
- L763 `test_no_frontmatter_unchanged(self)` (method)
- L767 `test_unclosed_frontmatter_unchanged(self)` (method)
- L771 `test_empty_body_returns_original(self)` (method)
- L782 `TestPromptBuilderConstants` (class)
- L783 `test_default_identity_non_empty(self)` (method)
- L786 `test_platform_hints_known_platforms(self)` (method)
- L795 `test_cli_hint_does_not_suggest_media_tags(self)` (method)
- L813 `test_platform_hints_mattermost(self)` (method)
- L819 `test_platform_hints_matrix(self)` (method)
- L825 `test_platform_hints_feishu(self)` (method)
- L831 `test_platform_hints_webui(self)` (method)
- L843 `TestEnvironmentHints` (class)
- L844 `test_wsl_hint_constant_mentions_mnt(self)` (method)
- L848 `test_build_environment_hints_on_wsl(self, monkeypatch)` (method)
- L859 `test_build_environment_hints_on_linux_local(self, monkeypatch)` (method)
- L879 `test_build_environment_hints_on_windows_local(self, monkeypatch)` (method)
- L896 `test_build_environment_hints_on_macos_local(self, monkeypatch)` (method)
- L910 `test_build_environment_hints_suppresses_host_on_docker_backend(self, monkeypatch)` (method) — Docker/remote backends must hide host info — the agent can only touch the backend.
- L930 `test_build_environment_hints_uses_terminal_cwd_over_launch_dir(self, monkeypatch, tmp_path)` (method) — THE BUG: gateway/cron set TERMINAL_CWD but the prompt emitted os.getcwd()
- L943 `test_build_environment_hints_falls_back_to_launch_dir(self, monkeypatch, tmp_path)` (method) — The #19242 local-CLI contract: no TERMINAL_CWD → the launch dir.
- L953 `test_build_environment_hints_uses_live_probe_when_available(self, monkeypatch)` (method) — When the probe succeeds, its output must appear in the hint block.
- L966 `test_remote_backend_list_covers_known_sandboxes(self)` (method) — Regression guard: if someone adds a remote backend, they must list it here.
- L975 `test_environment_hint_from_env_var_is_appended(self, monkeypatch)` (method) — HERMES_ENVIRONMENT_HINT lets an embedder describe the runtime env.
- L987 `test_environment_hint_env_var_overrides_config(self, monkeypatch)` (method) — Env var wins over config.yaml agent.environment_hint.
- L1002 `test_environment_hint_falls_back_to_config(self, monkeypatch)` (method) — With no env var, the config.yaml value is used.
- L1016 `test_environment_hint_empty_by_default(self, monkeypatch)` (method) — No hint configured anywhere → no embedder text, host block intact.
- L1032 `TestSkillShouldShow` (class)
- L1033 `test_no_filter_info_always_shows(self)` (method)
- L1036 `test_empty_conditions_always_shows(self)` (method)
- L1043 `test_fallback_hidden_when_toolset_available(self)` (method)
- L1048 `test_fallback_shown_when_toolset_unavailable(self)` (method)
- L1053 `test_requires_shown_when_toolset_available(self)` (method)
- L1058 `test_requires_hidden_when_toolset_missing(self)` (method)
- L1063 `test_fallback_for_tools_hidden_when_tool_available(self)` (method)
- L1068 `test_fallback_for_tools_shown_when_tool_missing(self)` (method)
- L1073 `test_requires_tools_hidden_when_tool_missing(self)` (method)
- L1078 `test_requires_tools_shown_when_tool_available(self)` (method)
- L1084 `TestBuildSkillsSystemPromptConditional` (class)
- L1086 `_clear_skills_cache(self)` (method)
- L1092 `test_fallback_skill_hidden_when_primary_available(self, monkeypatch, tmp_path)` (method)
- L1105 `test_fallback_skill_shown_when_primary_unavailable(self, monkeypatch, tmp_path)` (method)
- L1118 `test_requires_skill_hidden_when_toolset_missing(self, monkeypatch, tmp_path)` (method)
- L1131 `test_requires_skill_shown_when_toolset_available(self, monkeypatch, tmp_path)` (method)
- L1144 `test_unconditional_skill_always_shown(self, monkeypatch, tmp_path)` (method)
- L1157 `test_no_args_shows_all_skills(self, monkeypatch, tmp_path)` (method) — Backward compat: calling with no args shows everything.
- L1168 `test_null_metadata_does_not_crash(self, monkeypatch, tmp_path)` (method) — Regression: metadata key present but null should not AttributeError.
- L1183 `test_null_hermes_under_metadata_does_not_crash(self, monkeypatch, tmp_path)` (method) — Regression: metadata.hermes present but null should not crash.
- L1203 `TestToolUseEnforcementGuidance` (class)
- L1204 `test_guidance_mentions_tool_calls(self)` (method)
- L1207 `test_guidance_forbids_description_only(self)` (method)
- L1211 `test_guidance_requires_action(self)` (method)
- L1214 `test_enforcement_models_includes_gpt(self)` (method)
- L1217 `test_enforcement_models_includes_codex(self)` (method)
- L1220 `test_enforcement_models_includes_grok(self)` (method)
- L1223 `test_enforcement_models_includes_qwen(self)` (method)
- L1226 `test_enforcement_models_includes_deepseek(self)` (method)
- L1229 `test_enforcement_models_is_tuple(self)` (method)
- L1233 `TestOpenAIModelExecutionGuidance` (class) — Tests for GPT/Codex-specific execution discipline guidance.
- L1236 `test_guidance_covers_tool_persistence(self)` (method)
- L1242 `test_guidance_covers_prerequisite_checks(self)` (method)
- L1247 `test_guidance_covers_verification(self)` (method)
- L1252 `test_guidance_covers_missing_context(self)` (method)
- L1257 `test_guidance_uses_xml_tags(self)` (method)
- L1263 `test_guidance_is_string(self)` (method)
