---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:40'
updated: '2026-06-08T00:38:40'
---

# hermes_cli/prompt_size.py

Symbols in `hermes_cli/prompt_size.py`.

- L24 `_bytes(s: str)` (function)
- L28 `_build_inspection_agent(platform: str)` (function) — Construct an offline AIAgent for prompt inspection.
- L52 `compute_prompt_breakdown(platform: str='cli')` (function) — Return a dict of prompt-size measurements for a fresh session.
- L112 `_fmt_kb(n: int)` (function)
- L116 `render_breakdown(data: Dict[str, Any])` (function) — Render the breakdown as plain text suitable for a terminal.
- L141 `cmd_prompt_size(args: Any)` (function) — Entry point for ``hermes prompt-size``.
