---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:40'
updated: '2026-06-08T00:38:40'
---

# gateway/platforms/weixin.py

Symbols in `gateway/platforms/weixin.py`.

- L99 `_is_stale_session_ret(ret: 'Optional[int]', errcode: 'Optional[int]', errmsg: 'Optional[str]')` (function) — True when iLink returns ret=-2 / errcode=-2 with 'unknown error',
- L118 `_make_ssl_connector()` (function) — Return a TCPConnector with a certifi CA bundle, or None if certifi is unavailable.
- L156 `check_weixin_requirements()` (function) — Return True when runtime dependencies for Weixin are available.
- L161 `_safe_id(value: Optional[str], keep: int=8)` (function)
- L170 `_json_dumps(payload: Dict[str, Any])` (function)
- L174 `_pkcs7_pad(data: bytes, block_size: int=16)` (function)
- L179 `_aes128_ecb_encrypt(plaintext: bytes, key: bytes)` (function)
- L185 `_aes128_ecb_decrypt(ciphertext: bytes, key: bytes)` (function)
- L197 `_aes_padded_size(size: int)` (function)
- L201 `_random_wechat_uin()` (function)
- L206 `_base_info()` (function)
- L210 `_headers(token: Optional[str], body: str)` (function)
- L224 `_account_dir(hermes_home: str)` (function)
- L230 `_account_file(hermes_home: str, account_id: str)` (function)
- L234 `save_weixin_account(hermes_home: str, *, account_id: str, token: str, base_url: str, user_id: str='')` (function) — Persist account credentials for later reuse.
- L257 `load_weixin_account(hermes_home: str, account_id: str)` (function) — Load persisted account credentials.
- L268 `ContextTokenStore` (class) — Disk-backed ``context_token`` cache keyed by account + peer.
- L271 `__init__(self, hermes_home: str)` (method)
- L275 `_path(self, account_id: str)` (method)
- L278 `_key(self, account_id: str, user_id: str)` (method)
- L281 `restore(self, account_id: str)` (method)
- L298 `get(self, account_id: str, user_id: str)` (method)
- L301 `set(self, account_id: str, user_id: str, token: str)` (method)
- L305 `_persist(self, account_id: str)` (method)
- L318 `TypingTicketCache` (class) — Short-lived typing ticket cache from ``getconfig``.
- L321 `__init__(self, ttl_seconds: float=600.0)` (method)
- L325 `get(self, user_id: str)` (method)
- L334 `set(self, user_id: str, ticket: str)` (method)
- L338 `_cdn_download_url(cdn_base_url: str, encrypted_query_param: str)` (function)
- L342 `_cdn_upload_url(cdn_base_url: str, upload_param: str, filekey: str)` (function)
- L350 `_parse_aes_key(aes_key_b64: str)` (function)
- L361 `_guess_chat_type(message: Dict[str, Any], account_id: str)` (function)
- L370 `_api_post(session: 'aiohttp.ClientSession', *, base_url: str, endpoint: str, payload: Dict[str, Any], token: Optional[str], timeout_ms: int)` (function)
- L393 `_api_get(session: 'aiohttp.ClientSession', *, base_url: str, endpoint: str, timeout_ms: int)` (function)
- L417 `_get_updates(session: 'aiohttp.ClientSession', *, base_url: str, token: str, sync_buf: str, timeout_ms: int)` (function)
- L438 `_send_message(session: 'aiohttp.ClientSession', *, base_url: str, token: str, to: str, text: str, context_token: Optional[str], client_id: str)` (function) — Send a text message via iLink sendmessage API.
- L475 `_send_typing(session: 'aiohttp.ClientSession', *, base_url: str, token: str, to_user_id: str, typing_ticket: str, status: int)` (function)
- L498 `_get_config(session: 'aiohttp.ClientSession', *, base_url: str, token: str, user_id: str, context_token: Optional[str])` (function)
- L519 `_get_upload_url(session: 'aiohttp.ClientSession', *, base_url: str, token: str, to_user_id: str, media_type: int, filekey: str, rawsize: int, rawfilemd5: str, filesize: int, aeskey_hex: str)` (function)
- L551 `_upload_ciphertext(session: 'aiohttp.ClientSession', *, ciphertext: bytes, upload_url: str)` (function) — Upload encrypted media to the CDN.
- L579 `_download_bytes(session: 'aiohttp.ClientSession', *, url: str, timeout_seconds: float=60.0)` (function)
- L607 `_assert_weixin_cdn_url(url: str)` (function) — Raise ValueError if *url* does not point at a known WeChat CDN host.
- L627 `_media_reference(item: Dict[str, Any], key: str)` (function)
- L631 `_download_and_decrypt_media(session: 'aiohttp.ClientSession', *, cdn_base_url: str, encrypted_query_param: Optional[str], aes_key_b64: Optional[str], full_url: Optional[str], timeout_seconds: float)` (function)
- L656 `_mime_from_filename(filename: str)` (function)
- L660 `_split_table_row(line: str)` (function)
- L669 `_normalize_markdown_blocks(content: str)` (function)
- L699 `_wrap_copy_friendly_lines_for_weixin(content: str)` (function) — Wrap long display lines that are hard to copy in WeChat clients.
- L739 `_split_markdown_blocks(content: str)` (function)
- L777 `_split_delivery_units_for_weixin(content: str)` (function) — Split formatted content into chat-friendly delivery units.
- L816 `_looks_like_chatty_line_for_weixin(line: str)` (function) — Return True when a line looks like a standalone chat utterance.
- L836 `_looks_like_heading_line_for_weixin(line: str)` (function) — Return True when a short line behaves like a heading.
- L846 `_should_split_short_chat_block_for_weixin(block: str)` (function) — Split only chat-like multiline blocks into separate bubbles.
- L856 `_pack_markdown_blocks_for_weixin(content: str, max_length: int)` (function)
- L879 `_split_text_for_weixin_delivery(content: str, max_length: int, split_per_line: bool=False)` (function) — Split content into sequential Weixin messages.
- L923 `_coerce_bool(value: Any, default: bool=True)` (function) — Coerce a config value to bool, tolerating strings like ``"true"``.
- L941 `_extract_text(item_list: List[Dict[str, Any]])` (function)
- L970 `_message_type_from_media(media_types: List[str], text: str)` (function)
- L984 `_sync_buf_path(hermes_home: str, account_id: str)` (function)
- L988 `_load_sync_buf(hermes_home: str, account_id: str)` (function)
- L998 `_save_sync_buf(hermes_home: str, account_id: str, sync_buf: str)` (function)
- L1003 `qr_login(hermes_home: str, *, bot_type: str='3', timeout_seconds: int=480)` (function) — Run the interactive iLink QR login flow.
- L1138 `WeixinAdapter` (class) — Native Hermes adapter for Weixin personal accounts.
- L1147 `__init__(self, config: PlatformConfig)` (method)
- L1214 `_coerce_float_extra(self, key: str, default: float)` (method) — Read a float from ``config.extra``, guarding against bad/non-finite values.
- L1234 `_coerce_list(value: Any)` (method)
- L1243 `connect(self)` (method)
- L1291 `disconnect(self)` (method)
- L1316 `_poll_loop(self)` (method)
- L1376 `_process_message_safe(self, message: Dict[str, Any])` (method)
- L1382 `_process_message(self, message: Dict[str, Any])` (method)
- L1452 `_is_dm_allowed(self, sender_id: str)` (method)
- L1460 `enforces_own_access_policy(self)` (method) — Weixin gates DM/group access at intake via dm_policy/group_policy.
- L1470 `_text_batch_key(self, event: MessageEvent)` (method) — Session-scoped key for text message batching.
- L1479 `_enqueue_text_event(self, event: MessageEvent)` (method) — Buffer a text event and reset the flush timer.
- L1508 `_flush_text_batch(self, key: str)` (method) — Wait for quiet period then dispatch aggregated text.
- L1529 `_collect_media(self, item: Dict[str, Any], media_paths: List[str], media_types: List[str])` (method)
- L1552 `_download_image(self, item: Dict[str, Any])` (method)
- L1570 `_download_video(self, item: Dict[str, Any])` (method)
- L1586 `_download_file(self, item: Dict[str, Any])` (method)
- L1605 `_download_voice(self, item: Dict[str, Any])` (method)
- L1624 `_maybe_fetch_typing_ticket(self, user_id: str, context_token: Optional[str])` (method)
- L1643 `_split_text(self, content: str)` (method)
- L1648 `_send_text_chunk(self, *, chat_id: str, chunk: str, context_token: Optional[str], client_id: str)` (method) — Send a single text chunk with per-chunk retry and backoff.
- L1744 `send(self, chat_id: str, content: str, reply_to: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None)` (method)
- L1811 `send_typing(self, chat_id: str, metadata: Optional[Dict[str, Any]]=None)` (method)
- L1829 `stop_typing(self, chat_id: str)` (method)
- L1847 `send_image(self, chat_id: str, image_url: str, caption: str, reply_to: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None)` (method)
- L1872 `send_image_file(self, chat_id: str, image_path: str, caption: Optional[str]=None, reply_to: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None, **kwargs)` (method)
- L1889 `send_document(self, chat_id: str, file_path: str, caption: Optional[str]=None, file_name: Optional[str]=None, reply_to: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None, **kwargs)` (method)
- L1909 `send_video(self, chat_id: str, video_path: str, caption: Optional[str]=None, reply_to: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None)` (method)
- L1926 `send_voice(self, chat_id: str, audio_path: str, caption: Optional[str]=None, reply_to: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None)` (method)
- L1953 `_download_remote_media(self, url: str)` (method)
- L1972 `_send_file(self, chat_id: str, path: str, caption: str, force_file_attachment: bool=False)` (method)
- L2070 `_outbound_media_builder(self, path: str, force_file_attachment: bool=False)` (method)
- L2139 `get_chat_info(self, chat_id: str)` (method)
- L2143 `format_message(self, content: Optional[str])` (method)
- L2149 `send_weixin_direct(*, extra: Dict[str, Any], token: Optional[str], chat_id: str, message: str, media_files: Optional[List[Tuple[str, bool]]]=None)` (function) — One-shot send helper for ``send_message`` and cron delivery.
