---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/hermes_cli/test_gateway_service.py

Symbols in `tests/hermes_cli/test_gateway_service.py`.

- L21 `TestUserSystemdPrivateSocketPreflight` (class)
- L22 `test_preflight_accepts_private_socket_without_dbus_bus(self, monkeypatch)` (method)
- L30 `test_wait_for_user_dbus_socket_accepts_private_socket(self, monkeypatch)` (method)
- L41 `TestSystemdServiceRefresh` (class)
- L42 `test_systemd_install_repairs_outdated_unit_without_force(self, tmp_path, monkeypatch)` (method)
- L65 `test_systemd_start_refreshes_outdated_unit(self, tmp_path, monkeypatch)` (method)
- L88 `test_systemd_restart_refreshes_outdated_unit(self, tmp_path, monkeypatch)` (method)
- L121 `test_systemd_stop_marks_running_gateway_as_planned_stop(self, monkeypatch)` (method)
- L145 `test_systemd_stop_timeout_prints_status_guidance(self, monkeypatch, capsys)` (method)
- L169 `test_systemd_restart_timeout_prints_status_guidance(self, monkeypatch, capsys)` (method) — `hermes gateway restart` must not surface a raw TimeoutExpired traceback.
- L208 `test_run_gateway_refreshes_outdated_unit_on_boot(self, tmp_path, monkeypatch)` (method) — run_gateway() should refresh the systemd unit on boot so that
- L238 `test_refresh_refuses_to_bake_pytest_tmpdir_into_real_user_unit(self, tmp_path, monkeypatch)` (method) — Defense in depth: ``refresh_systemd_unit_if_needed()`` runs every
- L293 `TestRequireServiceInstalled` (class)
- L294 `test_exits_with_install_hint_when_unit_missing(self, tmp_path, monkeypatch, capsys)` (method)
- L306 `test_passes_when_unit_exists(self, tmp_path, monkeypatch)` (method)
- L314 `TestGeneratedSystemdUnits` (class)
- L315 `_expected_timeout_stop_sec(self)` (method)
- L319 `test_user_unit_avoids_recursive_execstop_and_uses_extended_stop_timeout(self, monkeypatch)` (method)
- L336 `test_user_unit_includes_resolved_node_directory_in_path(self, monkeypatch)` (method)
- L343 `test_user_unit_includes_wsl_windows_interop_paths(self, monkeypatch)` (method)
- L356 `test_user_unit_omits_windows_interop_paths_outside_wsl(self, monkeypatch)` (method)
- L365 `test_system_unit_includes_wsl_windows_interop_paths(self, monkeypatch)` (method)
- L380 `test_system_unit_avoids_recursive_execstop_and_uses_extended_stop_timeout(self, monkeypatch)` (method)
- L399 `TestGatewayStopCleanup` (class)
- L400 `test_stop_only_kills_current_profile_by_default(self, tmp_path, monkeypatch)` (method) — Without --all, stop uses systemd (if available) and does NOT call
- L427 `test_stop_all_sweeps_all_gateway_processes(self, tmp_path, monkeypatch)` (method) — With --all, stop uses systemd AND calls the global kill_gateway_processes().
- L453 `TestLaunchdServiceRecovery` (class)
- L454 `test_get_restart_drain_timeout_prefers_env_then_config_then_default(self, monkeypatch)` (method)
- L479 `test_launchd_install_repairs_outdated_plist_without_force(self, tmp_path, monkeypatch)` (method)
- L503 `test_launchd_start_reloads_unloaded_job_and_retries(self, tmp_path, monkeypatch)` (method)
- L530 `test_launchd_start_reloads_on_kickstart_exit_code_113(self, tmp_path, monkeypatch)` (method) — Exit code 113 ("Could not find service") should also trigger bootstrap recovery.
- L558 `test_launchd_restart_drains_running_gateway_before_kickstart(self, monkeypatch)` (method)
- L584 `test_launchd_restart_self_requests_graceful_restart_without_kickstart(self, monkeypatch, capsys)` (method)
- L607 `test_launchd_stop_uses_bootout_not_kill(self, monkeypatch)` (method) — launchd_stop must bootout the service so KeepAlive doesn't respawn it.
- L626 `test_launchd_stop_tolerates_already_unloaded(self, monkeypatch, capsys)` (method) — launchd_stop silently handles exit codes 3/113 (job not loaded).
- L646 `test_launchd_stop_waits_for_process_exit(self, monkeypatch)` (method) — launchd_stop calls _wait_for_gateway_exit after bootout.
- L664 `test_launchd_status_reports_local_stale_plist_when_unloaded(self, tmp_path, monkeypatch, capsys)` (method)
- L682 `test_launchd_domain_uses_user_domain(self)` (method)
- L687 `test_launchctl_domain_unsupported_recognizes_macos26_codes(self)` (method)
- L695 `test_launchd_start_reloads_on_kickstart_exit_code_125(self, tmp_path, monkeypatch)` (method) — Exit code 125 means the job is absent from the domain → bootstrap recovery.
- L725 `test_launchd_start_falls_back_to_detached_when_rebootstrap_fails(self, tmp_path, monkeypatch, capsys)` (method) — If even a fresh bootstrap can't manage the domain, spawn detached.
- L760 `test_launchd_install_falls_back_to_detached_on_bootstrap_5(self, tmp_path, monkeypatch, capsys)` (method) — macOS bootstrap error 5 should spawn a detached gateway, not crash.
- L784 `test_launchd_restart_falls_back_to_detached_on_error_5(self, monkeypatch, capsys)` (method) — kickstart -k error 5 (domain unmanageable) should relaunch detached.
- L812 `test_launchd_stop_tolerates_domain_unsupported_bootout(self, monkeypatch, capsys)` (method) — bootout exit 125 (macOS 26) must fall through to PID-based kill, not raise.
- L828 `test_launchd_fallback_exits_when_spawn_fails(self, monkeypatch, capsys)` (method) — If the detached spawn fails, surface the manual workaround and exit 1.
- L839 `TestGatewayServiceDetection` (class)
- L840 `test_supports_systemd_services_requires_systemctl_binary(self, monkeypatch)` (method)
- L847 `test_supports_systemd_services_returns_true_when_systemctl_present(self, monkeypatch)` (method)
- L855 `test_is_service_running_checks_system_scope_when_user_scope_is_inactive(self, monkeypatch)` (method)
- L879 `test_is_service_running_returns_false_when_systemctl_missing(self, monkeypatch)` (method)
- L896 `TestGatewaySystemServiceRouting` (class)
- L897 `test_systemd_restart_gracefully_restarts_running_service_and_waits(self, monkeypatch, capsys)` (method)
- L942 `test_systemd_restart_uses_systemd_main_pid_when_pid_file_is_missing(self, monkeypatch, capsys)` (method)
- L979 `test_wait_for_systemd_restart_waits_for_runtime_running(self, monkeypatch, capsys)` (method)
- L1001 `test_systemd_restart_reports_start_limit_hit(self, monkeypatch, capsys)` (method)
- L1033 `test_systemd_restart_recovers_failed_planned_restart(self, monkeypatch, capsys)` (method)
- L1089 `test_systemd_status_surfaces_planned_restart_failure(self, monkeypatch, capsys)` (method)
- L1122 `test_gateway_status_dispatches_full_flag(self, monkeypatch)` (method)
- L1159 `test_gateway_install_reports_termux_manual_mode(self, monkeypatch, capsys)` (method)
- L1177 `test_gateway_status_prefers_system_service_when_only_system_unit_exists(self, monkeypatch)` (method)
- L1201 `test_gateway_status_reports_manual_process_when_service_is_stopped(self, monkeypatch, capsys)` (method)
- L1237 `test_gateway_status_on_termux_shows_manual_guidance(self, monkeypatch, capsys)` (method)
- L1251 `test_gateway_restart_does_not_fallback_to_foreground_when_launchd_restart_fails(self, tmp_path, monkeypatch)` (method)
- L1280 `TestDetectVenvDir` (class) — Tests for _detect_venv_dir() virtualenv detection.
- L1283 `test_detects_active_virtualenv_via_sys_prefix(self, tmp_path, monkeypatch)` (method)
- L1292 `test_falls_back_to_dot_venv_directory(self, tmp_path, monkeypatch)` (method)
- L1305 `test_falls_back_to_venv_directory(self, tmp_path, monkeypatch)` (method)
- L1317 `test_prefers_dot_venv_over_venv(self, tmp_path, monkeypatch)` (method)
- L1329 `test_returns_none_when_no_virtualenv(self, tmp_path, monkeypatch)` (method)
- L1339 `TestSystemUnitHermesHome` (class) — HERMES_HOME in system units must reference the target user, not root.
- L1342 `test_system_unit_uses_target_user_home_not_calling_user(self, monkeypatch)` (method)
- L1360 `test_system_unit_remaps_profile_to_target_user(self, monkeypatch)` (method)
- L1378 `test_system_unit_preserves_custom_hermes_home(self, monkeypatch)` (method)
- L1395 `test_user_unit_unaffected_by_change(self)` (method)
- L1403 `TestHermesHomeForTargetUser` (class) — Unit tests for _hermes_home_for_target_user().
- L1406 `test_remaps_default_home(self, monkeypatch)` (method)
- L1413 `test_remaps_profile_path(self, monkeypatch)` (method)
- L1420 `test_keeps_custom_path(self, monkeypatch)` (method)
- L1427 `test_noop_when_same_user(self, monkeypatch)` (method)
- L1435 `TestGeneratedUnitUsesDetectedVenv` (class)
- L1436 `test_systemd_unit_uses_dot_venv_when_detected(self, tmp_path, monkeypatch)` (method)
- L1452 `TestGeneratedUnitIncludesLocalBin` (class) — ~/.local/bin must be in PATH so uvx/pipx tools are discoverable.
- L1455 `test_user_unit_includes_local_bin_in_path(self, monkeypatch)` (method)
- L1465 `test_system_unit_includes_local_bin_in_path(self, monkeypatch)` (method)
- L1476 `TestSystemServiceIdentityRootHandling` (class) — Root user handling in _system_service_identity().
- L1479 `test_auto_detected_root_is_rejected(self, monkeypatch)` (method) — When root is auto-detected (not explicitly requested), raise.
- L1489 `test_explicit_root_is_allowed(self, monkeypatch)` (method) — When root is explicitly passed via --run-as-user root, allow it.
- L1499 `test_non_root_user_passes_through(self, monkeypatch)` (method) — Normal non-root user works as before.
- L1514 `TestEnsureUserSystemdEnv` (class) — Tests for _ensure_user_systemd_env() D-Bus session bus auto-detection.
- L1517 `test_sets_xdg_runtime_dir_when_missing(self, tmp_path, monkeypatch)` (method)
- L1535 `test_sets_dbus_address_when_bus_socket_exists(self, tmp_path, monkeypatch)` (method)
- L1549 `test_preserves_existing_env_vars(self, monkeypatch)` (method)
- L1558 `test_no_dbus_when_bus_socket_missing(self, tmp_path, monkeypatch)` (method)
- L1571 `test_systemctl_cmd_calls_ensure_for_user_mode(self, monkeypatch)` (method)
- L1579 `test_systemctl_cmd_skips_ensure_for_system_mode(self, monkeypatch)` (method)
- L1588 `TestPreflightUserSystemd` (class) — Tests for _preflight_user_systemd() — D-Bus reachability before systemctl --user.
- L1596 `test_noop_when_bus_socket_exists(self, monkeypatch)` (method) — Socket already there (desktop / linger + prior login) → no-op.
- L1609 `test_raises_when_linger_disabled_and_loginctl_denied(self, monkeypatch)` (method) — Rick's scenario: no D-Bus, no linger, non-root SSH → clear error.
- L1641 `test_raises_when_loginctl_missing(self, monkeypatch)` (method) — No loginctl binary at all → suggest sudo install + manual fix.
- L1662 `test_linger_enabled_but_socket_still_missing(self, monkeypatch)` (method) — Edge case: linger says yes but the bus socket never came up.
- L1684 `test_enable_linger_succeeds_and_socket_appears(self, monkeypatch, capsys)` (method) — Happy remediation path: polkit allows enable-linger, socket spawns.
- L1718 `TestProfileArg` (class) — Tests for _profile_arg — returns '--profile <name>' for named profiles.
- L1721 `test_default_hermes_home_returns_empty(self, tmp_path, monkeypatch)` (method) — Default ~/.hermes should not produce a --profile flag.
- L1730 `test_named_profile_returns_flag(self, tmp_path, monkeypatch)` (method) — ~/.hermes/profiles/mybot should return '--profile mybot'.
- L1739 `test_hash_path_returns_empty(self, tmp_path, monkeypatch)` (method) — Arbitrary non-profile HERMES_HOME should return empty string.
- L1748 `test_nested_profile_path_returns_empty(self, tmp_path, monkeypatch)` (method) — ~/.hermes/profiles/mybot/subdir should NOT match — too deep.
- L1757 `test_invalid_profile_name_returns_empty(self, tmp_path, monkeypatch)` (method) — Profile names with invalid chars should not match the regex.
- L1766 `test_systemd_unit_includes_profile(self, tmp_path, monkeypatch)` (method) — generate_systemd_unit should include --profile in ExecStart for named profiles.
- L1777 `test_launchd_plist_includes_profile(self, tmp_path, monkeypatch)` (method) — generate_launchd_plist should include --profile in ProgramArguments for named profiles.
- L1788 `test_launchd_plist_supports_aqua_and_background_sessions(self)` (method)
- L1796 `test_launchd_plist_path_uses_real_user_home_not_profile_home(self, tmp_path, monkeypatch)` (method)
- L1814 `TestRemapPathForUser` (class) — Unit tests for _remap_path_for_user().
- L1817 `test_remaps_path_under_current_home(self, monkeypatch, tmp_path)` (method)
- L1826 `test_keeps_system_path_unchanged(self, monkeypatch, tmp_path)` (method)
- L1832 `test_noop_when_same_user(self, monkeypatch, tmp_path)` (method)
- L1840 `TestSystemUnitPathRemapping` (class) — System units must remap ALL paths from the caller's home to the target user.
- L1843 `test_system_unit_has_no_root_paths(self, monkeypatch, tmp_path)` (method)
- L1879 `TestDockerAwareGateway` (class) — Tests for Docker container awareness in gateway commands.
- L1882 `test_run_systemctl_raises_runtimeerror_when_missing(self, monkeypatch)` (method) — _run_systemctl raises RuntimeError with container guidance when systemctl is absent.
- L1894 `test_run_systemctl_passes_through_on_success(self, monkeypatch)` (method) — _run_systemctl delegates to subprocess.run when systemctl exists.
- L1909 `test_install_in_container_prints_docker_guidance(self, monkeypatch, capsys)` (method) — 'hermes gateway install' inside Docker exits 0 with container guidance.
- L1929 `test_uninstall_in_container_prints_docker_guidance(self, monkeypatch, capsys)` (method) — 'hermes gateway uninstall' inside Docker exits 0 with container guidance.
- L1947 `test_start_in_container_prints_docker_guidance(self, monkeypatch, capsys)` (method) — 'hermes gateway start' inside Docker exits 0 with container guidance.
- L1967 `TestLegacyHermesUnitDetection` (class) — Tests for _find_legacy_hermes_units / has_legacy_hermes_units.
- L1988 `_setup_search_paths(tmp_path, monkeypatch)` (method) — Redirect the legacy search to user_dir + system_dir under tmp_path.
- L2001 `test_detects_legacy_hermes_service_in_user_scope(self, tmp_path, monkeypatch)` (method)
- L2015 `test_detects_legacy_hermes_service_in_system_scope(self, tmp_path, monkeypatch)` (method)
- L2028 `test_ignores_profile_unit_hermes_gateway_coder(self, tmp_path, monkeypatch)` (method) — CRITICAL: profile units must NOT be flagged as legacy.
- L2053 `test_ignores_unrelated_hermes_service(self, tmp_path, monkeypatch)` (method) — Third-party ``hermes.service`` that isn't ours stays untouched.
- L2071 `test_returns_empty_when_no_legacy_files_exist(self, tmp_path, monkeypatch)` (method)
- L2077 `test_detects_both_scopes_simultaneously(self, tmp_path, monkeypatch)` (method) — When a user has BOTH user-scope and system-scope legacy units,
- L2089 `test_accepts_alternate_execstart_formats(self, tmp_path, monkeypatch)` (method) — Older installs may have used different python invocations.
- L2115 `test_print_legacy_unit_warning_is_noop_when_empty(self, tmp_path, monkeypatch, capsys)` (method)
- L2123 `test_print_legacy_unit_warning_shows_migration_hint(self, tmp_path, monkeypatch, capsys)` (method)
- L2134 `test_handles_unreadable_unit_file_gracefully(self, tmp_path, monkeypatch)` (method) — A permission error reading a unit file must not crash detection.
- L2154 `TestRemoveLegacyHermesUnits` (class) — Tests for remove_legacy_hermes_units (the migration action).
- L2163 `_setup(tmp_path, monkeypatch, as_root=False)` (method)
- L2184 `test_returns_zero_when_no_legacy_units(self, tmp_path, monkeypatch, capsys)` (method)
- L2193 `test_dry_run_lists_without_removing(self, tmp_path, monkeypatch, capsys)` (method)
- L2209 `test_removes_user_scope_legacy_unit(self, tmp_path, monkeypatch, capsys)` (method)
- L2225 `test_system_scope_without_root_defers_removal(self, tmp_path, monkeypatch, capsys)` (method)
- L2238 `test_system_scope_with_root_removes(self, tmp_path, monkeypatch, capsys)` (method)
- L2257 `test_removes_both_scopes_with_root(self, tmp_path, monkeypatch, capsys)` (method)
- L2271 `test_does_not_touch_profile_units_during_migration(self, tmp_path, monkeypatch, capsys)` (method) — Teknium's constraint: profile units (hermes-gateway-coder.service)
- L2291 `test_interactive_prompt_no_skips_removal(self, tmp_path, monkeypatch, capsys)` (method) — When interactive=True and user answers no, no removal happens.
- L2306 `TestMigrateLegacyCommand` (class) — Tests for the `hermes gateway migrate-legacy` subcommand dispatch.
- L2309 `test_migrate_legacy_subparser_accepts_dry_run_and_yes(self)` (method) — Verify the argparse subparser is registered and parses flags.
- L2336 `test_gateway_command_migrate_legacy_dispatches(self, tmp_path, monkeypatch, capsys)` (method) — gateway_command(args) with subcmd='migrate-legacy' calls the helper.
- L2359 `TestGatewayStatusParser` (class)
- L2360 `test_gateway_status_subparser_accepts_full_flag(self)` (method)
- L2375 `test_gateway_command_migrate_legacy_dry_run_passes_through(self, monkeypatch)` (method)
- L2396 `test_migrate_legacy_on_unsupported_platform_prints_message(self, monkeypatch, capsys)` (method)
- L2411 `TestSystemdInstallOffersLegacyRemoval` (class) — Verify that systemd_install prompts to remove legacy units first.
- L2414 `test_install_offers_removal_when_legacy_detected(self, tmp_path, monkeypatch, capsys)` (method) — When legacy units exist, install flow should call the removal
- L2455 `test_install_declines_legacy_removal_when_user_says_no(self, tmp_path, monkeypatch)` (method) — When legacy units exist and user declines, install still proceeds
- L2495 `test_install_skips_legacy_check_when_none_present(self, tmp_path, monkeypatch)` (method) — No legacy → no prompt, no helper call.
- L2537 `TestSystemScopeRequiresRootError` (class) — Tests for the SystemScopeRequiresRootError replacement of sys.exit(1).
- L2549 `test_require_root_raises_when_non_root(self, monkeypatch)` (method)
- L2562 `test_require_root_noop_when_root(self, monkeypatch)` (method)
- L2568 `test_error_is_runtime_error_subclass(self)` (method) — Wizards use ``except Exception`` guards — the error must be a
- L2579 `TestSystemScopeWizardPreCheck` (class) — Tests for _system_scope_wizard_would_need_root — the guard the
- L2586 `_setup_units(tmp_path, monkeypatch, system_present: bool, user_present: bool)` (method)
- L2601 `test_non_root_with_only_system_unit_returns_true(self, tmp_path, monkeypatch)` (method)
- L2607 `test_root_never_needs_root(self, tmp_path, monkeypatch)` (method)
- L2613 `test_non_root_with_user_unit_present_returns_false(self, tmp_path, monkeypatch)` (method)
- L2620 `test_non_root_with_no_units_returns_false(self, tmp_path, monkeypatch)` (method)
- L2626 `test_non_root_with_explicit_system_arg_returns_true(self, tmp_path, monkeypatch)` (method)
- L2634 `TestSystemScopeRemediationOutput` (class) — Tests for _print_system_scope_remediation — the actionable guidance
- L2639 `test_start_remediation_mentions_sudo_systemctl_and_uninstall(self, capsys, monkeypatch)` (method)
- L2651 `test_restart_remediation_uses_systemctl_restart(self, capsys, monkeypatch)` (method)
- L2660 `test_stop_remediation_uses_systemctl_stop(self, capsys, monkeypatch)` (method)
- L2670 `TestGatewayCommandCatchesSystemScopeError` (class) — The direct CLI path (``hermes gateway start --system`` etc.) must
- L2677 `test_non_root_system_start_exits_one_with_clean_message(self, tmp_path, monkeypatch, capsys)` (method)
- L2705 `TestServiceWorkingDirIsStable` (class) — The gateway service must anchor WorkingDirectory at a stable path
- L2711 `test_stable_working_dir_uses_hermes_home(self, tmp_path, monkeypatch)` (method)
- L2717 `test_stable_working_dir_falls_back_to_project_root(self, tmp_path, monkeypatch)` (method)
- L2723 `test_user_unit_workingdirectory_is_hermes_home_not_checkout(self, tmp_path, monkeypatch)` (method)
- L2735 `test_launchd_workingdirectory_is_hermes_home(self, tmp_path, monkeypatch)` (method)
- L2747 `test_launchd_plist_keepalive_unconditional(self, tmp_path, monkeypatch)` (method) — KeepAlive must be unconditional <true/> so the gateway restarts on clean exits.
