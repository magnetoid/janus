---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:40'
updated: '2026-06-08T00:38:40'
---

# hermes_cli/kanban_db.py

Symbols in `hermes_cli/kanban_db.py`.

- L125 `_resolve_claim_ttl_seconds(ttl_seconds: Optional[int]=None)` (function) — Return the effective claim TTL, honoring the kanban env override.
- L168 `_resolve_crash_grace_seconds()` (function) — Return the crash-detection grace period in seconds.
- L187 `_resolve_rate_limit_cooldown_seconds()` (function) — Return the rate-limit requeue cooldown in seconds.
- L233 `scoped_current_board(slug: str)` (function) — Temporarily pin the active board for the current context only.
- L249 `_normalize_board_slug(slug: Optional[str])` (function) — Lowercase + strip a slug; validate; return ``None`` for empty.
- L264 `kanban_home()` (function) — Return the shared Hermes root that anchors the kanban board.
- L287 `boards_root()` (function) — Return ``<root>/kanban/boards`` — the parent of non-default board dirs.
- L298 `current_board_path()` (function) — Return the path to ``<root>/kanban/current``.
- L308 `get_current_board()` (function) — Return the active board slug, honouring the resolution chain.
- L356 `set_current_board(slug: str)` (function) — Persist ``slug`` as the active board. Returns the file written.
- L373 `clear_current_board()` (function) — Remove ``<root>/kanban/current`` so the active board reverts to ``default``.
- L381 `board_dir(board: Optional[str]=None)` (function) — Return the on-disk directory for ``board``.
- L395 `board_exists(board: Optional[str]=None)` (function) — Return True if the board has persisted metadata or a DB on disk.
- L409 `kanban_db_path(board: Optional[str]=None)` (function) — Return the path to the ``kanban.db`` for ``board``.
- L434 `workspaces_root(board: Optional[str]=None)` (function) — Return the directory under which ``scratch`` workspaces are created.
- L456 `attachments_root(board: Optional[str]=None)` (function) — Return the directory under which task file attachments are stored.
- L486 `task_attachments_dir(task_id: str, board: Optional[str]=None)` (function) — Return the per-task attachment directory ``<root>/<task_id>/``.
- L491 `worker_logs_dir(board: Optional[str]=None)` (function) — Return the directory under which per-task worker logs are written.
- L507 `board_metadata_path(board: Optional[str]=None)` (function) — Return the path to ``board.json`` for ``board``.
- L518 `_default_board_display_name(slug: str)` (function) — Turn a slug into a reasonable default display name.
- L528 `read_board_metadata(board: Optional[str]=None)` (function) — Return ``board.json`` contents (or synthesized defaults).
- L562 `write_board_metadata(board: Optional[str], *, name: Optional[str]=None, description: Optional[str]=None, icon: Optional[str]=None, color: Optional[str]=None, archived: Optional[bool]=None, default_workdir: Optional[str]=None)` (function) — Create / update ``board.json`` for ``board``.
- L606 `create_board(slug: str, *, name: Optional[str]=None, description: Optional[str]=None, icon: Optional[str]=None, color: Optional[str]=None, default_workdir: Optional[str]=None)` (function) — Create a new board directory + DB + metadata. Idempotent.
- L637 `list_boards(*, include_archived: bool=True)` (function) — Enumerate all boards that exist on disk.
- L681 `remove_board(slug: str, *, archive: bool=True)` (function) — Remove or archive a board.
- L733 `Task` (class) — In-memory view of a row from the ``tasks`` table.
- L806 `from_row(cls, row: sqlite3.Row)` (method)
- L883 `Run` (class) — In-memory view of a ``task_runs`` row.
- L911 `from_row(cls, row: sqlite3.Row)` (method)
- L937 `Comment` (class)
- L946 `Attachment` (class) — In-memory view of a row from the ``task_attachments`` table.
- L960 `Event` (class)
- L1151 `_resolve_busy_timeout_ms()` (function) — Return the SQLite busy timeout for Kanban connections.
- L1169 `_sqlite_connect(path: Path)` (function) — Open a Kanban SQLite connection with consistent lock waiting.
- L1185 `_cross_process_init_lock(path: Path)` (function) — Serialize first-connect WAL/schema/integrity setup across processes.
- L1233 `_looks_like_tls_record_at(data: bytes, offset: int)` (function) — Return True for a TLS record header at ``data[offset:]``.
- L1249 `_validate_sqlite_header(path: Path)` (function) — Fail early with an actionable error for non-SQLite Kanban DB files.
- L1284 `KanbanDbCorruptError` (class) — Raised when an existing kanban DB file fails integrity checks.
- L1292 `__init__(self, db_path: Path, backup_path: Optional[Path], reason: str)` (method)
- L1303 `_backup_corrupt_db(path: Path)` (function) — Copy a corrupt DB (and its WAL/SHM sidecars) to a content-addressed backup.
- L1357 `_guard_existing_db_is_healthy(path: Path)` (function) — Run ``PRAGMA integrity_check`` on an existing non-empty DB file.
- L1415 `connect(db_path: Optional[Path]=None, *, board: Optional[str]=None)` (function) — Open (and initialize if needed) the kanban DB.
- L1493 `connect_closing(db_path: Optional[Path]=None, *, board: Optional[str]=None)` (function) — Open a kanban DB connection and guarantee it is closed on exit.
- L1527 `init_db(db_path: Optional[Path]=None, *, board: Optional[str]=None)` (function) — Create the schema if it doesn't exist; return the path used.
- L1557 `_add_column_if_missing(conn: sqlite3.Connection, table: str, column: str, ddl: str)` (function) — Run ``ALTER TABLE <table> ADD COLUMN <ddl>``, idempotent across races.
- L1576 `_migrate_add_optional_columns(conn: sqlite3.Connection)` (function) — Add columns that were introduced after v1 release to legacy DBs.
- L1867 `_table_has_drifted(conn: sqlite3.Connection, table: str)` (function) — True when ``table`` still carries the legacy (pre-AUTOINCREMENT) shape.
- L1882 `_rebuild_drifted_tables(conn: sqlite3.Connection)` (function) — Rebuild any kanban table whose column types drifted from SCHEMA_SQL.
- L1942 `_check_file_length_invariant(conn: sqlite3.Connection)` (function) — Read the SQLite header page_count and compare against actual file size.
- L1982 `write_txn(conn: sqlite3.Connection)` (function) — Context manager for an IMMEDIATE write transaction.
- L2016 `_new_task_id()` (function) — Generate a short, URL-safe task id.
- L2029 `_claimer_id()` (function) — Return a ``host:pid`` string that identifies this claimer.
- L2043 `_canonical_assignee(assignee: Optional[str])` (function) — Lowercase-assignee normalization for Kanban rows (dashboard/CLI parity).
- L2052 `create_task(conn: sqlite3.Connection, *, title: str, body: Optional[str]=None, assignee: Optional[str]=None, created_by: Optional[str]=None, workspace_kind: str='scratch', workspace_path: Optional[str]=None, branch_name: Optional[str]=None, tenant: Optional[str]=None, priority: int=0, parents: Iterable[str]=(), triage: bool=False, idempotency_key: Optional[str]=None, max_runtime_seconds: Optional[int]=None, skills: Optional[Iterable[str]]=None, max_retries: Optional[int]=None, goal_mode: bool=False, goal_max_turns: Optional[int]=None, initial_status: str='running', session_id: Optional[str]=None, board: Optional[str]=None)` (function) — Create a new task and optionally link it under parent tasks.
- L2292 `_find_missing_parents(conn: sqlite3.Connection, parents: Iterable[str])` (function)
- L2305 `get_task(conn: sqlite3.Connection, task_id: str)` (function)
- L2324 `list_tasks(conn: sqlite3.Connection, *, assignee: Optional[str]=None, status: Optional[str]=None, tenant: Optional[str]=None, session_id: Optional[str]=None, include_archived: bool=False, limit: Optional[int]=None, order_by: Optional[str]=None, workflow_template_id: Optional[str]=None, current_step_key: Optional[str]=None)` (function)
- L2376 `assign_task(conn: sqlite3.Connection, task_id: str, profile: Optional[str])` (function) — Assign or reassign a task.  Returns True on success.
- L2413 `link_tasks(conn: sqlite3.Connection, parent_id: str, child_id: str)` (function)
- L2443 `_would_cycle(conn: sqlite3.Connection, parent_id: str, child_id: str)` (function) — Return True if adding parent->child creates a cycle.
- L2466 `unlink_tasks(conn: sqlite3.Connection, parent_id: str, child_id: str)` (function)
- L2487 `parent_ids(conn: sqlite3.Connection, task_id: str)` (function)
- L2495 `child_ids(conn: sqlite3.Connection, task_id: str)` (function)
- L2503 `parent_results(conn: sqlite3.Connection, task_id: str)` (function) — Return ``(parent_id, result)`` for every done parent of ``task_id``.
- L2522 `add_comment(conn: sqlite3.Connection, task_id: str, author: str, body: str)` (function)
- L2544 `list_comments(conn: sqlite3.Connection, task_id: str)` (function)
- L2565 `add_attachment(conn: sqlite3.Connection, task_id: str, *, filename: str, stored_path: str, content_type: Optional[str]=None, size: int=0, uploaded_by: Optional[str]=None)` (function) — Record a file attachment for a task. Returns the new attachment id.
- L2614 `list_attachments(conn: sqlite3.Connection, task_id: str)` (function)
- L2634 `get_attachment(conn: sqlite3.Connection, attachment_id: int)` (function)
- L2652 `delete_attachment(conn: sqlite3.Connection, attachment_id: int)` (function) — Delete an attachment row and its on-disk blob. Returns the removed row.
- L2676 `list_events(conn: sqlite3.Connection, task_id: str)` (function)
- L2700 `_append_event(conn: sqlite3.Connection, task_id: str, kind: str, payload: Optional[dict]=None, *, run_id: Optional[int]=None)` (function) — Record an event row.  Called from within an already-open txn.
- L2724 `_end_run(conn: sqlite3.Connection, task_id: str, *, outcome: str, summary: Optional[str]=None, error: Optional[str]=None, metadata: Optional[dict]=None, status: Optional[str]=None)` (function) — Close the currently-active run for ``task_id`` and clear the pointer.
- L2781 `_current_run_id(conn: sqlite3.Connection, task_id: str)` (function)
- L2788 `_synthesize_ended_run(conn: sqlite3.Connection, task_id: str, *, outcome: str, summary: Optional[str]=None, error: Optional[str]=None, metadata: Optional[dict]=None)` (function) — Insert a zero-duration, already-closed run row.
- L2843 `_has_sticky_block(conn: sqlite3.Connection, task_id: str)` (function) — Return True when ``task_id`` is sticky-blocked by an explicit
- L2881 `recompute_ready(conn: sqlite3.Connection, failure_limit: int=None)` (function) — Promote ``todo`` tasks to ``ready`` when all parents are ``done`` or ``archived``.
- L2972 `claim_task(conn: sqlite3.Connection, task_id: str, *, ttl_seconds: Optional[int]=None, claimer: Optional[str]=None)` (function) — Atomically transition ``ready -> running``.
- L3086 `claim_review_task(conn: sqlite3.Connection, task_id: str, *, ttl_seconds: Optional[int]=None, claimer: Optional[str]=None)` (function) — Atomically transition ``review -> running``.
- L3161 `heartbeat_claim(conn: sqlite3.Connection, task_id: str, *, ttl_seconds: Optional[int]=None, claimer: Optional[str]=None)` (function) — Extend a running claim.  Returns True if we still own it.
- L3192 `release_stale_claims(conn: sqlite3.Connection, *, signal_fn=None)` (function) — Reset any ``running`` task whose claim has expired.
- L3330 `reclaim_task(conn: sqlite3.Connection, task_id: str, *, reason: Optional[str]=None, signal_fn=None)` (function) — Operator-driven reclaim: release the claim and reset to ``ready``.
- L3399 `reassign_task(conn: sqlite3.Connection, task_id: str, profile: Optional[str], *, reclaim_first: bool=False, reason: Optional[str]=None)` (function) — Reassign a task, optionally reclaiming a stuck running worker first.
- L3430 `_verify_created_cards(conn: sqlite3.Connection, completing_task_id: str, claimed_ids: Iterable[str])` (function) — Partition ``claimed_ids`` into (verified, phantom).
- L3511 `_scan_prose_for_phantom_ids(conn: sqlite3.Connection, text: str)` (function) — Regex-scan free-form text for ``t_<hex>`` references; return the
- L3544 `HallucinatedCardsError` (class) — Raised by ``complete_task`` when ``created_cards`` contains ids
- L3553 `__init__(self, phantom: list[str], completing_task_id: str)` (method)
- L3562 `complete_task(conn: sqlite3.Connection, task_id: str, *, result: Optional[str]=None, summary: Optional[str]=None, metadata: Optional[dict]=None, created_cards: Optional[Iterable[str]]=None, expected_run_id: Optional[int]=None)` (function) — Transition ``running|ready -> done`` and record ``result``.
- L3748 `_is_managed_scratch_path(p: Path)` (function) — Return True iff *p* is a strict descendant of a kanban-managed scratch root.
- L3823 `_cleanup_workspace(conn: sqlite3.Connection, task_id: str)` (function) — Remove a task's scratch workspace dir and kill its stale tmux session.
- L3892 `_try_cleanup_parent_workspaces(conn: sqlite3.Connection, task_id: str)` (function) — Clean up parent scratch workspaces now that *task_id* completed.
- L3932 `_cleanup_worker_tmux(conn: sqlite3.Connection, task_id: str)` (function) — Kill the tmux session associated with a task's assignee, if dead.
- L3987 `_scratch_tip_sentinel_path()` (function) — Path to the per-install scratch-workspace-tip sentinel file.
- L3992 `_scratch_tip_shown()` (function) — True iff the scratch-workspace tip has already been emitted on this
- L4002 `_mark_scratch_tip_shown()` (function) — Touch the sentinel so future scratch workspaces stay silent.
- L4016 `_maybe_emit_scratch_tip(conn: sqlite3.Connection, task_id: str, workspace_kind: Optional[str])` (function) — Emit the first-use scratch-workspace tip exactly once per install.
- L4045 `edit_completed_task_result(conn: sqlite3.Connection, task_id: str, *, result: str, summary: Optional[str]=None, metadata: Optional[dict]=None)` (function) — Backfill the user-visible result for an already completed task.
- L4112 `block_task(conn: sqlite3.Connection, task_id: str, *, reason: Optional[str]=None, expected_run_id: Optional[int]=None)` (function) — Transition ``running -> blocked``.
- L4168 `promote_task(conn: sqlite3.Connection, task_id: str, *, actor: str, reason: Optional[str]=None, force: bool=False, dry_run: bool=False)` (function) — Manually promote a `todo` or `blocked` task to `ready`.
- L4238 `unblock_task(conn: sqlite3.Connection, task_id: str)` (function) — Transition ``blocked``/``scheduled`` -> ready or todo.
- L4294 `specify_triage_task(conn: sqlite3.Connection, task_id: str, *, title: Optional[str]=None, body: Optional[str]=None, assignee: Optional[str]=None, author: Optional[str]=None)` (function) — Flesh out a triage task and promote it to ``todo``.
- L4385 `decompose_triage_task(conn: sqlite3.Connection, task_id: str, *, root_assignee: Optional[str], children: list[dict], author: Optional[str]=None, auto_promote: bool=True)` (function) — Fan a triage task out into child tasks and promote the root to ``todo``.
- L4608 `archive_task(conn: sqlite3.Connection, task_id: str)` (function)
- L4634 `delete_archived_task(conn: sqlite3.Connection, task_id: str)` (function) — Permanently remove an already-archived task and its related rows.
- L4660 `delete_task(conn: sqlite3.Connection, task_id: str)` (function) — Hard-delete a task and cascade to all related rows.
- L4687 `resolve_workspace(task: Task, *, board: Optional[str]=None)` (function) — Resolve (and create if needed) the workspace for a task.
- L4751 `set_workspace_path(conn: sqlite3.Connection, task_id: str, path: Path | str)` (function)
- L4762 `schedule_task(conn: sqlite3.Connection, task_id: str, *, reason: Optional[str]=None, expected_run_id: Optional[int]=None)` (function) — Park a task in ``scheduled`` so it is waiting on time, not human input.
- L4864 `DispatchResult` (class) — Outcome of a single ``dispatch`` pass.
- L4930 `_record_worker_exit(pid: int, raw_status: int)` (function) — Record a reaped child's exit status for later classification.
- L4953 `_classify_worker_exit(pid: int)` (function) — Classify a recently-reaped worker by pid.
- L4996 `reap_worker_zombies()` (function) — Reap all zombie children of this process without blocking.
- L5019 `_pid_alive(pid: Optional[int])` (function) — Return True if ``pid`` is still running on this host.
- L5083 `_terminate_reclaimed_worker(pid: Optional[int], claim_lock: Optional[str], *, signal_fn=None)` (function) — Best-effort host-local worker termination for reclaim paths.
- L5139 `heartbeat_worker(conn: sqlite3.Connection, task_id: str, *, note: Optional[str]=None, expected_run_id: Optional[int]=None)` (function) — Record a ``heartbeat`` event + touch ``last_heartbeat_at``.
- L5190 `enforce_max_runtime(conn: sqlite3.Connection, *, signal_fn=None)` (function) — Terminate workers whose per-task ``max_runtime_seconds`` has elapsed.
- L5310 `detect_stale_running(conn: sqlite3.Connection, *, stale_timeout_seconds: int=0, signal_fn=None)` (function) — Reclaim ``running`` tasks that show no progress (heartbeat) within the
- L5429 `_error_fingerprint(error_text: str)` (function) — Normalize an error message for grouping identical failures.
- L5440 `detect_crashed_workers(conn: sqlite3.Connection)` (function) — Reclaim ``running`` tasks whose worker PID is no longer alive.
- L5636 `_record_task_failure(conn: sqlite3.Connection, task_id: str, error: str, *, outcome: str, failure_limit: int=None, release_claim: bool=False, end_run: bool=False, event_payload_extra: Optional[dict]=None)` (function) — Record a non-success outcome (spawn_failed / crashed / timed_out)
- L5792 `_record_spawn_failure(conn: sqlite3.Connection, task_id: str, error: str, *, failure_limit: int=None)` (function)
- L5808 `_set_worker_pid(conn: sqlite3.Connection, task_id: str, pid: int)` (function) — Record the spawned child's pid + emit a ``spawned`` event.
- L5829 `_clear_failure_counter(conn: sqlite3.Connection, task_id: str)` (function) — Reset the unified consecutive-failures counter.
- L5851 `check_respawn_guard(conn: sqlite3.Connection, task_id: str)` (function) — Return a guard reason if ``task_id`` should NOT be re-spawned, else None.
- L5968 `has_spawnable_ready(conn: sqlite3.Connection)` (function) — Return True iff there is at least one ready+assigned+unclaimed task
- L6000 `has_spawnable_review(conn: sqlite3.Connection)` (function) — Return True iff there is at least one review+assigned+unclaimed task
- L6025 `dispatch_once(conn: sqlite3.Connection, *, spawn_fn=None, ttl_seconds: Optional[int]=None, dry_run: bool=False, max_spawn: Optional[int]=None, max_in_progress: Optional[int]=None, failure_limit: int=DEFAULT_SPAWN_FAILURE_LIMIT, stale_timeout_seconds: int=0, board: Optional[str]=None, default_assignee: Optional[str]=None, max_in_progress_per_profile: Optional[int]=None)` (function) — Run one dispatcher tick.
- L6415 `_positive_int(value: Any, default: int, *, minimum: int=1)` (function)
- L6423 `worker_log_rotation_config(kanban_cfg: Optional[dict]=None)` (function) — Return ``(rotate_bytes, backup_count)`` for worker log rotation.
- L6450 `_rotated_log_path(log_path: Path, generation: int)` (function)
- L6454 `_rotate_worker_log(log_path: Path, max_bytes: int, backup_count: int=DEFAULT_LOG_BACKUP_COUNT)` (function) — Rotate ``<log>`` when it exceeds ``max_bytes``.
- L6497 `_module_hermes_argv()` (function) — Return the interpreter-bound Hermes CLI invocation.
- L6505 `_absolute_hermes_path(path: str)` (function) — Return an absolute filesystem path for a resolved Hermes shim.
- L6511 `_looks_like_path(value: str)` (function) — Return true when a command override is an explicit path, not a name.
- L6523 `_is_windows_batch_shim(path: str)` (function) — Return true for Windows shell/batch shims that should not be argv[0].
- L6528 `_path_search_names(command: str)` (function) — Return executable names to try for an unqualified command.
- L6537 `_safe_which_no_cwd(command: str)` (function) — Resolve a bare command from PATH without implicit current-dir search.
- L6559 `_hermes_path_argv(path: str)` (function) — Return argv for a resolved Hermes executable path.
- L6572 `_resolve_hermes_argv()` (function) — Resolve the ``hermes`` invocation as argv parts for ``Popen``.
- L6613 `_kanban_worker_skill_available(hermes_home: Optional[str])` (function) — True if the bundled ``kanban-worker`` skill resolves for the home the
- L6648 `_worker_terminal_timeout_env(max_runtime_seconds: Optional[int], current_timeout: Optional[str])` (function) — Return a worker-scoped TERMINAL_TIMEOUT override, if needed.
- L6678 `_default_spawn(task: Task, workspace: str, *, board: Optional[str]=None)` (function) — Fire-and-forget ``hermes -p <profile> chat -q ...`` subprocess.
- L6856 `run_daemon(*, interval: float=60.0, max_spawn: Optional[int]=None, failure_limit: int=DEFAULT_SPAWN_FAILURE_LIMIT, stop_event=None, on_tick=None)` (function) — Run the dispatcher in a loop until interrupted.
- L6915 `build_worker_context(conn: sqlite3.Connection, task_id: str)` (function) — Return the full text a worker should read to understand its task.
- L7140 `board_stats(conn: sqlite3.Connection)` (function) — Per-status + per-assignee counts, plus the oldest ``ready`` age in
- L7176 `_to_epoch(val)` (function) — Normalise a timestamp to unix epoch seconds.
- L7204 `task_age(task: Task)` (function) — Return age metrics for a single task. All values are seconds or None.
- L7226 `add_notify_sub(conn: sqlite3.Connection, *, task_id: str, platform: str, chat_id: str, thread_id: Optional[str]=None, user_id: Optional[str]=None, notifier_profile: Optional[str]=None)` (function) — Register a gateway source that wants terminal-state notifications
- L7262 `list_notify_subs(conn: sqlite3.Connection, task_id: Optional[str]=None)` (function)
- L7274 `remove_notify_sub(conn: sqlite3.Connection, *, task_id: str, platform: str, chat_id: str, thread_id: Optional[str]=None)` (function)
- L7291 `unseen_events_for_sub(conn: sqlite3.Connection, *, task_id: str, platform: str, chat_id: str, thread_id: Optional[str]=None, kinds: Optional[Iterable[str]]=None)` (function) — Return ``(new_cursor, events)`` for a given subscription.
- L7340 `claim_unseen_events_for_sub(conn: sqlite3.Connection, *, task_id: str, platform: str, chat_id: str, thread_id: Optional[str]=None, kinds: Optional[Iterable[str]]=None)` (function) — Atomically claim unseen notification events for one subscription.
- L7391 `advance_notify_cursor(conn: sqlite3.Connection, *, task_id: str, platform: str, chat_id: str, thread_id: Optional[str]=None, new_cursor: int)` (function)
- L7408 `rewind_notify_cursor(conn: sqlite3.Connection, *, task_id: str, platform: str, chat_id: str, thread_id: Optional[str]=None, claimed_cursor: int, old_cursor: int)` (function) — Undo a notification claim when delivery fails.
- L7441 `gc_events(conn: sqlite3.Connection, *, older_than_seconds: int=30 * 24 * 3600)` (function) — Delete task_events rows older than ``older_than_seconds`` for tasks
- L7458 `gc_worker_logs(*, older_than_seconds: int=30 * 24 * 3600, board: Optional[str]=None)` (function) — Delete worker log files older than ``older_than_seconds``. Returns
- L7486 `worker_log_path(task_id: str, *, board: Optional[str]=None)` (function) — Return the path to a worker's log file. The file may not exist
- L7497 `read_worker_log(task_id: str, *, tail_bytes: Optional[int]=None, board: Optional[str]=None)` (function) — Read the worker log for ``task_id``. Returns None if the file
- L7532 `list_profiles_on_disk()` (function) — Return the set of assignee/profile names discovered on disk.
- L7567 `known_assignees(conn: sqlite3.Connection)` (function) — Return every assignee name known to the board or on disk.
- L7606 `list_runs(conn: sqlite3.Connection, task_id: str, *, include_active: bool=True, state_type: Optional[str]=None, state_name: Optional[str]=None)` (function) — Return all runs for ``task_id`` in start order.
- L7641 `get_run(conn: sqlite3.Connection, run_id: int)` (function)
- L7648 `latest_run(conn: sqlite3.Connection, task_id: str)` (function) — Return the most recent run regardless of outcome (active or closed).
- L7658 `latest_summary(conn: sqlite3.Connection, task_id: str)` (function) — Return the latest non-null ``task_runs.summary`` for ``task_id``.
- L7680 `latest_summaries(conn: sqlite3.Connection, task_ids: Iterable[str])` (function) — Batch-fetch latest non-null summaries for a list of task ids.
