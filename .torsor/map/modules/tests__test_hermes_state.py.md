---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:43'
updated: '2026-06-08T00:38:43'
---

# tests/test_hermes_state.py

Symbols in `tests/test_hermes_state.py`.

- L10 `_NoFtsCursor` (class) — Simulate a SQLite build without the fts5 module.
- L13 `execute(self, sql, parameters=())` (method)
- L24 `executescript(self, sql_script)` (method)
- L30 `_NoFtsConnection` (class)
- L31 `cursor(self, factory=None)` (method)
- L35 `_NoFtsExistingTableCursor` (class) — Simulate existing FTS virtual tables under a runtime without FTS5.
- L38 `execute(self, sql, parameters=())` (method)
- L48 `_NoFtsExistingTableConnection` (class)
- L49 `cursor(self, factory=None)` (method)
- L54 `db(tmp_path)` (function) — Create a SessionDB with a temp database file.
- L66 `TestSessionLifecycle` (class)
- L67 `test_create_and_get_session(self, db)` (method)
- L82 `test_get_nonexistent_session(self, db)` (method)
- L85 `test_end_session(self, db)` (method)
- L93 `test_end_session_preserves_original_end_reason(self, db)` (method) — The first end_reason wins — compression splits must not be
- L112 `test_end_session_after_reopen_allows_re_end(self, db)` (method) — reopen_session() is the explicit escape hatch for re-ending a
- L124 `test_update_system_prompt(self, db)` (method)
- L131 `test_update_token_counts(self, db)` (method)
- L140 `test_update_token_counts_tracks_api_call_count(self, db)` (method) — api_call_count increments with each update_token_counts call.
- L150 `test_update_token_counts_api_call_count_absolute(self, db)` (method) — absolute mode sets api_call_count directly.
- L161 `test_update_token_counts_backfills_model_when_null(self, db)` (method)
- L168 `test_update_token_counts_preserves_existing_model(self, db)` (method)
- L175 `test_update_session_model_overwrites_existing(self, db)` (method) — A mid-session /model switch must overwrite the stored model.
- L199 `test_parent_session(self, db)` (method)
- L206 `test_db_initializes_without_fts5_module(self, tmp_path, monkeypatch)` (method)
- L233 `test_existing_fts_tables_do_not_break_without_fts5(self, tmp_path, monkeypatch)` (method)
- L267 `test_old_schema_without_fts5_does_not_crash(self, tmp_path, monkeypatch)` (method)
- L299 `test_fts_runtime_restores_triggers_after_no_fts_open(self, tmp_path, monkeypatch)` (method)
- L338 `TestMessageStorage` (class)
- L339 `test_append_and_get_messages(self, db)` (method)
- L350 `test_message_increments_session_count(self, db)` (method)
- L358 `test_observed_flag_round_trips_for_gateway_replay(self, db)` (method)
- L380 `test_tool_response_does_not_increment_tool_count(self, db)` (method) — Tool responses (role=tool) should not increment tool_call_count.
- L391 `test_assistant_tool_calls_increment_by_count(self, db)` (method) — An assistant message with N tool_calls should increment by N.
- L402 `test_tool_call_count_matches_actual_calls(self, db)` (method) — tool_call_count should equal the number of tool calls made, not messages.
- L424 `test_tool_calls_serialization(self, db)` (method)
- L432 `test_multimodal_list_content_round_trip(self, db)` (method) — Multimodal ``content`` (list of parts) must survive the SQLite
- L463 `test_dict_content_round_trip(self, db)` (method) — Dict-shaped content (e.g. provider wrappers) also round-trips.
- L472 `test_string_content_unchanged_by_encoding(self, db)` (method) — Plain strings must not be wrapped — FTS search and legacy
- L486 `test_replace_messages_persists_tool_name(self, db)` (method) — `replace_messages` (used by /retry, /undo, /compress) must write
- L503 `test_replace_messages_handles_multimodal_content(self, db)` (method) — `replace_messages` (used by /retry, /undo, /compress) must also
- L525 `test_get_messages_as_conversation(self, db)` (method)
- L535 `test_platform_message_id_round_trips(self, db)` (method) — Platform-side message ids (yuanbao msg_id, telegram update_id, …)
- L555 `test_replace_messages_preserves_platform_message_id(self, db)` (method) — ``rewrite_transcript`` (which goes through replace_messages) must
- L571 `test_get_messages_as_conversation_includes_ancestor_chain(self, db)` (method)
- L588 `test_get_messages_as_conversation_avoids_repeated_resume_prompts_from_ancestors(self, db)` (method)
- L600 `test_finish_reason_stored(self, db)` (method)
- L607 `test_get_messages_as_conversation_strips_leaked_memory_context(self, db)` (method)
- L625 `test_reasoning_persisted_and_restored(self, db)` (method) — Reasoning text is stored for assistant messages and restored by
- L650 `test_reasoning_details_persisted_and_restored(self, db)` (method) — reasoning_details (structured array) is round-tripped through JSON
- L672 `test_finish_reason_restored_by_get_messages_as_conversation(self, db)` (method) — finish_reason on assistant messages must survive conversation replay.
- L693 `test_reasoning_content_persisted_and_restored(self, db)` (method) — reasoning_content must survive session replay as its own field.
- L709 `test_reasoning_content_empty_string_restored_for_assistant(self, db)` (method) — Empty reasoning_content still needs to round-trip for strict replays.
- L725 `test_codex_message_items_persisted_and_restored(self, db)` (method) — codex_message_items must round-trip through JSON serialization.
- L752 `test_reasoning_not_set_for_non_assistant(self, db)` (method) — reasoning is never leaked onto user or tool messages.
- L762 `test_reasoning_empty_string_not_restored(self, db)` (method) — Empty string reasoning is treated as absent.
- L770 `test_codex_reasoning_items_persisted_and_restored(self, db)` (method) — codex_reasoning_items (encrypted blobs for Codex Responses API) are
- L795 `TestFTS5Search` (class)
- L796 `test_search_finds_content(self, db)` (method)
- L807 `test_search_empty_query(self, db)` (method)
- L811 `test_search_with_source_filter(self, db)` (method)
- L823 `test_search_default_sources_include_acp(self, db)` (method)
- L831 `test_search_default_includes_all_platforms(self, db)` (method) — Default search (no source_filter) should find sessions from any platform.
- L842 `test_search_with_role_filter(self, db)` (method)
- L851 `test_search_returns_context(self, db)` (method)
- L862 `test_search_context_uses_session_neighbors_when_ids_are_interleaved(self, db)` (method)
- L881 `test_search_special_chars_do_not_crash(self, db)` (method) — FTS5 special characters in queries must not raise OperationalError.
- L902 `test_search_sanitized_query_still_finds_content(self, db)` (method) — Sanitization must not break normal keyword search.
- L912 `test_search_hyphenated_term_does_not_crash(self, db)` (method) — Hyphenated terms like 'chat-send' must not crash FTS5.
- L923 `test_search_dotted_term_does_not_crash(self, db)` (method) — Dotted terms like 'P2.2' or 'simulate.p2.test.ts' should not crash FTS5.
- L937 `test_search_colon_query_still_finds_content(self, db)` (method) — Queries containing ':' must not silently return empty.
- L958 `test_search_quoted_phrase_preserved(self, db)` (method) — User-provided quoted phrases should be preserved for exact matching.
- L971 `test_sanitize_fts5_query_strips_dangerous_chars(self)` (method) — Unit test for _sanitize_fts5_query static method.
- L992 `test_sanitize_fts5_preserves_quoted_phrases(self)` (method) — Properly paired double-quoted phrases should be preserved.
- L1007 `test_sanitize_fts5_quotes_hyphenated_terms(self)` (method) — Hyphenated terms should be wrapped in quotes for exact matching.
- L1029 `test_sanitize_fts5_quotes_dotted_terms(self)` (method) — Dotted terms should be wrapped in quotes to avoid FTS5 query parse edge cases.
- L1050 `test_sanitize_fts5_quotes_underscored_terms(self)` (method) — Underscored terms should be wrapped in quotes for exact matching.
- L1079 `TestCJKSearchFallback` (class) — Regression tests for CJK search (see #11511).
- L1089 `test_cjk_detection_covers_all_ranges(self)` (method)
- L1106 `test_chinese_multichar_query_returns_results(self, db)` (method) — The headline bug: multi-char Chinese query must not return [].
- L1117 `test_chinese_bigram_query(self, db)` (method)
- L1123 `test_korean_query_returns_results(self, db)` (method) — Guards against Hangul range typos (\uac00-\ud7af, not \ud7a0-).
- L1130 `test_japanese_query_returns_results(self, db)` (method)
- L1136 `test_cjk_fallback_preserves_source_filter(self, db)` (method) — Guards against the SQL-builder bug where filter clauses land
- L1148 `test_cjk_fallback_preserves_exclude_sources(self, db)` (method)
- L1159 `test_cjk_fallback_preserves_role_filter(self, db)` (method)
- L1168 `test_cjk_snippet_is_centered_on_match(self, db)` (method) — Snippet should contain the search term, not just the first N chars.
- L1182 `test_english_query_still_uses_fts5_fast_path(self, db)` (method) — English queries must not trigger the LIKE fallback (fast path regression).
- L1193 `test_cjk_query_with_no_matches_returns_empty(self, db)` (method)
- L1199 `test_mixed_cjk_english_query(self, db)` (method) — Mixed queries should still fall back to LIKE when FTS5 misses.
- L1209 `test_cjk_partial_fts5_results_supplemented_by_like(self, db)` (method) — When FTS5 returns *some* CJK results, LIKE must still find all matches.
- L1225 `test_cjk_like_dedup_no_duplicates(self, db)` (method) — When FTS5 and LIKE both find the same message, no duplicates.
- L1232 `test_cjk_like_escapes_wildcards(self, db)` (method) — Special characters (%, _) in CJK queries are treated as literals.
- L1243 `test_cjk_trigram_preserves_boolean_operators(self, db)` (method) — Boolean operators (OR, AND, NOT) work in CJK trigram queries.
- L1254 `test_cjk_or_combined_short_tokens_returns_results(self, db)` (method) — Regression test for #20494.
- L1276 `test_cjk_short_token_or_query_preserves_filters(self, db)` (method) — Source filter applies correctly in the short-token LIKE path (#20494).
- L1292 `TestSearchSessions` (class)
- L1293 `test_list_all_sessions(self, db)` (method)
- L1300 `test_filter_by_source(self, db)` (method)
- L1308 `test_pagination(self, db)` (method)
- L1323 `TestCounts` (class)
- L1324 `test_session_count(self, db)` (method)
- L1330 `test_session_count_by_source(self, db)` (method)
- L1337 `test_message_count_total(self, db)` (method)
- L1344 `test_message_count_per_session(self, db)` (method)
- L1358 `TestDeleteAndExport` (class)
- L1359 `test_delete_session(self, db)` (method)
- L1367 `test_delete_nonexistent(self, db)` (method)
- L1370 `test_resolve_session_id_exact(self, db)` (method)
- L1374 `test_resolve_session_id_unique_prefix(self, db)` (method)
- L1378 `test_resolve_session_id_ambiguous_prefix_returns_none(self, db)` (method)
- L1383 `test_resolve_session_id_escapes_like_wildcards(self, db)` (method)
- L1388 `test_export_session(self, db)` (method)
- L1398 `test_export_nonexistent(self, db)` (method)
- L1401 `test_export_all(self, db)` (method)
- L1409 `test_export_all_with_source(self, db)` (method)
- L1422 `TestPruneSessions` (class)
- L1423 `test_prune_old_ended_sessions(self, db)` (method)
- L1444 `test_prune_skips_active_sessions(self, db)` (method)
- L1457 `test_prune_with_source_filter(self, db)` (method)
- L1472 `test_prune_with_multilevel_chain(self, db)` (method) — Pruning old sessions orphans newer children instead of crashing on FK.
- L1507 `test_prune_entire_old_chain(self, db)` (method) — All sessions in a chain are old — entire chain is pruned.
- L1530 `TestDeleteSessionOrphansChildren` (class)
- L1531 `test_delete_orphans_children(self, db)` (method) — Deleting a parent session orphans its children.
- L1551 `TestBulkDeleteSessions` (class) — ``delete_sessions(ids)`` — the bulk-delete primitive backing the
- L1574 `test_deletes_listed_sessions(self, db)` (method)
- L1587 `test_returns_real_count_skipping_unknown_ids(self, db)` (method) — Unknown IDs are silently skipped — the return value reflects
- L1597 `test_empty_list_is_noop(self, db)` (method) — ``[]`` returns 0 without touching the DB. Guards against a
- L1606 `test_drops_non_string_entries(self, db)` (method) — Stray ``None`` / empty strings in the input list are
- L1617 `test_dedupes_duplicate_ids(self, db)` (method) — The same ID listed twice counts as one deletion. Defends
- L1625 `test_orphans_children_of_deleted_parents(self, db)` (method) — Bulk-deleting a parent leaves its children alive but
- L1640 `test_deletes_archived_and_active_when_selected(self, db)` (method) — Unlike the safety-gated ``delete_empty_sessions`` sweep,
- L1656 `test_cleans_up_transcript_files(self, db, tmp_path)` (method) — When ``sessions_dir`` is provided, on-disk transcripts are
- L1672 `TestDeleteEmptySessions` (class) — ``delete_empty_sessions`` sweeps every ended, non-archived session
- L1691 `test_count_and_delete_empties_only(self, db)` (method)
- L1712 `test_skips_active_empty_sessions(self, db)` (method) — A live (un-ended) empty session is what you get during the
- L1724 `test_skips_archived_empty_sessions(self, db)` (method) — Archived = soft-hidden by the user. They explicitly chose to
- L1737 `test_returns_zero_when_nothing_to_delete(self, db)` (method) — No-op path: no candidate rows → return 0, no error.
- L1747 `test_orphans_children_of_deleted_empty_parent(self, db)` (method) — Even an empty parent can have a child (e.g. a branch session
- L1767 `test_cleans_up_on_disk_transcript_files(self, db, tmp_path)` (method) — When ``sessions_dir`` is provided, transcript files left
- L1796 `TestSessionTitle` (class)
- L1797 `test_set_and_get_title(self, db)` (method)
- L1804 `test_set_title_nonexistent_session(self, db)` (method)
- L1807 `test_title_initially_none(self, db)` (method)
- L1812 `test_update_title(self, db)` (method)
- L1820 `test_title_in_search_sessions(self, db)` (method)
- L1830 `test_title_in_export(self, db)` (method)
- L1838 `test_title_with_special_characters(self, db)` (method)
- L1846 `test_title_empty_string_normalized_to_none(self, db)` (method) — Empty strings are normalized to None (clearing the title).
- L1856 `test_multiple_empty_titles_no_conflict(self, db)` (method) — Multiple sessions can have empty-string (normalized to NULL) titles.
- L1866 `test_title_survives_end_session(self, db)` (method)
- L1876 `TestSanitizeTitle` (class) — Tests for SessionDB.sanitize_title() validation and cleaning.
- L1879 `test_normal_title_unchanged(self)` (method)
- L1882 `test_strips_whitespace(self)` (method)
- L1885 `test_collapses_internal_whitespace(self)` (method)
- L1888 `test_tabs_and_newlines_collapsed(self)` (method)
- L1891 `test_none_returns_none(self)` (method)
- L1894 `test_empty_string_returns_none(self)` (method)
- L1897 `test_whitespace_only_returns_none(self)` (method)
- L1900 `test_control_chars_stripped(self)` (method)
- L1905 `test_del_char_stripped(self)` (method)
- L1908 `test_zero_width_chars_stripped(self)` (method)
- L1913 `test_rtl_override_stripped(self)` (method)
- L1917 `test_bom_stripped(self)` (method)
- L1921 `test_only_control_chars_returns_none(self)` (method)
- L1924 `test_max_length_allowed(self)` (method)
- L1928 `test_exceeds_max_length_raises(self)` (method)
- L1933 `test_unicode_emoji_allowed(self)` (method)
- L1936 `test_cjk_characters_allowed(self)` (method)
- L1939 `test_accented_characters_allowed(self)` (method)
- L1942 `test_special_punctuation_allowed(self)` (method)
- L1946 `test_sanitize_applied_in_set_session_title(self, db)` (method) — set_session_title applies sanitize_title internally.
- L1952 `test_too_long_title_rejected_by_set(self, db)` (method) — set_session_title raises ValueError for overly long titles.
- L1959 `TestSchemaInit` (class)
- L1960 `test_wal_mode(self, db)` (method)
- L1965 `test_foreign_keys_enabled(self, db)` (method)
- L1969 `test_tables_exist(self, db)` (method)
- L1978 `test_schema_version(self, db)` (method)
- L1984 `test_title_column_exists(self, db)` (method) — Verify the title column was created in the sessions table.
- L1990 `test_topic_mode_schema_is_not_auto_migrated_on_open(self, tmp_path)` (method) — Opening an old DB should not add topic-mode columns until /topic opts in.
- L2061 `test_apply_telegram_topic_migration_creates_topic_tables_explicitly(self, tmp_path)` (method) — The /topic opt-in path owns the DB migration for Telegram topic mode.
- L2136 `test_telegram_topic_binding_roundtrip_requires_explicit_schema(self, tmp_path)` (method)
- L2164 `test_telegram_topic_binding_refuses_to_relink_session_to_another_topic(self, tmp_path)` (method)
- L2189 `test_list_unlinked_telegram_sessions_for_user_excludes_bound_and_other_users(self, tmp_path)` (method)
- L2226 `test_migration_from_v2(self, tmp_path)` (method) — Simulate a v2 database and verify migration adds title column.
- L2300 `test_reconciliation_adds_missing_columns(self, tmp_path)` (method) — Columns present in SCHEMA_SQL but missing from the live table
- L2403 `test_reconciliation_is_idempotent(self, tmp_path)` (method) — Opening the same database twice doesn't error or duplicate columns.
- L2416 `test_schema_sql_is_source_of_truth(self, db)` (method) — Every column in SCHEMA_SQL exists in the live database.
- L2439 `TestTitleUniqueness` (class) — Tests for unique title enforcement and title-based lookups.
- L2442 `test_duplicate_title_raises(self, db)` (method) — Setting a title already used by another session raises ValueError.
- L2450 `test_same_session_can_keep_title(self, db)` (method) — A session can re-set its own title without error.
- L2457 `test_null_titles_not_unique(self, db)` (method) — Multiple sessions can have NULL titles (no constraint violation).
- L2465 `test_get_session_by_title(self, db)` (method)
- L2472 `test_get_session_by_title_not_found(self, db)` (method)
- L2475 `test_get_session_title(self, db)` (method)
- L2481 `test_get_session_title_nonexistent(self, db)` (method)
- L2485 `TestTitleLineage` (class) — Tests for title lineage resolution and auto-numbering.
- L2488 `test_resolve_exact_title(self, db)` (method)
- L2493 `test_resolve_returns_latest_numbered(self, db)` (method) — When numbered variants exist, return the most recent one.
- L2507 `test_resolve_exact_numbered(self, db)` (method) — Resolving an exact numbered title returns that specific session.
- L2516 `test_resolve_nonexistent_title(self, db)` (method)
- L2519 `test_next_title_no_existing(self, db)` (method) — With no existing sessions, base title is returned as-is.
- L2523 `test_next_title_first_continuation(self, db)` (method) — First continuation after the original gets #2.
- L2529 `test_next_title_increments(self, db)` (method) — Each continuation increments the number.
- L2539 `test_next_title_strips_existing_number(self, db)` (method) — Passing a numbered title strips the number and finds the base.
- L2549 `TestTitleSqlWildcards` (class) — Titles containing SQL LIKE wildcards (%, _) must not cause false matches.
- L2552 `test_resolve_title_with_underscore(self, db)` (method) — A title like 'test_project' should not match 'testXproject #2'.
- L2561 `test_resolve_title_with_percent(self, db)` (method) — A title with '%' should not wildcard-match unrelated sessions.
- L2570 `test_next_lineage_with_underscore(self, db)` (method) — get_next_title_in_lineage with underscores doesn't match wrong sessions.
- L2580 `TestListSessionsRich` (class) — Tests for enhanced session listing with preview and last_active.
- L2583 `test_preview_from_first_user_message(self, db)` (method)
- L2592 `test_preview_truncated_at_60(self, db)` (method)
- L2600 `test_preview_empty_when_no_user_messages(self, db)` (method)
- L2606 `test_last_active_from_latest_message(self, db)` (method)
- L2616 `test_last_active_fallback_to_started_at(self, db)` (method)
- L2622 `test_order_by_last_active_surfaces_recently_touched_older_session_first(self, db)` (method)
- L2655 `test_order_by_last_active_uses_compression_tip_activity(self, db)` (method) — A compression root whose tip was touched recently must rank above
- L2713 `test_rich_list_includes_title(self, db)` (method)
- L2719 `test_rich_list_source_filter(self, db)` (method)
- L2726 `test_preview_newlines_collapsed(self, db)` (method)
- L2733 `test_branch_session_visible_in_list(self, db)` (method) — Branch sessions (parent ended with 'branched') must appear in list_sessions_rich.
- L2744 `test_branch_session_visible_after_parent_reopen_and_reend(self, db)` (method) — Branch sessions stay visible after the parent is reopened and re-ended.
- L2782 `test_subagent_session_still_hidden(self, db)` (method) — Sub-agent children (parent NOT ended with 'branched') remain hidden.
- L2792 `test_compression_child_still_hidden(self, db)` (method) — Compression continuation sessions remain hidden (parent ended with 'compression').
- L2814 `TestCompressionChainProjection` (class) — Tests for lineage-aware list_sessions_rich — compressed conversations
- L2819 `_build_compression_chain(self, db, t0: float)` (method) — Helper: builds root -> delegate -> compression-child -> tip chain.
- L2870 `test_get_compression_tip_walks_full_chain(self, db)` (method)
- L2877 `test_get_compression_tip_returns_self_for_uncompressed(self, db)` (method)
- L2881 `test_get_compression_tip_skips_delegate_children(self, db)` (method) — Delegate subagents have parent_session_id set but were created
- L2892 `test_list_surfaces_tip_for_compressed_root(self, db)` (method) — The list must show the tip's id/message_count/preview in place of
- L2921 `test_list_projection_uses_tip_cwd(self, db)` (method) — Projected lineage rows should carry cwd from the live tip row.
- L2939 `test_list_without_projection_returns_raw_root(self, db)` (method) — project_compression_tips=False returns the raw parent-NULL root
- L2956 `test_list_preserves_sort_by_started_at(self, db)` (method) — Chronological ordering uses the ROOT's started_at (conversation
- L2979 `test_list_handles_broken_chain_gracefully(self, db)` (method) — A compression root with no child (e.g. DB corruption or a partial
- L3007 `TestExcludeSources` (class) — Tests for exclude_sources on list_sessions_rich and search_messages.
- L3010 `test_list_sessions_rich_excludes_tool_source(self, db)` (method)
- L3020 `test_list_sessions_rich_no_exclusion_returns_all(self, db)` (method)
- L3028 `test_list_sessions_rich_source_and_exclude_combined(self, db)` (method) — When source= is explicit, exclude_sources should not conflict.
- L3038 `test_list_sessions_rich_exclude_multiple_sources(self, db)` (method)
- L3050 `test_search_messages_excludes_tool_source(self, db)` (method)
- L3060 `test_search_messages_no_exclusion_returns_all_sources(self, db)` (method)
- L3070 `test_search_messages_source_include_and_exclude(self, db)` (method) — source_filter (include) and exclude_sources can coexist.
- L3086 `TestResolveSessionByNameOrId` (class) — Tests for the main.py helper that resolves names or IDs.
- L3089 `test_resolve_by_id(self, db)` (method)
- L3095 `test_resolve_by_title_falls_back(self, db)` (method)
- L3106 `TestConcurrentWriteSafety` (class)
- L3107 `test_create_session_insert_or_ignore_is_idempotent(self, db)` (method) — create_session with the same ID twice must not raise (INSERT OR IGNORE).
- L3117 `test_ensure_session_creates_missing_row(self, db)` (method) — ensure_session must create a minimal row when the session doesn't exist.
- L3126 `test_ensure_session_is_idempotent(self, db)` (method) — ensure_session on an existing row must be a no-op (no overwrite).
- L3135 `test_ensure_session_allows_append_message_after_failed_create(self, db)` (method) — Messages can be flushed even when create_session failed at startup.
- L3152 `test_sqlite_timeout_is_at_least_30s(self, db)` (method) — Connection timeout should be >= 30s to survive CLI/gateway contention.
- L3168 `TestStateMeta` (class)
- L3169 `test_get_meta_missing_returns_none(self, db)` (method)
- L3172 `test_set_then_get_meta(self, db)` (method)
- L3176 `test_set_meta_upsert(self, db)` (method) — set_meta overwrites existing value (ON CONFLICT DO UPDATE).
- L3183 `TestVacuum` (class)
- L3184 `test_vacuum_runs_without_error(self, db)` (method) — VACUUM must succeed on a fresh DB (no rows to reclaim).
- L3192 `TestOptimizeFts` (class)
- L3193 `test_optimize_returns_index_count(self, db)` (method) — A fresh DB has both FTS indexes; optimize merges both.
- L3199 `test_optimize_preserves_search_and_snippet(self, db)` (method) — Optimize is layout-only: MATCH results + snippets are unchanged.
- L3221 `test_optimize_skips_missing_trigram_table(self, db)` (method) — When the trigram index is absent, optimize handles only the porter
- L3240 `test_optimize_idempotent(self, db)` (method) — Running optimize twice is safe (second pass is a no-op merge).
- L3250 `TestAutoMaintenance` (class)
- L3251 `_make_old_ended(self, db, sid: str, days_old: int=100)` (method) — Create a session that is ended and was started `days_old` days ago.
- L3261 `test_first_run_prunes_and_vacuums(self, db)` (method)
- L3275 `test_second_call_within_interval_skips(self, db)` (method)
- L3293 `test_second_call_after_interval_runs_again(self, db)` (method)
- L3308 `test_no_prunable_sessions_no_vacuum(self, db)` (method) — When prune deletes 0 rows, VACUUM is skipped (wasted I/O).
- L3318 `test_vacuum_disabled_via_flag(self, db)` (method)
- L3324 `test_corrupt_last_run_marker_treated_as_no_prior_run(self, db)` (method) — A non-numeric marker must not break maintenance.
- L3332 `test_state_meta_survives_vacuum(self, db)` (method) — Marker written just before VACUUM must still be readable after.
- L3341 `test_auto_prune_deletes_transcript_files(self, db, tmp_path)` (method) — Issue #3015: auto-prune must also delete on-disk transcript files.
- L3370 `test_auto_prune_without_sessions_dir_preserves_files(self, db, tmp_path)` (method) — Backward-compat: no sessions_dir = DB-only cleanup (legacy behavior).
- L3382 `test_prune_sessions_deletes_files_for_pruned_only(self, db, tmp_path)` (method) — Active-session transcripts must never be deleted by prune.
- L3401 `TestFTS5ToolCallIndexing` (class) — Regression tests: search_messages must see tool_name and tool_calls.
- L3409 `test_tool_name_is_searchable(self, db)` (method)
- L3418 `test_tool_calls_args_are_searchable(self, db)` (method)
- L3434 `test_tool_function_name_in_tool_calls_is_searchable(self, db)` (method)
- L3447 `test_delete_message_row_does_not_crash(self, db)` (method) — DELETE on messages must not raise when FTS rows reference tool fields.
- L3474 `test_update_message_reindexes_tool_fields(self, db)` (method) — UPDATE must refresh the FTS row so old tokens drop out and new tokens appear.
- L3494 `TestFTS5ToolCallMigration` (class) — v11 migration: pre-existing state.db with old external-content FTS tables
- L3498 `test_v10_to_v11_upgrade_backfills_tool_fields(self, tmp_path)` (method) — Simulate an existing user: build a v10-shaped DB by hand, insert a
- L3598 `TestApplyWalProbe` (class) — Unit tests for the journal_mode probe in apply_wal_with_fallback.
- L3601 `test_skips_set_pragma_when_already_wal(self, tmp_path)` (method) — Already-WAL connection must not trigger the set-pragma.
- L3635 `test_sets_wal_on_fresh_connection(self, tmp_path)` (method) — Probe sees 'delete', then set-pragma runs and returns 'wal'.
- L3661 `test_apply_wal_concurrent_connects_no_eio(self, tmp_path)` (method) — 20 threads calling connect() on the same DB must not see disk I/O error.
- L3706 `test_fallback_to_delete_still_works(self, tmp_path)` (method) — When set-pragma raises a WAL-incompat error, falls back to DELETE.
- L3733 `test_probe_failure_falls_through_to_set_pragma(self, tmp_path)` (method) — When the read probe raises OperationalError, fall through to set-pragma.
- L3759 `test_no_downgrade_from_wal_to_delete_on_eio(self, tmp_path)` (method) — OperationalError NOT in _WAL_INCOMPAT_MARKERS must propagate, not downgrade.
- L3786 `test_returns_wal_not_delete_from_probe(self, tmp_path)` (method) — Early-return only on 'wal'; 'delete' or 'memory' must fall through to set-pragma.
- L3814 `TestSessionArchive` (class) — Soft-archiving hides a session from default listings without deleting it.
- L3817 `_seed(self, db, sid, *, archived=False)` (method)
- L3823 `test_set_session_archived_roundtrip(self, db)` (method)
- L3830 `test_set_session_archived_missing_row(self, db)` (method)
- L3833 `test_archived_excluded_by_default(self, db)` (method)
- L3841 `test_archived_only_and_include(self, db)` (method)
- L3855 `TestSessionIdSearch` (class) — Session id search backs Desktop's Search Sessions UX.
- L3858 `_seed(self, db, sid, *, content='ordinary message', archived=False)` (method)
- L3864 `test_search_sessions_by_id_matches_exact_prefix_and_substring(self, db)` (method)
- L3874 `test_search_sessions_by_id_respects_limit_and_prioritizes_exact_matches(self, db)` (method)
- L3883 `test_search_sessions_by_id_can_include_or_exclude_archived(self, db)` (method)
- L3893 `test_search_sessions_by_id_matches_projected_lineage_root_id(self, db)` (method)
- L3908 `TestListCronJobRuns` (class) — ``list_cron_job_runs`` powers the desktop cron run-history endpoint.
- L3916 `_seed_run(self, db, job_id: str, idx: int, started_at: float)` (method)
- L3928 `test_scopes_to_job_newest_first_and_enriched(self, db)` (method)
- L3948 `test_prefix_match_excludes_substring_collision(self, db)` (method) — A job whose id contains the target id as a substring must not leak.
- L3965 `test_ignores_non_cron_sessions(self, db)` (method)
- L3980 `test_limit_and_offset_paging(self, db)` (method)
- L3995 `test_uses_index_range_scan(self, db)` (method) — The query must use the (source, id) index, not a full table scan.
