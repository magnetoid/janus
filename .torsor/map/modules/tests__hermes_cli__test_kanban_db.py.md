---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/hermes_cli/test_kanban_db.py

Symbols in `tests/hermes_cli/test_kanban_db.py`.

- L20 `kanban_home(tmp_path, monkeypatch)` (function) — Isolated HERMES_HOME with an empty kanban DB.
- L34 `test_init_db_is_idempotent(kanban_home)` (function)
- L45 `test_init_creates_expected_tables(kanban_home)` (function)
- L54 `test_connect_honors_kanban_busy_timeout_env(kanban_home, monkeypatch)` (function) — All kanban connections should use the explicit busy-timeout knob.
- L70 `test_cross_process_init_lock_uses_windows_byte_range_lock(tmp_path, monkeypatch)` (function) — Windows must use a real process lock, not a no-op sidecar open.
- L91 `test_connect_rejects_tls_record_in_sqlite_header(tmp_path, monkeypatch)` (function) — Kanban should classify TLS-looking page-0 clobbers before WAL setup.
- L112 `test_connect_migrates_legacy_db_before_optional_column_indexes(tmp_path)` (function) — Legacy DBs missing additive indexed columns must migrate cleanly.
- L197 `test_create_task_no_parents_is_ready(kanban_home)` (function)
- L207 `test_create_task_with_parent_is_todo_until_parent_done(kanban_home)` (function)
- L216 `test_create_task_unknown_parent_errors(kanban_home)` (function)
- L221 `test_workspace_kind_validation(kanban_home)` (function)
- L226 `test_create_task_persists_worktree_branch_name(kanban_home, tmp_path)` (function)
- L245 `test_branch_name_requires_worktree_workspace(kanban_home)` (function)
- L259 `test_link_demotes_ready_child_to_todo_when_parent_not_done(kanban_home)` (function)
- L268 `test_link_keeps_ready_child_when_parent_already_done(kanban_home)` (function)
- L278 `test_link_rejects_self_loop(kanban_home)` (function)
- L285 `test_link_detects_cycle(kanban_home)` (function)
- L296 `test_recompute_ready_cascades_through_chain(kanban_home)` (function)
- L309 `test_recompute_ready_promotes_blocked_with_done_parents(kanban_home)` (function) — blocked tasks with all parents done should be promoted to ready,
- L338 `test_recompute_ready_fan_in_waits_for_all_parents(kanban_home)` (function)
- L353 `test_claim_once_wins_second_loses(kanban_home)` (function)
- L362 `test_claim_uses_env_default_ttl(kanban_home, monkeypatch)` (function)
- L372 `test_claim_fails_on_non_ready(kanban_home)` (function)
- L382 `test_schedule_task_parks_time_delay_without_dispatching(kanban_home)` (function)
- L394 `test_unblock_scheduled_rechecks_parent_gate(kanban_home)` (function)
- L410 `test_stale_claim_reclaimed(kanban_home, monkeypatch)` (function)
- L438 `test_stale_claim_with_live_pid_extends_instead_of_reclaiming(kanban_home, monkeypatch)` (function) — A stale-by-TTL claim whose worker PID is still alive should be
- L481 `test_stale_claim_with_live_pid_uses_env_ttl_override(kanban_home, monkeypatch)` (function)
- L508 `test_stale_claim_reclaim_event_records_diagnostic_payload(kanban_home, monkeypatch)` (function) — ``reclaimed`` events should carry claim_expires, last_heartbeat_at,
- L546 `test_detect_crashed_workers_systemic_failure_fast_block(kanban_home, monkeypatch)` (function) — When many tasks crash with the same error, trip the breaker faster.
- L577 `test_detect_crashed_workers_isolated_failure_normal_retry(kanban_home, monkeypatch)` (function) — Below the systemic threshold, tasks retain normal retry budget.
- L608 `test_detect_crashed_workers_skips_freshly_claimed_tasks(kanban_home, monkeypatch)` (function) — Grace period prevents reclaim of freshly-started tasks.
- L640 `test_detect_crashed_workers_grace_period_env_override(kanban_home, monkeypatch)` (function) — HERMES_KANBAN_CRASH_GRACE_SECONDS env var adjusts the window.
- L670 `test_resolve_crash_grace_seconds_handles_bad_env(monkeypatch)` (function) — Bad env values fall back to DEFAULT_CRASH_GRACE_SECONDS.
- L691 `_exited_status(code: int)` (function) — Raw wait-status for a WIFEXITED child with the given exit code.
- L696 `test_classify_worker_exit_recognizes_rate_limit_sentinel(kanban_home)` (function)
- L710 `test_rate_limit_exit_requeues_without_counting_failure(kanban_home, monkeypatch)` (function) — A rate-limit sentinel exit releases the task to ``ready`` and leaves
- L772 `test_real_crash_still_counts_and_trips_breaker(kanban_home, monkeypatch)` (function) — Sanity: a genuine non-zero crash (not the sentinel) still increments
- L801 `test_respawn_guard_defers_rate_limited_within_cooldown(kanban_home, monkeypatch)` (function) — Within the cooldown after a rate-limit requeue, the guard defers the
- L840 `test_respawn_guard_rate_limit_cooldown_zero_allows_immediately(kanban_home, monkeypatch)` (function) — Cooldown of 0 disables the wait — task is spawnable on the next tick,
- L870 `test_resolve_rate_limit_cooldown_handles_bad_env(monkeypatch)` (function)
- L883 `test_max_runtime_uses_current_run_start_after_retry(kanban_home, monkeypatch)` (function) — A retry should get a fresh max-runtime window.
- L931 `test_heartbeat_extends_claim(kanban_home)` (function)
- L945 `test_heartbeat_uses_env_default_ttl(kanban_home, monkeypatch)` (function)
- L959 `test_concurrent_claims_only_one_wins(kanban_home)` (function) — Fire N threads claiming the same task; exactly one must win.
- L980 `test_complete_records_result(kanban_home)` (function)
- L990 `test_block_then_unblock(kanban_home)` (function)
- L1000 `test_unblock_resets_failure_counters(kanban_home)` (function) — unblock_task must reset consecutive_failures and last_failure_error.
- L1020 `test_recompute_ready_skips_tasks_at_failure_limit(kanban_home)` (function) — recompute_ready must not auto-recover tasks whose consecutive_failures
- L1066 `test_recompute_ready_recovers_below_limit(kanban_home)` (function) — recompute_ready auto-recovers blocked tasks that haven't hit the
- L1096 `test_recompute_ready_honours_dispatcher_failure_limit(kanban_home)` (function) — The guard's effective limit must follow the same resolution order
- L1143 `test_recompute_ready_per_task_max_retries_overrides_dispatcher(kanban_home)` (function) — A per-task ``max_retries`` wins over the dispatcher failure_limit,
- L1167 `test_claim_rejects_when_parents_not_done(kanban_home)` (function) — claim_task must refuse ready->running if any parent isn't 'done'.
- L1206 `test_claim_succeeds_once_parents_done(kanban_home)` (function) — After parents complete, recompute_ready -> claim_task must succeed.
- L1222 `test_create_with_parents_stays_todo_until_parents_done(kanban_home)` (function) — kanban_create(parents=[...]) must land in 'todo' and only promote on parent done.
- L1242 `test_unblock_with_pending_parents_goes_to_todo(kanban_home)` (function) — unblock_task must re-gate on parent completion (Fix 3).
- L1269 `test_unblock_without_parents_goes_to_ready(kanban_home)` (function) — Parent-free unblock still produces 'ready' (behavior preserved).
- L1279 `test_assign_refuses_while_running(kanban_home)` (function)
- L1287 `test_assign_reassigns_when_not_running(kanban_home)` (function)
- L1294 `test_assignee_normalized_to_lowercase_on_create_and_assign(kanban_home)` (function) — Dashboard/CLI may pass title-cased profile labels; DB + spawn use canonical id.
- L1303 `test_list_tasks_assignee_filter_case_insensitive(kanban_home)` (function)
- L1310 `test_archive_hides_from_default_list(kanban_home)` (function)
- L1319 `test_delete_archived_task_removes_related_rows(kanban_home)` (function)
- L1343 `test_delete_archived_task_rejects_non_archived_rows(kanban_home)` (function)
- L1350 `test_list_tasks_order_by(kanban_home)` (function)
- L1382 `test_delete_task_removes_task_and_cascades(kanban_home)` (function)
- L1394 `test_delete_task_returns_false_for_missing_task(kanban_home)` (function)
- L1399 `test_delete_task_cascades_links(kanban_home)` (function)
- L1415 `test_comments_recorded_in_order(kanban_home)` (function)
- L1425 `test_empty_comment_rejected(kanban_home)` (function)
- L1432 `test_events_capture_lifecycle(kanban_home)` (function)
- L1444 `test_worker_context_includes_parent_results_and_comments(kanban_home)` (function)
- L1461 `test_dispatch_dry_run_does_not_claim(kanban_home, all_assignees_spawnable)` (function)
- L1473 `test_dispatch_skips_unassigned(kanban_home)` (function)
- L1482 `test_dispatch_skips_nonspawnable_into_separate_bucket(kanban_home, monkeypatch)` (function) — Tasks whose assignee fails profile_exists() must NOT land in
- L1497 `test_has_spawnable_ready_false_when_only_terminal_lanes(kanban_home, monkeypatch)` (function) — ``has_spawnable_ready`` returns False when every ready task is
- L1509 `test_has_spawnable_ready_true_when_real_profile_present(kanban_home, monkeypatch)` (function) — ``has_spawnable_ready`` returns True as soon as ANY ready task
- L1523 `test_has_spawnable_ready_false_on_empty_queue(kanban_home)` (function) — Empty queue is the trivial false case — no ready tasks at all.
- L1529 `test_dispatch_promotes_ready_and_spawns(kanban_home, all_assignees_spawnable)` (function)
- L1550 `test_dispatch_spawn_failure_releases_claim(kanban_home, all_assignees_spawnable)` (function)
- L1562 `test_dispatch_max_spawn_counts_existing_running_tasks(kanban_home, all_assignees_spawnable)` (function) — max_spawn is a live concurrency cap, not a per-tick spawn cap.
- L1590 `test_dispatch_max_spawn_fills_remaining_capacity(kanban_home, all_assignees_spawnable)` (function) — When below cap, dispatch only fills available worker slots.
- L1613 `test_dispatch_reclaims_stale_before_spawning(kanban_home)` (function)
- L1629 `test_respawn_guard_none_on_fresh_task(kanban_home)` (function) — A fresh task with no failures or runs is not guarded.
- L1637 `test_respawn_guard_blocker_auth_on_quota_error(kanban_home)` (function) — 'quota' in last_failure_error triggers blocker_auth.
- L1649 `test_respawn_guard_blocker_auth_on_auth_error(kanban_home)` (function) — 'unauthorized' in last_failure_error triggers blocker_auth.
- L1661 `test_respawn_guard_blocker_auth_on_authentication_error(kanban_home)` (function) — Full word 'Authentication' triggers blocker_auth (regex covers auth\w*).
- L1673 `test_respawn_guard_blocker_auth_on_authorization_error(kanban_home)` (function) — Full word 'authorization' triggers blocker_auth (regex covers auth\w*).
- L1685 `test_respawn_guard_recent_success(kanban_home)` (function) — A completed run within the guard window triggers recent_success.
- L1699 `test_respawn_guard_stale_success_not_guarded(kanban_home)` (function) — A completed run outside the guard window does not block re-spawn.
- L1713 `test_respawn_guard_active_pr_in_comment(kanban_home)` (function) — A GitHub PR URL in a recent comment triggers active_pr.
- L1725 `test_respawn_guard_old_pr_comment_not_guarded(kanban_home)` (function) — A GitHub PR URL in a comment older than the PR window does not block.
- L1740 `test_dispatch_respawn_guard_defers_auth_error_without_auto_block(kanban_home, all_assignees_spawnable)` (function) — dispatch_once defers (does NOT auto-block) a ready task whose last
- L1785 `test_dispatch_respawn_guard_skips_recent_success(kanban_home, all_assignees_spawnable)` (function) — dispatch_once skips (but does not block) a task with a recent completed run.
- L1811 `test_dispatch_respawn_guard_skips_active_pr(kanban_home, all_assignees_spawnable)` (function) — dispatch_once skips (but does not block) a task with an active PR comment.
- L1835 `test_dispatch_respawn_guard_dry_run_no_auto_block(kanban_home, all_assignees_spawnable)` (function) — In dry_run mode, blocker_auth tasks are recorded in respawn_guarded (not auto-blocked).
- L1853 `test_dispatch_respawn_guard_allows_clean_task(kanban_home, all_assignees_spawnable)` (function) — A task with no guard triggers is spawned normally.
- L1871 `test_dispatch_respawn_guard_emits_event_for_skipped_task(kanban_home, all_assignees_spawnable)` (function) — dispatch_once emits a respawn_guarded task_event so operators can diagnose stuck-ready tasks.
- L1898 `test_scratch_workspace_created_under_hermes_home(kanban_home)` (function)
- L1908 `test_dir_workspace_honors_given_path(kanban_home, tmp_path)` (function)
- L1920 `test_worktree_workspace_returns_intended_path(kanban_home, tmp_path)` (function)
- L1936 `test_cleanup_workspace_removes_managed_scratch_dir(kanban_home)` (function) — A scratch workspace under the kanban workspaces root is removed.
- L1948 `test_cleanup_workspace_refuses_path_outside_scratch_root(kanban_home, tmp_path)` (function) — A scratch task with a user path outside the workspaces root must NOT be deleted (#28818).
- L1979 `test_cleanup_workspace_honors_workspaces_root_env_override(tmp_path, monkeypatch)` (function) — ``HERMES_KANBAN_WORKSPACES_ROOT`` extends the managed-scratch set.
- L2013 `test_cleanup_workspace_deferred_while_child_active(kanban_home)` (function) — A scratch parent's workspace survives completion while a child is still active.
- L2036 `test_cleanup_workspace_swept_after_last_child_completes(kanban_home)` (function) — Once all children are terminal, the deferred parent scratch dir is removed.
- L2063 `test_dir_child_completion_unblocks_deferred_scratch_parent(kanban_home, tmp_path)` (function) — A non-scratch ('dir') child completing must still sweep its scratch parent.
- L2094 `test_is_managed_scratch_path_accepts_per_board_workspaces(kanban_home, tmp_path)` (function) — Per-board scratch dirs under ``<kanban_home>/kanban/boards/<slug>/workspaces`` are managed.
- L2101 `test_is_managed_scratch_path_rejects_real_source_tree(kanban_home, tmp_path)` (function) — A path outside any managed root (e.g. a user's repo) is NOT managed.
- L2108 `test_is_managed_scratch_path_rejects_kanban_metadata_subtrees(kanban_home)` (function) — Hermes' own DB/metadata/log subtrees under ``<kanban_home>/kanban`` are NOT managed.
- L2153 `test_tenant_column_filters_listings(kanban_home)` (function)
- L2164 `test_list_tasks_filters_workflow_template_and_step(kanban_home)` (function)
- L2183 `test_list_runs_state_filter_requires_pair_and_valid_type(kanban_home)` (function)
- L2195 `test_list_runs_filters_by_outcome_value(kanban_home)` (function)
- L2205 `test_tenant_propagates_to_events(kanban_home)` (function)
- L2218 `test_create_task_stamps_session_id(kanban_home)` (function)
- L2228 `test_create_task_session_id_defaults_to_none(kanban_home)` (function)
- L2236 `test_session_id_filters_listings(kanban_home)` (function)
- L2251 `test_session_id_index_exists(kanban_home)` (function) — The migration creates an index on session_id for cheap per-session
- L2264 `test_session_id_compose_with_tenant_filter(kanban_home)` (function) — A client may want both `tenant=scarf:foo` AND `session=acp-x` —
- L2294 `TestSharedBoardPaths` (class) — `kanban_home`/`kanban_db_path`/`workspaces_root`/`worker_log_path`
- L2298 `_set_home(self, monkeypatch, tmp_path, hermes_home)` (method)
- L2303 `test_default_install_anchors_at_home_dot_hermes(self, tmp_path, monkeypatch)` (method)
- L2319 `test_profile_worker_resolves_to_shared_root(self, tmp_path, monkeypatch)` (method)
- L2346 `test_dispatcher_and_profile_worker_converge(self, tmp_path, monkeypatch)` (method)
- L2373 `test_docker_custom_hermes_home_uses_env_path_directly(self, tmp_path, monkeypatch)` (method)
- L2387 `test_docker_profile_layout_uses_grandparent(self, tmp_path, monkeypatch)` (method)
- L2401 `test_explicit_override_via_hermes_kanban_home(self, tmp_path, monkeypatch)` (method)
- L2420 `test_empty_override_falls_through(self, tmp_path, monkeypatch)` (method)
- L2430 `test_dispatcher_and_worker_share_a_real_database(self, tmp_path, monkeypatch)` (method)
- L2454 `test_hermes_kanban_db_pin_beats_kanban_home(self, tmp_path, monkeypatch)` (method)
- L2477 `test_hermes_kanban_workspaces_root_pin_beats_kanban_home(self, tmp_path, monkeypatch)` (method)
- L2497 `test_empty_per_path_overrides_fall_through(self, tmp_path, monkeypatch)` (method)
- L2512 `test_dispatcher_spawn_injects_kanban_db_and_workspaces_root(self, tmp_path, monkeypatch)` (method)
- L2566 `test_latest_summary_returns_none_when_no_runs(kanban_home)` (function) — A freshly-created task has no runs and therefore no summary.
- L2573 `test_latest_summary_returns_summary_after_complete(kanban_home)` (function) — ``complete_task(summary=...)`` is the canonical kanban-worker
- L2584 `test_latest_summary_picks_newest_when_multiple_runs(kanban_home)` (function) — When a task has been re-run (block → unblock → complete), the
- L2606 `test_latest_summary_skips_empty_string(kanban_home)` (function) — A run with an empty-string summary should not mask an earlier
- L2623 `test_latest_summaries_batch_omits_tasks_without_summary(kanban_home)` (function) — ``latest_summaries`` is the dashboard's N+1 escape hatch — it
- L2644 `test_connect_falls_back_to_delete_on_locking_protocol(tmp_path, monkeypatch, caplog)` (function) — kanban_db.connect() must handle ``locking protocol`` on NFS/SMB.
- L2705 `test_unlink_tasks_triggers_recompute_ready(kanban_home)` (function) — Regression test for issue #22459.
- L2739 `test_archive_task_triggers_recompute_ready_for_dependents(kanban_home)` (function) — Archiving a parent must immediately unblock its children.
- L2763 `test_add_column_if_missing_is_idempotent_on_race(kanban_home)` (function) — ``_add_column_if_missing`` must swallow 'duplicate column name' errors.
- L2797 `test_migrate_add_optional_columns_tolerates_concurrent_migration(kanban_home)` (function) — Full _migrate_add_optional_columns must not raise when columns already
- L2859 `test_resolve_hermes_argv_prefers_path_shim(monkeypatch)` (function) — When `hermes` is on PATH, use the shim — preserves familiar ps output.
- L2870 `test_resolve_hermes_argv_absolutizes_relative_exe_shim(monkeypatch, tmp_path)` (function) — A relative executable override must not remain workspace-cwd-dependent.
- L2881 `test_resolve_hermes_argv_avoids_implicit_windows_batch_shim(monkeypatch, tmp_path)` (function) — Implicit .cmd/.bat shims use the module fallback, not batch argv[0].
- L2897 `test_resolve_hermes_argv_honors_hermes_bin_path_override(monkeypatch, tmp_path)` (function) — An explicit path-like HERMES_BIN lets service managers pin the executable.
- L2911 `test_resolve_hermes_argv_hermes_bin_bare_name_uses_path(monkeypatch, tmp_path)` (function) — Bare HERMES_BIN values keep PATH semantics instead of cwd shadowing.
- L2930 `test_resolve_hermes_argv_hermes_bin_bare_name_ignores_cwd(monkeypatch, tmp_path)` (function) — Bare HERMES_BIN does not accept current-directory shadow executables.
- L2944 `test_resolve_hermes_argv_hermes_bin_bare_cmd_uses_module_fallback(monkeypatch, tmp_path)` (function) — A PATH-resolved HERMES_BIN batch shim is not used as worker argv[0].
- L2960 `test_resolve_hermes_argv_hermes_bin_unresolved_bare_name_falls_back(monkeypatch)` (function) — Unresolved HERMES_BIN command names do not delegate cwd search to Popen.
- L2971 `test_resolve_hermes_argv_falls_back_to_module_form_when_no_path_shim(monkeypatch)` (function) — When the shim is not on PATH, fall back to `python -m hermes_cli.main`.
- L2989 `test_resolve_hermes_argv_module_actually_runs()` (function) — The fallback module name must be importable + runnable.
- L3030 `_make_task(**overrides)` (function) — Minimal Task with all required fields filled in. Override anything.
- L3053 `test_safe_int_accepts_int_and_int_string()` (function) — Sanity: well-typed values pass through.
- L3061 `test_safe_int_returns_none_on_corrupt_inputs()` (function) — All the failure modes that used to crash task_age.
- L3076 `test_task_age_handles_corrupt_created_at()` (function) — Pre-fix this raised ValueError and 500'd /api/plugins/kanban/board.
- L3085 `test_task_age_handles_corrupt_started_and_completed()` (function) — All three timestamp fields share the same _safe_int treatment.
- L3098 `test_task_age_well_formed_task()` (function) — Regression: the safe-int path must not change behavior for normal data.
- L3113 `test_task_dict_survives_corrupt_created_at(tmp_path, monkeypatch)` (function) — Defense in depth: even if task_age ever raised, plugin_api must not 500.
- L3156 `test_create_task_scratch_without_workspace_ignores_board_default_workdir(kanban_home, monkeypatch)` (function) — Scratch tasks must NOT inherit board.default_workdir — would point auto-cleanup
- L3170 `test_create_task_dir_without_workspace_inherits_board_default_workdir(kanban_home, monkeypatch)` (function) — Board default_workdir is for persistent dir/worktree workspaces, not scratch.
- L3187 `test_create_task_without_workspace_no_default_stays_none(kanban_home)` (function) — Board without default_workdir → create_task without workspace_path → stays None.
- L3198 `test_create_task_with_explicit_workspace_ignores_board_default(kanban_home)` (function) — create_task with explicit workspace_path → ignores board default.
- L3216 `test_dispatch_max_in_progress_skips_when_at_limit(kanban_home, all_assignees_spawnable)` (function) — When max_in_progress=N and N tasks are already running, spawn nothing.
- L3237 `test_dispatch_max_in_progress_spawns_up_to_cap(kanban_home, all_assignees_spawnable)` (function) — When max_in_progress=3 and only 1 is running, spawn up to 2 more.
- L3257 `test_dispatch_max_in_progress_none_is_unlimited(kanban_home, all_assignees_spawnable)` (function) — Default None means no limit — all ready tasks are spawned.
- L3275 `_set_task_status(conn: sqlite3.Connection, task_id: str, status: str)` (function) — Test helper: set a task's status directly.
- L3280 `test_claim_review_task_transitions_to_running(kanban_home)` (function) — claim_review_task atomically transitions review -> running.
- L3291 `test_claim_review_task_fails_on_non_review(kanban_home)` (function) — claim_review_task returns None if task is not in review status.
- L3300 `test_claim_review_task_fails_when_already_claimed(kanban_home)` (function) — claim_review_task returns None if the task was already claimed.
- L3311 `test_dispatch_review_dry_run(kanban_home, all_assignees_spawnable)` (function) — dispatch_once dry-run sees review tasks and reports them as spawned.
- L3324 `test_dispatch_review_spawns_with_correct_skills(kanban_home, all_assignees_spawnable)` (function) — Review tasks get sdlc-review skill set before spawning.
- L3343 `test_dispatch_review_skips_unassigned(kanban_home)` (function) — Unassigned review tasks go to skipped_unassigned, not spawned.
- L3353 `test_dispatch_review_counts_toward_max_spawn(kanban_home, all_assignees_spawnable)` (function) — Review spawns count against max_spawn alongside ready tasks.
- L3375 `test_dispatch_review_spawns_when_ready_empty(kanban_home, all_assignees_spawnable)` (function) — When only review tasks exist, they still get dispatched.
- L3393 `test_has_spawnable_review_true(kanban_home)` (function) — has_spawnable_review returns True when review tasks exist with real profiles.
- L3402 `test_has_spawnable_review_false_on_empty(kanban_home)` (function) — has_spawnable_review returns False when no review tasks exist.
- L3408 `test_has_spawnable_review_false_when_only_terminal_lanes(kanban_home, monkeypatch)` (function) — has_spawnable_review returns False when review tasks are terminal lanes.
- L3420 `test_dispatch_review_skips_nonspawnable(kanban_home, monkeypatch)` (function) — Review tasks with non-existent profiles go to skipped_nonspawnable.
- L3432 `test_review_status_in_valid_statuses()` (function) — 'review' is a valid task status.
- L3437 `test_dispatch_review_does_not_claim_ready_tasks(kanban_home, all_assignees_spawnable)` (function) — Review dispatch uses claim_review_task, which only claims review tasks.
- L3450 `test_detect_stale_returns_running_task_with_no_heartbeat(kanban_home, monkeypatch)` (function) — A task running > timeout with zero heartbeats gets reclaimed as stale.
- L3482 `test_detect_stale_returns_task_with_stale_heartbeat(kanban_home, monkeypatch)` (function) — A task running > timeout with a heartbeat older than 1h gets reclaimed.
- L3515 `test_detect_stale_skips_task_with_recent_heartbeat(kanban_home, monkeypatch)` (function) — A task running > timeout but with a recent heartbeat is NOT reclaimed.
- L3546 `test_detect_stale_skips_recently_started_task(kanban_home, monkeypatch)` (function) — A task started < timeout ago is NOT reclaimed even with no heartbeat.
- L3575 `test_detect_stale_skips_when_timeout_zero(kanban_home, monkeypatch)` (function) — stale_timeout_seconds=0 disables stale detection entirely.
- L3601 `test_detect_stale_skips_blocked_tasks(kanban_home, monkeypatch)` (function) — Blocked tasks are NOT reclaimed by stale detection.
- L3631 `test_detect_stale_does_not_tick_failure_counter(kanban_home, monkeypatch)` (function) — Stale reclaim must NOT tick consecutive_failures.
- L3697 `_write_corrupt_db(path: Path)` (function) — Write a kanban DB with a VALID SQLite header but malformed page content.
- L3719 `test_init_db_refuses_corrupt_existing_file(tmp_path)` (function)
- L3739 `test_connect_refuses_corrupt_existing_file(tmp_path)` (function)
- L3748 `test_repeated_corrupt_open_reuses_single_backup(tmp_path)` (function) — Repeated quarantines of the same corrupt bytes must not amplify disk usage.
- L3786 `test_locked_healthy_db_does_not_classify_as_corrupt(tmp_path, monkeypatch)` (function) — A transient lock during the probe must not produce a .corrupt backup
- L3817 `test_init_db_allows_missing_then_healthy(tmp_path)` (function)
- L3836 `test_maybe_emit_scratch_tip_fires_once_per_install(kanban_home, caplog)` (function) — First scratch workspace materialization warns + emits an event.
- L3904 `test_maybe_emit_scratch_tip_skips_non_scratch_workspaces(kanban_home, caplog)` (function) — worktree/dir workspaces are preserved on completion and must not
- L3942 `test_connect_sets_secure_delete_on(tmp_path)` (function) — secure_delete=ON must be active on every new connection.
- L3951 `test_connect_sets_cell_size_check_on(tmp_path)` (function) — cell_size_check=ON must be active on every new connection.
- L3960 `test_connect_sets_synchronous_full(tmp_path)` (function) — synchronous must be FULL (=2), not NORMAL (=1).
- L3969 `test_connect_pragmas_applied_on_reconnect(tmp_path)` (function) — All three pragmas must be re-applied on every connect(), not just the first.
- L3986 `test_pragmas_not_accidentally_disabled_by_migrate_path(tmp_path)` (function) — Migration path must not reset connection pragmas.
- L4004 `test_write_txn_preserves_original_exception_when_rollback_fails(kanban_home)` (function) — When a write inside write_txn raises an OperationalError that SQLite
- L4057 `test_write_txn_healthy_commit_no_exception(tmp_path)` (function) — Normal commit does not trigger the torn-extend check.
- L4073 `test_write_txn_raises_on_truncated_file(tmp_path)` (function) — A mocked smaller file size triggers the torn-extend check.
- L4097 `test_write_txn_post_commit_check_fires_every_call(tmp_path)` (function) — The invariant check runs on every write_txn call.
- L4122 `test_connect_sets_wal_autocheckpoint_100(tmp_path)` (function) — connect() sets wal_autocheckpoint to 100.
- L4132 `test_write_txn_check_reads_correct_header_fields(tmp_path)` (function) — Synthetic DB file with mismatched header page_count triggers the check.
- L4165 `test_reap_worker_zombies_returns_count()` (function) — reap_worker_zombies() returns the list of reaped PIDs.
- L4185 `test_reap_worker_zombies_noop_on_windows(monkeypatch)` (function) — reap_worker_zombies() returns 0 and never calls os.waitpid on Windows.
- L4196 `test_reap_worker_zombies_noop_no_children()` (function) — reap_worker_zombies() returns 0 without error when there are no children.
- L4205 `test_reap_worker_zombies_records_exit_status()` (function) — reap_worker_zombies() calls _record_worker_exit for each reaped pid.
- L4228 `test_reap_worker_zombies_handles_waitpid_os_error()` (function) — reap_worker_zombies() does not propagate generic OSError from os.waitpid.
- L4237 `test_zombie_reaper_runs_despite_board_connect_failure()` (function) — reap_worker_zombies runs even when a board tick raises an error.
- L4263 `test_zombie_reaper_survives_all_boards_failing()` (function) — reap_worker_zombies runs each tick regardless of board tick failures.
- L4294 `test_dispatch_once_still_reaps_via_extracted_fn(kanban_home)` (function) — The reaper inside dispatch_once still works after refactor to reap_worker_zombies().
- L4325 `test_connect_closing_closes_connection_on_exit(tmp_path)` (function) — The new context manager MUST actually close the underlying FD.
- L4337 `test_connect_closing_closes_on_exception(tmp_path)` (function) — Connection closed even when the body raises.
- L4350 `test_connect_closing_yields_usable_connection(tmp_path)` (function) — Smoke test: schema is initialized and basic ops work.
- L4361 `test_bare_connect_does_not_close_on_context_exit(tmp_path)` (function) — Document the leak that connect_closing exists to prevent.
