---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/hermes_cli/test_gmi_provider.py

Symbols in `tests/hermes_cli/test_gmi_provider.py`.

- L33 `_clear_provider_env(monkeypatch)` (function)
- L48 `TestGmiAliases` (class)
- L50 `test_alias_resolves(self, alias, monkeypatch)` (method)
- L54 `test_models_normalize_provider(self)` (method)
- L58 `test_providers_normalize_provider(self)` (method)
- L65 `TestGmiConfigRegistry` (class)
- L66 `test_optional_env_vars_include_gmi(self)` (method)
- L82 `TestGmiModelCatalog` (class)
- L83 `test_canonical_provider_entry(self)` (method)
- L87 `test_provider_model_ids_prefers_live_api(self, monkeypatch)` (method)
- L110 `test_provider_model_ids_falls_back_to_static_models(self, monkeypatch)` (method)
- L125 `TestGmiProvidersModule` (class)
- L126 `test_overlay_exists(self)` (method)
- L137 `test_provider_label(self)` (method)
- L141 `TestGmiDoctor` (class)
- L142 `test_provider_env_hints_include_gmi(self)` (method)
- L147 `test_run_doctor_checks_gmi_models_endpoint(self, monkeypatch, tmp_path)` (method)
- L219 `TestGmiModelMetadata` (class)
- L220 `test_url_to_provider(self)` (method)
- L225 `test_provider_prefixes(self)` (method)
- L232 `test_infer_from_url(self)` (method)
- L237 `test_known_gmi_endpoint_still_uses_endpoint_metadata(self)` (method)
- L261 `TestGmiAuxiliary` (class)
- L262 `test_resolve_provider_client_uses_gmi_aux_default(self, monkeypatch)` (method)
- L279 `test_gmi_profile_declares_hermes_user_agent(self)` (method) — The GMI plugin sets a HermesAgent/<ver> User-Agent on its profile.
- L290 `test_resolve_provider_client_accepts_gmi_alias(self, monkeypatch)` (method)
- L301 `TestGmiMainFlow` (class)
- L302 `test_chat_parser_accepts_gmi_provider(self, monkeypatch)` (method)
- L318 `test_select_provider_and_model_routes_gmi_to_generic_flow(self, monkeypatch)` (method)
- L338 `test_model_flow_api_key_provider_persists_gmi_selection(self, monkeypatch)` (method)
