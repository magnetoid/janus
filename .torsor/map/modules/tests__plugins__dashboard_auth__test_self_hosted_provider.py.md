---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:43'
updated: '2026-06-08T00:38:43'
---

# tests/plugins/dashboard_auth/test_self_hosted_provider.py

Symbols in `tests/plugins/dashboard_auth/test_self_hosted_provider.py`.

- L60 `rsa_keypair()` (function)
- L91 `_mint_id_token(rsa_keypair: Dict[str, Any], *, iss: str=_ISSUER, aud: str=_CLIENT_ID, sub: str='usr_abc', email: str | None='alice@example.com', name: str | None='Alice Example', groups: Any=None, org_id: str | None=None, ttl_seconds: int=900, extra_claims: Dict[str, Any] | None=None)` (function)
- L130 `_make_provider(rsa_keypair, *, scopes: str | None=None)` (function) — Construct a provider with discovery + JWKS stubbed (no network).
- L150 `_mock_post(status_code: int, body: Any, *, ctype: str='application/json')` (function)
- L168 `TestConstruction` (class)
- L169 `test_protocol_compliance(self)` (method)
- L172 `test_name_and_display(self)` (method)
- L177 `test_strips_trailing_slash_from_issuer(self)` (method)
- L183 `test_requires_issuer(self)` (method)
- L187 `test_requires_client_id(self)` (method)
- L191 `test_rejects_non_https_issuer(self)` (method)
- L197 `test_allows_http_localhost_issuer(self)` (method)
- L204 `test_default_scopes(self)` (method)
- L208 `test_empty_scopes_falls_back_to_default(self)` (method)
- L220 `TestDiscovery` (class)
- L221 `_provider(self)` (method)
- L226 `_mock_get(self, status_code, body, *, ctype='application/json')` (method)
- L234 `test_discovery_url(self)` (method)
- L240 `test_fetches_and_caches(self)` (method)
- L256 `test_discovery_404_raises(self)` (method)
- L265 `test_discovery_unreachable_raises(self)` (method)
- L274 `test_discovery_missing_endpoint_raises(self)` (method)
- L285 `test_discovery_issuer_mismatch_raises(self)` (method)
- L296 `test_discovery_issuer_trailing_slash_tolerated(self)` (method)
- L307 `test_discovery_rejects_non_https_endpoint(self)` (method)
- L324 `TestStartLogin` (class)
- L326 `provider(self, rsa_keypair)` (method)
- L329 `test_returns_login_start(self, provider)` (method)
- L335 `test_redirect_url_targets_authorize_endpoint(self, provider)` (method)
- L341 `test_authorize_url_has_required_params(self, provider)` (method)
- L355 `test_custom_scopes_used(self, rsa_keypair)` (method)
- L364 `test_code_verifier_length(self, provider)` (method)
- L372 `test_state_in_cookie_matches_url(self, provider)` (method)
- L382 `test_code_challenge_is_s256_of_verifier(self, provider)` (method)
- L399 `test_two_calls_differ(self, provider)` (method)
- L407 `test_rejects_wrong_callback_path(self, provider)` (method)
- L411 `test_allows_http_localhost_redirect(self, provider)` (method)
- L421 `TestCompleteLogin` (class)
- L423 `provider(self, rsa_keypair)` (method)
- L426 `test_happy_path_returns_session(self, provider, rsa_keypair)` (method)
- L455 `test_tolerates_missing_refresh_token(self, provider, rsa_keypair)` (method)
- L471 `test_missing_id_token_raises(self, provider)` (method)
- L486 `test_400_raises_invalid_code(self, provider)` (method)
- L499 `test_500_raises_provider_error(self, provider)` (method)
- L512 `test_network_error_raises_provider_error(self, provider)` (method)
- L525 `test_unexpected_token_type_raises(self, provider, rsa_keypair)` (method)
- L541 `test_posts_authorization_code_grant(self, provider, rsa_keypair)` (method)
- L565 `TestVerifySession` (class)
- L567 `provider(self, rsa_keypair)` (method)
- L570 `test_happy_path(self, provider, rsa_keypair)` (method)
- L578 `test_expired_returns_none(self, provider, rsa_keypair)` (method)
- L582 `test_wrong_audience_raises(self, provider, rsa_keypair)` (method)
- L587 `test_wrong_issuer_raises(self, provider, rsa_keypair)` (method)
- L592 `test_failure_message_surfaces_claims(self, provider, rsa_keypair)` (method)
- L600 `test_missing_sub_raises(self, provider, rsa_keypair)` (method)
- L605 `test_display_name_falls_back_to_preferred_username(self, provider, rsa_keypair)` (method)
- L618 `test_org_id_from_org_claim(self, provider, rsa_keypair)` (method)
- L624 `test_org_id_from_groups_when_no_org_claim(self, provider, rsa_keypair)` (method)
- L630 `test_org_id_empty_when_neither_present(self, provider, rsa_keypair)` (method)
- L636 `test_jwks_unreachable_raises(self, provider, rsa_keypair)` (method)
- L652 `TestRefreshAndRevoke` (class)
- L654 `provider(self, rsa_keypair)` (method)
- L657 `test_refresh_happy_path_rotates(self, provider, rsa_keypair)` (method)
- L680 `test_refresh_keeps_previous_rt_when_idp_omits(self, provider, rsa_keypair)` (method)
- L690 `test_refresh_400_raises_refresh_expired(self, provider)` (method)
- L698 `test_refresh_empty_token_no_network(self, provider)` (method)
- L704 `test_refresh_network_error_raises_provider_error(self, provider)` (method)
- L712 `test_revoke_posts_to_revocation_endpoint(self, provider)` (method)
- L722 `test_revoke_empty_token_noop(self, provider)` (method)
- L729 `test_revoke_swallows_errors(self, provider)` (method)
- L737 `test_revoke_noop_when_no_revocation_endpoint(self, provider)` (method)
- L751 `TestPluginRegister` (class)
- L753 `clear_env(self, monkeypatch)` (method)
- L762 `patch_config(self, monkeypatch)` (method)
- L771 `test_skips_when_unconfigured(self, patch_config)` (method)
- L779 `test_skips_when_only_issuer_set(self, patch_config, monkeypatch)` (method)
- L786 `test_registers_from_env(self, patch_config, monkeypatch)` (method)
- L800 `test_registers_from_config_yaml(self, patch_config)` (method)
- L811 `test_env_overrides_config(self, patch_config, monkeypatch)` (method)
- L828 `test_empty_env_does_not_shadow_config(self, patch_config, monkeypatch)` (method)
- L840 `test_custom_scopes_from_config(self, patch_config)` (method)
- L855 `test_config_load_failure_falls_through(self, monkeypatch)` (method)
- L864 `test_non_dict_oauth_section_tolerated(self, monkeypatch)` (method)
- L873 `test_non_https_issuer_skips_with_reason(self, patch_config, monkeypatch)` (method)
