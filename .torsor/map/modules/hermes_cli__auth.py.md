---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:40'
updated: '2026-06-08T00:38:40'
---

# hermes_cli/auth.py

Symbols in `hermes_cli/auth.py`.

- L151 `ProviderConfig` (class) — Describes a known inference provider.
- L483 `get_anthropic_key()` (function) — Return the first usable Anthropic credential, or ``""``.
- L518 `_resolve_kimi_base_url(api_key: str, default_url: str, env_override: str)` (function) — Return the correct Kimi base URL based on the API key prefix.
- L551 `has_usable_secret(value: Any, *, min_length: int=4)` (function) — Return True when a configured secret looks usable, not empty/placeholder.
- L563 `_resolve_api_key_provider_secret(provider_id: str, pconfig: ProviderConfig)` (function) — Resolve an API-key provider's token and indicate where it came from.
- L624 `detect_zai_endpoint(api_key: str, timeout: float=8.0)` (function) — Probe z.ai endpoints to find one that accepts this API key.
- L662 `_resolve_zai_base_url(api_key: str, default_url: str, env_override: str)` (function) — Return the correct Z.AI base URL by probing endpoints.
- L721 `AuthError` (class) — Structured auth error with UX mapping hints.
- L724 `__init__(self, message: str, *, provider: str='', code: Optional[str]=None, relogin_required: bool=False)` (method)
- L738 `is_rate_limited_auth_error(error: Exception)` (function) — True when an :class:`AuthError` represents upstream rate-limiting / quota
- L753 `_parse_retry_after_seconds(headers: Any)` (function) — Best-effort parse of a ``Retry-After`` header into whole seconds.
- L774 `format_auth_error(error: Exception)` (function) — Map auth failures to concise user-facing guidance.
- L807 `_format_nous_entitlement_auth_error(error: AuthError)` (function)
- L826 `_token_fingerprint(token: Any)` (function) — Return a short hash fingerprint for telemetry without leaking token bytes.
- L836 `_oauth_trace_enabled()` (function)
- L841 `_oauth_trace(event: str, *, sequence_id: Optional[str]=None, **fields: Any)` (function)
- L855 `_auth_file_path()` (function)
- L877 `_global_auth_file_path()` (function) — Return the global-root auth.json when the process is in profile mode.
- L908 `_load_global_auth_store()` (function) — Load the global-root auth store (read-only fallback).
- L944 `_auth_lock_path()` (function)
- L952 `_file_lock(lock_path: Path, holder: threading.local, timeout_seconds: float, timeout_message: str)` (function) — Cross-process advisory flock helper.
- L1024 `_auth_store_lock(timeout_seconds: float=AUTH_LOCK_TIMEOUT_SECONDS)` (function) — Cross-process advisory lock for auth.json reads+writes.  Reentrant.
- L1042 `_load_auth_store(auth_file: Optional[Path]=None)` (function)
- L1082 `_save_auth_store(auth_store: Dict[str, Any])` (function)
- L1131 `_load_provider_state(auth_store: Dict[str, Any], provider_id: str)` (function) — Return a provider's persisted state.
- L1160 `_save_provider_state(auth_store: Dict[str, Any], provider_id: str, state: Dict[str, Any])` (function)
- L1169 `_store_provider_state(auth_store: Dict[str, Any], provider_id: str, state: Dict[str, Any], *, set_active: bool=True)` (function)
- L1185 `is_known_auth_provider(provider_id: str)` (function)
- L1190 `get_auth_provider_display_name(provider_id: str)` (function)
- L1197 `read_credential_pool(provider_id: Optional[str]=None)` (function) — Return the persisted credential pool, or one provider slice.
- L1244 `write_credential_pool(provider_id: str, entries: List[Dict[str, Any]])` (function) — Persist one provider's credential pool under auth.json.
- L1265 `suppress_credential_source(provider_id: str, source: str)` (function) — Mark a credential source as suppressed so it won't be re-seeded.
- L1276 `is_source_suppressed(provider_id: str, source: str)` (function) — Check if a credential source has been suppressed by the user.
- L1286 `unsuppress_credential_source(provider_id: str, source: str)` (function) — Clear a suppression marker so the source will be re-seeded on the next load.
- L1308 `get_provider_auth_state(provider_id: str)` (function) — Return persisted auth state for a provider, or None.
- L1325 `get_active_provider()` (function) — Return the currently active provider ID from auth store.
- L1331 `is_provider_explicitly_configured(provider_id: str)` (function) — Return True only if the user has explicitly configured this provider.
- L1382 `clear_provider_auth(provider_id: Optional[str]=None)` (function) — Clear auth state for a provider. Used by `hermes logout`.
- L1422 `deactivate_provider()` (function) — Clear active_provider in auth.json without deleting credentials.
- L1439 `_get_config_hint_for_unknown_provider(provider_name: str)` (function) — Return a helpful hint string when provider resolution fails.
- L1464 `resolve_provider(requested: Optional[str]=None, *, explicit_api_key: Optional[str]=None, explicit_base_url: Optional[str]=None)` (function) — Determine which inference provider to use.
- L1601 `_parse_iso_timestamp(value: Any)` (function)
- L1618 `_is_expiring(expires_at_iso: Any, skew_seconds: int)` (function)
- L1625 `_coerce_ttl_seconds(expires_in: Any)` (function)
- L1633 `_optional_base_url(value: Any)` (function)
- L1653 `_validate_nous_inference_url_from_network(url: Optional[str])` (function) — Validate a Portal-returned inference URL against the host allowlist.
- L1700 `_decode_jwt_claims(token: Any)` (function)
- L1713 `_scope_values(raw_scope: Any)` (function)
- L1729 `_nous_invoke_jwt_status(token: Any, *, scope: Any=None, expires_at: Any=None, min_ttl_seconds: int=NOUS_INVOKE_JWT_MIN_TTL_SECONDS)` (function) — Return None when the token can be used for inference, else a reason.
- L1758 `_nous_invoke_jwt_is_usable(token: Any, *, scope: Any=None, expires_at: Any=None, min_ttl_seconds: int=NOUS_INVOKE_JWT_MIN_TTL_SECONDS)` (function)
- L1776 `_assert_nous_inference_jwt_usable(state: Dict[str, Any], *, access_token: Any=None)` (function)
- L1798 `_log_nous_invoke_jwt_selected(*, access_token: Any, sequence_id: Optional[str]=None)` (function)
- L1811 `_nous_jwt_expires_at(token: Any, fallback_expires_at: Any=None)` (function)
- L1822 `_set_nous_agent_key_from_invoke_jwt(state: Dict[str, Any], *, obtained_at: Optional[str]=None)` (function)
- L1860 `_select_nous_invoke_jwt(state: Dict[str, Any], *, access_token: Any=None, sequence_id: Optional[str]=None)` (function)
- L1884 `_nous_effective_provider_state(state: Dict[str, Any])` (function)
- L1892 `_codex_access_token_is_expiring(access_token: Any, skew_seconds: int)` (function)
- L1900 `_qwen_cli_auth_path()` (function)
- L1904 `_read_qwen_cli_tokens()` (function)
- L1929 `_save_qwen_cli_tokens(tokens: Dict[str, Any])` (function)
- L1960 `_qwen_access_token_is_expiring(expiry_date_ms: Any, skew_seconds: int=QWEN_ACCESS_TOKEN_REFRESH_SKEW_SECONDS)` (function)
- L1968 `_refresh_qwen_cli_tokens(tokens: Dict[str, Any], timeout_seconds: float=20.0)` (function)
- L2040 `_mark_qwen_oauth_active(creds: Dict[str, Any])` (function) — Set active_provider to qwen-oauth in auth.json.
- L2059 `resolve_qwen_runtime_credentials(*, force_refresh: bool=False, refresh_if_expiring: bool=True, refresh_skew_seconds: int=QWEN_ACCESS_TOKEN_REFRESH_SKEW_SECONDS)` (function)
- L2091 `get_qwen_auth_status()` (function)
- L2122 `_mark_google_gemini_cli_active(creds: Dict[str, Any])` (function) — Set active_provider to google-gemini-cli in auth.json.
- L2140 `resolve_gemini_oauth_runtime_credentials(*, force_refresh: bool=False)` (function) — Resolve runtime OAuth creds for google-gemini-cli.
- L2182 `get_gemini_oauth_auth_status()` (function) — Return a status dict for `hermes auth list` / `hermes status`.
- L2209 `_spotify_scope_list(raw_scope: Optional[str]=None)` (function)
- L2221 `_spotify_scope_string(raw_scope: Optional[str]=None)` (function)
- L2225 `_spotify_client_id(explicit: Optional[str]=None, state: Optional[Dict[str, Any]]=None)` (function)
- L2248 `_spotify_redirect_uri(explicit: Optional[str]=None, state: Optional[Dict[str, Any]]=None)` (function)
- L2268 `_spotify_api_base_url(state: Optional[Dict[str, Any]]=None)` (function)
- L2283 `_spotify_accounts_base_url(state: Optional[Dict[str, Any]]=None)` (function)
- L2298 `_spotify_code_verifier(length: int=64)` (function)
- L2303 `_spotify_code_challenge(code_verifier: str)` (function)
- L2308 `_oauth_pkce_code_verifier(length: int=64)` (function)
- L2313 `_oauth_pkce_code_challenge(code_verifier: str)` (function)
- L2318 `_spotify_build_authorize_url(*, client_id: str, redirect_uri: str, scope: str, state: str, code_challenge: str, accounts_base_url: str)` (function)
- L2339 `_spotify_validate_redirect_uri(redirect_uri: str)` (function)
- L2363 `_make_spotify_callback_handler(expected_path: str)` (function)
- L2401 `_spotify_wait_for_callback(redirect_uri: str, *, timeout_seconds: float=180.0)` (function)
- L2440 `_xai_validate_loopback_redirect_uri(redirect_uri: str)` (function)
- L2464 `_xai_callback_cors_origin(origin: Optional[str])` (function)
- L2475 `_make_xai_callback_handler(expected_path: str)` (function)
- L2589 `_xai_start_callback_server(preferred_port: int=XAI_OAUTH_REDIRECT_PORT)` (function)
- L2629 `_xai_wait_for_callback(server: HTTPServer, thread: threading.Thread, result: dict[str, Any], *, timeout_seconds: float=180.0)` (function)
- L2664 `_spotify_token_payload_to_state(token_payload: Dict[str, Any], *, client_id: str, redirect_uri: str, requested_scope: str, accounts_base_url: str, api_base_url: str, previous_state: Optional[Dict[str, Any]]=None)` (function)
- L2700 `_spotify_exchange_code_for_tokens(*, client_id: str, code: str, redirect_uri: str, code_verifier: str, accounts_base_url: str, timeout_seconds: float=20.0)` (function)
- L2747 `_refresh_spotify_oauth_state(state: Dict[str, Any], *, timeout_seconds: float=20.0)` (function)
- L2811 `resolve_spotify_runtime_credentials(*, force_refresh: bool=False, refresh_if_expiring: bool=True, refresh_skew_seconds: int=SPOTIFY_ACCESS_TOKEN_REFRESH_SKEW_SECONDS)` (function)
- L2859 `get_spotify_auth_status()` (function)
- L2878 `_spotify_interactive_setup(redirect_uri_hint: str)` (function) — Walk the user through creating a Spotify developer app, persist the
- L2939 `login_spotify_command(args)` (function)
- L3037 `_is_remote_session()` (function) — Detect environments where loopback OAuth can't reach the local browser.
- L3087 `_can_open_graphical_browser()` (function) — Return True only when a *graphical* browser is likely to open.
- L3144 `_parse_pasted_callback(raw: str)` (function) — Parse a pasted callback URL / query string into the loopback shape.
- L3194 `_prompt_manual_callback_paste(redirect_uri: str)` (function) — Read a callback URL from stdin as a fallback for browser-only remotes.
- L3221 `_ssh_user_at_host()` (function) — Return best-effort 'user@hostname' for the SSH tunnel hint command.
- L3236 `_print_loopback_ssh_hint(redirect_uri: str, *, docs_url: str | None=None)` (function) — Print an SSH tunnel hint when running a loopback-redirect OAuth flow on a
- L3290 `_read_codex_tokens(*, _lock: bool=True)` (function) — Read Codex OAuth tokens from Hermes auth store (~/.hermes/auth.json).
- L3339 `_sync_codex_pool_entries(auth_store: Dict[str, Any], tokens: Dict[str, str], last_refresh: Optional[str])` (function) — Mirror a fresh Codex re-auth into the credential_pool OAuth entries.
- L3410 `_save_codex_tokens(tokens: Dict[str, str], last_refresh: str=None, label: str=None)` (function) — Save Codex OAuth tokens to Hermes auth store (~/.hermes/auth.json).
- L3427 `refresh_codex_oauth_pure(access_token: str, refresh_token: str, *, timeout_seconds: float=20.0)` (function) — Refresh Codex OAuth tokens without mutating Hermes auth state.
- L3555 `_refresh_codex_auth_tokens(tokens: Dict[str, str], timeout_seconds: float)` (function) — Refresh Codex access token using the refresh token.
- L3576 `_import_codex_cli_tokens()` (function) — Try to read tokens from ~/.codex/auth.json (Codex CLI shared file).
- L3610 `resolve_codex_runtime_credentials(*, force_refresh: bool=False, refresh_if_expiring: bool=True, refresh_skew_seconds: int=CODEX_ACCESS_TOKEN_REFRESH_SKEW_SECONDS)` (function) — Resolve runtime credentials from Hermes's own Codex token store.
- L3683 `_pool_codex_access_token()` (function) — Return the most-recent usable access_token from the openai-codex pool.
- L3727 `_read_xai_oauth_tokens(*, _lock: bool=True)` (function)
- L3773 `_save_xai_oauth_tokens(tokens: Dict[str, Any], *, discovery: Optional[Dict[str, Any]]=None, redirect_uri: str='', last_refresh: Optional[str]=None)` (function)
- L3796 `_xai_access_token_is_expiring(access_token: str, skew_seconds: int=0)` (function)
- L3814 `_xai_validate_oauth_endpoint(url: str, *, field: str)` (function) — Refuse any OIDC discovery endpoint that isn't HTTPS on the xAI origin.
- L3855 `_xai_validate_inference_base_url(value: str, *, fallback: str)` (function) — Refuse a non-xAI base_url for the OAuth-authenticated inference path.
- L3911 `_xai_oauth_discovery(timeout_seconds: float=15.0)` (function)
- L3960 `refresh_xai_oauth_pure(access_token: str, refresh_token: str, *, token_endpoint: str='', timeout_seconds: float=20.0)` (function)
- L4058 `_refresh_xai_oauth_tokens(tokens: Dict[str, Any], *, token_endpoint: str, redirect_uri: str='', timeout_seconds: float)` (function)
- L4089 `resolve_xai_oauth_runtime_credentials(*, force_refresh: bool=False, refresh_if_expiring: bool=True, refresh_skew_seconds: int=XAI_ACCESS_TOKEN_REFRESH_SKEW_SECONDS)` (function)
- L4175 `_default_verify()` (function) — Platform-aware default SSL verify for httpx clients.
- L4193 `_resolve_verify(*, insecure: Optional[bool]=None, ca_bundle: Optional[str]=None, auth_state: Optional[Dict[str, Any]]=None)` (function)
- L4232 `_request_device_code(client: httpx.Client, portal_base_url: str, client_id: str, scope: Optional[str])` (function) — POST to the device code endpoint. Returns device_code, user_code, etc.
- L4259 `_poll_for_token(client: httpx.Client, portal_base_url: str, client_id: str, device_code: str, expires_in: int, poll_interval: int)` (function) — Poll the token endpoint until the user approves or the code expires.
- L4335 `_nous_shared_auth_dir()` (function) — Resolve the directory that holds the shared Nous token store.
- L4355 `_nous_shared_store_path()` (function)
- L4381 `_nous_shared_store_lock(timeout_seconds: float=AUTH_LOCK_TIMEOUT_SECONDS)` (function) — Cross-profile lock for the shared Nous OAuth store.
- L4408 `_merge_shared_nous_oauth_state(state: Dict[str, Any])` (function) — Copy fresher shared OAuth tokens into a profile-local Nous state.
- L4443 `_write_shared_nous_state(state: Dict[str, Any])` (function) — Persist a minimal copy of the Nous OAuth state to the shared store.
- L4510 `_read_shared_nous_state()` (function) — Return the shared Nous OAuth state if present and well-formed.
- L4540 `_clear_shared_nous_state(reason: str)` (function) — Remove the shared Nous OAuth store after a terminal token failure.
- L4554 `_is_terminal_nous_refresh_error(exc: Exception)` (function) — True when retrying the same Nous refresh token cannot succeed.
- L4564 `_is_terminal_xai_oauth_refresh_error(exc: Exception)` (function) — True when retrying the same xAI OAuth refresh token cannot succeed.
- L4581 `_is_terminal_codex_oauth_refresh_error(exc: Exception)` (function) — True when retrying the same Codex OAuth refresh token cannot succeed.
- L4604 `_quarantine_nous_oauth_state(state: Dict[str, Any], error: AuthError, *, reason: str)` (function) — Keep routing metadata but remove dead OAuth material so it is not replayed.
- L4637 `_quarantine_nous_pool_entries(auth_store: Dict[str, Any], error: AuthError, *, reason: str)` (function) — Remove singleton-seeded Nous pool entries that contain dead OAuth state.
- L4670 `_try_import_shared_nous_state(*, timeout_seconds: float=15.0)` (function) — Attempt to rehydrate Nous OAuth state from the shared store.
- L4741 `_refresh_access_token(*, client: httpx.Client, portal_base_url: str, client_id: str, refresh_token: str)` (function)
- L4799 `fetch_nous_models(*, inference_base_url: str, api_key: str, timeout_seconds: float=15.0, verify: bool | str=True)` (function) — Fetch available model IDs from the Nous inference API.
- L4856 `_agent_key_is_usable(state: Dict[str, Any], min_ttl_seconds: int)` (function)
- L4868 `resolve_nous_access_token(*, timeout_seconds: float=15.0, insecure: Optional[bool]=None, ca_bundle: Optional[str]=None, refresh_skew_seconds: int=ACCESS_TOKEN_REFRESH_SKEW_SECONDS)` (function) — Resolve a refresh-aware Nous Portal access token for managed tool gateways.
- L4973 `refresh_nous_oauth_pure(access_token: str, refresh_token: str, client_id: str, portal_base_url: str, inference_base_url: str, *, token_type: str='Bearer', scope: str=DEFAULT_NOUS_SCOPE, obtained_at: Optional[str]=None, expires_at: Optional[str]=None, agent_key: Optional[str]=None, agent_key_expires_at: Optional[str]=None, timeout_seconds: float=15.0, insecure: Optional[bool]=None, ca_bundle: Optional[str]=None, force_refresh: bool=False, on_state_update: Optional[Callable[[Dict[str, Any], str], None]]=None)` (function) — Refresh Nous OAuth state without mutating auth.json directly.
- L5070 `refresh_nous_oauth_from_state(state: Dict[str, Any], *, timeout_seconds: float=15.0, force_refresh: bool=False, on_state_update: Optional[Callable[[Dict[str, Any], str], None]]=None)` (function) — Refresh Nous OAuth from a state dict. Thin wrapper around refresh_nous_oauth_pure.
- L5099 `persist_nous_credentials(creds: Dict[str, Any], *, label: Optional[str]=None)` (function) — Persist Nous OAuth credentials as the singleton provider state
- L5158 `_sync_nous_pool_from_auth_store()` (function) — Best-effort pool reseed after providers.nous changes; never fail login.
- L5168 `resolve_nous_runtime_credentials(*, timeout_seconds: float=15.0, insecure: Optional[bool]=None, ca_bundle: Optional[str]=None, force_refresh: bool=False)` (function) — Resolve Nous inference credentials for runtime use.
- L5401 `_empty_nous_auth_status()` (function)
- L5414 `_snapshot_nous_pool_status()` (function) — Best-effort status from the credential pool.
- L5485 `_auth_file_mtime()` (function)
- L5494 `invalidate_nous_auth_status_cache()` (function) — Clear the get_nous_auth_status() process-level memo.
- L5506 `get_nous_auth_status()` (function) — Status snapshot for Nous auth.
- L5538 `_compute_nous_auth_status()` (function) — Uncached implementation of get_nous_auth_status(). See that function.
- L5590 `get_codex_auth_status()` (function) — Status snapshot for Codex auth.
- L5639 `get_xai_oauth_auth_status()` (function)
- L5681 `get_api_key_provider_status(provider_id: str)` (function) — Status snapshot for API-key providers (z.ai, Kimi, MiniMax).
- L5712 `get_external_process_provider_status(provider_id: str)` (function) — Status snapshot for providers that run a local subprocess.
- L5742 `get_auth_status(provider_id: Optional[str]=None)` (function) — Generic auth status dispatcher.
- L5779 `_get_azure_foundry_auth_status()` (function) — Return structural auth status for Azure Foundry.
- L5856 `resolve_api_key_provider_credentials(provider_id: str)` (function) — Resolve API key and base URL for an API-key provider.
- L5901 `resolve_external_process_provider_credentials(provider_id: str)` (function) — Resolve runtime details for local subprocess-backed providers.
- L5945 `_update_config_for_provider(provider_id: str, inference_base_url: str, default_model: Optional[str]=None)` (function) — Update config.yaml and auth.json to reflect the active provider.
- L6011 `_get_config_provider()` (function) — Return model.provider from config.yaml, normalized, if present.
- L6029 `_config_provider_matches(provider_id: Optional[str])` (function) — Return True when config.yaml currently selects *provider_id*.
- L6036 `_should_reset_config_provider_on_logout(provider_id: Optional[str])` (function) — Return True when logout should reset the model provider config.
- L6044 `_logout_default_provider_from_config()` (function) — Fallback logout target when auth.json has no active provider.
- L6059 `_reset_config_provider()` (function) — Reset config.yaml provider back to auto after logout.
- L6078 `_prompt_model_selection(model_ids: List[str], current_model: str='', pricing: Optional[Dict[str, Dict[str, str]]]=None, unavailable_models: Optional[List[str]]=None, portal_url: str='', unavailable_message: str='')` (function) — Interactive model selection. Puts current_model first with a marker. Returns chosen model ID or None.
- L6264 `_save_model_choice(model_id: str)` (function) — Save the selected model to config.yaml (single source of truth).
- L6281 `login_command(args)` (function) — Deprecated: use 'hermes model' or 'hermes setup' instead.
- L6289 `_login_openai_codex(args, pconfig: ProviderConfig, *, force_new_login: bool=False)` (function) — OpenAI Codex login via device code flow. Tokens stored in ~/.hermes/auth.json.
- L6363 `_login_xai_oauth(args, pconfig: ProviderConfig, *, force_new_login: bool=False)` (function)
- L6423 `_xai_oauth_build_authorize_url(*, authorization_endpoint: str, redirect_uri: str, code_challenge: str, state: str, nonce: str)` (function)
- L6452 `_xai_oauth_exchange_code_for_tokens(*, token_endpoint: str, code: str, redirect_uri: str, code_verifier: str, code_challenge: str, timeout_seconds: float=20.0)` (function) — POST the authorization code to xAI's token endpoint and return
- L6572 `_xai_oauth_loopback_login(*, timeout_seconds: float=20.0, open_browser: bool=True, manual_paste: bool=False)` (function) — Run the xAI OAuth PKCE flow.
- L6770 `_codex_device_code_login()` (function) — Run the OpenAI device code login flow and return credentials dict.
- L6917 `_minimax_pkce_pair()` (function) — Generate (code_verifier, code_challenge_S256, state) for MiniMax OAuth.
- L6928 `_minimax_request_user_code(client: httpx.Client, *, portal_base_url: str, client_id: str, code_challenge: str, state: str)` (function)
- L6968 `_minimax_expired_in_looks_like_unix_ms(expired_in: int, *, now_ms: int)` (function) — True if ``expired_in`` is plausibly a unix-ms absolute time (vs TTL seconds).
- L6973 `_minimax_resolve_token_expiry_unix(expired_in: int, *, now: datetime)` (function) — Return access-token expiry as unix seconds (MiniMax uses ms epoch or TTL seconds).
- L6982 `_minimax_poll_token(client: httpx.Client, *, portal_base_url: str, client_id: str, user_code: str, code_verifier: str, expired_in: int, interval_ms: Optional[int])` (function)
- L7045 `_minimax_save_auth_state(auth_state: Dict[str, Any])` (function) — Persist MiniMax OAuth state to Hermes auth store (~/.hermes/auth.json).
- L7053 `_minimax_oauth_login(*, region: str='global', open_browser: bool=True, timeout_seconds: float=15.0)` (function) — Run MiniMax OAuth flow, persist tokens, return auth state dict.
- L7136 `_refresh_minimax_oauth_state(state: Dict[str, Any], *, timeout_seconds: float=15.0, force: bool=False)` (function) — Refresh MiniMax OAuth access token if close to expiry (or forced).
- L7202 `_minimax_oauth_quarantine_on_terminal_refresh(state: Dict[str, Any], exc: AuthError)` (function) — Wipe dead tokens from auth.json after a terminal refresh failure.
- L7227 `build_minimax_oauth_token_provider()` (function) — Return a zero-arg callable that yields a fresh MiniMax access token.
- L7274 `resolve_minimax_oauth_runtime_credentials(*, min_token_ttl_seconds: int=MINIMAX_OAUTH_REFRESH_SKEW_SECONDS, as_token_provider: bool=False)` (function) — Return {provider, api_key, base_url, source} for minimax-oauth.
- L7315 `get_minimax_oauth_auth_status()` (function) — Return auth status dict for MiniMax OAuth provider.
- L7333 `_login_minimax_oauth(args, pconfig: ProviderConfig)` (function) — CLI entry for MiniMax OAuth login.
- L7347 `_nous_device_code_login(*, portal_base_url: Optional[str]=None, inference_base_url: Optional[str]=None, client_id: Optional[str]=None, scope: Optional[str]=None, open_browser: bool=True, timeout_seconds: float=15.0, insecure: bool=False, ca_bundle: Optional[str]=None)` (function) — Run the Nous device-code flow and return full OAuth state without persisting.
- L7476 `_login_nous(args, pconfig: ProviderConfig)` (function) — Nous Portal device authorization flow.
- L7677 `logout_command(args)` (function) — Clear auth state for a provider.
