---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:41'
updated: '2026-06-08T00:38:41'
---

# tests/agent/test_model_metadata.py

Symbols in `tests/agent/test_model_metadata.py`.

- L38 `TestEstimateTokensRough` (class)
- L39 `test_empty_string(self)` (method)
- L42 `test_none_returns_zero(self)` (method)
- L45 `test_known_length(self)` (method)
- L48 `test_short_text(self)` (method)
- L52 `test_proportional(self)` (method)
- L57 `test_unicode_multibyte(self)` (method) — Unicode chars are still 1 Python char each — 4 chars/token holds.
- L63 `TestEstimateMessagesTokensRough` (class)
- L64 `test_empty_list(self)` (method)
- L67 `test_single_message_concrete_value(self)` (method) — Verify against known str(msg) length (ceiling division).
- L75 `test_multiple_messages_additive(self)` (method)
- L85 `test_tool_call_message(self)` (method) — Tool call messages with no 'content' key still contribute tokens.
- L93 `test_message_with_list_content(self)` (method) — Vision messages with multimodal content arrays.
- L110 `test_message_with_huge_base64_image_stays_bounded(self)` (method) — A 1MB base64 PNG must not explode to ~250K tokens.
- L125 `TestDefaultContextLengths` (class)
- L126 `test_grok_substring_matching(self)` (method)
- L159 `test_xai_oauth_grok_build_uses_xai_models_dev_context(self)` (method) — xAI OAuth should share the xAI provider metadata path.
- L185 `test_deepseek_v4_models_1m_context(self)` (method)
- L227 `TestCodexOAuthContextLength` (class) — ChatGPT Codex OAuth imposes lower context limits than the direct
- L235 `setup_method(self)` (method)
- L240 `test_fallback_table_used_without_token(self)` (method) — With no access token, the hardcoded Codex fallback table wins
- L271 `test_live_probe_overrides_fallback(self)` (method) — When a token is provided, the live /models probe is preferred
- L303 `test_probe_failure_falls_back_to_hardcoded(self)` (method) — If the probe fails (non-200 / network error), we still return
- L323 `test_non_codex_providers_unaffected(self)` (method) — Resolving gpt-5.5 on non-Codex providers must NOT use the Codex
- L348 `test_stale_codex_cache_over_400k_is_invalidated(self, tmp_path, monkeypatch)` (method) — Pre-PR #14935 builds cached gpt-5.5 at 1.05M (from models.dev)
- L393 `test_fresh_codex_cache_under_400k_is_respected(self, tmp_path, monkeypatch)` (method) — Codex entries at the correct 272k must NOT be invalidated —
- L418 `test_stale_invalidation_scoped_to_codex_provider(self, tmp_path, monkeypatch)` (method) — A cached 1M entry for a non-Codex provider (e.g. Anthropic opus on
- L445 `TestNousPortalContextResolution` (class) — Nous Portal /v1/models is authoritative for what Nous infra enforces
- L461 `setup_method(self)` (method)
- L468 `test_portal_value_wins_over_openrouter_catalog(self, mock_or, mock_portal, tmp_path, monkeypatch)` (method) — The motivating case: OR catalog says 1M for qwen3.6-plus, but
- L496 `test_portal_value_is_persisted_to_disk(self, mock_or, mock_portal, tmp_path, monkeypatch)` (method) — Portal-derived value should land in the persistent cache so
- L525 `test_openrouter_fallback_is_not_persisted(self, mock_or, mock_portal, tmp_path, monkeypatch)` (method) — When the portal can't resolve a model (network blip, auth glitch,
- L559 `test_stale_cache_is_bypassed_and_overwritten_by_portal(self, mock_or, mock_portal, tmp_path, monkeypatch)` (method) — Users upgrading from pre-fix builds have ``qwen3.6-plus@…nous… =
- L603 `test_stale_cache_survives_when_portal_unreachable(self, mock_or, mock_portal, tmp_path, monkeypatch)` (method) — When the portal is unreachable AND we have a (potentially stale)
- L639 `test_bypass_keyed_on_url_not_provider_string(self, mock_or, mock_portal, tmp_path, monkeypatch)` (method) — Some call sites pass ``provider=""`` or ``provider="openrouter"``
- L679 `TestGetModelContextLength` (class)
- L681 `test_known_model_from_api(self, mock_fetch)` (method)
- L688 `test_fallback_to_defaults(self, mock_fetch)` (method)
- L693 `test_unknown_model_returns_first_probe_tier(self, mock_fetch)` (method)
- L698 `test_partial_match_in_defaults(self, mock_fetch)` (method)
- L703 `test_qwen3_coder_plus_context_length(self, mock_fetch)` (method) — qwen3-coder-plus has a 1M context window, not the generic 128K Qwen default.
- L709 `test_qwen3_coder_context_length(self, mock_fetch)` (method) — qwen3-coder has a 256K context window, not the generic 128K Qwen default.
- L715 `test_qwen3_6_plus_context_length(self, mock_fetch)` (method) — qwen3.6-plus has a 1M context window, not the generic 128K Qwen default.
- L725 `test_qwen_generic_context_length(self, mock_fetch)` (method) — Generic qwen models still get the 128K default.
- L731 `test_api_missing_context_length_key(self, mock_fetch)` (method) — Model in API but without context_length → defaults to the top
- L738 `test_cache_takes_priority_over_api(self, mock_fetch, tmp_path)` (method) — Persistent cache should be checked BEFORE API metadata.
- L748 `test_no_base_url_skips_cache(self, mock_fetch, tmp_path)` (method) — Without base_url, cache lookup is skipped.
- L760 `test_custom_endpoint_metadata_beats_fuzzy_default(self, mock_endpoint_fetch, mock_fetch)` (method)
- L776 `test_custom_endpoint_without_metadata_skips_name_based_default(self, mock_endpoint_fetch, mock_fetch)` (method)
- L790 `test_custom_endpoint_single_model_fallback(self, mock_endpoint_fetch, mock_fetch)` (method) — Single-model servers: use the only model even if name doesn't match.
- L807 `test_custom_endpoint_fuzzy_substring_match(self, mock_endpoint_fetch, mock_fetch)` (method) — Fuzzy match: configured model name is substring of endpoint model.
- L824 `test_config_context_length_overrides_all(self, mock_fetch)` (method) — Explicit config_context_length takes priority over everything.
- L838 `test_config_context_length_zero_is_ignored(self, mock_fetch)` (method) — config_context_length=0 should be treated as unset.
- L850 `test_config_context_length_none_is_ignored(self, mock_fetch)` (method) — config_context_length=None should be treated as unset.
- L866 `TestBedrockContextResolution` (class) — Regression tests for Bedrock context-length resolution order.
- L879 `test_bedrock_provider_returns_static_table_before_probe(self, mock_fetch)` (method) — provider='bedrock' resolves via static table, bypasses /models probe.
- L892 `test_bedrock_url_without_provider_hint(self, mock_fetch)` (method) — bedrock-runtime host infers Bedrock even when provider is omitted.
- L902 `test_non_bedrock_url_still_probes(self, mock_fetch)` (method) — Non-Bedrock hosts still reach the custom-endpoint probe.
- L917 `TestStripProviderPrefix` (class)
- L918 `test_known_provider_prefix_is_stripped(self)` (method)
- L924 `test_ollama_model_tag_preserved(self)` (method) — Ollama model:tag format must NOT be stripped.
- L931 `test_http_urls_preserved(self)` (method)
- L935 `test_no_colon_returns_unchanged(self)` (method)
- L940 `test_ollama_model_tag_not_mangled_in_context_lookup(self, mock_fetch)` (method) — Ensure 'qwen3.5:27b' is NOT reduced to '27b' during context length lookup.
- L961 `TestFetchModelMetadata` (class)
- L962 `_reset_cache(self)` (method)
- L968 `test_caches_result(self, mock_get)` (method)
- L986 `test_api_failure_returns_empty_on_cold_cache(self, mock_get)` (method)
- L993 `test_api_failure_returns_stale_cache(self, mock_get)` (method) — On API failure with existing cache, stale data is returned.
- L1005 `test_canonical_slug_aliasing(self, mock_get)` (method) — Models with canonical_slug get indexed under both IDs.
- L1027 `test_provider_prefixed_models_get_bare_aliases(self, mock_get)` (method)
- L1046 `test_ttl_expiry_triggers_refetch(self, mock_get)` (method) — Cache expires after _MODEL_CACHE_TTL seconds.
- L1067 `test_malformed_json_no_data_key(self, mock_get)` (method) — API returns JSON without 'data' key — empty cache, no crash.
- L1083 `TestContextProbeTiers` (class)
- L1084 `test_tiers_descending(self)` (method)
- L1089 `TestGetNextProbeTier` (class)
- L1090 `test_from_256k(self)` (method)
- L1093 `test_from_128k(self)` (method)
- L1096 `test_from_64k(self)` (method)
- L1099 `test_from_32k(self)` (method)
- L1102 `test_from_8k_returns_none(self)` (method)
- L1105 `test_from_below_min_returns_none(self)` (method)
- L1108 `test_from_arbitrary_value(self)` (method)
- L1111 `test_above_max_tier(self)` (method) — Value above 256K should return 256K.
- L1115 `test_zero_returns_none(self)` (method)
- L1123 `TestParseContextLimitFromError` (class)
- L1124 `test_openai_format(self)` (method)
- L1128 `test_context_length_exceeded(self)` (method)
- L1132 `test_context_size_exceeded(self)` (method)
- L1136 `test_no_limit_in_message(self)` (method)
- L1139 `test_unreasonable_small_number_rejected(self)` (method)
- L1142 `test_ollama_format(self)` (method)
- L1146 `test_anthropic_format(self)` (method)
- L1151 `test_lmstudio_format(self)` (method)
- L1155 `test_minimax_delta_only_message_returns_none(self)` (method)
- L1159 `test_completely_unrelated_error(self)` (method)
- L1162 `test_empty_string(self)` (method)
- L1165 `test_number_outside_reasonable_range(self)` (method) — Very large number (>10M) should be rejected.
- L1175 `TestContextLengthCache` (class)
- L1176 `test_save_and_load(self, tmp_path)` (method)
- L1182 `test_missing_cache_returns_none(self, tmp_path)` (method)
- L1187 `test_multiple_models_cached(self, tmp_path)` (method)
- L1195 `test_same_model_different_providers(self, tmp_path)` (method)
- L1203 `test_idempotent_save(self, tmp_path)` (method)
- L1212 `test_update_existing_value(self, tmp_path)` (method) — Saving a different value for the same key overwrites it.
- L1220 `test_corrupted_yaml_returns_empty(self, tmp_path)` (method) — Corrupted cache file is handled gracefully.
- L1227 `test_wrong_structure_returns_none(self, tmp_path)` (method) — YAML that loads but has wrong structure.
- L1235 `test_cached_value_takes_priority(self, mock_fetch, tmp_path)` (method)
- L1242 `test_special_chars_in_model_name(self, tmp_path)` (method) — Model names with colons, slashes, etc. don't break the cache.
- L1252 `TestGrok43StaleCacheGuard` (class) — Pre-catalog builds resolved grok-4.3 via the generic 'grok-4' catch-all
- L1260 `test_suggests_grok_4_3(self)` (method)
- L1269 `test_stale_grok_4_3_dropped_and_reresolves_to_1m(self, tmp_path, monkeypatch)` (method)
- L1281 `test_correct_grok_4_3_cache_preserved(self, tmp_path, monkeypatch)` (method)
- L1293 `test_grok_4_not_clobbered(self, tmp_path, monkeypatch)` (method)
