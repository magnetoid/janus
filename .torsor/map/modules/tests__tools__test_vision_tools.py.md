---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:44'
updated: '2026-06-08T00:38:44'
---

# tests/tools/test_vision_tools.py

Symbols in `tests/tools/test_vision_tools.py`.

- L33 `TestValidateImageUrl` (class) — Tests for URL validation, including urlparse-based netloc check.
- L36 `test_valid_https_url(self)` (method)
- L42 `test_valid_http_url(self)` (method)
- L48 `test_valid_url_without_extension(self)` (method) — CDN endpoints that redirect to images should still pass.
- L55 `test_valid_url_with_query_params(self)` (method)
- L61 `test_localhost_url_blocked_by_ssrf(self)` (method) — localhost URLs are now blocked by SSRF protection.
- L65 `test_valid_url_with_port(self)` (method)
- L71 `test_valid_url_with_path_only(self)` (method)
- L77 `test_rejects_empty_string(self)` (method)
- L80 `test_rejects_none(self)` (method)
- L83 `test_rejects_non_string(self)` (method)
- L86 `test_rejects_ftp_scheme(self)` (method)
- L89 `test_rejects_file_scheme(self)` (method)
- L92 `test_rejects_no_scheme(self)` (method)
- L95 `test_rejects_javascript_scheme(self)` (method)
- L98 `test_rejects_http_without_netloc(self)` (method) — http:// alone has no network location — urlparse catches this.
- L102 `test_rejects_https_without_netloc(self)` (method)
- L105 `test_rejects_http_colon_only(self)` (method)
- L108 `test_rejects_data_url(self)` (method)
- L111 `test_rejects_whitespace_only(self)` (method)
- L114 `test_rejects_boolean(self)` (method)
- L117 `test_rejects_list(self)` (method)
- L126 `TestDetermineMimeType` (class)
- L127 `test_jpg(self)` (method)
- L130 `test_jpeg(self)` (method)
- L133 `test_png(self)` (method)
- L136 `test_gif(self)` (method)
- L139 `test_webp(self)` (method)
- L142 `test_unknown_extension_defaults_to_jpeg(self)` (method)
- L151 `TestImageToBase64DataUrl` (class)
- L152 `test_returns_data_url(self, tmp_path)` (method)
- L158 `test_custom_mime_type(self, tmp_path)` (method)
- L164 `test_file_not_found_raises(self, tmp_path)` (method)
- L174 `TestHandleVisionAnalyze` (class) — Verify _handle_vision_analyze returns an Awaitable and builds correct prompt.
- L177 `test_returns_awaitable(self)` (method) — The handler must return an Awaitable (coroutine) since it's registered as async.
- L194 `test_prompt_contains_question(self)` (method) — The full prompt should incorporate the user's question.
- L213 `test_uses_auxiliary_vision_model_env(self)` (method) — AUXILIARY_VISION_MODEL env var should override DEFAULT_VISION_MODEL.
- L230 `test_falls_back_to_default_model(self)` (method) — Without AUXILIARY_VISION_MODEL, model should be None (let call_llm resolve default).
- L251 `test_empty_args_graceful(self)` (method) — Missing keys should default to empty strings, not raise.
- L267 `TestErrorLoggingExcInfo` (class) — Verify that exc_info=True is used in error/warning log calls.
- L271 `test_download_failure_logs_exc_info(self, tmp_path, caplog)` (method) — After max retries, the download error should include exc_info.
- L297 `test_analysis_error_logs_exc_info(self, caplog)` (method) — When vision_analyze_tool encounters an error, it should log with exc_info.
- L319 `test_cleanup_error_logs_exc_info(self, tmp_path, caplog)` (method) — Temp file cleanup failure should log warning with exc_info.
- L370 `TestVisionConfig` (class)
- L372 `test_vision_uses_configured_temperature_and_timeout(self, tmp_path)` (method)
- L402 `test_vision_defaults_temperature_when_config_omits_it(self, tmp_path)` (method)
- L430 `TestVisionSafetyGuards` (class)
- L432 `test_local_non_image_file_rejected_before_llm_call(self, tmp_path)` (method)
- L444 `test_blocked_remote_url_short_circuits_before_download(self)` (method)
- L464 `test_download_blocks_redirected_final_url(self, tmp_path)` (method)
- L508 `TestVisionRequirements` (class)
- L509 `test_check_requirements_returns_bool(self)` (method)
- L513 `test_check_requirements_accepts_codex_auth(self, monkeypatch, tmp_path)` (method)
- L540 `TestTildeExpansion` (class) — Verify that ~/path style paths are expanded correctly.
- L544 `test_tilde_path_expanded_to_local_file(self, tmp_path, monkeypatch)` (method) — vision_analyze_tool should expand ~ in file paths.
- L580 `test_tilde_path_nonexistent_file_gives_error(self, tmp_path, monkeypatch)` (method) — A tilde path that doesn't resolve to a real file should fail gracefully.
- L599 `TestFileUriSupport` (class) — Verify that file:// URIs resolve as local file paths.
- L603 `test_file_uri_resolved_as_local_path(self, tmp_path)` (method) — file:///absolute/path should be treated as a local file.
- L631 `test_file_uri_nonexistent_gives_error(self, tmp_path)` (method) — file:// pointing to a missing file should fail gracefully.
- L645 `TestBase64SizeLimit` (class) — Verify that oversized images are rejected before hitting the API.
- L649 `test_oversized_image_rejected_before_api_call(self, tmp_path)` (method) — Images exceeding the 20 MB hard limit should fail with a clear error.
- L664 `test_small_image_not_rejected(self, tmp_path)` (method) — Images well under the limit should pass the size check.
- L691 `TestErrorClassification` (class) — Verify that API 400 errors produce actionable guidance.
- L695 `test_invalid_request_error_gives_image_guidance(self, tmp_path)` (method) — An invalid_request_error from the API should mention image size/format.
- L723 `TestVisionRegistration` (class)
- L724 `test_vision_analyze_registered(self)` (method)
- L732 `test_schema_has_required_fields(self)` (method)
- L743 `test_handler_is_callable(self)` (method)
- L755 `TestResizeImageForVision` (class) — Tests for the auto-resize function.
- L758 `test_small_image_returned_as_is(self, tmp_path)` (method) — Images under the limit should be returned unchanged.
- L773 `test_large_image_is_resized(self, tmp_path)` (method) — Images over the default target should be auto-resized to fit.
- L790 `test_custom_max_bytes(self, tmp_path)` (method) — The max_base64_bytes parameter should be respected.
- L805 `test_jpeg_output_for_non_png(self, tmp_path)` (method) — Non-PNG images should be resized as JPEG.
- L819 `test_constants_sane(self)` (method) — Hard limit should be larger than resize target.
- L825 `test_extreme_aspect_ratio_preserved(self, tmp_path)` (method) — Extreme aspect ratios should be preserved during resize.
- L855 `test_tall_narrow_image_preserved(self, tmp_path)` (method) — Tall narrow images should also preserve aspect ratio.
- L881 `test_no_pillow_returns_original(self, tmp_path)` (method) — Without Pillow, oversized images should be returned as-is.
- L902 `TestImageExceedsDimension` (class) — The proactive embed path checks pixel dimensions, not just bytes.
- L912 `test_tall_small_byte_image_flagged(self, tmp_path)` (method)
- L923 `test_small_image_not_flagged(self, tmp_path)` (method)
- L933 `test_exactly_at_cap_not_flagged(self, tmp_path)` (method)
- L944 `test_missing_pillow_returns_false(self, tmp_path)` (method)
- L953 `test_corrupt_file_returns_false(self, tmp_path)` (method)
- L968 `TestIsImageSizeError` (class) — Tests for the size-error detection helper.
- L971 `test_too_large_message(self)` (method)
- L974 `test_413_status(self)` (method)
- L977 `test_invalid_request(self)` (method)
- L980 `test_exceeds_limit(self)` (method)
- L983 `test_unrelated_error(self)` (method)
- L986 `test_auth_error(self)` (method)
- L989 `test_empty_message(self)` (method)
- L993 `TestDownloadRetryClassification` (class) — Error-class-aware retry: 4xx fail-fast, 429/5xx/transient retried (issue #32296).
- L997 `_status_error(status_code)` (method)
- L1006 `_make_client_raising_status(self, status_code)` (method) — AsyncClient whose response.raise_for_status() raises HTTPStatusError.
- L1018 `test_is_retryable_classification(self)` (method)
- L1034 `test_404_fails_fast_without_retry(self, tmp_path)` (method) — A 404 must raise on the first attempt — no backoff sleep, no extra GETs.
- L1054 `test_503_retries_then_raises(self, tmp_path)` (method) — A 5xx is retried up to max_retries, sleeping between attempts.
