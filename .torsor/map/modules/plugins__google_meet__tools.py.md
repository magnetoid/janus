---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:41'
updated: '2026-06-08T00:38:41'
---

# plugins/google_meet/tools.py

Symbols in `plugins/google_meet/tools.py`.

- L26 `check_meet_requirements()` (function) — Return True when the plugin can actually run LOCALLY.
- L52 `_resolve_node_client(node: Optional[str])` (function) — Return (NodeClient, node_name) for *node*, or (None, None) to run local.
- L228 `_json(obj: Any)` (function)
- L232 `_err(msg: str, **extra)` (function)
- L236 `handle_meet_join(args: Dict[str, Any], **_kw)` (function)
- L281 `handle_meet_status(args: Dict[str, Any], **_kw)` (function)
- L296 `handle_meet_transcript(args: Dict[str, Any], **_kw)` (function)
- L318 `handle_meet_leave(args: Dict[str, Any], **_kw)` (function)
- L333 `handle_meet_say(args: Dict[str, Any], **_kw)` (function)
