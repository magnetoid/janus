---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/hermes_cli/test_backup.py

Symbols in `tests/hermes_cli/test_backup.py`.

- L18 `_make_hermes_tree(root: Path)` (function) — Create a realistic ~/.hermes directory structure for testing.
- L71 `_symlink_file_or_skip(link: Path, target: Path)` (function)
- L82 `TestShouldExclude` (class)
- L83 `test_excludes_hermes_agent(self)` (method)
- L88 `test_excludes_pycache(self)` (method)
- L92 `test_excludes_pyc_files(self)` (method)
- L96 `test_excludes_pid_files(self)` (method)
- L101 `test_excludes_checkpoints(self)` (method) — checkpoints/ is session-local trajectory cache — hash-keyed,
- L108 `test_excludes_backups_dir(self)` (method) — backups/ is excluded so pre-update backups don't nest exponentially.
- L113 `test_excludes_sqlite_sidecars(self)` (method) — SQLite WAL/SHM/journal sidecars must not ship alongside the
- L125 `test_includes_config(self)` (method)
- L129 `test_includes_env(self)` (method)
- L133 `test_includes_skills(self)` (method)
- L137 `test_includes_profiles(self)` (method)
- L141 `test_includes_sessions(self)` (method)
- L145 `test_includes_logs(self)` (method)
- L154 `TestBackup` (class)
- L155 `test_creates_zip(self, tmp_path, monkeypatch)` (method) — Backup creates a valid zip containing expected files.
- L189 `test_excludes_hermes_agent(self, tmp_path, monkeypatch)` (method) — Backup does NOT include hermes-agent/ directory.
- L209 `test_excludes_pycache(self, tmp_path, monkeypatch)` (method) — Backup does NOT include __pycache__ dirs.
- L229 `test_excludes_pid_files(self, tmp_path, monkeypatch)` (method) — Backup does NOT include PID files.
- L249 `test_default_output_path(self, tmp_path, monkeypatch)` (method) — When no output path given, zip goes to ~/hermes-backup-*.zip.
- L267 `test_skips_symlinked_files(self, tmp_path, monkeypatch)` (method) — Backup must not dereference symlinks and leak files outside HERMES_HOME.
- L295 `TestValidateBackupZip` (class)
- L296 `_make_zip(self, zip_path: Path, filenames: list[str])` (method)
- L301 `test_state_db_passes(self, tmp_path)` (method) — A zip containing state.db is accepted as a valid Hermes backup.
- L310 `test_old_wrong_db_name_fails(self, tmp_path)` (method) — A zip with only hermes_state.db (old wrong name) is rejected.
- L319 `test_config_yaml_passes(self, tmp_path)` (method) — A zip containing config.yaml is accepted (existing behaviour preserved).
- L333 `TestImport` (class)
- L334 `_make_backup_zip(self, zip_path: Path, files: dict[str, str | bytes])` (method) — Create a test zip with given files.
- L343 `test_restores_files(self, tmp_path, monkeypatch)` (method) — Import extracts files into hermes home.
- L368 `test_strips_hermes_prefix(self, tmp_path, monkeypatch)` (method) — Import strips .hermes/ prefix if all entries share it.
- L389 `test_rejects_empty_zip(self, tmp_path, monkeypatch)` (method) — Import rejects an empty zip.
- L406 `test_rejects_non_hermes_zip(self, tmp_path, monkeypatch)` (method) — Import rejects a zip that doesn't look like a hermes backup.
- L425 `test_blocks_path_traversal(self, tmp_path, monkeypatch)` (method) — Import blocks zip entries with path traversal.
- L449 `test_confirmation_prompt_abort(self, tmp_path, monkeypatch)` (method) — Import aborts when user says no to confirmation.
- L472 `test_force_skips_confirmation(self, tmp_path, monkeypatch)` (method) — Import with --force skips confirmation and overwrites.
- L492 `test_missing_file_exits(self, tmp_path, monkeypatch)` (method) — Import exits with error for nonexistent file.
- L505 `test_restores_secret_files_with_0600_perms(self, tmp_path, monkeypatch)` (method) — Secret files must end up at 0600 after restore (zipfile drops mode bits).
- L535 `TestRoundTrip` (class)
- L536 `test_backup_then_import(self, tmp_path, monkeypatch)` (method) — Full round-trip: backup -> import to a new location -> verify.
- L581 `TestFormatSize` (class)
- L582 `test_bytes(self)` (method)
- L586 `test_kilobytes(self)` (method)
- L590 `test_megabytes(self)` (method)
- L594 `test_gigabytes(self)` (method)
- L598 `test_terabytes(self)` (method)
- L603 `TestValidation` (class)
- L604 `test_validate_with_config(self)` (method) — Zip with config.yaml passes validation.
- L617 `test_validate_with_env(self)` (method) — Zip with .env passes validation.
- L630 `test_validate_rejects_random(self)` (method) — Zip without hermes markers fails validation.
- L643 `test_detect_prefix_hermes(self)` (method) — Detects .hermes/ prefix wrapping all entries.
- L656 `test_detect_prefix_none(self)` (method) — No prefix when entries are at root.
- L669 `test_detect_prefix_only_dirs(self)` (method) — Prefix detection returns empty for zip with only directory entries.
- L688 `TestBackupEdgeCases` (class)
- L689 `test_nonexistent_hermes_home(self, tmp_path, monkeypatch)` (method) — Backup exits when hermes home doesn't exist.
- L701 `test_output_is_directory(self, tmp_path, monkeypatch)` (method) — When output path is a directory, zip is created inside it.
- L721 `test_output_without_zip_suffix(self, tmp_path, monkeypatch)` (method) — Output path without .zip gets suffix appended.
- L739 `test_empty_hermes_home(self, tmp_path, monkeypatch)` (method) — Backup handles empty hermes home (no files to back up).
- L758 `test_permission_error_during_backup(self, tmp_path, monkeypatch)` (method) — Backup handles permission errors gracefully.
- L785 `test_pre1980_timestamp_skipped(self, tmp_path, monkeypatch)` (method) — Backup skips files with pre-1980 timestamps (ZIP limitation).
- L813 `test_skips_output_zip_inside_hermes(self, tmp_path, monkeypatch)` (method) — Backup skips its own output zip if it's inside hermes root.
- L835 `TestImportEdgeCases` (class)
- L836 `_make_backup_zip(self, zip_path: Path, files: dict[str, str | bytes])` (method)
- L841 `test_not_a_zip(self, tmp_path, monkeypatch)` (method) — Import rejects a non-zip file.
- L856 `test_eof_during_confirmation(self, tmp_path, monkeypatch)` (method) — Import handles EOFError during confirmation prompt.
- L874 `test_keyboard_interrupt_during_confirmation(self, tmp_path, monkeypatch)` (method) — Import handles KeyboardInterrupt during confirmation prompt.
- L892 `test_permission_error_during_import(self, tmp_path, monkeypatch)` (method) — Import handles permission errors during extraction.
- L921 `test_progress_with_many_files(self, tmp_path, monkeypatch)` (method) — Import shows progress with 500+ files.
- L948 `TestProfileRestoration` (class)
- L949 `_make_backup_zip(self, zip_path: Path, files: dict[str, str | bytes])` (method)
- L954 `test_import_creates_profile_wrappers(self, tmp_path, monkeypatch)` (method) — Import auto-creates wrapper scripts for restored profiles.
- L990 `test_import_skips_profile_dirs_without_config(self, tmp_path, monkeypatch)` (method) — Import doesn't create wrappers for profile dirs without config.
- L1016 `test_import_without_profiles_module(self, tmp_path, monkeypatch)` (method) — Import gracefully handles missing profiles module (fresh install).
- L1051 `TestSafeCopyDb` (class)
- L1052 `test_copies_valid_database(self, tmp_path)` (method)
- L1071 `test_copies_wal_mode_database(self, tmp_path)` (method)
- L1096 `TestQuickSnapshot` (class)
- L1098 `hermes_home(self, tmp_path)` (method) — Create a fake HERMES_HOME with critical state files.
- L1117 `test_creates_snapshot(self, hermes_home)` (method)
- L1125 `test_label_in_id(self, hermes_home)` (method)
- L1130 `test_state_db_safely_copied(self, hermes_home)` (method)
- L1142 `test_copies_nested_files(self, hermes_home)` (method)
- L1147 `test_missing_files_skipped(self, hermes_home)` (method)
- L1155 `test_empty_home_returns_none(self, tmp_path)` (method)
- L1161 `test_list_snapshots(self, hermes_home)` (method)
- L1171 `test_list_limit(self, hermes_home)` (method)
- L1178 `test_restore_config(self, hermes_home)` (method)
- L1189 `test_restore_state_db(self, hermes_home)` (method)
- L1205 `test_restore_nonexistent(self, hermes_home)` (method)
- L1209 `test_auto_prune(self, hermes_home)` (method)
- L1216 `test_manual_prune(self, hermes_home)` (method)
- L1224 `test_snapshot_includes_pairing_directories(self, hermes_home)` (method) — Pairing JSONs live outside state.db — snapshot must capture them
- L1265 `test_restore_recovers_pairing_data(self, hermes_home)` (method) — After restore, deleted pairing files reappear with original content.
- L1291 `test_empty_pairing_dir_does_not_fail(self, hermes_home)` (method) — An empty pairing directory should be silently skipped.
- L1305 `TestPreUpdateBackup` (class) — Tests for create_pre_update_backup — the auto-backup ``hermes update``
- L1310 `hermes_home(self, tmp_path)` (method)
- L1316 `test_creates_backup_under_backups_dir(self, hermes_home)` (method)
- L1325 `test_backup_contents_match_full_backup(self, hermes_home)` (method) — Pre-update backup should include the same user data that
- L1346 `test_does_not_recurse_into_prior_backups(self, hermes_home)` (method) — The ``backups/`` directory must be excluded so that each backup
- L1363 `test_rotation_keeps_only_n(self, hermes_home)` (method) — After more than ``keep`` backups are created, older ones are
- L1386 `test_rotation_preserves_manual_files(self, hermes_home)` (method) — Hand-dropped zips in ``backups/`` must not be touched by
- L1402 `test_returns_none_if_root_missing(self, tmp_path)` (method)
- L1406 `test_keep_zero_does_not_delete_freshly_created_backup(self, hermes_home)` (method) — Regression: ``backup_keep: 0`` previously triggered ``backups[0:]``
- L1421 `test_keep_negative_does_not_delete_freshly_created_backup(self, hermes_home)` (method) — Mirror coverage: any value <1 should be floored, not literally
- L1429 `test_keep_zero_still_prunes_older_backups(self, hermes_home)` (method) — The floor preserves the new backup but should NOT regress the
- L1453 `test_skips_symlinked_files(self, hermes_home, tmp_path)` (method) — Pre-update backups must not dereference symlinks outside HERMES_HOME.
- L1469 `TestRunPreUpdateBackup` (class) — Tests for the ``_run_pre_update_backup`` wrapper in main.py —
- L1474 `hermes_home(self, tmp_path, monkeypatch)` (method)
- L1488 `test_backup_flag_creates_backup(self, hermes_home, capsys)` (method) — --backup forces the pre-update backup for one run even when config is off.
- L1502 `test_default_disabled_is_silent(self, hermes_home, capsys)` (method) — With the default-off config and no --backup flag, the hook is silent
- L1513 `test_no_backup_flag_skips(self, hermes_home, capsys)` (method)
- L1524 `test_config_enabled_creates_backup(self, hermes_home, capsys)` (method) — Users who explicitly set updates.pre_update_backup: true still get
- L1545 `test_config_disabled_is_silent(self, hermes_home, capsys)` (method) — Explicit pre_update_backup: false behaves the same as the default —
- L1566 `test_cli_flag_overrides_enabled_config(self, hermes_home, capsys)` (method) — --no-backup wins even when config says pre_update_backup: true.
- L1588 `TestPreMigrationBackup` (class) — Tests for create_pre_migration_backup — the auto-backup
- L1593 `hermes_home(self, tmp_path)` (method)
- L1599 `test_creates_backup_under_backups_dir(self, hermes_home)` (method)
- L1610 `test_backup_uses_shared_exclusion_rules(self, hermes_home)` (method) — Pre-migration backup reuses the same exclusion rules as
- L1627 `test_restorable_with_hermes_import(self, hermes_home, tmp_path)` (method) — The zip produced by pre-migration backup must be a valid Hermes
- L1637 `test_does_not_recurse_into_prior_backups(self, hermes_home)` (method)
- L1647 `test_rotation_keeps_only_n(self, hermes_home)` (method)
- L1661 `test_missing_hermes_home_returns_none(self, tmp_path)` (method) — Fresh install with no ~/.hermes yet — nothing to back up.
- L1668 `test_does_not_touch_pre_update_backups(self, hermes_home)` (method) — Pre-migration rotation must only prune pre-migration-*.zip files,
- L1688 `TestRestoreCronJobsIfEmptied` (class) — `hermes update` config migration can leave cron/jobs.json valid-but-empty,
- L1694 `_seed_jobs(path: Path, jobs)` (method)
- L1698 `_make_snapshot(self, hermes_home: Path, label='pre-update')` (method)
- L1702 `test_restores_when_emptied_after_migration(self, tmp_path)` (method)
- L1724 `test_noop_when_live_file_still_has_jobs(self, tmp_path)` (method)
- L1735 `test_noop_when_snapshot_had_no_jobs(self, tmp_path)` (method)
- L1747 `test_noop_when_live_file_unreadable(self, tmp_path)` (method) — An unparseable live file is left alone — that's a different failure
- L1762 `test_noop_when_snapshot_id_missing(self, tmp_path)` (method)
- L1770 `test_restores_legacy_bare_list_snapshot_shape(self, tmp_path)` (method) — A legacy snapshot storing a bare JSON list (not {"jobs": [...]}) is
