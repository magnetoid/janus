---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:41'
updated: '2026-06-08T00:38:41'
---

# tests/cron/test_jobs.py

Symbols in `tests/cron/test_jobs.py`.

- L31 `TestParseDuration` (class)
- L32 `test_minutes(self)` (method)
- L39 `test_hours(self)` (method)
- L46 `test_days(self)` (method)
- L51 `test_whitespace_tolerance(self)` (method)
- L55 `test_invalid_raises(self)` (method)
- L70 `TestParseSchedule` (class)
- L71 `test_duration_becomes_once(self)` (method)
- L83 `test_every_becomes_interval(self)` (method)
- L88 `test_every_case_insensitive(self)` (method)
- L93 `test_cron_expression(self)` (method)
- L99 `test_iso_timestamp(self)` (method)
- L104 `test_invalid_schedule_raises(self)` (method)
- L108 `test_invalid_cron_raises(self)` (method)
- L118 `TestComputeNextRun` (class)
- L119 `test_once_future_returns_time(self)` (method)
- L124 `test_once_recent_past_within_grace_returns_time(self, monkeypatch)` (method)
- L133 `test_once_past_returns_none(self)` (method)
- L138 `test_once_with_last_run_returns_none_even_within_grace(self, monkeypatch)` (method)
- L147 `test_interval_first_run(self)` (method)
- L154 `test_interval_subsequent_run(self)` (method)
- L162 `test_cron_returns_future(self)` (method)
- L172 `test_unknown_kind_returns_none(self)` (method)
- L181 `tmp_cron_dir(tmp_path, monkeypatch)` (function) — Redirect cron storage to a temp directory.
- L189 `TestJobCRUD` (class)
- L190 `test_create_and_get(self, tmp_cron_dir)` (method)
- L201 `test_list_jobs(self, tmp_cron_dir)` (method)
- L207 `test_list_jobs_normalizes_partial_legacy_records(self, tmp_cron_dir)` (method)
- L227 `test_remove_job(self, tmp_cron_dir)` (method)
- L232 `test_remove_job_rejects_unsafe_legacy_id_before_output_cleanup(self, tmp_cron_dir)` (method) — Legacy unsafe IDs left over from before the create-time guard
- L249 `test_remove_nonexistent_returns_false(self, tmp_cron_dir)` (method)
- L252 `test_auto_repeat_for_once(self, tmp_cron_dir)` (method)
- L256 `test_interval_no_auto_repeat(self, tmp_cron_dir)` (method)
- L260 `test_default_delivery_origin(self, tmp_cron_dir)` (method)
- L267 `test_default_delivery_local_no_origin(self, tmp_cron_dir)` (method)
- L272 `TestUpdateJob` (class)
- L273 `test_update_name(self, tmp_cron_dir)` (method)
- L288 `test_update_schedule(self, tmp_cron_dir)` (method)
- L305 `test_update_enable_disable(self, tmp_cron_dir)` (method)
- L313 `test_update_nonexistent_returns_none(self, tmp_cron_dir)` (method)
- L317 `test_update_rejects_id_change(self, tmp_cron_dir)` (method) — Job IDs are filesystem path components — must be immutable.
- L329 `TestPauseResumeJob` (class)
- L330 `test_pause_sets_state(self, tmp_cron_dir)` (method)
- L338 `test_resume_reenables_job(self, tmp_cron_dir)` (method)
- L349 `TestResolveJobRef` (class) — Name-based job lookup for CLI/tool callers (PR #2627, @buntingszn).
- L352 `test_resolve_by_exact_id(self, tmp_cron_dir)` (method)
- L358 `test_resolve_by_name(self, tmp_cron_dir)` (method)
- L364 `test_resolve_by_name_case_insensitive(self, tmp_cron_dir)` (method)
- L371 `test_resolve_returns_none_when_not_found(self, tmp_cron_dir)` (method)
- L378 `test_resolve_id_wins_over_name(self, tmp_cron_dir)` (method) — If a job's name happens to equal another job's ID, ID match wins.
- L389 `test_resolve_ambiguous_name_raises(self, tmp_cron_dir)` (method) — Two jobs sharing a name → refuse to pick, surface both IDs.
- L403 `test_trigger_by_name(self, tmp_cron_dir)` (method)
- L411 `test_pause_by_name(self, tmp_cron_dir)` (method)
- L418 `test_remove_by_name(self, tmp_cron_dir)` (method)
- L423 `test_mutations_refuse_ambiguous_name(self, tmp_cron_dir)` (method) — pause/resume/trigger/remove must refuse to act on an ambiguous name.
- L436 `TestMarkJobRun` (class)
- L437 `test_increments_completed(self, tmp_cron_dir)` (method)
- L444 `test_repeat_limit_removes_job(self, tmp_cron_dir)` (method)
- L450 `test_repeat_negative_one_is_infinite(self, tmp_cron_dir)` (method)
- L461 `test_repeat_zero_is_infinite(self, tmp_cron_dir)` (method)
- L468 `test_error_status(self, tmp_cron_dir)` (method)
- L475 `test_delivery_error_tracked_separately(self, tmp_cron_dir)` (method) — Agent succeeds but delivery fails — both tracked independently.
- L484 `test_delivery_error_cleared_on_success(self, tmp_cron_dir)` (method) — Successful delivery clears the previous delivery error.
- L495 `test_both_agent_and_delivery_error(self, tmp_cron_dir)` (method) — Agent fails AND delivery fails — both errors recorded.
- L505 `test_recurring_cron_not_disabled_when_croniter_missing(self, tmp_cron_dir, monkeypatch)` (method) — Regression test for issue #16265.
- L536 `test_recurring_interval_not_disabled_when_next_run_is_none(self, tmp_cron_dir, monkeypatch)` (method) — Defensive sibling of the cron test — any recurring schedule that
- L556 `test_oneshot_still_completes_when_next_run_is_none(self, tmp_cron_dir)` (method) — One-shot jobs must still flip to enabled=false, state=completed
- L588 `TestAdvanceNextRun` (class) — Tests for advance_next_run() — crash-safety for recurring jobs.
- L591 `test_advances_interval_job(self, tmp_cron_dir)` (method) — Interval jobs should have next_run_at bumped to the next future occurrence.
- L608 `test_advances_cron_job(self, tmp_cron_dir)` (method) — Cron-expression jobs should have next_run_at bumped to the next occurrence.
- L626 `test_skips_oneshot_job(self, tmp_cron_dir)` (method) — One-shot jobs should NOT be advanced — they need to retry on restart.
- L637 `test_nonexistent_job_returns_false(self, tmp_cron_dir)` (method)
- L641 `test_already_future_stays_future(self, tmp_cron_dir)` (method) — If next_run_at is already in the future, advance keeps it in the future (no harm).
- L652 `test_crash_safety_scenario(self, tmp_cron_dir)` (method) — Simulate the crash-loop scenario: after advance, the job should NOT be due.
- L672 `TestGetDueJobs` (class)
- L673 `test_past_due_within_window_returned(self, tmp_cron_dir)` (method) — Jobs within the dynamic grace window are still considered due (not stale).
- L688 `test_stale_past_due_skipped(self, tmp_cron_dir)` (method) — Recurring jobs past their dynamic grace window are fast-forwarded, not fired.
- L707 `test_future_not_returned(self, tmp_cron_dir)` (method)
- L712 `test_disabled_not_returned(self, tmp_cron_dir)` (method)
- L722 `test_broken_recent_one_shot_without_next_run_is_recovered(self, tmp_cron_dir, monkeypatch)` (method)
- L754 `test_broken_stale_one_shot_without_next_run_is_not_recovered(self, tmp_cron_dir, monkeypatch)` (method)
- L783 `test_broken_cron_without_next_run_is_recovered(self, tmp_cron_dir, monkeypatch)` (method)
- L817 `test_broken_interval_without_next_run_is_recovered(self, tmp_cron_dir, monkeypatch)` (method)
- L852 `TestEnabledToolsets` (class)
- L853 `test_enabled_toolsets_stored(self, tmp_cron_dir)` (method)
- L857 `test_enabled_toolsets_persisted(self, tmp_cron_dir)` (method)
- L862 `test_enabled_toolsets_none_when_omitted(self, tmp_cron_dir)` (method)
- L866 `test_enabled_toolsets_empty_list_normalizes_to_none(self, tmp_cron_dir)` (method)
- L870 `test_enabled_toolsets_whitespace_entries_stripped(self, tmp_cron_dir)` (method)
- L874 `test_enabled_toolsets_updated_via_update_job(self, tmp_cron_dir)` (method)
- L881 `TestMarkJobRunConcurrency` (class) — Regression tests for concurrent parallel job state writes.
- L890 `test_three_concurrent_mark_job_run_no_overwrites(self, tmp_cron_dir)` (method) — Run mark_job_run() for 3 jobs in parallel threads; all must land correctly.
- L940 `test_repeated_concurrent_runs_accumulate_completed_count(self, tmp_cron_dir)` (method) — Stress test: 10 threads each call mark_job_run on a different job once.
- L975 `TestSaveJobOutput` (class)
- L976 `test_creates_output_file(self, tmp_cron_dir)` (method)
- L983 `test_rejects_unsafe_job_id(self, tmp_cron_dir, bad_job_id)` (method) — Path-escape attempts must fail closed and never create dirs.
- L989 `test_rejects_absolute_job_id(self, tmp_cron_dir)` (method) — Absolute paths as job IDs must fail closed.
