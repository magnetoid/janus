---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:41'
updated: '2026-06-08T00:38:41'
---

# tests/agent/test_credits_tracker.py

Symbols in `tests/agent/test_credits_tracker.py`.

- L20 `micros(dollars: float)` (function) — Convert a dollar amount to a micros string for header fixtures.
- L28 `_base_headers(**overrides)` (function) — Base headers present in every valid response.
- L153 `TestHealthyState` (class)
- L154 `test_parses_successfully(self)` (method)
- L158 `test_from_header_set(self)` (method)
- L162 `test_captured_at_set(self)` (method)
- L168 `test_remaining_fields(self)` (method)
- L173 `test_subscription_fields(self)` (method)
- L180 `test_rollover_and_purchased(self)` (method)
- L186 `test_tool_pool(self)` (method)
- L191 `test_denominator_and_access(self)` (method)
- L197 `test_used_fraction(self)` (method)
- L202 `test_has_data(self)` (method)
- L206 `test_not_depleted(self)` (method)
- L210 `test_age_seconds_reasonable(self)` (method)
- L219 `TestSub90Pct` (class)
- L220 `test_parses_successfully(self)` (method)
- L224 `test_used_fraction_90pct(self)` (method)
- L229 `test_paid_access(self)` (method)
- L238 `TestGrantExhausted` (class)
- L239 `test_used_fraction_100pct(self)` (method)
- L245 `test_paid_access_still_true(self)` (method)
- L254 `TestPurchasedOnly` (class)
- L255 `test_parses_successfully(self)` (method)
- L259 `test_denominator_kind_none(self)` (method)
- L263 `test_used_fraction_is_none_no_limit(self)` (method)
- L268 `test_no_limit_pair(self)` (method)
- L277 `TestToolPoolFree` (class)
- L278 `test_parses_successfully(self)` (method)
- L282 `test_tool_pool_gated_off_false(self)` (method)
- L286 `test_tool_pool_micros(self)` (method)
- L290 `test_paid_access(self)` (method)
- L298 `TestDepleted` (class)
- L299 `test_parses_successfully(self)` (method)
- L303 `test_paid_access_false(self)` (method)
- L307 `test_depleted_true(self)` (method)
- L311 `test_disabled_reason(self)` (method)
- L315 `test_remaining_zero(self)` (method)
- L323 `TestDebt` (class)
- L324 `test_parses_successfully(self)` (method)
- L329 `test_negative_subscription_accepted(self)` (method)
- L333 `test_negative_subscription_usd_accepted(self)` (method)
- L337 `test_paid_access_false(self)` (method)
- L346 `TestMissing` (class)
- L347 `test_no_credits_headers_returns_none(self)` (method)
- L351 `test_completely_empty_dict(self)` (method)
- L358 `TestNoOrg` (class)
- L359 `test_irrelevant_headers_return_none(self)` (method)
- L368 `test_api_key_path_no_org_returns_none(self)` (method)
- L380 `TestVersionValidation` (class)
- L381 `test_version_string_1_parses(self)` (method)
- L387 `test_version_2_returns_none(self)` (method)
- L392 `test_version_absent_returns_none(self)` (method)
- L397 `test_version_greater_than_1_warns_once(self, caplog)` (method) — Version > 1 must log a warning, and ONLY ONCE across multiple calls.
- L419 `test_version_0_returns_none(self)` (method)
- L423 `test_version_non_int_returns_none(self)` (method)
- L431 `TestBoolStringTrap` (class) — Explicit tests for the bool("false") == True trap.
- L434 `test_paid_access_string_false_means_depleted(self)` (method) — paid_access='false' must yield paid_access=False — NOT True.
- L442 `test_paid_access_string_true_means_not_depleted(self)` (method)
- L449 `test_paid_access_case_insensitive_FALSE(self)` (method)
- L455 `test_paid_access_case_insensitive_True(self)` (method)
- L461 `test_tool_pool_gated_off_false(self)` (method)
- L467 `test_tool_pool_gated_off_true(self)` (method)
- L477 `TestToolPoolOptional` (class) — x-nous-tool-pool-* headers are optional; absent → defaults; present-but-malformed → miss.
- L480 `_no_tool_pool_headers(self)` (method) — Base headers with BOTH tool-pool headers removed.
- L487 `test_absent_tool_pool_headers_parse_succeeds(self)` (method) — Valid credits headers with no x-nous-tool-pool-* → parse succeeds.
- L492 `test_absent_tool_pool_micros_defaults_to_zero(self)` (method)
- L496 `test_absent_tool_pool_gated_off_defaults_to_false(self)` (method)
- L500 `test_present_malformed_tool_pool_micros_returns_none(self)` (method) — x-nous-tool-pool-micros present but non-int → parse miss (returns None).
- L505 `test_present_negative_tool_pool_micros_returns_none(self)` (method) — x-nous-tool-pool-micros present but negative → parse miss (returns None).
- L510 `test_only_tool_pool_micros_absent_still_succeeds(self)` (method) — Only micros absent (gated-off still present) → tool_pool_micros = 0, parse succeeds.
- L522 `TestHalfPairLimit` (class)
- L523 `test_only_limit_micros_present_both_absent(self)` (method) — Only -micros present → both None, parse SUCCEEDS.
- L536 `test_only_limit_usd_present_both_absent(self)` (method) — Only -usd present → both None, parse SUCCEEDS.
- L549 `test_half_pair_used_fraction_is_none(self)` (method) — With no limit pair, used_fraction is None regardless of denominator_kind.
- L561 `test_full_pair_present_parsed_correctly(self)` (method) — Both present → both populated, used_fraction computable.
- L582 `TestNegativeValues` (class)
- L583 `test_negative_remaining_micros_returns_none(self)` (method)
- L587 `test_negative_purchased_micros_returns_none(self)` (method)
- L591 `test_negative_rollover_micros_returns_none(self)` (method)
- L595 `test_negative_limit_micros_returns_none(self)` (method)
- L605 `test_negative_subscription_accepted(self)` (method) — subscription_micros is the ONLY field allowed to be negative.
- L617 `TestUsdValidation` (class)
- L618 `test_valid_usd_format(self)` (method)
- L624 `test_usd_one_decimal_returns_none(self)` (method) — '18.0' does not match ^-?\d+\.\d{2}$
- L629 `test_usd_no_decimal_returns_none(self)` (method)
- L633 `test_usd_with_dollar_sign_returns_none(self)` (method)
- L637 `test_usd_with_comma_returns_none(self)` (method)
- L641 `test_usd_negative_valid(self)` (method) — Negative USD string should parse (e.g. subscription debt).
- L657 `TestMicrosValidation` (class)
- L658 `test_non_int_micros_string_returns_none(self)` (method)
- L662 `test_float_string_micros_returns_none(self)` (method) — '1.5' is not an integer string — should fail validation.
- L667 `test_non_int_purchased_returns_none(self)` (method)
- L675 `TestAsOfMs` (class)
- L676 `test_junk_as_of_ms_returns_none(self)` (method)
- L680 `test_valid_as_of_ms(self)` (method)
- L690 `TestDenominatorKind` (class)
- L691 `test_subscription_cap_valid(self)` (method)
- L703 `test_none_valid(self)` (method)
- L709 `test_invalid_denominator_kind_returns_none(self)` (method)
- L717 `TestZeroDivisionGuard` (class)
- L718 `test_subscription_limit_zero_used_fraction_is_none(self)` (method) — subscription_limit_micros='0' + subscription_cap → used_fraction is None (no ZeroDivisionError).
- L736 `TestUnknownHeaders` (class)
- L737 `test_unknown_extra_header_ignored(self)` (method)
- L746 `test_mixed_with_other_providers_headers(self)` (method)
- L759 `TestHeaderNormalization` (class)
- L760 `test_uppercase_headers_parsed(self)` (method)
- L765 `test_mixed_case_headers_parsed(self)` (method)
- L789 `TestCreditsStateDefaults` (class)
- L790 `test_default_state(self)` (method)
- L811 `test_has_data_false_when_no_captured_at(self)` (method)
- L815 `test_age_seconds_inf_when_no_data(self)` (method)
- L819 `test_depleted_false_by_default(self)` (method)
- L823 `test_used_fraction_none_by_default(self)` (method)
- L831 `TestDepletedProperty` (class)
- L832 `test_depleted_equals_not_paid_access(self)` (method) — depleted must be exactly `not paid_access`, never `remaining==0`.
- L837 `test_not_depleted_when_paid_access_true(self)` (method)
- L842 `test_depleted_independent_of_remaining(self)` (method) — Even with remaining > 0, if paid_access is False, depleted is True.
- L851 `TestUsedFraction` (class)
- L852 `test_none_without_limit(self)` (method)
- L860 `test_none_when_limit_zero(self)` (method)
- L869 `test_clamped_at_zero(self)` (method) — If subscription_micros > limit (over-credited), fraction clamps to 0.
- L879 `test_clamped_at_one(self)` (method) — If subscription_micros is very negative (debt), fraction clamps to 1.0.
- L889 `test_guarded_by_limit_field_not_denominator(self)` (method) — used_fraction depends on subscription_limit_micros being truthy, not denominator_kind.
- L901 `test_none_when_denominator_cap_but_no_limit(self)` (method) — denominator_kind=subscription_cap but no limit pair → None.
