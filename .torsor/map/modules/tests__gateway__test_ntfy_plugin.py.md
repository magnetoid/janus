---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/gateway/test_ntfy_plugin.py

Symbols in `tests/gateway/test_ntfy_plugin.py`.

- L40 `_run(coro)` (function) — Run an async coroutine synchronously.
- L50 `test_platform_enum_resolves_via_plugin_scan()` (function) — The plugin filesystem scan should expose Platform("ntfy").
- L64 `TestNtfyRequirements` (class)
- L66 `test_returns_false_when_httpx_unavailable(self, monkeypatch)` (method)
- L71 `test_returns_false_when_topic_not_set(self, monkeypatch)` (method)
- L76 `test_returns_true_when_topic_set_via_env(self, monkeypatch)` (method)
- L81 `test_validate_config_requires_topic(self, monkeypatch)` (method)
- L88 `test_is_connected_from_extra(self, monkeypatch)` (method)
- L93 `test_is_connected_from_env(self, monkeypatch)` (method)
- L103 `TestNtfyAdapterInit` (class)
- L105 `test_default_server_url(self, monkeypatch)` (method)
- L111 `test_topic_read_from_extra(self)` (method)
- L116 `test_topic_read_from_env(self, monkeypatch)` (method)
- L122 `test_publish_topic_falls_back_to_topic(self, monkeypatch)` (method)
- L128 `test_publish_topic_uses_extra_value(self)` (method)
- L136 `test_token_read_from_extra(self)` (method)
- L141 `test_token_read_from_env(self, monkeypatch)` (method)
- L147 `test_server_trailing_slash_stripped(self)` (method)
- L155 `test_initial_state(self)` (method)
- L168 `TestAuthHeaders` (class)
- L170 `_make_adapter(self, token='')` (method)
- L174 `test_no_token_returns_empty_dict(self)` (method)
- L178 `test_bearer_token_for_plain_token(self)` (method)
- L183 `test_basic_auth_for_user_colon_password(self)` (method)
- L191 `test_bearer_token_used_when_no_colon(self)` (method)
- L196 `test_auth_header_key_is_authorization(self)` (method)
- L207 `TestDeduplication` (class)
- L209 `_make_adapter(self)` (method)
- L212 `test_first_message_not_duplicate(self)` (method)
- L216 `test_second_occurrence_is_duplicate(self)` (method)
- L221 `test_different_ids_not_duplicate(self)` (method)
- L226 `test_many_messages_recorded(self)` (method)
- L232 `test_cache_pruned_on_overflow(self)` (method)
- L238 `test_expired_id_can_be_seen_again(self)` (method)
- L252 `TestConnect` (class)
- L254 `test_connect_fails_when_httpx_unavailable(self, monkeypatch)` (method)
- L260 `test_connect_fails_when_no_topic(self, monkeypatch)` (method)
- L268 `test_connect_starts_stream_task(self, monkeypatch)` (method)
- L286 `test_disconnect_clears_state(self)` (method)
- L299 `test_disconnect_cancels_stream_task(self)` (method)
- L319 `TestSend` (class)
- L321 `_make_adapter(self, topic='hermes-in', publish_topic='', token='', markdown=False)` (method)
- L329 `test_send_fails_without_http_client(self)` (method)
- L335 `test_send_posts_to_publish_topic(self)` (method)
- L353 `test_send_falls_back_to_subscribe_topic(self)` (method)
- L369 `test_send_uses_metadata_publish_topic(self)` (method)
- L387 `test_send_handles_http_error_status(self)` (method)
- L402 `test_send_handles_timeout(self)` (method)
- L421 `test_send_truncates_to_max_length(self)` (method)
- L437 `test_send_typing_is_noop(self)` (method)
- L441 `test_get_chat_info_returns_dict(self)` (method)
- L447 `test_send_includes_bearer_auth_header(self)` (method)
- L463 `test_send_emits_markdown_header_when_enabled(self)` (method)
- L476 `test_send_omits_markdown_header_when_disabled(self)` (method)
- L489 `test_send_emits_echo_tag_header(self)` (method) — Outgoing messages carry the echo-prevention tag so the adapter
- L511 `TestOnMessage` (class)
- L513 `_make_adapter(self)` (method)
- L516 `test_message_dispatched_to_handler(self)` (method)
- L536 `test_empty_message_skipped(self)` (method)
- L549 `test_duplicate_message_skipped(self)` (method)
- L562 `test_own_tagged_message_skipped(self)` (method) — An incoming event carrying the adapter's echo tag is the agent's
- L583 `test_message_with_other_tags_still_dispatched(self)` (method) — Tags unrelated to the echo sentinel must not suppress genuine
- L603 `test_timestamp_parsed_from_event(self)` (method)
- L622 `test_message_id_set_from_event(self)` (method)
- L639 `test_title_not_used_as_user_id(self)` (method) — title field must not be used for identity — it is publisher-controlled.
- L659 `test_unknown_publisher_cannot_impersonate_allowed_user(self)` (method) — An unknown publisher setting title=admin must not gain admin identity.
- L679 `test_source_chat_id_is_topic(self)` (method)
- L702 `TestEnvEnablement` (class)
- L704 `test_returns_none_without_topic(self, monkeypatch)` (method)
- L708 `test_seeds_topic_and_server(self, monkeypatch)` (method)
- L716 `test_custom_server_url(self, monkeypatch)` (method)
- L722 `test_publish_topic_seeded(self, monkeypatch)` (method)
- L728 `test_token_seeded(self, monkeypatch)` (method)
- L734 `test_markdown_truthy_values(self, monkeypatch)` (method)
- L740 `test_markdown_falsy_values(self, monkeypatch)` (method)
- L746 `test_home_channel_defaults_to_topic(self, monkeypatch)` (method)
- L753 `test_home_channel_override(self, monkeypatch)` (method)
- L767 `TestStandaloneSend` (class)
- L769 `test_errors_without_topic(self, monkeypatch)` (method)
- L778 `test_posts_to_server(self, monkeypatch)` (method)
- L802 `test_emits_echo_tag_header(self, monkeypatch)` (method) — Out-of-process cron / send_message deliveries also carry the echo
- L824 `test_emits_bearer_token_when_configured(self, monkeypatch)` (method)
- L844 `test_basic_auth_when_token_has_colon(self, monkeypatch)` (method)
- L864 `test_returns_error_on_http_failure(self, monkeypatch)` (method)
- L890 `test_register_calls_register_platform()` (function)
- L913 `test_adapter_factory_returns_ntfy_adapter()` (function)
- L927 `TestTokenHygiene` (class) — ``_build_auth_header`` must strip pasted-token whitespace; pasted
- L931 `test_trailing_whitespace_stripped(self)` (method)
- L934 `test_trailing_newline_stripped(self)` (method)
- L937 `test_whitespace_only_returns_empty(self)` (method)
- L940 `test_basic_auth_token_also_stripped(self)` (method)
- L946 `test_adapter_strips_token_via_helper(self)` (method) — The adapter delegates to _build_auth_header, so token whitespace
- L954 `TestFatalErrorPropagation` (class) — When the stream hits 401/404, the adapter must transition to the
- L959 `test_401_sets_fatal_unauthorized(self)` (method)
- L982 `test_404_sets_fatal_topic_not_found(self)` (method)
- L1005 `TestTruncateHelper` (class) — ``_truncate_body`` is shared between adapter.send() (inline truncation
- L1010 `test_short_message_passes_through(self)` (method)
- L1013 `test_long_message_truncated(self)` (method)
- L1019 `test_unicode_message_encoded(self)` (method)
