---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/gateway/test_stream_consumer.py

Symbols in `tests/gateway/test_stream_consumer.py`.

- L15 `TestCleanForDisplay` (class) — Verify MEDIA: directives and internal markers are stripped from display text.
- L18 `test_no_media_passthrough(self)` (method) — Text without MEDIA: passes through unchanged.
- L23 `test_media_tag_stripped(self)` (method) — Basic MEDIA:<path> tag is removed.
- L30 `test_media_tag_with_space(self)` (method) — MEDIA: tag with space after colon is removed.
- L37 `test_media_tag_with_quotes(self)` (method) — MEDIA: tags wrapped in quotes or backticks are removed.
- L44 `test_audio_as_voice_stripped(self)` (method) — [[audio_as_voice]] directive is removed.
- L51 `test_multiple_media_tags(self)` (method) — Multiple MEDIA: tags are all removed.
- L58 `test_excessive_newlines_collapsed(self)` (method) — Blank lines left by removed tags are collapsed.
- L65 `test_media_only_response(self)` (method) — Response that is entirely MEDIA: tags returns empty/whitespace.
- L71 `test_media_mid_sentence(self)` (method) — MEDIA: tag embedded in prose is stripped cleanly.
- L79 `test_preserves_non_media_colons(self)` (method) — Normal colons and text with 'MEDIA' as a word aren't stripped.
- L91 `TestFinalizeCapabilityGate` (class) — Verify REQUIRES_EDIT_FINALIZE gates the redundant final edit.
- L102 `test_identical_text_skip_respects_adapter_flag(self)` (method) — _send_or_edit short-circuits identical-text only when the
- L136 `TestEditMessageFinalizeSignature` (class) — Every concrete platform adapter must accept the ``finalize`` kwarg.
- L161 `test_edit_message_accepts_finalize(self, module_path, class_name)` (method)
- L173 `TestSendOrEditMediaStripping` (class) — Verify _send_or_edit strips MEDIA: before sending to the platform.
- L177 `test_first_send_strips_media(self)` (method) — Initial send removes MEDIA: tags from visible text.
- L193 `test_edit_strips_media(self)` (method) — Edit call removes MEDIA: tags from visible text.
- L213 `test_media_only_skips_send(self)` (method) — If text is entirely MEDIA: tags, the send is skipped.
- L225 `test_cursor_only_update_skips_send(self)` (method) — A bare streaming cursor should not be sent as its own message.
- L241 `test_short_text_with_cursor_skips_new_message(self)` (method) — Short text + cursor should not create a standalone new message.
- L271 `test_longer_text_with_cursor_sends_new_message(self)` (method) — Text >= 4 visible chars + cursor should create a new message normally.
- L288 `test_short_text_without_cursor_sends_normally(self)` (method) — Short text without cursor (e.g. final edit) should send normally.
- L306 `test_short_text_cursor_edit_existing_message_allowed(self)` (method) — Short text + cursor editing an existing message should proceed.
- L328 `TestStreamRunMediaStripping` (class) — End-to-end: deltas with MEDIA: produce clean visible text.
- L332 `test_stream_with_media_tag(self)` (method) — Full stream run strips MEDIA: from the final visible message.
- L367 `TestSegmentBreakOnToolBoundary` (class) — Verify that on_delta(None) finalizes the current message and starts a
- L372 `test_segment_break_creates_new_message(self)` (method) — After a None boundary, next text creates a fresh message.
- L404 `test_segment_break_no_text_before(self)` (method) — A None boundary with no preceding text is a no-op.
- L427 `test_segment_break_removes_cursor(self)` (method) — The finalized segment message should not have a cursor.
- L465 `test_multiple_segment_breaks(self)` (method) — Multiple tool boundaries create multiple message segments.
- L491 `test_already_sent_stays_true_after_segment(self)` (method) — already_sent remains True after a segment break.
- L511 `test_edit_failure_sends_only_unsent_tail_at_finish(self)` (method) — If an edit fails mid-stream, send only the missing tail once at finish.
- L541 `test_segment_break_clears_failed_edit_fallback_state(self)` (method) — A tool boundary after edit failure must flush the undelivered tail
- L573 `test_segment_break_after_mid_stream_edit_failure_preserves_tail(self)` (method) — Regression for #8124: when an earlier edit succeeded but later edits
- L624 `test_no_message_id_enters_fallback_mode(self)` (method) — Platform returns success but no message_id (Signal) — must not
- L656 `test_no_message_id_single_delta_marks_already_sent(self)` (method) — When the entire response fits in one delta and platform returns no
- L678 `test_no_message_id_segment_breaks_do_not_resend(self)` (method) — On a platform that never returns a message_id (e.g. webhook with
- L717 `test_fallback_final_splits_long_continuation_without_dropping_text(self)` (method) — Long continuation tails should be chunked when fallback final-send runs.
- L747 `test_fallback_final_sends_full_text_at_tool_boundary(self)` (method) — After a tool call, the streamed prefix is stale (from the pre-tool
- L797 `test_fallback_final_deletes_partial_after_chunks_succeed(self)` (method) — After fallback chunks land, the frozen partial must be deleted so
- L824 `test_fallback_final_does_not_delete_when_no_chunks_reach_user(self)` (method) — If every fallback send fails, the partial is the only thing the
- L848 `test_fallback_final_skips_delete_when_adapter_lacks_method(self)` (method) — Platforms without delete_message must not crash the fallback path.
- L870 `TestFinalResponseDeliveryGuard` (class) — Regression coverage for #10748 — _final_response_sent must reflect
- L877 `test_split_overflow_failed_send_does_not_mark_final_sent(self)` (method) — Split-overflow path: if every chunk send fails on done frame,
- L913 `test_split_overflow_partial_send_marks_final_sent(self)` (method) — Split-overflow path: if at least one chunk lands on done frame,
- L942 `TestFinalContentDeliveredGuard` (class) — Regression coverage for #25010 — _final_content_delivered must only be
- L950 `test_mid_stream_edit_success_does_not_mark_content_delivered(self)` (method) — When the mid-stream edit with finalize=True succeeds but the
- L1000 `test_final_edit_success_does_mark_content_delivered(self)` (method) — When the final finalize edit succeeds, _final_content_delivered
- L1026 `test_fallback_partial_send_does_not_mark_final_sent(self)` (method) — When fallback final send delivers only some chunks before failing,
- L1069 `TestEditOverflowSplitAndDeliver` (class) — When edit_message split-and-delivers an oversized payload across the
- L1077 `test_consumer_advances_message_id_on_split_and_deliver(self)` (method)
- L1119 `TestInterimCommentaryMessages` (class)
- L1121 `test_commentary_message_stays_separate_from_final_stream(self)` (method)
- L1147 `test_failed_final_send_does_not_mark_final_response_sent(self)` (method)
- L1168 `test_success_without_message_id_marks_visible_and_sends_only_tail(self)` (method)
- L1197 `TestCancelledConsumerSetsFlags` (class) — Cancellation must set final_response_sent when already_sent is True.
- L1207 `test_cancelled_with_already_sent_marks_final_response_sent(self)` (method) — Cancelling after content was sent should set final_response_sent.
- L1243 `test_cancelled_without_any_sends_does_not_mark_final(self)` (method) — Cancelling before anything was sent should NOT set final_response_sent.
- L1281 `_make_consumer()` (function) — Create a bare consumer for unit-testing the filter (no adapter needed).
- L1287 `TestFilterAndAccumulate` (class) — Unit tests for _filter_and_accumulate think-block suppression.
- L1290 `test_plain_text_passes_through(self)` (method)
- L1295 `test_complete_think_block_stripped(self)` (method)
- L1300 `test_think_block_in_middle(self)` (method)
- L1305 `test_think_block_split_across_deltas(self)` (method)
- L1311 `test_opening_tag_split_across_deltas(self)` (method)
- L1319 `test_closing_tag_split_across_deltas(self)` (method)
- L1326 `test_multiple_think_blocks(self)` (method)
- L1334 `test_multiple_think_blocks_with_text_between(self)` (method) — Think tag after non-whitespace is NOT a boundary (prose safety).
- L1344 `test_thinking_tag_variant(self)` (method)
- L1349 `test_thought_tag_variant(self)` (method)
- L1354 `test_reasoning_scratchpad_variant(self)` (method)
- L1361 `test_case_insensitive_THINKING(self)` (method)
- L1366 `test_prose_mention_not_stripped(self)` (method) — <think> mentioned mid-line in prose should NOT trigger filtering.
- L1373 `test_prose_mention_after_text(self)` (method) — <think> after non-whitespace on same line is not a block boundary.
- L1379 `test_think_at_line_start_is_stripped(self)` (method) — <think> at start of a new line IS a block boundary.
- L1385 `test_think_with_only_whitespace_before(self)` (method) — <think> preceded by only whitespace on its line is a boundary.
- L1392 `test_flush_think_buffer_on_non_tag(self)` (method) — Partial tag that turns out not to be a tag is flushed.
- L1401 `test_flush_think_buffer_when_inside_block(self)` (method) — Flush while inside a think block does NOT emit buffered content.
- L1408 `test_unclosed_think_block_suppresses(self)` (method) — An unclosed <think> suppresses all subsequent content.
- L1414 `test_multiline_think_block(self)` (method)
- L1421 `test_segment_reset_preserves_think_state(self)` (method) — _reset_segment_state should NOT clear think-block filter state.
- L1431 `TestFilterAndAccumulateIntegration` (class) — Integration: verify think blocks don't leak through the full run() path.
- L1435 `test_think_block_not_sent_to_platform(self)` (method) — Think blocks should be filtered before platform edit.
- L1480 `TestBufferOnlyMode` (class) — Verify buffer_only mode suppresses intermediate edits and only
- L1485 `test_suppresses_intermediate_edits(self)` (method) — Time-based and size-based edits are skipped; only got_done flushes.
- L1506 `test_flushes_on_segment_break(self)` (method) — A segment break (tool call boundary) flushes accumulated text.
- L1532 `test_flushes_on_commentary(self)` (method) — An interim commentary message flushes in buffer_only mode.
- L1558 `test_default_mode_still_triggers_intermediate_edits(self)` (method) — Regression: buffer_only=False (default) still does progressive edits.
- L1583 `TestCursorStrippingOnFallback` (class) — Regression: cursor must be stripped when fallback continuation is empty (#7183).
- L1593 `test_cursor_stripped_when_continuation_empty(self)` (method) — _send_fallback_final must attempt a final edit to strip the cursor.
- L1619 `test_cursor_not_stripped_when_no_cursor_configured(self)` (method) — No edit attempted when cursor is not configured.
- L1639 `test_cursor_strip_edit_failure_handled(self)` (method) — If the cursor-stripping edit itself fails, it must not crash and
- L1667 `TestOnNewMessageCallback` (class) — The on_new_message callback fires whenever a fresh content bubble
- L1681 `test_callback_fires_on_first_send(self)` (method) — First-send of a new content bubble fires on_new_message.
- L1702 `test_callback_fires_once_per_segment(self)` (method) — A new first-send fires the callback again after segment break.
- L1731 `test_callback_not_fired_on_edit(self)` (method) — Subsequent edits of the same bubble do NOT fire the callback.
- L1759 `test_callback_fires_on_commentary(self)` (method) — Commentary messages are fresh bubbles too — fire the callback.
- L1780 `test_callback_error_swallowed(self)` (method) — Exceptions in the callback do not crash the consumer.
- L1803 `test_no_callback_when_none(self)` (method) — Consumer works correctly when on_new_message is None (default).
- L1820 `TestUtf16OverflowDetection` (class) — Regression coverage for #11170 — Telegram counts message length in
- L1826 `_make_telegram_like_adapter(self)` (method) — Construct a minimal BasePlatformAdapter subclass that overrides
- L1848 `test_emoji_text_exceeding_utf16_limit_triggers_overflow_split(self)` (method) — A response that is under 4096 codepoints but over 4096 UTF-16
- L1899 `test_codepoint_only_adapter_falls_back_to_len(self)` (method) — Adapters without message_len_fn override (or test MagicMocks)
