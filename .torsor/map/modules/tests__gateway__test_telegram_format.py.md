---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/gateway/test_telegram_format.py

Symbols in `tests/gateway/test_telegram_format.py`.

- L22 `_ensure_telegram_mock()` (function)
- L51 `adapter()` (function)
- L61 `TestEscapeMdv2` (class)
- L62 `test_escapes_all_special_characters(self)` (method)
- L71 `test_empty_string(self)` (method)
- L74 `test_no_special_characters(self)` (method)
- L77 `test_backslash_escaped(self)` (method)
- L80 `test_dot_escaped(self)` (method)
- L83 `test_exclamation_escaped(self)` (method)
- L86 `test_mixed_text_and_specials(self)` (method)
- L96 `TestFormatMessageBasic` (class)
- L97 `test_empty_string(self, adapter)` (method)
- L100 `test_none_input(self, adapter)` (method)
- L104 `test_plain_text_specials_escaped(self, adapter)` (method)
- L109 `test_plain_text_no_markdown(self, adapter)` (method)
- L119 `TestFormatMessageCodeBlocks` (class)
- L120 `test_fenced_code_block_preserved(self, adapter)` (method)
- L128 `test_inline_code_preserved(self, adapter)` (method)
- L136 `test_code_block_special_chars_not_escaped(self, adapter)` (method)
- L142 `test_inline_code_special_chars_not_escaped(self, adapter)` (method)
- L147 `test_multiple_code_blocks(self, adapter)` (method)
- L155 `test_inline_code_backslashes_escaped(self, adapter)` (method) — Backslashes in inline code must be escaped for MarkdownV2.
- L161 `test_fenced_code_block_backslashes_escaped(self, adapter)` (method) — Backslashes in fenced code blocks must be escaped for MarkdownV2.
- L167 `test_fenced_code_block_backticks_escaped(self, adapter)` (method) — Backticks inside fenced code blocks must be escaped for MarkdownV2.
- L173 `test_inline_code_no_double_escape(self, adapter)` (method) — Already-escaped backslashes should not be quadruple-escaped.
- L186 `TestFormatMessageBoldItalic` (class)
- L187 `test_bold_converted(self, adapter)` (method)
- L194 `test_italic_converted(self, adapter)` (method)
- L199 `test_bold_with_special_chars(self, adapter)` (method)
- L204 `test_italic_with_special_chars(self, adapter)` (method)
- L208 `test_bold_and_italic_in_same_line(self, adapter)` (method)
- L213 `test_reload_mcp_summary_escapes_dynamic_server_names(self, adapter)` (method)
- L232 `TestFormatMessageHeaders` (class)
- L233 `test_h1_converted_to_bold(self, adapter)` (method)
- L240 `test_h2_converted(self, adapter)` (method)
- L244 `test_header_with_inner_bold_stripped(self, adapter)` (method)
- L253 `test_header_with_special_chars(self, adapter)` (method)
- L259 `test_multiline_headers(self, adapter)` (method)
- L272 `TestFormatMessageLinks` (class)
- L273 `test_markdown_link_converted(self, adapter)` (method)
- L277 `test_link_display_text_escaped(self, adapter)` (method)
- L282 `test_link_url_parentheses_escaped(self, adapter)` (method)
- L287 `test_link_with_surrounding_text(self, adapter)` (method)
- L298 `TestItalicNewlineBug` (class) — Italic regex ``\*([^*]+)\*`` matched across newlines, corrupting content.
- L305 `test_bullet_list_not_corrupted(self, adapter)` (method) — Bullet list items using * must NOT be merged into italic.
- L316 `test_asterisk_list_items_preserved(self, adapter)` (method) — Each * list item should remain as a separate line, not become italic.
- L327 `test_italic_does_not_span_lines(self, adapter)` (method) — *text on
- L335 `test_single_line_italic_still_works(self, adapter)` (method) — Normal single-line italic must still convert correctly.
- L347 `TestFormatMessageStrikethrough` (class)
- L348 `test_strikethrough_converted(self, adapter)` (method)
- L353 `test_strikethrough_with_special_chars(self, adapter)` (method)
- L357 `test_strikethrough_in_code_not_converted(self, adapter)` (method)
- L361 `test_strikethrough_with_bold(self, adapter)` (method)
- L372 `TestFormatMessageSpoiler` (class)
- L373 `test_spoiler_converted(self, adapter)` (method)
- L377 `test_spoiler_with_special_chars(self, adapter)` (method)
- L381 `test_spoiler_in_code_not_converted(self, adapter)` (method)
- L385 `test_spoiler_pipes_not_escaped(self, adapter)` (method) — The || delimiters must not be escaped as \|\|.
- L397 `TestFormatMessageBlockquote` (class)
- L398 `test_blockquote_converted(self, adapter)` (method)
- L404 `test_blockquote_with_special_chars(self, adapter)` (method)
- L409 `test_blockquote_multiline(self, adapter)` (method)
- L416 `test_blockquote_in_code_not_converted(self, adapter)` (method)
- L420 `test_nested_blockquote(self, adapter)` (method)
- L425 `test_gt_in_middle_of_line_still_escaped(self, adapter)` (method) — Only > at line start is a blockquote; mid-line > should be escaped.
- L430 `test_expandable_blockquote(self, adapter)` (method) — Expandable blockquote prefix **> and trailing || must NOT be escaped.
- L438 `test_single_asterisk_gt_not_blockquote(self, adapter)` (method) — Single asterisk before > should not be treated as blockquote prefix.
- L444 `test_regular_blockquote_with_pipes_escaped(self, adapter)` (method) — Regular blockquote ending with || should escape the pipes.
- L457 `TestFormatMessageComplex` (class)
- L458 `test_code_block_with_bold_outside(self, adapter)` (method)
- L464 `test_bold_inside_code_not_converted(self, adapter)` (method) — Bold markers inside code blocks should not be converted.
- L470 `test_link_inside_code_not_converted(self, adapter)` (method)
- L475 `test_header_after_code_block(self, adapter)` (method)
- L481 `test_multiple_bold_segments(self, adapter)` (method)
- L485 `test_special_chars_in_plain_text(self, adapter)` (method)
- L492 `test_empty_bold(self, adapter)` (method) — **** (empty bold) should not crash.
- L497 `test_empty_code_block(self, adapter)` (method)
- L501 `test_placeholder_collision(self, adapter)` (method) — Many formatting elements should not cause placeholder collisions.
- L524 `TestStripMdv2` (class)
- L525 `test_removes_escape_backslashes(self)` (method)
- L528 `test_removes_bold_markers(self)` (method)
- L531 `test_removes_italic_markers(self)` (method)
- L534 `test_removes_both_bold_and_italic(self)` (method)
- L538 `test_preserves_snake_case(self)` (method)
- L541 `test_preserves_multi_underscore_identifier(self)` (method)
- L544 `test_plain_text_unchanged(self)` (method)
- L547 `test_empty_string(self)` (method)
- L550 `test_removes_strikethrough_markers(self)` (method)
- L553 `test_removes_spoiler_markers(self)` (method)
- L562 `TestWrapMarkdownTables` (class) — _wrap_markdown_tables rewrites GFM pipe tables into Telegram-friendly
- L566 `test_basic_table_rewritten_as_row_groups(self)` (method)
- L590 `test_bare_pipe_table_rewritten(self)` (method) — Tables without outer pipes (GFM allows this) are still detected.
- L600 `test_alignment_separators(self)` (method) — Separator rows with :--- / ---: / :---: alignment markers match.
- L616 `test_two_consecutive_tables_rewritten_separately(self)` (method)
- L635 `test_plain_text_with_pipes_not_wrapped(self)` (method) — A bare pipe in prose must NOT trigger wrapping.
- L640 `test_horizontal_rule_not_wrapped(self)` (method) — A lone '---' horizontal rule must not be mistaken for a separator.
- L645 `test_existing_code_block_with_pipes_left_alone(self)` (method) — A table already inside a fenced code block must not be re-wrapped.
- L656 `test_no_pipe_character_short_circuits(self)` (method)
- L660 `test_no_dash_short_circuits(self)` (method)
- L664 `test_single_column_separator_not_matched(self)` (method) — Single-column tables (rare) are not detected — we require at
- L671 `test_row_group_uses_single_newlines_within_group(self)` (method) — Regression: each bullet within a row-group must be separated by
- L703 `test_row_label_column_preserves_first_bullet(self)` (method) — When the table has a row-label column (data rows have one more
- L722 `TestFormatMessageTables` (class) — End-to-end: pipe tables become readable Telegram-native text instead
- L726 `test_table_rendered_as_bullets(self, adapter)` (method)
- L741 `test_text_after_table_still_formatted(self, adapter)` (method)
- L759 `test_multiple_tables_in_single_message(self, adapter)` (method)
- L778 `test_send_escapes_chunk_indicator_for_markdownv2(adapter)` (function)
- L806 `TestEditMessageStreamingSafety` (class)
- L808 `test_non_final_edit_uses_plain_text_without_markdown(self)` (method)
- L823 `test_final_edit_uses_markdownv2_with_plain_fallback(self)` (method)
- L842 `test_message_too_long_splits_into_continuations_not_silent_truncation(self)` (method) — When edit_message_text exceeds Telegram's 4096 UTF-16 limit, the
- L879 `test_message_too_long_continuations_preserve_topic_metadata(self)` (method) — Overflow continuations should stay in the originating Telegram topic.
- L910 `_guest_test_adapter(*, guest_mode=True, require_mention=True, allowed_chats=None)` (function)
- L932 `_guest_group_message(text, *, chat_id=-100201, entities=None, reply_to_bot=False)` (function)
- L946 `_guest_mention_entity(text, mention='@hermes_bot')` (function)
- L950 `TestTelegramGuestMentionGating` (class)
- L951 `test_guest_mode_allows_explicit_mention_outside_allowed_chats(self)` (method)
- L962 `test_guest_mode_does_not_allow_reply_outside_allowed_chats(self)` (method)
- L968 `test_guest_mode_disabled_keeps_allowed_chats_as_hard_gate_for_mentions(self)` (method)
- L979 `test_guest_mode_allows_bot_command_entity_outside_allowed_chats(self)` (method) — ``/cmd@botname`` is a ``bot_command`` entity, not ``mention``.
- L991 `test_guest_mode_allows_text_mention_entity_outside_allowed_chats(self)` (method) — MessageEntity(type=text_mention) tags a user by ID — recognised as mention.
- L1002 `test_guest_mode_allows_mention_in_caption_outside_allowed_chats(self)` (method) — Media caption @mention should bypass allowed_chats via guest_mode.
