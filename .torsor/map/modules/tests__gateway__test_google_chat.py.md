---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/gateway/test_google_chat.py

Symbols in `tests/gateway/test_google_chat.py`.

- L35 `_FakeHttpError` (class) — Stand-in for googleapiclient.errors.HttpError with .resp.status.
- L38 `__init__(self, status=500, content=b'', reason='')` (method)
- L46 `_ensure_google_mocks()` (function) — Install mock google-* modules so GoogleChatAdapter can be imported.
- L147 `_base_config(**extra)` (function)
- L159 `adapter(tmp_path)` (function) — Build an adapter with its loop captured and Chat client mocked.
- L188 `_make_pubsub_message(data: dict, *, attributes=None)` (function) — Build a Mock Pub/Sub Message with ack/nack trackers.
- L198 `_make_chat_envelope(text='hello', sender_email='u@example.com', sender_type='HUMAN', msg_name=None, thread_name=None, attachments=None, slash_command=None)` (function) — Build a realistic Google Chat CloudEvents-style envelope body.
- L235 `TestPlatformRegistration` (class)
- L236 `test_enum_value(self)` (method)
- L239 `test_requirements_check_returns_true_when_available(self)` (method)
- L249 `TestEnvConfigLoading` (class)
- L261 `_clean_env(self, monkeypatch)` (method)
- L269 `test_missing_subscription_does_not_enable(self, monkeypatch)` (method)
- L276 `test_missing_project_does_not_enable(self, monkeypatch)` (method)
- L291 `TestHelpers` (class)
- L292 `test_mime_image_maps_to_photo(self)` (method)
- L295 `test_mime_audio_maps_to_audio(self)` (method)
- L298 `test_mime_video_maps_to_video(self)` (method)
- L301 `test_mime_other_maps_to_document(self)` (method)
- L304 `test_mime_empty_maps_to_document(self)` (method)
- L308 `TestRedactSensitive` (class)
- L309 `test_redacts_subscription_path(self)` (method)
- L315 `test_redacts_topic_path(self)` (method)
- L320 `test_redacts_service_account_email(self)` (method)
- L326 `test_empty_text_passes_through(self)` (method)
- L331 `TestGoogleOwnedHost` (class)
- L338 `test_accepts_google_hosts(self, url)` (method)
- L350 `test_rejects_non_google_or_insecure(self, url)` (method)
- L359 `TestValidateConfig` (class)
- L360 `test_missing_project_raises(self)` (method)
- L365 `test_missing_subscription_raises(self)` (method)
- L372 `test_subscription_format_rejected(self)` (method)
- L378 `test_subscription_project_mismatch_rejected(self)` (method)
- L387 `test_validate_config_happy(self)` (method)
- L399 `TestChunkText` (class)
- L400 `test_empty_returns_empty_list(self, adapter)` (method)
- L403 `test_short_returns_single_chunk(self, adapter)` (method)
- L406 `test_long_splits_into_multiple(self, adapter)` (method)
- L413 `test_splits_on_newline_near_boundary(self, adapter)` (method)
- L428 `TestOnPubsubMessage` (class) — Pub/Sub callback routing. The callback runs in a thread and dispatches
- L433 `test_shutting_down_nacks(self, adapter)` (method)
- L440 `test_malformed_json_acks_without_dispatch(self, adapter)` (method)
- L450 `test_membership_created_caches_bot_user_id(self, adapter, tmp_path, monkeypatch)` (method)
- L469 `test_membership_deleted_acks_no_dispatch(self, adapter)` (method)
- L485 `test_bot_sender_is_filtered(self, adapter)` (method)
- L493 `test_relay_flat_bot_sender_is_filtered_end_to_end(self, adapter)` (method) — Format 3 end-to-end: a relay envelope declaring sender_type=BOT
- L516 `test_relay_flat_human_sender_dispatches(self, adapter)` (method) — Format 3 negative control: an envelope without sender_type
- L536 `test_duplicate_message_dropped(self, adapter)` (method)
- L546 `test_text_message_submits_to_loop(self, adapter)` (method)
- L554 `test_callback_exception_does_not_escape(self, adapter)` (method)
- L565 `TestExtractMessagePayload` (class) — Three Pub/Sub envelope formats are accepted.
- L576 `test_native_chat_api_format_extracts_msg_and_space(self)` (method) — Format 2: top-level ``message`` + ``space`` + ``type=MESSAGE``.
- L607 `test_native_chat_api_format_drops_non_message_events(self)` (method) — Format 2 with ``type != MESSAGE`` returns None — caller acks.
- L616 `test_relay_flat_format_synthesizes_chat_api_shape(self)` (method) — Format 3: flat fields from a custom Cloud Run relay.
- L654 `test_relay_flat_honors_declared_sender_type_bot(self)` (method) — Format 3 propagates ``envelope.sender_type`` so the downstream
- L678 `test_relay_flat_defaults_sender_type_human_when_absent(self)` (method) — Backward compatibility: relays that don't declare sender_type
- L693 `test_relay_flat_coerces_unknown_sender_type_to_human(self)` (method) — Defensive coercion: only ``HUMAN`` and ``BOT`` are accepted;
- L722 `test_unrecognized_envelope_returns_none(self)` (method) — Random JSON with no known shape returns None (caller acks).
- L733 `TestBuildMessageEvent` (class)
- L735 `test_dm_first_message_in_thread_is_main_flow(self, adapter)` (method) — Google Chat DMs spawn a fresh thread per top-level user
- L767 `test_dm_second_message_in_same_thread_is_side_thread(self, adapter)` (method) — If we've SEEN a thread before (count > 0), the user explicitly
- L789 `test_dm_side_thread_caches_thread_for_outbound(self, adapter)` (method) — When a thread is identified as side-thread, the cache MUST
- L809 `test_dm_main_flow_after_side_thread_clears_cache(self, adapter)` (method) — User was in a side thread, then returns to top-level (input
- L830 `test_dm_different_top_level_threads_share_session(self, adapter)` (method) — Three separate top-level user messages → three different
- L844 `test_group_keeps_thread_id_on_source(self, adapter)` (method) — In group spaces, threads are real conversational containers —
- L858 `test_slash_command_yields_command_type(self, adapter)` (method)
- L869 `test_attachment_image_triggers_download(self, adapter)` (method)
- L893 `TestSend` (class)
- L895 `test_text_send_creates_message(self, adapter)` (method)
- L905 `test_create_message_passes_messageReplyOption_when_thread_set(self, adapter)` (method) — Critical Google Chat API quirk: when messages.create is called
- L938 `test_create_message_omits_messageReplyOption_when_no_thread(self, adapter)` (method) — No thread.name in body → no messageReplyOption needed.
- L952 `test_with_typing_card_patches_instead_of_creating(self, adapter)` (method)
- L974 `test_long_text_splits_and_sends_multiple(self, adapter)` (method)
- L984 `test_403_sets_fatal_error(self, adapter)` (method)
- L992 `test_404_returns_target_not_found(self, adapter)` (method)
- L1000 `test_429_increments_rate_limit_counter_and_raises(self, adapter)` (method)
- L1013 `TestTypingLifecycle` (class)
- L1015 `test_send_typing_posts_and_tracks(self, adapter)` (method)
- L1026 `test_send_typing_skips_when_already_tracking(self, adapter)` (method)
- L1033 `test_send_typing_inherits_inbound_thread(self, adapter)` (method) — The typing card must be created in the same thread as the
- L1052 `test_send_typing_no_thread_when_cache_empty(self, adapter)` (method) — If no inbound thread has been seen yet, typing card creates
- L1066 `test_send_typing_concurrent_calls_create_only_one_card(self, adapter)` (method) — When _keep_typing fires send_typing twice in flight (the
- L1104 `test_send_typing_survives_caller_cancellation(self, adapter)` (method) — base.py's _keep_typing wraps send_typing in
- L1146 `test_orphan_typing_cards_reaped_on_completion(self, adapter)` (method) — If a background send_typing task created a card AFTER send()
- L1177 `test_stop_typing_is_noop_for_live_card(self, adapter)` (method) — Anti-tombstone: stop_typing leaves a real msg_id in place so
- L1192 `test_stop_typing_pops_sentinel(self, adapter)` (method) — After send() patches the typing card, the slot holds the
- L1201 `test_stop_typing_noop_when_nothing_tracked(self, adapter)` (method)
- L1208 `test_on_processing_complete_pops_sentinel_on_success(self, adapter)` (method) — SUCCESS path: send() set the sentinel; cleanup just pops it.
- L1221 `test_on_processing_complete_patches_stranded_card(self, adapter)` (method) — CANCELLED path: send() never ran. Patch the typing card with a
- L1246 `TestEditMessage` (class)
- L1248 `test_edit_message_patches_via_messages_patch(self, adapter)` (method)
- L1263 `test_edit_message_truncates_overlong_text(self, adapter)` (method)
- L1275 `test_edit_message_missing_id_returns_failure(self, adapter)` (method)
- L1280 `test_edit_message_429_increments_rate_limit_counter(self, adapter)` (method)
- L1290 `test_edit_message_overrides_base_so_progress_pipeline_runs(self, adapter)` (method) — The gateway tool-progress flow at gateway/run.py:10199 gates on
- L1301 `TestDeleteMessage` (class)
- L1303 `test_delete_message_calls_api(self, adapter)` (method)
- L1312 `test_delete_message_swallows_404(self, adapter)` (method)
- L1320 `test_delete_message_missing_id_returns_false(self, adapter)` (method)
- L1339 `TestNativeAttachmentDelivery` (class)
- L1341 `test_send_file_posts_setup_notice_when_no_user_oauth(self, adapter, tmp_path)` (method) — Without user creds, _send_file posts a clear setup notice and
- L1364 `test_send_file_two_step_native_upload_when_user_oauth_ready(self, adapter, tmp_path)` (method) — With user creds, _send_file calls media.upload then
- L1401 `test_send_file_falls_back_to_notice_on_401(self, adapter, tmp_path)` (method) — A 401 from media.upload (token revoked / scope missing) should
- L1431 `test_send_file_returns_error_on_unrelated_http_error(self, adapter, tmp_path)` (method) — Non-auth HTTP errors propagate as SendResult.error without
- L1456 `TestSetupFilesSlashCommand` (class)
- L1458 `test_slash_command_intercepted_before_agent(self, adapter)` (method) — /setup-files is bot-side admin, not agent input. The dispatch
- L1483 `test_no_arg_status_when_unconfigured(self, adapter, tmp_path, monkeypatch)` (method) — Without client_secret AND without token, status reply tells the
- L1501 `test_revoke_clears_in_memory_creds(self, adapter, tmp_path, monkeypatch)` (method)
- L1518 `TestUserOAuthHelper` (class)
- L1520 `_assert_private_json_file(path, expected)` (method)
- L1526 `test_load_user_credentials_returns_none_when_no_token(self, tmp_path, monkeypatch)` (method) — Missing token file is the expected no-op case (user hasn't
- L1533 `test_load_user_credentials_returns_none_on_corrupt_token(self, tmp_path, monkeypatch)` (method)
- L1539 `test_scopes_are_minimal(self)` (method) — The OAuth flow should request ONLY chat.messages.create — no
- L1545 `test_sanitize_email_lowercases_and_replaces_unsafe_chars(self)` (method) — Path components must be filesystem-safe across users.
- L1558 `test_per_user_token_path_isolated_from_legacy(self, tmp_path, monkeypatch)` (method) — Per-user files live under a dedicated subdirectory so the
- L1571 `test_load_user_credentials_per_email_returns_none_when_missing(self, tmp_path, monkeypatch)` (method) — A user who has not authorized has no token file; load returns
- L1580 `test_list_authorized_emails_lists_per_user_files(self, tmp_path, monkeypatch)` (method) — ``list_authorized_emails`` enumerates the per-user dir; the
- L1598 `test_list_authorized_emails_empty_when_dir_missing(self, tmp_path, monkeypatch)` (method)
- L1605 `test_pending_auth_path_is_per_user_when_email_given(self, tmp_path, monkeypatch)` (method) — Two users running /setup-files start in parallel must not
- L1620 `test_persist_credentials_writes_private_json(self, tmp_path, monkeypatch)` (method)
- L1653 `test_store_client_secret_writes_private_json(self, tmp_path, monkeypatch)` (method)
- L1668 `test_save_pending_auth_writes_private_json(self, tmp_path, monkeypatch)` (method)
- L1693 `TestPerUserAttachmentRouting` (class) — The bot must use the *requesting user's* OAuth token when sending
- L1701 `test_build_message_event_caches_sender_email(self, adapter)` (method) — The asker's email is captured per chat_id at inbound time so
- L1713 `test_send_file_uses_per_user_token_when_sender_known(self, adapter, tmp_path, monkeypatch)` (method) — sender_email maps to a per-user file → that user's API client
- L1763 `test_send_file_falls_back_to_legacy_when_per_user_missing(self, adapter, tmp_path, monkeypatch)` (method) — sender known but no per-user token → legacy creds fill in.
- L1798 `test_send_file_no_creds_anywhere_posts_setup_notice(self, adapter, tmp_path)` (method) — Sender unknown AND no legacy fallback → setup-instructions
- L1826 `test_send_file_per_user_401_evicts_only_that_user(self, adapter, tmp_path, monkeypatch)` (method) — A 401 from one user's token must NOT clobber another user's
- L1867 `test_setup_files_writes_to_per_user_path(self, adapter, tmp_path, monkeypatch)` (method) — ``/setup-files <code>`` from sender alice writes to alice's
- L1900 `test_setup_files_revoke_drops_only_that_user(self, adapter, tmp_path, monkeypatch)` (method) — Per-user revoke clears alice's slot; bob and the legacy
- L1943 `TestThreadCountStore` (class)
- L1944 `test_missing_file_returns_zero_counts(self, tmp_path)` (method)
- L1950 `test_corrupt_json_treated_as_empty(self, tmp_path)` (method) — A garbage file shouldn't crash the adapter — log warn, treat
- L1967 `test_incr_returns_pre_increment_value(self, tmp_path)` (method) — The PRE-increment count is the heuristic input — it answers
- L1979 `test_round_trip_persists_across_load(self, tmp_path)` (method) — Two store instances on the same file behave like a single
- L2000 `test_invalid_shape_dropped_silently(self, tmp_path)` (method) — If someone hand-edits the file with weird shapes, drop the
- L2018 `test_outbound_thread_tracked_for_user_reply_in_bot_thread(self, adapter)` (method) — The bug Ramón hit on the live mac-mini: when the bot replies
- L2063 `test_side_thread_detection_survives_restart(self, adapter, tmp_path)` (method) — End-to-end regression for the bug Ramón hit across 4
- L2117 `TestAttachmentSSRFGuard` (class)
- L2119 `test_drive_picker_only_skipped_when_no_resource_name(self, adapter)` (method) — Pure Drive-picker shares (source=DRIVE_FILE, no resourceName)
- L2132 `test_drive_file_with_resource_name_uses_bot_path(self, adapter, tmp_path, monkeypatch)` (method) — Drag-and-drop chat uploads ALSO carry source=DRIVE_FILE but
- L2165 `test_rejects_non_google_host(self, adapter)` (method)
- L2175 `test_rejects_metadata_endpoint(self, adapter)` (method)
- L2189 `TestOutboundThreadRouting` (class)
- L2190 `test_resolve_uses_metadata_thread_id(self, adapter)` (method)
- L2198 `test_resolve_falls_back_to_cached_thread_for_dm(self, adapter)` (method) — In DMs the source.thread_id is None, so the metadata passed
- L2211 `test_resolve_metadata_overrides_cache(self, adapter)` (method) — Explicit metadata (e.g. agent replying to a specific event)
- L2222 `test_resolve_returns_none_when_no_inputs(self, adapter)` (method)
- L2234 `TestMediaDelegation` (class)
- L2236 `test_send_voice_delegates_to_document_with_audio_mime(self, adapter, tmp_path)` (method)
- L2248 `test_send_video_delegates_with_video_mime(self, adapter, tmp_path)` (method)
- L2260 `test_send_animation_delegates_to_image(self, adapter)` (method) — Google Chat has no native animation type; the adapter falls back
- L2277 `test_send_file_missing_path_returns_error(self, adapter)` (method)
- L2289 `TestOutboundRetry` (class) — Outbound message creation retries on transient failures.
- L2301 `test_retries_on_503_then_succeeds(self, adapter, monkeypatch)` (method) — A 503 from messages.create triggers backoff + retry.
- L2329 `test_gives_up_after_max_attempts(self, adapter, monkeypatch)` (method) — Three consecutive 503s exhaust the retry budget; the call raises.
- L2346 `test_does_not_retry_on_400(self, adapter, monkeypatch)` (method) — A 400 (client error) is permanent — no retry, fails immediately.
- L2362 `test_is_retryable_error_classifier(self)` (method) — Spot-check the retryable-error taxonomy.
- L2382 `TestFormatMessage` (class) — Markdown→Chat dialect conversion + invisible Unicode stripping.
- L2394 `test_bold_double_asterisk_to_single(self)` (method) — **bold** → *bold* (Chat's bold syntax uses single asterisks).
- L2399 `test_bold_italic_combo_to_chat_dialect(self)` (method) — ***x*** → *_x_* (bold-italic compound).
- L2404 `test_markdown_link_to_chat_anglebracket(self)` (method) — [text](url) → <url|text> (Slack-style anglebracket links).
- L2409 `test_header_to_bold_at_line_start_only(self)` (method) — # Title → *Title* but only at line-start; mid-line `#` untouched.
- L2414 `test_fenced_code_block_protected(self)` (method) — **asterisks** inside a fenced code block do NOT convert.
- L2428 `test_inline_code_protected(self)` (method) — `**text**` inside inline backticks does NOT convert.
- L2433 `test_url_with_parens_in_path(self)` (method) — `[txt](https://x.com/foo(bar))` — pin the documented limitation.
- L2447 `test_mixed_bold_italic_orderings(self)` (method) — **bold** _italic_ in the same line — both surface conversions.
- L2455 `test_strips_zwj_and_variation_selector(self)` (method) — ZWJ (U+200D) + Variation Selector 16 (U+FE0F) get stripped.
- L2472 `test_strips_bom_and_bidi_marks(self)` (method) — BOM, LTR/RTL marks stripped — they break Chat's font rendering.
- L2481 `test_empty_and_none_safe(self)` (method) — Empty / None pass through without raising.
- L2494 `test_unmatched_asterisks_left_alone(self)` (method) — A lone `**` with no closing pair is not transformed.
- L2505 `TestADCFallback` (class) — When no SA JSON is configured, fall back to Application Default Credentials.
- L2513 `test_load_credentials_uses_adc_when_no_sa_path(self, adapter, monkeypatch)` (method) — No SA path → google.auth.default() is called.
- L2534 `test_load_credentials_raises_when_no_sa_and_adc_unavailable(self, adapter, monkeypatch)` (method) — ADC failure surfaces a useful error pointing at the two fixes.
- L2557 `TestGoogleChatInteractiveSetup` (class)
- L2558 `test_interactive_setup_uses_shared_cli_prompt_helpers(self, monkeypatch)` (method) — Google Chat setup should not import prompt helpers from config.py.
- L2617 `TestSupervisorReconnect` (class)
- L2619 `test_fatal_after_max_retries(self, adapter, monkeypatch)` (method) — Simulate 10+ failing subscribe() calls and assert fatal error set.
- L2643 `TestAuthorizationEmailMatch` (class) — `GOOGLE_CHAT_ALLOWED_USERS=email` matches naturally without a bridge.
- L2653 `test_allowlist_matches_when_user_id_is_email(self, monkeypatch)` (method) — Email allowlist match — the canonical case.
- L2679 `test_allowlist_denies_wrong_email(self, monkeypatch)` (method)
- L2700 `test_allowlist_falls_back_to_resource_name_when_no_email(self, monkeypatch)` (method) — If sender has no email, ``user_id`` falls back to the resource
- L2739 `TestCronSchedulerRegistry` (class)
- L2740 `_ensure_registered(self)` (method) — Force the plugin system to register the Google Chat adapter.
- L2772 `test_google_chat_is_known_delivery_platform(self)` (method)
- L2778 `test_google_chat_home_env_var_resolves(self)` (method)
- L2788 `_FakeAiohttpResponse` (class)
- L2789 `__init__(self, status: int, payload, text_body: str='')` (method)
- L2794 `json(self)` (method)
- L2797 `text(self)` (method)
- L2800 `__aenter__(self)` (method)
- L2803 `__aexit__(self, exc_type, exc, tb)` (method)
- L2807 `_FakeAiohttpSession` (class)
- L2808 `__init__(self, scripts)` (method)
- L2812 `__aenter__(self)` (method)
- L2815 `__aexit__(self, exc_type, exc, tb)` (method)
- L2818 `post(self, url, **kwargs)` (method)
- L2825 `_install_fake_aiohttp(monkeypatch, session)` (function)
- L2833 `_install_fake_google_auth_transport(monkeypatch)` (function)
- L2839 `TestGoogleChatStandaloneSend` (class)
- L2842 `test_standalone_send_refreshes_token_and_posts_message(self, monkeypatch, tmp_path)` (method)
- L2888 `test_standalone_send_returns_error_on_invalid_chat_id(self, monkeypatch)` (method)
- L2899 `test_standalone_send_propagates_api_failure(self, monkeypatch, tmp_path)` (method)
- L2939 `test_standalone_send_rejects_chat_id_with_path_traversal(self, monkeypatch)` (method)
