---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:43'
updated: '2026-06-08T00:38:43'
---

# tests/tools/test_delegate.py

Symbols in `tests/tools/test_delegate.py`.

- L38 `_make_mock_parent(depth=0)` (function) — Create a mock parent agent with the fields delegate_task expects.
- L61 `TestDelegateRequirements` (class)
- L62 `test_always_available(self)` (method)
- L65 `test_schema_valid(self)` (method)
- L78 `test_schema_description_advertises_runtime_limits(self)` (method) — The model must see the user's actual concurrency / spawn-depth caps,
- L111 `test_schema_overrides_applied_via_get_definitions(self)` (method) — Registry.get_definitions() must apply dynamic_schema_overrides so
- L128 `TestChildSystemPrompt` (class)
- L129 `test_goal_only(self)` (method)
- L135 `test_goal_with_context(self)` (method)
- L141 `test_empty_context_ignored(self)` (method)
- L146 `TestStripBlockedTools` (class)
- L147 `test_removes_blocked_toolsets(self)` (method)
- L151 `test_preserves_allowed_toolsets(self)` (method)
- L155 `test_empty_input(self)` (method)
- L160 `TestDelegateTask` (class)
- L161 `test_no_parent_agent(self)` (method)
- L166 `test_depth_limit(self)` (method)
- L172 `test_no_goal_or_tasks(self)` (method)
- L177 `test_empty_goal(self)` (method)
- L182 `test_task_missing_goal(self)` (method)
- L188 `test_single_task_mode(self, mock_run)` (method)
- L202 `test_batch_mode(self, mock_run)` (method)
- L220 `test_batch_mode_accepts_json_string_tasks(self, mock_run)` (method)
- L253 `test_batch_mode_rejects_non_object_tasks(self, mock_run)` (method)
- L265 `test_batch_mode_rejects_malformed_json_string_tasks(self, mock_run)` (method)
- L277 `test_batch_capped_at_3(self, mock_run)` (method)
- L292 `test_batch_ignores_toplevel_goal(self, mock_run)` (method) — When tasks array is provided, top-level goal/context/toolsets are ignored.
- L309 `test_failed_child_included_in_results(self, mock_run)` (method)
- L320 `test_depth_increments(self)` (method) — Verify child gets parent's depth + 1.
- L334 `test_active_children_tracking(self)` (method) — Verify children are registered/unregistered for interrupt propagation.
- L348 `test_child_inherits_runtime_credentials(self)` (method)
- L372 `test_child_inherits_parent_print_fn(self)` (method)
- L394 `test_child_uses_thinking_callback_when_progress_callback_available(self)` (method)
- L418 `TestToolNamePreservation` (class) — Verify _last_resolved_tool_names is restored after subagent runs.
- L421 `test_global_tool_names_restored_after_delegation(self)` (method) — The process-global _last_resolved_tool_names must be restored
- L442 `test_global_tool_names_restored_after_child_failure(self)` (method) — Even when the child agent raises, the global must be restored.
- L460 `test_build_child_agent_does_not_raise_name_error(self)` (method) — Regression: _build_child_agent must not reference _saved_tool_names.
- L488 `test_saved_tool_names_set_on_child_before_run(self)` (method) — _run_single_child must set _delegate_saved_tool_names on the child
- L514 `TestDelegateObservability` (class) — Tests for enriched metadata returned by _run_single_child.
- L517 `test_observability_fields_present(self)` (method) — Completed child should return tool_trace, tokens, model, exit_reason.
- L558 `test_tool_trace_handles_list_content_blocks(self)` (method) — Tool-result content blocks should not crash observability metadata.
- L589 `test_output_tail_flattens_list_content_blocks(self)` (method) — _extract_output_tail (live overlay) must flatten content-block lists
- L623 `test_tool_trace_detects_error(self)` (method) — Tool results containing 'error' should be marked as error status.
- L650 `test_parallel_tool_calls_paired_correctly(self)` (method) — Parallel tool calls should each get their own result via tool_call_id matching.
- L699 `test_exit_reason_interrupted(self)` (method) — Interrupted child should report exit_reason='interrupted'.
- L720 `test_exit_reason_max_iterations(self)` (method) — Child that didn't complete and wasn't interrupted hit max_iterations.
- L742 `TestSubagentCostRollup` (class) — Port of Kilo-Org/kilocode#9448 — parent's session_estimated_cost_usd
- L746 `_make_parent_with_cost_counters(self, depth=0, starting_cost=0.0)` (method)
- L756 `test_single_child_cost_folded_into_parent(self)` (method)
- L782 `test_batch_children_costs_sum_into_parent(self)` (method)
- L834 `test_zero_cost_children_leave_parent_source_untouched(self)` (method) — If every child reports 0 cost (e.g. free local model), we should
- L854 `test_parent_with_real_source_not_overwritten(self)` (method) — If the parent already has its own cost billed (cost_source != 'none'),
- L878 `test_rollup_tolerates_missing_cost_fields(self)` (method) — Older fixtures / fabricated error entries may not carry
- L899 `TestBlockedTools` (class)
- L900 `test_blocked_tools_constant(self)` (method)
- L904 `test_constants(self)` (method)
- L916 `TestDelegationCredentialResolution` (class) — Tests for provider:model credential resolution in delegation config.
- L919 `test_no_provider_returns_none_credentials(self)` (method) — When delegation.provider is empty, all credentials are None (inherit parent).
- L930 `test_model_only_no_provider(self)` (method) — When only model is set (no provider), model is returned but credentials are None.
- L942 `test_direct_endpoint_uses_configured_base_url_and_api_key(self)` (method)
- L957 `test_direct_endpoint_auto_detects_anthropic_messages_suffix(self)` (method)
- L974 `test_direct_endpoint_honors_explicit_api_mode(self)` (method)
- L988 `test_direct_endpoint_explicit_api_mode_overrides_url_detection(self)` (method)
- L1001 `test_direct_endpoint_invalid_api_mode_falls_back_to_detection(self)` (method)
- L1014 `test_direct_endpoint_returns_none_api_key_when_not_configured(self)` (method)
- L1027 `test_direct_endpoint_no_raise_when_only_provider_env_key_present(self)` (method)
- L1048 `test_provider_resolution_failure_raises_valueerror(self, mock_resolve)` (method) — When provider resolution fails, ValueError is raised with helpful message.
- L1059 `test_provider_resolves_but_no_api_key_raises(self, mock_resolve)` (method) — When provider resolves but has no API key, ValueError is raised.
- L1073 `test_missing_config_keys_inherit_parent(self)` (method) — When config dict has no model/provider keys at all, inherits parent.
- L1082 `test_named_custom_provider_preserves_provider_name(self, mock_resolve)` (method) — Named custom provider (e.g. crof.ai) resolves to 'custom' at runtime level
- L1109 `test_standard_provider_not_overwritten_by_configured_name(self, mock_resolve)` (method) — Standard (non-custom) providers must still return runtime identity,
- L1128 `test_custom_provider_with_empty_configured_provider_falls_back_to_runtime(self, mock_resolve)` (method) — When configured_provider is empty/None, the early return kicks in and
- L1147 `test_runtime_missing_provider_key_returns_none(self, mock_resolve)` (method) — When resolve_runtime_provider returns a dict without 'provider' key,
- L1165 `TestDelegationProviderIntegration` (class) — Integration tests: delegation config → _run_single_child → AIAgent construction.
- L1170 `test_config_provider_credentials_reach_child_agent(self, mock_creds, mock_cfg)` (method) — When delegation.provider is configured, child agent gets resolved credentials.
- L1204 `test_cross_provider_delegation(self, mock_creds, mock_cfg)` (method) — Parent on Nous, subagent on OpenRouter — full credential switch.
- L1242 `test_provider_override_clears_parent_openrouter_filters(self, mock_creds, mock_cfg)` (method) — Delegated provider should not inherit parent provider-preference filters.
- L1284 `test_direct_endpoint_credentials_reach_child_agent(self, mock_creds, mock_cfg)` (method)
- L1318 `test_empty_config_inherits_parent(self, mock_creds, mock_cfg)` (method) — When delegation config is empty, child inherits parent credentials.
- L1346 `test_credential_error_returns_json_error(self, mock_creds, mock_cfg)` (method) — When credential resolution fails, delegate_task returns a JSON error.
- L1361 `test_batch_mode_all_children_get_credentials(self, mock_creds, mock_cfg)` (method) — In batch mode, all children receive the resolved credentials.
- L1401 `test_delegation_acp_runtime_reaches_child_agent(self, mock_creds, mock_cfg)` (method) — Resolved ACP runtime command/args must be forwarded to child agents.
- L1440 `test_model_only_no_provider_inherits_parent_credentials(self, mock_creds, mock_cfg)` (method) — Setting only model (no provider) changes model but keeps parent credentials.
- L1473 `TestChildCredentialPoolResolution` (class)
- L1474 `test_same_provider_shares_parent_pool(self)` (method)
- L1482 `test_no_provider_inherits_parent_pool(self)` (method)
- L1490 `test_different_provider_loads_own_pool(self)` (method)
- L1501 `test_different_provider_empty_pool_returns_none(self)` (method)
- L1512 `test_different_provider_load_failure_returns_none(self)` (method)
- L1521 `test_build_child_agent_assigns_parent_pool_when_shared(self)` (method)
- L1544 `test_build_child_agent_preserves_mcp_toolsets_by_default(self, mock_cfg)` (method)
- L1572 `test_build_child_agent_strict_intersection_when_opted_out(self, mock_cfg)` (method)
- L1597 `TestChildCredentialLeasing` (class)
- L1598 `test_run_single_child_acquires_and_releases_lease(self)` (method)
- L1628 `test_run_single_child_releases_lease_after_failure(self)` (method)
- L1648 `TestDelegateHeartbeat` (class) — Heartbeat propagates child activity to parent during delegation.
- L1655 `test_heartbeat_touches_parent_activity_during_child_run(self)` (method) — Parent's _touch_activity is called while child.run_conversation blocks.
- L1695 `test_heartbeat_stops_after_child_completes(self)` (method) — Heartbeat thread is cleaned up when the child finishes.
- L1728 `test_heartbeat_stops_after_child_error(self)` (method) — Heartbeat thread is cleaned up even when the child raises.
- L1766 `test_heartbeat_includes_child_activity_desc_when_no_tool(self)` (method) — When child has no current_tool, heartbeat uses last_activity_desc.
- L1801 `test_heartbeat_does_not_trip_idle_stale_while_inside_tool(self)` (method) — A long-running tool (no iteration advance, but current_tool set)
- L1861 `TestDelegationReasoningEffort` (class) — Tests for delegation.reasoning_effort config override.
- L1866 `test_inherits_parent_reasoning_when_no_override(self, MockAgent, mock_cfg)` (method) — With no delegation.reasoning_effort, child inherits parent's config.
- L1883 `test_override_reasoning_effort_from_config(self, MockAgent, mock_cfg)` (method) — delegation.reasoning_effort overrides the parent's level.
- L1900 `test_override_reasoning_effort_none_disables(self, MockAgent, mock_cfg)` (method) — delegation.reasoning_effort: 'none' disables thinking for subagents.
- L1917 `test_invalid_reasoning_effort_falls_back_to_parent(self, MockAgent, mock_cfg)` (method) — Invalid delegation.reasoning_effort falls back to parent's config.
- L1937 `TestDispatchDelegateTask` (class) — Tests for the _dispatch_delegate_task helper and full param forwarding.
- L1942 `test_acp_args_forwarded(self, mock_creds, mock_cfg)` (method) — Both acp_command and acp_args reach delegate_task via the helper.
- L1972 `TestDelegateEventEnum` (class) — Tests for DelegateEvent enum and back-compat aliases.
- L1975 `test_enum_values_are_strings(self)` (method)
- L1980 `test_legacy_map_covers_all_old_names(self)` (method)
- L1985 `test_legacy_map_values_are_delegate_events(self)` (method)
- L1989 `test_progress_callback_normalises_tool_started(self)` (method) — _build_child_progress_callback handles tool.started via enum.
- L2001 `test_progress_callback_normalises_thinking(self)` (method) — Both _thinking and reasoning.available route to TASK_THINKING.
- L2016 `test_progress_callback_tool_completed_is_noop(self)` (method) — tool.completed is normalised but produces no display output.
- L2026 `test_progress_callback_ignores_unknown_events(self)` (method) — Unknown event types are silently ignored.
- L2036 `test_progress_callback_accepts_enum_value_directly(self)` (method) — cb(DelegateEvent.TASK_THINKING, ...) must route to the thinking
- L2052 `test_progress_callback_accepts_new_style_string(self)` (method) — cb('delegate.task_thinking', ...) — the string form of the
- L2066 `test_progress_callback_task_progress_not_misrendered(self)` (method) — 'subagent_progress' (legacy name for TASK_PROGRESS) carries a
- L2095 `TestConcurrencyDefaults` (class) — Tests for the concurrency default and no hard ceiling.
- L2099 `test_default_is_three(self, mock_cfg)` (method)
- L2106 `test_no_upper_ceiling(self, mock_cfg)` (method) — Users can raise concurrency as high as they want — no hard cap.
- L2112 `test_very_high_values_honored(self, mock_cfg)` (method)
- L2117 `test_zero_clamped_to_one(self, mock_cfg)` (method) — Floor of 1 is enforced; zero or negative values raise to 1.
- L2122 `test_env_var_honored_uncapped(self, mock_cfg)` (method)
- L2128 `test_configured_value_returned(self, mock_cfg)` (method)
- L2136 `TestMaxSpawnDepth` (class) — Tests for _get_max_spawn_depth clamping and fallback behavior.
- L2140 `test_max_spawn_depth_defaults_to_1(self, mock_cfg)` (method)
- L2146 `test_max_spawn_depth_clamped_below_one(self, mock_cfg)` (method)
- L2156 `test_max_spawn_depth_no_upper_ceiling(self, mock_cfg)` (method) — No upper ceiling — high values pass through unchanged (cost is the limiter).
- L2163 `test_max_spawn_depth_invalid_falls_back_to_default(self, mock_cfg)` (method)
- L2178 `TestOrchestratorRoleSchema` (class) — Tests that the role param reaches the child via dispatch.
- L2184 `_run_with_mock_child(self, role_arg, mock_cfg, mock_creds)` (method)
- L2208 `test_default_role_is_leaf(self)` (method)
- L2212 `test_explicit_orchestrator_role_stashed(self)` (method) — role='orchestrator' reaches _build_child_agent and is stashed.
- L2219 `test_unknown_role_coerces_to_leaf(self)` (method) — role='nonsense' → _normalize_role warns and returns 'leaf'.
- L2227 `test_schema_has_role_top_level_and_per_task(self)` (method)
- L2236 `test_acp_command_description_has_do_not_set_guidance(self)` (method)
- L2252 `test_acp_command_description_has_no_claude_as_example(self)` (method)
- L2272 `_make_role_mock_child()` (function) — Helper: mock child with minimal fields for delegate_task to process.
- L2287 `TestOrchestratorRoleBehavior` (class) — Tests that role='orchestrator' actually changes toolset + prompt.
- L2293 `test_orchestrator_role_keeps_delegation_at_depth_1(self, mock_cfg, mock_creds)` (method) — role='orchestrator' + depth-0 parent with max_spawn_depth=2 →
- L2317 `test_orchestrator_blocked_at_max_spawn_depth(self, mock_cfg, mock_creds)` (method) — Parent at depth 1 with max_spawn_depth=2 spawns child
- L2338 `test_orchestrator_blocked_at_default_flat_depth(self, mock_cfg, mock_creds)` (method) — With default max_spawn_depth=1 (flat), role='orchestrator'
- L2360 `test_orchestrator_enabled_false_forces_leaf(self, mock_creds)` (method) — Kill switch delegation.orchestrator_enabled=false overrides
- L2382 `test_leaf_prompt_does_not_mention_delegation(self)` (method)
- L2390 `test_orchestrator_prompt_mentions_delegation_capability(self)` (method)
- L2401 `test_orchestrator_prompt_at_depth_floor_says_children_are_leaves(self)` (method) — With max_spawn_depth=2 and child_depth=1, the orchestrator's
- L2410 `test_orchestrator_prompt_below_floor_allows_more_nesting(self)` (method) — With max_spawn_depth=3 and child_depth=1, the orchestrator's
- L2424 `test_batch_mode_per_task_role_override(self, mock_cfg, mock_creds)` (method) — Per-task role beats top-level; no top-level role → "leaf".
- L2460 `test_intersection_preserves_delegation_bound(self, mock_cfg, mock_creds)` (method) — Design decision: orchestrator capability is granted by role,
- L2487 `TestOrchestratorEndToEnd` (class) — End-to-end: parent -> orchestrator -> two-leaf nested orchestration.
- L2506 `test_end_to_end_nested_orchestration(self, mock_cfg, mock_creds)` (method)
- L2586 `TestSubagentApprovalCallback` (class) — Subagent worker threads must have a non-interactive approval callback
- L2596 `test_auto_deny_returns_deny(self)` (method)
- L2603 `test_auto_approve_returns_once(self)` (method)
- L2611 `test_getter_defaults_to_deny(self, _mock_cfg)` (method)
- L2622 `test_getter_explicit_false_is_deny(self, _mock_cfg)` (method)
- L2633 `test_getter_true_is_approve(self, _mock_cfg)` (method)
- L2644 `test_getter_truthy_string_is_approve(self, _mock_cfg)` (method) — is_truthy_value accepts 'yes'/'1'/'true' as truthy.
- L2652 `test_executor_initializer_installs_callback_in_worker(self)` (method) — The initializer sets the callback on the worker thread's TLS,
- L2684 `TestFallbackModelInheritance` (class) — Subagents must inherit the parent's fallback provider chain.
- L2687 `test_child_inherits_fallback_chain(self)` (method) — _build_child_agent passes parent._fallback_chain as fallback_model.
- L2709 `test_child_gets_no_fallback_when_parent_chain_empty(self)` (method) — When parent._fallback_chain is empty, fallback_model is None.
