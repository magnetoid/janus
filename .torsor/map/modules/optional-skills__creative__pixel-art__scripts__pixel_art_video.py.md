---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:41'
updated: '2026-06-08T00:38:41'
---

# optional-skills/creative/pixel-art/scripts/pixel_art_video.py

Symbols in `optional-skills/creative/pixel-art/scripts/pixel_art_video.py`.

- L31 `_px(draw, x, y, color, size=2)` (function)
- L38 `_pixel_cross(draw, x, y, color, arm=2)` (function)
- L47 `init_stars(rng, W, H)` (function)
- L50 `draw_stars(draw, stars, t, W, H)` (function)
- L56 `init_fireflies(rng, W, H)` (function)
- L61 `draw_fireflies(draw, ff, t, W, H)` (function)
- L71 `init_leaves(rng, W, H)` (function)
- L78 `draw_leaves(draw, leaves, t, W, H)` (function)
- L86 `init_dust_motes(rng, W, H)` (function)
- L91 `draw_dust_motes(draw, motes, t, W, H)` (function)
- L100 `init_sparkles(rng, W, H)` (function)
- L106 `draw_sparkles(draw, sparkles, t, W, H)` (function)
- L112 `init_rain(rng, W, H)` (function)
- L116 `draw_rain(draw, rain, t, W, H)` (function)
- L123 `init_lightning(rng, W, H)` (function)
- L126 `draw_lightning(draw, state, t, W, H)` (function)
- L139 `init_bubbles(rng, W, H)` (function)
- L144 `draw_bubbles(draw, bubbles, t, W, H)` (function)
- L152 `init_embers(rng, W, H)` (function)
- L158 `draw_embers(draw, embers, t, W, H)` (function)
- L166 `init_snowflakes(rng, W, H)` (function)
- L172 `draw_snowflakes(draw, flakes, t, W, H)` (function)
- L182 `init_neon_pulse(rng, W, H)` (function)
- L187 `draw_neon_pulse(draw, points, t, W, H)` (function)
- L193 `init_heat_shimmer(rng, W, H)` (function)
- L197 `draw_heat_shimmer(draw, points, t, W, H)` (function)
- L239 `_ensure_ffmpeg()` (function)
- L247 `pixel_art_video(base_image, output_path, scene='night', duration=6, fps=15, seed=None, export_gif=False)` (function) — Overlay pixel animations onto a base image and encode to MP4.
- L323 `main()` (function)
