---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/gateway/test_platform_base.py

Symbols in `tests/gateway/test_platform_base.py`.

- L20 `TestSecretCaptureGuidance` (class)
- L21 `test_gateway_secret_capture_message_points_to_local_setup(self)` (method)
- L27 `TestSafeUrlForLog` (class)
- L28 `test_strips_query_fragment_and_userinfo(self)` (method)
- L39 `test_truncates_long_values(self)` (method)
- L45 `test_handles_small_and_non_positive_max_len(self)` (method)
- L57 `TestMessageEventIsCommand` (class)
- L58 `test_slash_command(self)` (method)
- L62 `test_regular_text(self)` (method)
- L66 `test_empty_text(self)` (method)
- L70 `test_slash_only(self)` (method)
- L75 `TestMessageEventGetCommand` (class)
- L76 `test_simple_command(self)` (method)
- L80 `test_command_with_args(self)` (method)
- L84 `test_not_a_command(self)` (method)
- L88 `test_command_is_lowercased(self)` (method)
- L92 `test_slash_only_returns_empty(self)` (method)
- L96 `test_command_with_at_botname(self)` (method)
- L100 `test_command_with_at_botname_and_args(self)` (method)
- L104 `test_command_mixed_case_with_at_botname(self)` (method)
- L109 `TestMessageEventGetCommandArgs` (class)
- L110 `test_command_with_args(self)` (method)
- L114 `test_command_without_args(self)` (method)
- L118 `test_not_a_command_returns_full_text(self)` (method)
- L128 `TestExtractImages` (class)
- L129 `test_no_images(self)` (method)
- L134 `test_markdown_image_with_image_ext(self)` (method)
- L142 `test_markdown_image_jpg(self)` (method)
- L149 `test_markdown_image_jpeg(self)` (method)
- L156 `test_markdown_image_gif(self)` (method)
- L163 `test_markdown_image_webp(self)` (method)
- L170 `test_fal_media_cdn(self)` (method)
- L177 `test_fal_cdn_url(self)` (method)
- L184 `test_replicate_delivery(self)` (method)
- L191 `test_non_image_ext_not_extracted(self)` (method) — Markdown image with non-image extension should not be extracted.
- L198 `test_html_img_tag(self)` (method)
- L206 `test_html_img_self_closing(self)` (method)
- L213 `test_html_img_with_closing_tag(self)` (method)
- L220 `test_multiple_images(self)` (method)
- L227 `test_mixed_markdown_and_html(self)` (method)
- L232 `test_cleaned_content_trims_excess_newlines(self)` (method)
- L237 `test_non_http_url_not_matched(self)` (method)
- L242 `test_non_image_link_preserved_when_mixed_with_images(self)` (method) — Regression: non-image markdown links must not be silently removed
- L261 `TestExtractMedia` (class)
- L262 `test_no_media(self)` (method)
- L267 `test_single_media_tag(self)` (method)
- L274 `test_media_with_voice_directive(self)` (method)
- L281 `test_multiple_media_tags(self)` (method)
- L286 `test_voice_directive_removed_from_content(self)` (method)
- L293 `test_media_with_text_before(self)` (method)
- L299 `test_cleaned_content_trims_excess_newlines(self)` (method)
- L304 `test_media_tag_allows_optional_whitespace_after_colon(self)` (method)
- L310 `test_media_tag_strips_wrapping_quotes_and_backticks(self)` (method)
- L320 `test_media_tag_supports_quoted_paths_with_spaces(self)` (method)
- L327 `test_media_tag_supports_unquoted_flac_paths_with_spaces(self)` (method)
- L333 `test_as_document_directive_stripped_from_cleaned_text(self)` (method) — [[as_document]] is a routing directive — strip it from
- L343 `test_as_document_directive_alone_does_not_attach_voice_flag(self)` (method) — [[as_document]] is independent of [[audio_as_voice]] — combining
- L351 `test_both_directives_can_coexist(self)` (method) — A response could (rarely) contain both [[audio_as_voice]] for an
- L366 `test_media_tag_windows_backslash_path(self)` (method) — extract_media should recognise Windows backslash paths.
- L374 `test_media_tag_windows_forward_slash_path(self)` (method) — extract_media should recognise Windows forward-slash paths.
- L382 `test_media_tag_windows_drive_root(self)` (method) — extract_media should recognise a path at the drive root.
- L390 `test_media_tag_unix_paths_still_work(self)` (method) — Unix absolute and tilde paths must still extract after Windows change.
- L396 `test_relative_path_still_ignored(self)` (method) — Relative Windows-style paths (no drive letter) must not match.
- L405 `test_media_in_fenced_code_block_ignored(self)` (method) — MEDIA: inside ``` fenced code blocks must not be extracted.
- L412 `test_media_in_inline_code_ignored(self)` (method) — MEDIA: inside backtick inline code must not be extracted.
- L419 `test_media_in_blockquote_ignored(self)` (method) — MEDIA: inside a > blockquote must not be extracted.
- L426 `test_media_outside_code_blocks_still_extracted(self)` (method) — Real MEDIA: tags outside protected regions must still work.
- L433 `test_media_mixed_code_and_prose(self)` (method) — Real MEDIA: in prose + example in code block: only prose extracted,
- L453 `test_inline_code_survives_when_real_media_present(self)` (method) — When a real MEDIA: tag is delivered, an inline-code example in the
- L462 `TestMediaInsideSerializedJson` (class) — Regression coverage for #34375 — MEDIA: embedded in serialized JSON
- L469 `test_media_in_json_value_not_extracted(self)` (method)
- L474 `test_media_in_pretty_json_value_not_extracted(self)` (method)
- L479 `test_media_in_json_array_not_extracted(self)` (method)
- L484 `test_media_in_nested_json_value_not_extracted(self)` (method)
- L489 `test_media_in_embedded_serialized_reply_not_extracted(self)` (method) — A serialized tool result that embeds a prior reply's MEDIA: tag.
- L500 `test_media_at_line_start_still_extracted(self)` (method)
- L504 `test_media_after_prose_same_line_still_extracted(self)` (method)
- L510 `test_media_indented_still_extracted(self)` (method)
- L514 `test_quoted_path_media_still_extracted(self)` (method) — MEDIA:"..." quoted-path form (a real LLM output) is not JSON-masked.
- L521 `test_tts_two_line_still_extracted(self)` (method)
- L530 `test_json_embedded_media_kept_verbatim_in_cleaned_text(self)` (method) — A real tag is delivered+stripped; a JSON-embedded MEDIA: stays as
- L539 `test_cleaned_text_after_directive_not_truncated(self)` (method) — Stripping a tag preceded by a [[as_document]] directive must not
- L549 `TestMediaExtensionAllowlistParity` (class) — Regression coverage for issue #34517 — the MEDIA: extension black hole.
- L564 `test_previously_dropped_extensions_now_extract(self)` (method)
- L570 `test_extract_media_and_local_files_share_one_extension_set(self)` (method)
- L577 `test_unknown_extension_not_black_holed_by_cleanup(self)` (method) — A MEDIA: tag with an unknown extension is NOT stripped from the
- L588 `test_known_extension_tag_is_stripped_from_body(self)` (method)
- L597 `TestMediaDeliveryPathValidation` (class)
- L598 `_patch_roots(self, monkeypatch, *roots)` (method)
- L613 `test_allows_existing_file_inside_safe_root(self, tmp_path, monkeypatch)` (method)
- L622 `test_rejects_existing_file_outside_safe_root(self, tmp_path, monkeypatch)` (method)
- L631 `test_rejects_symlink_escape_from_safe_root(self, tmp_path, monkeypatch)` (method)
- L645 `test_filter_keeps_safe_media_and_drops_unsafe(self, tmp_path, monkeypatch)` (method)
- L661 `test_allows_operator_configured_extra_root(self, tmp_path, monkeypatch)` (method)
- L671 `test_recency_trust_allows_freshly_produced_file(self, tmp_path, monkeypatch)` (method) — A PDF the agent just wrote to /tmp should be deliverable.
- L690 `test_recency_trust_rejects_old_file(self, tmp_path, monkeypatch)` (method) — A pre-existing host file (~/.bashrc, /etc/passwd shape) is rejected.
- L709 `test_recency_trust_disabled_falls_back_to_pure_allowlist(self, tmp_path, monkeypatch)` (method) — Setting trust_recent_files=false reverts to pre-existing strict behavior.
- L720 `test_recency_trust_denies_system_paths_even_when_fresh(self, tmp_path, monkeypatch)` (method) — A freshly-touched file under /etc must NOT be uploaded.
- L743 `test_recency_trust_allows_pdf_in_project_dir(self, tmp_path, monkeypatch)` (method) — The motivating case: agent produces a PDF in a project directory.
- L762 `test_filter_keeps_recently_produced_files(self, tmp_path, monkeypatch)` (method) — End-to-end: filter_local_delivery_paths routes a fresh PDF through.
- L776 `TestMediaDeliveryDefaultMode` (class) — Default (non-strict) mode — denylist gates delivery, nothing else.
- L785 `_patch_roots(self, monkeypatch, *roots)` (method)
- L798 `test_accepts_stale_file_outside_allowlist(self, tmp_path, monkeypatch)` (method) — The motivating case — agent says ``MEDIA:/home/user/notes.md``
- L813 `test_accepts_any_extension_not_on_denylist(self, tmp_path, monkeypatch)` (method) — No extension allowlist — .md, .txt, .json, .py all deliver.
- L822 `test_denylist_still_blocks_credentials(self, tmp_path, monkeypatch)` (method) — Default mode is permissive but not naive — credential paths
- L837 `test_denylist_blocks_system_prefixes(self, tmp_path, monkeypatch)` (method) — Files under /etc, /proc, /sys, /root, /boot, /var/{log,lib,run}
- L856 `test_denylist_blocks_hermes_credentials(self, tmp_path, monkeypatch)` (method) — ~/.hermes/.env and ~/.hermes/auth.json stay blocked even in
- L876 `test_denylist_blocks_hermes_config_in_active_profile(self, tmp_path, monkeypatch)` (method) — The active profile config stays blocked in default mode.
- L893 `test_denylist_blocks_shared_hermes_root_config_for_profiles(self, tmp_path, monkeypatch)` (method) — Profile-mode gateways must still block the shared Hermes root config.
- L915 `test_strict_mode_envvar_restores_legacy_behavior(self, tmp_path, monkeypatch)` (method) — Setting HERMES_MEDIA_DELIVERY_STRICT=1 reactivates the older
- L931 `test_strict_mode_truthy_aliases(self, monkeypatch, tmp_path)` (method) — ``HERMES_MEDIA_DELIVERY_STRICT=true|yes|on|1`` all enable strict mode.
- L944 `test_filter_passes_default_files_through(self, tmp_path, monkeypatch)` (method) — End-to-end: filter_local_delivery_paths accepts a stale .md in
- L957 `test_root_home_deliverable_is_accepted(self, tmp_path, monkeypatch)` (method) — The motivating bug (#38106): a root-run gateway has ``$HOME=/root``,
- L982 `test_root_home_credential_subdir_still_blocked(self, tmp_path, monkeypatch)` (method) — The $HOME exception must NOT un-block credential sub-dirs inside
- L1003 `test_root_home_hermes_env_still_blocked(self, tmp_path, monkeypatch)` (method) — ``~/.hermes/.env`` stays blocked under the $HOME exception — it is a
- L1023 `test_other_users_home_still_blocked_for_nonroot(self, tmp_path, monkeypatch)` (method) — The exception only un-blocks the *running user's own* home. A
- L1048 `test_root_home_workdir_symlink_to_credential_blocked(self, tmp_path, monkeypatch)` (method) — A symlink in the workdir pointing at a credential is rejected on its
- L1076 `TestShouldSendMediaAsAudio` (class) — Audio-routing policy shared by gateway + scheduler + send_message.
- L1079 `test_unknown_extension_returns_false(self)` (method)
- L1084 `test_non_telegram_platforms_route_all_audio(self)` (method)
- L1090 `test_telegram_mp3_and_m4a_route_to_audio(self)` (method)
- L1095 `test_telegram_wav_and_flac_fall_through_to_document(self)` (method)
- L1100 `test_telegram_ogg_opus_only_when_voice_flagged(self)` (method)
- L1107 `test_accepts_platform_enum(self)` (method)
- L1120 `TestTruncateMessage` (class)
- L1121 `_adapter(self)` (method) — Create a minimal adapter instance for testing static/instance methods.
- L1142 `test_short_message_single_chunk(self)` (method)
- L1147 `test_exact_length_single_chunk(self)` (method)
- L1153 `test_long_message_splits(self)` (method)
- L1164 `test_chunks_have_indicators(self)` (method)
- L1171 `test_code_block_first_chunk_closed(self)` (method)
- L1180 `test_code_block_language_tag_carried(self)` (method)
- L1191 `test_continuation_chunks_have_balanced_fences(self)` (method) — Regression: continuation chunks must close reopened code blocks.
- L1203 `test_each_chunk_under_max_length(self)` (method)
- L1219 `TestGetHumanDelay` (class)
- L1220 `test_off_mode(self)` (method)
- L1224 `test_default_is_off(self)` (method)
- L1229 `test_natural_mode_range(self)` (method)
- L1234 `test_natural_mode_ignores_malformed_custom_env_vars(self)` (method)
- L1244 `test_custom_mode_uses_env_vars(self)` (method)
- L1254 `test_custom_mode_tolerates_malformed_env_vars(self)` (method)
- L1274 `TestUtf16Len` (class) — Verify the UTF-16 length helper.
- L1277 `test_ascii(self)` (method)
- L1280 `test_bmp_cjk(self)` (method)
- L1284 `test_emoji_surrogate_pair(self)` (method)
- L1288 `test_mixed(self)` (method)
- L1292 `test_musical_symbol(self)` (method)
- L1296 `test_empty(self)` (method)
- L1300 `TestPrefixWithinUtf16Limit` (class) — Verify UTF-16-aware prefix truncation.
- L1303 `test_fits_entirely(self)` (method)
- L1306 `test_ascii_truncation(self)` (method)
- L1311 `test_does_not_split_surrogate_pair(self)` (method)
- L1317 `test_emoji_at_limit(self)` (method)
- L1322 `test_all_emoji(self)` (method)
- L1328 `test_empty(self)` (method)
- L1332 `TestTruncateMessageUtf16` (class) — Verify truncate_message respects UTF-16 lengths when len_fn=utf16_len.
- L1335 `test_short_emoji_message_no_split(self)` (method) — A short message under the UTF-16 limit should not be split.
- L1342 `test_emoji_near_limit_triggers_split(self)` (method) — A message at 4096 codepoints but >4096 UTF-16 units must split.
- L1363 `test_each_utf16_chunk_within_limit(self)` (method) — All chunks produced with utf16_len must fit the limit.
- L1375 `test_all_content_preserved(self)` (method) — Splitting with utf16_len must not lose content.
- L1384 `test_code_blocks_preserved_with_utf16(self)` (method) — Code block fence handling should work with utf16_len too.
- L1397 `TestProxyKwargsForAiohttp` (class) — Verify proxy_kwargs_for_aiohttp routes all schemes through ProxyConnector.
- L1400 `test_none_returns_empty(self)` (method)
- L1407 `test_http_proxy_uses_connector_when_aiohttp_socks_available(self)` (method)
- L1421 `test_socks_proxy_uses_connector(self)` (method)
- L1432 `test_http_proxy_falls_back_without_aiohttp_socks(self)` (method)
- L1441 `TestMediaDeliveryDiagnosability` (class) — Diagnosable rejection logging + crafted-path robustness (#33251).
- L1444 `test_rejected_path_appears_in_log(self, tmp_path, caplog)` (method)
- L1456 `test_crafted_null_path_does_not_abort_batch(self, tmp_path, monkeypatch)` (method) — One crafted ~\x00 path must not drop every other attachment.
- L1467 `test_extract_media_tolerates_crafted_null_path(self)` (method) — extract_media must not raise on a crafted ~\x00 MEDIA tag.
- L1474 `test_log_safe_path_neutralises_line_breaks(self)` (method)
- L1481 `test_canonical_cache_roots_present(self)` (method)
