---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:43'
updated: '2026-06-08T00:38:43'
---

# tests/hermes_cli/test_tools_config.py

Symbols in `tests/hermes_cli/test_tools_config.py`.

- L30 `test_agent_disabled_toolsets_suppresses_across_platforms()` (function) — agent.disabled_toolsets in config.yaml should remove those toolsets
- L45 `test_agent_disabled_toolsets_with_explicit_platform_config()` (function) — agent.disabled_toolsets should still suppress even when the platform
- L61 `test_agent_disabled_toolsets_empty_list_is_noop()` (function) — Empty or missing disabled_toolsets should not change behavior.
- L74 `test_get_platform_tools_uses_default_when_platform_not_configured()` (function)
- L83 `test_gui_toolset_label_strips_leading_emoji()` (function)
- L90 `test_configurable_toolsets_include_messaging()` (function)
- L94 `test_configurable_toolsets_include_context_engine()` (function)
- L98 `test_get_platform_tools_active_context_engine_is_enabled_for_explicit_config()` (function)
- L111 `test_get_platform_tools_context_engine_not_added_for_default_compressor()` (function)
- L122 `test_get_platform_tools_context_engine_respects_explicit_empty_selection()` (function)
- L133 `test_get_platform_tools_default_telegram_includes_messaging()` (function)
- L139 `test_get_platform_tools_default_whatsapp_includes_web()` (function)
- L145 `test_get_platform_tools_homeassistant_platform_keeps_homeassistant_toolset()` (function)
- L151 `test_get_platform_tools_homeassistant_toolset_enabled_for_cron_when_hass_token_set(monkeypatch)` (function) — HA toolset is runtime-gated by check_fn (requires HASS_TOKEN).
- L172 `test_get_platform_tools_homeassistant_toolset_off_for_cron_when_hass_token_missing(monkeypatch)` (function) — Without HASS_TOKEN, HA stays off by default — preserves #14798's behavior
- L181 `test_get_platform_tools_x_search_auto_enabled_when_xai_oauth_present(monkeypatch)` (function) — x_search toolset auto-enables across platforms when xAI Grok OAuth
- L200 `test_get_platform_tools_x_search_auto_enabled_when_xai_api_key_present(monkeypatch)` (function) — x_search toolset auto-enables when XAI_API_KEY is set, even without
- L209 `test_get_platform_tools_x_search_off_when_no_xai_credentials(monkeypatch)` (function) — Without any xAI credentials, x_search stays off — preserves the
- L221 `test_get_platform_tools_x_search_respects_explicit_config(monkeypatch)` (function) — Once the user has saved an explicit toolset list via `hermes tools`,
- L237 `test_get_platform_tools_expands_composite_when_mixed_with_configurable()` (function) — ``[hermes-cli, spotify]`` (composite + configurable) must keep the full
- L254 `test_get_platform_tools_composite_only_unchanged()` (function) — Composite-only config (no configurable in list) must still take the
- L268 `test_get_platform_tools_configurable_only_no_expansion()` (function) — Configurable-only list (no composite) must not pull in unrelated
- L282 `test_get_platform_tools_mixed_does_not_resurrect_default_off()` (function) — Expansion must subtract _DEFAULT_OFF_TOOLSETS from the implicit
- L295 `test_get_platform_tools_preserves_explicit_empty_selection()` (function)
- L312 `test_apply_toolset_change_from_default_does_not_enable_default_off_toolsets()` (function) — Disabling one default toolset on a fresh config must not persist
- L327 `test_apply_toolset_change_can_enable_default_off_toolset_from_default()` (function)
- L338 `test_get_platform_tools_handles_null_platform_toolsets()` (function) — YAML `platform_toolsets:` with no value parses as None — the old
- L351 `test_platform_toolset_summary_uses_explicit_platform_list()` (function)
- L360 `test_get_platform_tools_includes_enabled_mcp_servers_by_default()` (function)
- L376 `test_get_platform_tools_keeps_enabled_mcp_servers_with_explicit_builtin_selection()` (function)
- L393 `test_get_platform_tools_no_mcp_sentinel_excludes_all_mcp_servers()` (function) — The 'no_mcp' sentinel in platform_toolsets excludes all MCP servers.
- L412 `test_get_platform_tools_no_mcp_sentinel_does_not_affect_other_platforms()` (function) — The 'no_mcp' sentinel only affects the platform it's configured on.
- L432 `test_toolset_has_keys_for_vision_accepts_codex_auth(tmp_path, monkeypatch)` (function)
- L449 `test_save_platform_tools_preserves_mcp_server_names()` (function) — Ensure MCP server names are preserved when saving platform tools.
- L475 `test_save_platform_tools_handles_empty_existing_config()` (function) — Saving platform tools works when no existing config exists.
- L487 `test_save_platform_tools_handles_invalid_existing_config()` (function) — Saving platform tools works when existing config is not a list.
- L502 `test_save_platform_tools_does_not_preserve_platform_default_toolsets()` (function) — Platform default toolsets (hermes-cli, hermes-telegram, etc.) must NOT
- L553 `test_save_platform_tools_does_not_preserve_hermes_telegram()` (function) — Same bug for Telegram — hermes-telegram must not be preserved.
- L573 `test_save_platform_tools_still_preserves_mcp_with_platform_default_present()` (function) — MCP server names must still be preserved even when platform defaults
- L606 `test_visible_providers_include_nous_subscription_when_logged_in(monkeypatch)` (function)
- L629 `test_visible_providers_show_nous_subscription_when_logged_out(monkeypatch)` (function) — Nous-managed Tool Gateway rows are always listed, even logged out.
- L652 `test_visible_providers_show_nous_subscription_when_paid_access_is_false(monkeypatch)` (function) — Logged-in-but-unpaid users still see the managed rows.
- L675 `test_visible_providers_force_fresh_shows_nous_subscription_after_upgrade(monkeypatch)` (function)
- L708 `test_local_browser_provider_is_saved_explicitly(monkeypatch)` (function)
- L721 `test_fresh_install_browser_default_is_free_local_not_paid_nous()` (function) — On a fresh install the browser picker must default to the free local
- L738 `test_fresh_install_tts_default_is_free_edge_not_paid_nous()` (function) — TTS picker defaults to the free Edge backend on a fresh install.
- L748 `test_reconfigure_lists_enabled_web_without_existing_provider_config(monkeypatch)` (function)
- L775 `test_first_install_nous_auto_configures_managed_defaults(monkeypatch)` (function)
- L834 `test_first_install_nous_auto_configures_video_gen(monkeypatch)` (function) — When a Nous subscriber checks video_gen in the toolset checklist,
- L895 `TestPlatformToolsetConsistency` (class) — Every platform in tools_config.PLATFORMS must have a matching toolset.
- L898 `test_all_platforms_have_toolset_definitions(self)` (method) — Each platform's default_toolset must exist in TOOLSETS.
- L910 `test_gateway_toolset_includes_all_messaging_platforms(self)` (method) — hermes-gateway includes list should cover all messaging platforms.
- L927 `test_skills_config_covers_tools_config_platforms(self)` (method) — skills_config.PLATFORMS should have entries for all gateway platforms.
- L942 `test_numeric_mcp_server_name_does_not_crash_sorted()` (function) — YAML parses bare numeric keys (e.g. ``12306:``) as int.
- L972 `test_toolset_has_keys_treats_no_key_providers_as_configured()` (function)
- L978 `test_computer_use_needs_configuration_when_cua_driver_post_setup_pending()` (function) — No-key providers can still need setup when their post_setup is unsatisfied.
- L988 `test_computer_use_skips_configuration_when_cua_driver_already_installed()` (function) — Installed post_setup dependencies should keep returning-user toggles no-op.
- L997 `test_computer_use_respects_custom_cua_driver_command()` (function) — The setup gate should match runtime's HERMES_CUA_DRIVER_CMD override.
- L1007 `test_computer_use_blank_custom_driver_command_falls_back_to_default()` (function) — Blank overrides should not make the setup gate look for an empty command.
- L1017 `test_computer_use_post_setup_respects_custom_driver_command_when_installed()` (function) — post_setup already-installed checks should version-probe the override.
- L1034 `test_computer_use_post_setup_missing_override_does_not_accept_default_binary()` (function) — A default cua-driver binary must not satisfy a missing runtime override.
- L1057 `TestImagegenBackendRegistry` (class) — IMAGEGEN_BACKENDS tags drive the model picker flow in tools_config.
- L1060 `test_fal_backend_registered(self)` (method)
- L1064 `test_fal_catalog_loads_lazily(self)` (method) — catalog_fn should defer import to avoid import cycles.
- L1072 `test_image_gen_providers_tagged_with_fal_backend(self)` (method) — Both Nous Subscription and FAL.ai providers must carry the
- L1083 `TestImagegenModelPicker` (class) — _configure_imagegen_model writes selection to config and respects
- L1087 `test_picker_writes_chosen_model_to_config(self)` (method)
- L1097 `test_picker_with_gpt_image_does_not_prompt_quality(self)` (method) — GPT-Image quality is pinned to medium in the tool's defaults —
- L1125 `test_picker_no_op_for_unknown_backend(self)` (method)
- L1131 `test_picker_repairs_corrupt_config_section(self)` (method) — When image_gen is a non-dict (user-edit YAML), the picker should
- L1142 `test_save_platform_tools_normalizes_numeric_entries()` (function) — YAML may parse bare numeric toolset names as int. They should be
- L1160 `test_save_platform_tools_clears_no_mcp_sentinel()` (function) — `hermes tools` has no UI for no_mcp, so saving from the picker clears
- L1178 `test_save_platform_tools_preserves_mcp_server_names()` (function) — Non-sentinel passthrough entries (MCP server names) must still survive
- L1196 `test_get_platform_tools_recovers_non_configurable_toolsets_from_composite()` (function) — Non-configurable toolsets whose tools are in the composite but not in
- L1229 `test_get_platform_tools_second_pass_skips_fully_claimed_toolsets()` (function) — Toolsets whose tools are fully covered by configurable keys should NOT
- L1238 `test_get_platform_tools_discord_both_off_by_default()` (function) — Both `discord` and `discord_admin` are opt-in via `hermes tools`,
- L1247 `test_discord_toolsets_in_configurable_toolsets()` (function)
- L1253 `test_discord_toolsets_in_default_off()` (function)
- L1258 `test_discord_toolsets_not_available_on_other_platforms()` (function) — Platform-scoping: discord / discord_admin should not appear on CLI,
- L1273 `test_discord_toolsets_user_enabled_are_honored()` (function) — When the user opts in via `hermes tools`, the toolset appears.
- L1281 `test_save_platform_tools_strips_restricted_toolsets()` (function) — Hand-edited or all-platforms checklist with `discord` selected for
- L1294 `test_get_platform_tools_feishu_includes_doc_and_drive()` (function)
- L1300 `test_get_platform_tools_feishu_tools_not_on_other_platforms()` (function)
- L1307 `test_get_effective_configurable_toolsets_dedupes_bundled_plugins()` (function) — Bundled plugins (plugins/spotify) share their toolset key with the
- L1338 `test_reconfigure_provider_syncs_use_gateway(monkeypatch, provider, config_key, expected)` (function)
- L1350 `test_reconfigure_browser_provider_overwrites_stale_use_gateway()` (function)
- L1361 `test_reconfigure_provider_runs_post_setup_for_env_var_providers(monkeypatch, provider_name, post_setup_key)` (function) — _reconfigure_provider() must call _run_post_setup() for providers that have
- L1387 `test_configure_managed_provider_blocks_when_not_entitled(monkeypatch)` (function) — Selecting a Nous-managed backend without paid access writes no config.
- L1407 `test_configure_managed_provider_enables_when_entitled(monkeypatch)` (function) — Once entitled, selecting the managed backend sets use_gateway=True.
- L1427 `test_configure_non_managed_provider_skips_portal_gate(monkeypatch)` (function) — A self-hosted provider must never trigger the Nous Portal login gate.
- L1448 `test_apply_provider_selection_web_sets_backend()` (function) — Selecting a web provider persists the backend without prompting for keys.
- L1459 `test_apply_provider_selection_tts_sets_provider()` (function) — Selecting a TTS provider persists tts.provider.
- L1470 `test_apply_provider_selection_unknown_provider_raises_keyerror()` (function)
- L1477 `test_apply_provider_selection_unknown_toolset_raises_keyerror()` (function)
- L1484 `test_apply_provider_selection_does_not_prompt_or_post_setup(monkeypatch)` (function) — The non-interactive selection must not invoke prompts or post-setup hooks.
- L1505 `test_checklist_toolset_keys_excludes_kanban()` (function) — ``kanban`` is check_fn-gated and never appears in the checklist, so it
- L1515 `test_kanban_not_reported_as_removed_in_diff()` (function) — Reproduces the false-signal bug: `hermes tools` printed ``- kanban``
- L1539 `test_real_configurable_changes_still_reported_in_diff()` (function) — Scoping the diff to the checklist universe must NOT swallow genuine
