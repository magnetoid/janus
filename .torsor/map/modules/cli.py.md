---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:40'
updated: '2026-06-08T00:38:40'
---

# cli.py

Symbols in `cli.py`.

- L91 `CanonicalUsage(*args, **kwargs)` (function)
- L97 `estimate_usage_cost(*args, **kwargs)` (function)
- L103 `format_duration_compact(*args, **kwargs)` (function)
- L118 `format_token_count_compact(*args, **kwargs)` (function)
- L142 `is_table_divider(*args, **kwargs)` (function)
- L148 `looks_like_table_row(*args, **kwargs)` (function)
- L154 `realign_markdown_tables(*args, **kwargs)` (function)
- L192 `_strip_reasoning_tags(text: str)` (function) — Remove reasoning/thinking blocks from displayed text.
- L264 `_assistant_content_as_text(content: Any)` (function)
- L279 `_assistant_copy_text(content: Any)` (function)
- L287 `_load_prefill_messages(file_path: str)` (function) — Load ephemeral prefill messages from a JSON file.
- L316 `_resolve_prefill_messages_file(config: Dict[str, Any])` (function) — Resolve the prefill file path from env/config.
- L335 `_parse_reasoning_config(effort: str)` (function) — Parse a reasoning effort level into an OpenRouter reasoning config dict.
- L344 `_parse_service_tier_config(raw: str)` (function) — Parse a persisted service-tier preference into a Responses API value.
- L354 `load_cli_config()` (function) — Load CLI configuration from config files.
- L802 `AIAgent(*args, **kwargs)` (function)
- L808 `get_tool_definitions(*args, **kwargs)` (function)
- L816 `get_toolset_for_tool(*args, **kwargs)` (function)
- L826 `get_all_toolsets(*args, **kwargs)` (function)
- L832 `get_toolset_info(*args, **kwargs)` (function)
- L838 `validate_toolset(*args, **kwargs)` (function)
- L844 `_sync_process_session_id(session_id: str)` (function) — Keep process-local session-id consumers aligned after CLI switches.
- L851 `get_job(*args, **kwargs)` (function)
- L860 `_cleanup_all_terminals(*args, **kwargs)` (function)
- L866 `set_sudo_password_callback(*args, **kwargs)` (function)
- L872 `set_approval_callback(*args, **kwargs)` (function)
- L878 `set_secret_capture_callback(*args, **kwargs)` (function)
- L884 `_cleanup_all_browsers(*args, **kwargs)` (function)
- L901 `_mark_tui_input_modes_active()` (function) — Record that the TUI app started, so _run_cleanup resets input modes.
- L907 `_prepare_deferred_agent_startup()` (function) — Run Termux-deferred agent discovery before the first real agent turn.
- L953 `_run_cleanup()` (function) — Run resource cleanup exactly once.
- L1016 `_emit_interrupted_session_end(cli, *, reason: str='keyboard_interrupt')` (function) — Best-effort on_session_end hook for interrupted non-interactive runs.
- L1052 `_reset_terminal_input_modes_on_exit()` (function) — Best-effort: disable focus reporting + mouse tracking on TUI exit so they
- L1104 `_normalize_git_bash_path(p: Optional[str])` (function) — Translate a Git Bash-style path (``/c/Users/...``) to the native
- L1133 `_git_repo_root()` (function) — Return the git repo root for CWD, or None if not in a repo.
- L1154 `_path_is_within_root(path: Path, root: Path)` (function) — Return True when a resolved path stays within the expected root.
- L1163 `_setup_worktree(repo_root: str=None)` (function) — Create an isolated git worktree for this CLI session.
- L1291 `_worktree_has_unpushed_commits(worktree_path: str, timeout: int=10)` (function) — Return whether a worktree has commits not reachable from any remote branch.
- L1322 `_cleanup_worktree(info: Dict[str, str]=None)` (function) — Remove a worktree and its branch on exit.
- L1375 `_run_state_db_auto_maintenance(session_db)` (function) — Call ``SessionDB.maybe_auto_prune_and_vacuum`` using current config.
- L1429 `_run_checkpoint_auto_maintenance()` (function) — Call ``checkpoint_manager.maybe_auto_prune_checkpoints`` using current config.
- L1453 `_prune_stale_worktrees(repo_root: str, max_age_hours: int=24)` (function) — Remove stale worktrees and orphaned branches on startup.
- L1519 `_prune_orphaned_branches(repo_root: str)` (function) — Delete local ``hermes/hermes-*`` and ``pr-*`` branches with no worktree.
- L1605 `_hex_to_ansi(hex_color: str, *, bold: bool=False)` (function) — Convert a hex color like '#268bd2' to a true-color ANSI escape.
- L1646 `_luminance_from_hex(hex_str: str)` (function)
- L1660 `_query_osc11_background()` (function) — Ask the terminal for its background color via OSC 11.
- L1733 `_detect_light_mode()` (function)
- L1824 `_maybe_remap_for_light_mode(hex_color: str)` (function) — If we're in light mode, remap a dark-mode-tuned color to a
- L1842 `_install_skin_light_mode_hook()` (function) — Wrap SkinConfig.get_color at import time so EVERY skin color read goes
- L1878 `_SkinAwareAnsi` (class) — Lazy ANSI escape that resolves from the skin engine on first use.
- L1885 `__init__(self, skin_key: str, fallback_hex: str='#FFD700', *, bold: bool=False)` (method)
- L1891 `__str__(self)` (method)
- L1903 `__add__(self, other: str)` (method)
- L1906 `__radd__(self, other: str)` (method)
- L1909 `reset(self)` (method) — Clear cache so the next access re-reads the skin.
- L1923 `_accent_hex()` (function) — Return the active skin accent color for legacy CLI output lines.
- L1932 `_rich_text_from_ansi(text: str)` (function) — Safely render assistant/tool output that may contain ANSI escapes.
- L1941 `_strip_markdown_syntax(text: str)` (function) — Best-effort markdown marker removal for plain-text display.
- L1976 `_preserve_windows_dot_segments_for_markdown(text: str)` (function) — Keep Windows path separators before hidden directories in Markdown.
- L1993 `_terminal_width_for_streaming()` (function) — Display cells available inside the streamed response box.
- L2011 `_render_final_assistant_content(text: str, mode: str='render')` (function) — Render final assistant content as markdown, stripped text, or raw text.
- L2056 `_coerce_output_history_limit(value)` (function)
- L2063 `_configure_output_history(enabled: bool, max_lines=200)` (function) — Configure recent CLI output replayed after terminal redraws.
- L2071 `_clear_output_history()` (function)
- L2076 `_suspend_output_history()` (function)
- L2086 `_record_output_history_entry(entry)` (function)
- L2092 `_record_output_history(text: str)` (function)
- L2102 `_replay_output_history()` (function) — Repaint recent output above the prompt after a full screen clear.
- L2135 `_cprint(text: str)` (function) — Print ANSI-colored text through prompt_toolkit's native renderer.
- L2240 `_prepend_note_to_message(message, note: str)` (function) — Prepend a one-shot system-style note to a user message.
- L2288 `_termux_example_image_path(filename: str='cat.png')` (function) — Return a realistic example media path for the current Termux setup.
- L2302 `_split_path_input(raw: str)` (function) — Split a leading file path token from trailing free-form text.
- L2345 `_resolve_attachment_path(raw_path: str)` (function) — Resolve a user-supplied local attachment path.
- L2409 `_detect_file_drop(user_input: str)` (function) — Detect if *user_input* starts with a real local file path.
- L2481 `_format_image_attachment_badges(attached_images: list[Path], image_counter: int, width: int | None=None)` (function) — Format the attached-image badge row for the interactive CLI.
- L2514 `_should_auto_attach_clipboard_image_on_paste(pasted_text: str)` (function) — Auto-attach clipboard images only for image-only paste gestures.
- L2519 `_strip_leaked_bracketed_paste_wrappers(text: str)` (function) — Strip leaked bracketed-paste wrapper markers from user-visible text.
- L2545 `_apply_bracketed_paste_timeout_patch()` (function) — Patch prompt_toolkit to recover from torn bracketed-paste sequences.
- L2658 `_preserve_ctrl_enter_newline()` (function) — Detect environments where Ctrl+Enter must produce a newline, not submit.
- L2696 `_bind_prompt_submit_keys(kb, handler)` (function) — Bind terminal Enter forms to the submit handler.
- L2715 `_disable_prompt_toolkit_cpr_warning(app)` (function) — Let prompt_toolkit fall back from CPR without printing into the prompt.
- L2723 `_strip_leaked_terminal_responses_with_meta(text: str)` (function) — Strip leaked terminal control-response sequences from user input.
- L2766 `_strip_leaked_terminal_responses(text: str)` (function) — Compatibility wrapper returning only cleaned text.
- L2772 `_collect_query_images(query: str | None, image_arg: str | None=None)` (function) — Collect local image attachments for single-query CLI flows.
- L2802 `ChatConsole` (class) — Rich Console adapter for prompt_toolkit's patch_stdout context.
- L2811 `__init__(self)` (method)
- L2821 `print(self, *args, **kwargs)` (method)
- L2832 `status(self, *_args, **_kwargs)` (method) — Provide a no-op Rich-compatible status context.
- L2871 `_build_compact_banner()` (function) — Build a compact banner that fits the current terminal width.
- L2925 `_looks_like_slash_command(text: str)` (function) — Return True if *text* looks like a slash command, not a file path.
- L2951 `_ensure_skill_commands()` (function)
- L2960 `get_skill_commands()` (function)
- L2964 `build_skill_invocation_message(*args, **kwargs)` (function)
- L2970 `build_preloaded_skills_prompt(*args, **kwargs)` (function)
- L2976 `get_skill_bundles()` (function)
- L2985 `build_bundle_invocation_message(*args, **kwargs)` (function)
- L2991 `_get_plugin_cmd_handler_names()` (function) — Return plugin command names (without slash prefix) for dispatch matching.
- L3000 `_parse_skills_argument(skills: str | list[str] | tuple[str, ...] | None)` (function) — Normalize a CLI skills flag into a deduplicated list of skill identifiers.
- L3024 `save_config_value(key_path: str, value: any)` (function) — Save a value to the active config file at the specified key path.
- L3071 `HermesCLI` (class) — Interactive CLI for the Hermes Agent.
- L3079 `__init__(self, model: str=None, toolsets: List[str]=None, provider: str=None, api_key: str=None, base_url: str=None, max_turns: int=None, verbose: Optional[bool]=None, compact: bool=False, resume: str=None, checkpoints: bool=False, pass_session_id: bool=False, ignore_rules: bool=False)` (method) — Initialize the Hermes CLI.
- L3481 `_invalidate(self, min_interval: float=0.25)` (method) — Throttled UI repaint — prevents terminal blinking on slow/SSH connections.
- L3490 `_force_full_redraw(self)` (method) — Force a clean full-screen repaint of the prompt_toolkit UI.
- L3515 `_clear_prompt_toolkit_screen(self, app, *, rebuild_scrollback: bool=False)` (method) — Clear the terminal and reset prompt_toolkit renderer state.
- L3536 `_recover_after_resize(self, app, original_on_resize)` (method) — Recover a resized classic CLI without desynchronizing cursor state.
- L3569 `_schedule_resize_recovery(self, app, original_on_resize, delay: float=0.12)` (method) — Debounce resize redraws so footer chrome is not stamped into scrollback.
- L3614 `_status_bar_context_style(self, percent_used: Optional[int])` (method)
- L3626 `_compression_count_style(count: int)` (method) — Return a style class reflecting context compression pressure.
- L3634 `_build_context_bar(self, percent_used: Optional[int], width: int=10)` (method)
- L3640 `_format_prompt_elapsed(prompt_start_time: Optional[float], prompt_duration: float, live: bool=False)` (method) — Format per-prompt elapsed time for the status bar.
- L3677 `_get_status_bar_snapshot(self)` (method)
- L3769 `_status_bar_display_width(text: str)` (method) — Return terminal cell width for status-bar text.
- L3784 `_trim_status_bar_text(cls, text: str, max_width: int)` (method) — Trim status-bar text to a single terminal row.
- L3812 `_get_tui_terminal_width(default: tuple[int, int]=(80, 24))` (method) — Return the live prompt_toolkit width, falling back to ``shutil``.
- L3825 `_use_minimal_tui_chrome(self, width: Optional[int]=None)` (method) — Hide low-value chrome on narrow/mobile terminals to preserve rows.
- L3832 `_scrollback_box_width(width: Optional[int]=None)` (method) — Return the full viewport width for printed scrollback box rules.
- L3855 `_tui_input_rule_height(self, position: str, width: Optional[int]=None)` (method) — Return the visible height for the top/bottom input separator rules.
- L3865 `_agent_spacer_height(self, width: Optional[int]=None)` (method) — Return the spacer height shown above the status bar while the agent runs.
- L3871 `_spinner_widget_height(self, width: Optional[int]=None)` (method) — Return the visible height for the spinner/status text line above the status bar.
- L3885 `_render_spinner_text(self)` (method) — Return the live spinner/status text exactly as rendered in the TUI.
- L3904 `_voice_record_key_label(self)` (method) — Return the configured voice push-to-talk key formatted for UI.
- L3927 `set_voice_record_key_cache(self, raw_key: object)` (method) — Populate the voice label cache from a raw ``voice.record_key``.
- L3939 `_get_voice_status_fragments(self, width: Optional[int]=None)` (method) — Return the voice status bar fragments for the interactive TUI.
- L3958 `_build_status_bar_text(self, width: Optional[int]=None)` (method) — Return a compact one-line session status string for the TUI footer.
- L4017 `_get_status_bar_fragments(self)` (method)
- L4126 `_normalize_model_for_provider(self, resolved_provider: str)` (method) — Normalize provider-specific model IDs and routing.
- L4229 `_on_thinking(self, text: str)` (method) — Called by agent when thinking starts/stops. Updates TUI spinner.
- L4237 `_on_notice(self, notice)` (method) — Queue an out-of-band AgentNotice for rendering at the next clean boundary.
- L4257 `_flush_credit_notices(self)` (method) — Print any queued credit notices as level-colored lines. Called at turn end
- L4276 `_on_notice_clear(self, key: str)` (method) — Notice cleared. The REPL prints lines (no persistent slot to wipe), so
- L4285 `_current_reasoning_callback(self)` (method) — Return the active reasoning display callback for the current mode.
- L4293 `_emit_reasoning_preview(self, reasoning_text: str)` (method) — Render a buffered reasoning preview as a single [thinking] block.
- L4328 `_flush_reasoning_preview(self, *, force: bool=False)` (method) — Flush buffered reasoning text at natural boundaries.
- L4377 `_format_submitted_user_message_preview(self, user_input: str)` (method) — Format the submitted user-message scrollback preview.
- L4413 `_expand_paste_references(self, text: str | None)` (method) — Expand [Pasted text #N -> file] placeholders into file contents.
- L4432 `_print_user_message_preview(self, user_input: str)` (method) — Render a user message using the normal chat scrollback style.
- L4441 `_stream_reasoning_delta(self, text: str)` (method) — Stream reasoning/thinking tokens into a dim box above the response.
- L4477 `_close_reasoning_box(self)` (method) — Close the live reasoning box if it's open.
- L4495 `_stream_delta(self, text)` (method) — Line-buffered streaming callback for real-time token rendering.
- L4635 `_emit_stream_text(self, text: str)` (method) — Emit filtered text to the streaming display.
- L4730 `_flush_stream(self)` (method) — Emit any remaining partial line from the stream buffer and close the box.
- L4780 `_reset_stream_state(self)` (method) — Reset streaming state before each agent invocation.
- L4796 `_slow_command_status(self, command: str)` (method) — Return a user-facing status message for slower slash commands.
- L4817 `_command_spinner_frame(self)` (method) — Return the current spinner frame for slow slash commands.
- L4823 `_busy_command(self, status: str)` (method) — Expose a temporary busy state in the TUI while a slash command runs.
- L4836 `_open_external_editor(self, buffer=None)` (method) — Open the active input buffer in an external editor.
- L4869 `_ensure_runtime_credentials(self)` (method) — Ensure runtime credentials are resolved before agent use.
- L5017 `_resolve_turn_agent_config(self, user_message: str)` (method) — Build the effective model/runtime config for a single user turn.
- L5061 `_install_tool_callbacks(self)` (method) — Install tool callbacks that need the live prompt UI.
- L5076 `_ensure_tirith_security(self)` (method) — Check tirith availability once before tools can run terminal commands.
- L5096 `_init_agent(self, *, model_override: str=None, runtime_override: dict=None, request_overrides: dict | None=None)` (method) — Initialize the agent on first use.
- L5312 `_show_security_advisories(self)` (method) — Show a startup banner if any unacked security advisories match.
- L5337 `show_banner(self)` (method) — Display the welcome banner in Claude Code style.
- L5421 `_restore_session_cwd(self, session_meta: dict, *, quiet: bool=False)` (method) — Relaunch a resumed session in the directory it was started from.
- L5474 `_preload_resumed_session(self)` (method) — Load a resumed session's history from the DB early (before first chat).
- L5552 `_display_resumed_history(self)` (method) — Render a compact recap of previous conversation messages.
- L5719 `_render_resume_history_panel_lines(self, panel)` (method) — Render the resume panel at the current terminal width for resize replay.
- L5736 `_try_attach_clipboard_image(self)` (method) — Check clipboard for an image and attach it if found.
- L5755 `_handle_rollback_command(self, command: str)` (method) — Handle /rollback — list, diff, or restore filesystem checkpoints.
- L5849 `_resolve_checkpoint_ref(self, ref: str, checkpoints: list)` (method) — Resolve a checkpoint number or hash to a full commit hash.
- L5862 `_handle_snapshot_command(self, command: str)` (method) — Handle /snapshot — lightweight state snapshots for Hermes config/state.
- L5949 `_handle_stop_command(self)` (method) — Handle /stop — kill all running background processes.
- L5968 `_handle_agents_command(self)` (method) — Handle /agents — show background processes and agent status.
- L5988 `_handle_paste_command(self)` (method) — Handle /paste — explicitly check clipboard for an image.
- L6013 `_write_osc52_clipboard(self, text: str)` (method) — Copy *text* to terminal clipboard via OSC 52.
- L6030 `_recover_terminal_input_modes(self, *, reason: str)` (method) — Best-effort reset when leaked mouse reports indicate mode drift.
- L6061 `_handle_copy_command(self, cmd_original: str)` (method) — Handle /copy [number] — copy assistant output to clipboard.
- L6099 `_handle_image_command(self, cmd_original: str)` (method) — Handle /image <path> — attach a local image file for the next prompt.
- L6123 `_preprocess_images_with_vision(self, text: str, images: list, *, announce: bool=True)` (method) — Analyze attached images via the vision tool and return enriched text.
- L6189 `_show_tool_availability_warnings(self)` (method) — Show warnings about disabled tools due to missing API keys.
- L6211 `_show_status(self)` (method) — Show compact startup status line.
- L6255 `_show_session_status(self)` (method) — Show gateway-style status for the current CLI session.
- L6308 `_fast_command_available(self)` (method)
- L6317 `_command_available(self, slash_command: str)` (method)
- L6322 `show_help(self)` (method) — Display help information with categorized commands.
- L6373 `show_tools(self)` (method) — Display available tools with kawaii ASCII art.
- L6415 `_handle_tools_command(self, cmd: str)` (method) — Handle /tools [list|disable|enable] slash commands.
- L6494 `show_toolsets(self)` (method) — Display available toolsets with kawaii ASCII art.
- L6525 `_handle_profile_command(self)` (method) — Display active profile name and home directory.
- L6538 `show_config(self)` (method) — Display current configuration with kawaii ASCII art.
- L6596 `_list_recent_sessions(self, limit: int=10)` (method) — Return recent CLI sessions for in-chat browsing/resume affordances.
- L6610 `_show_recent_sessions(self, *, reason: str='history', limit: int=10)` (method) — Render recent sessions inline from the active chat TUI.
- L6640 `show_history(self)` (method) — Display conversation history.
- L6707 `_notify_session_boundary(self, event_type: str)` (method) — Fire a session-boundary plugin hook (on_session_finalize or on_session_reset).
- L6724 `new_session(self, silent=False, title=None)` (method) — Start a fresh session with a new session ID and cleared agent state.
- L6826 `_handle_handoff_command(self, cmd_original: str)` (method) — Handle ``/handoff <platform>`` — transfer this CLI session to a gateway platform.
- L6976 `_handle_resume_command(self, cmd_original: str)` (method) — Handle /resume <session_id_or_title> — switch to a previous session mid-conversation.
- L7125 `_consume_pending_resume_selection(self, text: str)` (method) — Resolve a bare numeric reply that follows a bare ``/resume`` prompt.
- L7163 `_handle_sessions_command(self, cmd_original: str)` (method) — Handle /sessions [list|<id_or_title>] — browse or resume previous sessions.
- L7195 `_handle_branch_command(self, cmd_original: str)` (method) — Handle /branch [name] — fork the current session into a new independent copy.
- L7330 `save_conversation(self)` (method) — Save the current conversation to a JSON snapshot under ~/.hermes/sessions/saved/.
- L7365 `retry_last(self)` (method) — Retry the last user message by removing the last exchange and re-sending.
- L7394 `undo_last(self, n: int=1, prefill: bool=True)` (method) — Back up N user turns: truncate history, soft-delete on disk, prefill.
- L7518 `_undo_content_to_text(content)` (method) — Flatten message content (str or content-part list) to plain text.
- L7531 `_prefill_input_buffer(self, text: str)` (method) — Place ``text`` in the active prompt_toolkit buffer, editable.
- L7545 `_run_curses_picker(self, title: str, items: list[str], default_index: int=0)` (method) — Run curses_single_select via run_in_terminal so prompt_toolkit handles terminal ownership cleanly.
- L7575 `_prompt_text_input(self, prompt_text: str)` (method) — Prompt for free-text input safely inside or outside prompt_toolkit.
- L7619 `_prompt_text_input_modal(self, *, title: str, detail: str, choices: list[tuple[str, str, str]], timeout: float=120)` (method) — Prompt through the prompt_toolkit composer instead of raw input().
- L7747 `_submit_slash_confirm_response(self, value: str | None)` (method)
- L7756 `_normalize_slash_confirm_choice(self, raw: str | None, choices: list[tuple[str, str, str]])` (method)
- L7790 `_get_slash_confirm_display_fragments(self)` (method) — Render the /new-/clear-style confirmation panel.
- L7870 `_open_model_picker(self, providers: list, current_model: str, current_provider: str, user_provs=None, custom_provs=None)` (method) — Open prompt_toolkit-native /model picker modal.
- L7885 `_close_model_picker(self)` (method)
- L7891 `_compute_model_picker_viewport(selected: int, scroll_offset: int, n: int, term_rows: int, reserved_below: int=6, panel_chrome: int=6, min_visible: int=3)` (method) — Resolve (scroll_offset, visible) for the /model picker viewport.
- L7918 `_apply_model_switch_result(self, result, persist_global: bool)` (method)
- L8002 `_handle_model_picker_selection(self, persist_global: bool=False)` (method)
- L8065 `_handle_model_switch(self, cmd_original: str)` (method) — Handle /model command — switch model for this session.
- L8251 `_handle_codex_runtime(self, cmd_original: str)` (method) — Handle /codex-runtime — toggle the codex app-server runtime opt-in.
- L8291 `_should_handle_model_command_inline(self, text: str, has_images: bool=False)` (method) — Return True when /model should be handled immediately on the UI thread.
- L8303 `_should_handle_steer_command_inline(self, text: str, has_images: bool=False)` (method) — Return True when /steer should be dispatched immediately while the agent is running.
- L8327 `_output_console(self)` (method) — Use prompt_toolkit-safe Rich rendering once the TUI is live.
- L8333 `_console_print(self, *args, **kwargs)` (method) — Print through the active command-safe console.
- L8338 `_resolve_personality_prompt(value)` (method) — Accept string or dict personality value; return system prompt string.
- L8349 `_handle_gquota_command(self, cmd_original: str)` (method) — Show Google Gemini Code Assist quota usage for the current OAuth account.
- L8395 `_handle_personality_command(self, cmd: str)` (method) — Handle the /personality command to set predefined personalities.
- L8440 `_handle_cron_command(self, cmd: str)` (method) — Handle the /cron command to manage scheduled tasks.
- L8685 `_handle_curator_command(self, cmd: str)` (method) — Handle /curator slash command.
- L8707 `_handle_kanban_command(self, cmd: str)` (method) — Handle the /kanban command — delegate to the shared kanban CLI.
- L8728 `_handle_skills_command(self, cmd: str)` (method) — Handle /skills slash command — delegates to hermes_cli.skills_hub.
- L8733 `_show_gateway_status(self)` (method) — Show status of the gateway and connected messaging platforms.
- L8790 `process_command(self, command: str)` (method) — Process a slash command.
- L9316 `_handle_background_command(self, cmd: str)` (method) — Handle /background <prompt> — run a prompt in a separate background session.
- L9471 `_try_launch_chrome_debug(port: int, system: str)` (method) — Try to launch a Chromium-family browser with remote debugging enabled.
- L9481 `_handle_bundles_command(self, cmd: str)` (method) — In-session ``/bundles`` — show installed skill bundles.
- L9519 `_handle_browser_command(self, cmd: str)` (method) — Handle /browser connect|disconnect|status — manage live Chromium-family CDP connection.
- L9726 `_get_goal_manager(self)` (method) — Return the GoalManager bound to the current session_id.
- L9759 `_handle_goal_command(self, cmd: str)` (method) — Dispatch /goal subcommands: set / status / pause / resume / clear.
- L9825 `_handle_subgoal_command(self, cmd: str)` (method) — Dispatch /subgoal subcommands.
- L9900 `_maybe_continue_goal_after_turn(self)` (method) — Hook run after every CLI turn. Judges + maybe re-queues.
- L10016 `_handle_skin_command(self, cmd: str)` (method) — Handle /skin [name] — show or change the display skin.
- L10058 `_handle_footer_command(self, cmd_original: str)` (method) — Toggle or inspect ``display.runtime_footer.enabled`` from the CLI.
- L10110 `_toggle_verbose(self)` (method) — Cycle tool progress mode: off → new → all → verbose → off.
- L10143 `_transfer_session_yolo(self, old_session_id: str, new_session_id: str)` (method) — Move YOLO bypass state from an old session key to a new one.
- L10171 `_is_session_yolo_active(self)` (method) — Whether YOLO bypass is currently enabled for this CLI session.
- L10197 `_toggle_yolo(self)` (method) — Toggle YOLO mode — skip all dangerous command approval prompts.
- L10234 `_handle_reasoning_command(self, cmd: str)` (method) — Handle /reasoning — manage effort level and display toggle.
- L10295 `_handle_busy_command(self, cmd: str)` (method) — Handle /busy — control what Enter does while Hermes is working.
- L10337 `_handle_fast_command(self, cmd: str)` (method) — Handle /fast — toggle fast mode (OpenAI Priority Processing / Anthropic Fast Mode).
- L10380 `_on_reasoning(self, reasoning_text: str)` (method) — Callback for intermediate reasoning display during tool-call loops.
- L10387 `_manual_compress(self, cmd_original: str='')` (method) — Manually trigger context compression on the current conversation.
- L10535 `_handle_debug_command(self)` (method) — Handle /debug — upload debug report + logs and print paste URLs.
- L10543 `_handle_update_command(self)` (method) — Handle /update — update Hermes Agent to the latest version.
- L10595 `_show_usage(self)` (method) — Rate limits + session token usage (when a live agent exists) + Nous credits.
- L10725 `_print_nous_credits_block(self)` (method) — Print the Nous credits magnitudes + monthly-grant gauge when a Nous account
- L10747 `_show_insights(self, command: str='/insights')` (method) — Show usage insights and analytics from session history.
- L10783 `_check_config_mcp_changes(self)` (method) — Detect mcp_servers changes in config.yaml and auto-reload MCP connections.
- L10846 `_split_destructive_skip(cls, cmd_text: Optional[str])` (method) — Split inline-skip tokens out of a destructive slash command.
- L10876 `_confirm_destructive_slash(self, command: str, detail: str, cmd_original: Optional[str]=None)` (method) — Prompt the user to confirm a destructive session slash command.
- L10962 `_confirm_and_reload_mcp(self, cmd_original: str='')` (method) — Interactive /reload-mcp — confirm with the user, then reload.
- L11029 `_reload_mcp(self)` (method) — Reload MCP servers: disconnect all, re-read config.yaml, reconnect.
- L11114 `_reload_skills(self)` (method) — Reload skills: rescan ~/.hermes/skills/ and queue a note for the
- L11196 `_on_tool_gen_start(self, tool_name: str)` (method) — Called when the model begins generating tool-call arguments.
- L11216 `_on_tool_progress(self, event_type: str, function_name: str=None, preview: str=None, function_args: dict=None, **kwargs)` (method) — Called on tool lifecycle events (tool.started, tool.completed, reasoning.available, etc.).
- L11298 `_on_tool_start(self, tool_call_id: str, function_name: str, function_args: dict)` (method) — Capture local before-state for write-capable tools.
- L11309 `_on_tool_complete(self, tool_call_id: str, function_name: str, function_args: dict, function_result: str)` (method) — Render file edits with inline diff after write-capable tools complete.
- L11329 `_voice_start_recording(self)` (method) — Start capturing audio from the microphone.
- L11447 `_voice_stop_and_transcribe(self)` (method) — Stop recording, transcribe via STT, and queue the transcript as input.
- L11554 `_voice_speak_response_async(self, text: str)` (method) — Schedule TTS and mark it pending before continuous recording can restart.
- L11565 `_voice_speak_response(self, text: str)` (method) — Speak the agent's response aloud using TTS (runs in background thread).
- L11617 `_handle_voice_command(self, command: str)` (method) — Handle /voice [on|off|tts|status] command.
- L11640 `_voice_beeps_enabled(self)` (method) — Return whether CLI voice mode should play record start/stop beeps.
- L11651 `_enable_voice_mode(self)` (method) — Enable voice mode after checking requirements.
- L11711 `_disable_voice_mode(self)` (method) — Disable voice mode, cancel any active recording, and stop TTS.
- L11743 `_toggle_voice_tts(self)` (method) — Toggle TTS output for voice mode.
- L11760 `_show_voice_status(self)` (method) — Show current voice mode status.
- L11779 `_clarify_callback(self, question, choices)` (method) — Platform callback for the clarify tool. Called from the agent thread.
- L11846 `_sudo_password_callback(self)` (method) — Prompt for sudo password through the prompt_toolkit UI.
- L11892 `_approval_callback(self, command: str, description: str, *, allow_permanent: bool=True)` (method) — Prompt for dangerous command approval through the prompt_toolkit UI.
- L11947 `_approval_choices(self, command: str, *, allow_permanent: bool=True)` (method) — Return approval choices for a dangerous command prompt.
- L11954 `_computer_use_approval_callback(self, action: str, args: dict, summary: str)` (method) — Adapt the generic approval UI for the computer_use tool.
- L11975 `_handle_approval_selection(self)` (method) — Process the currently selected dangerous-command approval choice.
- L12001 `_get_approval_display_fragments(self)` (method) — Render the dangerous-command approval panel for the prompt_toolkit UI.
- L12165 `_secret_capture_callback(self, var_name: str, prompt: str, metadata=None)` (method)
- L12168 `_capture_modal_input_snapshot(self)` (method) — Temporarily clear the input buffer and save the user's in-progress draft.
- L12182 `_restore_modal_input_snapshot(self)` (method) — Restore any draft text that was present before a modal prompt opened.
- L12195 `_submit_secret_response(self, value: str)` (method)
- L12203 `_cancel_secret_capture(self)` (method)
- L12206 `_clear_secret_input_buffer(self)` (method)
- L12213 `chat(self, message, images: list=None)` (method) — Send a message to the agent and get a response.
- L12850 `_clear_terminal_on_exit(self)` (method) — Clear screen + scrollback so nothing is stranded above the exit summary.
- L12884 `_print_exit_summary(self)` (method) — Print session resume info on exit, similar to Claude Code.
- L12951 `_get_tui_prompt_symbols(self)` (method) — Return ``(normal_prompt, state_suffix)`` for the active skin.
- L12990 `_audio_level_bar(self)` (method) — Return a visual audio level indicator based on current RMS.
- L13002 `_get_tui_prompt_fragments(self)` (method) — Return the prompt_toolkit fragments for the current interactive state.
- L13042 `_get_tui_prompt_text(self)` (method) — Return the visible prompt text for width calculations.
- L13046 `_build_tui_style_dict(self)` (method) — Layer the active skin's prompt_toolkit colors over the base TUI style.
- L13091 `_apply_tui_skin_style(self)` (method) — Refresh prompt_toolkit styling for a running interactive TUI.
- L13101 `_get_extra_tui_widgets(self)` (method) — Return extra prompt_toolkit widgets to insert into the TUI layout.
- L13110 `_register_extra_tui_keybindings(self, kb, *, input_area)` (method) — Register extra keybindings on the TUI ``KeyBindings`` object.
- L13125 `_build_tui_layout_children(self, *, sudo_widget, secret_widget, approval_widget, slash_confirm_widget=None, clarify_widget, model_picker_widget=None, spinner_widget=None, spacer, status_bar, input_rule_top, image_bar, input_area, input_rule_bot, voice_status_bar, completions_menu)` (method) — Assemble the ordered list of children for the root ``HSplit``.
- L13172 `run(self)` (method) — Run the interactive CLI loop with persistent input at bottom.
- L15610 `_run_kanban_goal_loop_q(cli: 'HermesCLI', first_response: str)` (function) — Drive a kanban goal_mode worker through the Ralph-style goal loop.
- L15700 `main(query: str=None, q: str=None, image: str=None, toolsets: str=None, skills: str | list[str] | tuple[str, ...]=None, model: str=None, provider: str=None, api_key: str=None, base_url: str=None, max_turns: int=None, verbose: Optional[bool]=None, quiet: bool=False, compact: bool=False, list_tools: bool=False, list_toolsets: bool=False, gateway: bool=False, resume: str=None, worktree: bool=False, w: bool=False, checkpoints: bool=False, pass_session_id: bool=False, ignore_user_config: bool=False, ignore_rules: bool=False)` (function) — Hermes Agent CLI - Interactive AI Assistant
