---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:41'
updated: '2026-06-08T00:38:41'
---

# tests/agent/test_memory_provider.py

Symbols in `tests/agent/test_memory_provider.py`.

- L15 `FakeMemoryProvider` (class) — Minimal concrete provider for testing.
- L18 `__init__(self, name='fake', available=True, tools=None)` (method)
- L35 `name(self)` (method)
- L38 `is_available(self)` (method)
- L41 `initialize(self, session_id, **kwargs)` (method)
- L45 `system_prompt_block(self)` (method)
- L48 `prefetch(self, query, *, session_id='')` (method)
- L52 `queue_prefetch(self, query, *, session_id='')` (method)
- L55 `sync_turn(self, user_content, assistant_content, *, session_id='')` (method)
- L58 `get_tool_schemas(self)` (method)
- L61 `handle_tool_call(self, tool_name, args, **kwargs)` (method)
- L64 `shutdown(self)` (method)
- L67 `on_turn_start(self, turn_number, message)` (method)
- L70 `on_session_end(self, messages)` (method)
- L73 `on_pre_compress(self, messages)` (method)
- L76 `on_memory_write(self, action, target, content)` (method)
- L80 `MetadataMemoryProvider` (class) — Provider that opts into write metadata.
- L83 `on_memory_write(self, action, target, content, metadata=None)` (method)
- L87 `MessagesMemoryProvider` (class) — Provider that opts into completed-turn message context.
- L90 `sync_turn(self, user_content, assistant_content, *, session_id='', messages=None)` (method)
- L99 `TestMemoryProviderABC` (class)
- L100 `test_cannot_instantiate_abstract(self)` (method) — ABC cannot be instantiated directly.
- L105 `test_concrete_provider_works(self)` (method) — Concrete implementation can be instantiated.
- L111 `test_default_optional_hooks_are_noop(self)` (method) — Optional hooks have default no-op implementations.
- L129 `TestMemoryManager` (class)
- L130 `test_empty_manager(self)` (method)
- L138 `test_add_provider(self)` (method)
- L145 `test_get_provider_by_name(self)` (method)
- L152 `test_builtin_plus_external(self)` (method)
- L160 `test_second_external_rejected(self)` (method) — Only one non-builtin provider is allowed.
- L172 `test_system_prompt_merges_blocks(self)` (method)
- L185 `test_system_prompt_skips_empty(self)` (method)
- L197 `test_prefetch_merges_results(self)` (method)
- L212 `test_prefetch_skips_empty(self)` (method)
- L224 `test_queue_prefetch_all(self)` (method)
- L235 `test_sync_all(self)` (method)
- L246 `test_sync_all_passes_messages_to_opted_in_provider(self)` (method)
- L259 `test_sync_all_omits_messages_for_legacy_provider(self)` (method)
- L268 `test_sync_failure_doesnt_block_others(self)` (method) — If one provider's sync fails, others still run.
- L283 `test_tool_schemas_collected(self)` (method)
- L298 `test_tool_name_conflict_first_wins(self)` (method)
- L314 `test_handle_unknown_tool(self)` (method)
- L319 `test_tool_routing(self)` (method)
- L337 `test_on_turn_start(self)` (method)
- L344 `test_on_session_end(self)` (method)
- L351 `test_on_pre_compress(self)` (method)
- L358 `test_shutdown_all_reverse_order(self)` (method)
- L371 `test_initialize_all(self)` (method)
- L386 `test_prefetch_failure_doesnt_block(self)` (method)
- L398 `test_system_prompt_failure_doesnt_block(self)` (method)
- L411 `TestPluginMemoryDiscovery` (class) — Memory providers are discovered from plugins/memory/ directory.
- L414 `test_discover_finds_providers(self)` (method) — discover_memory_providers returns available providers.
- L421 `test_load_provider_by_name(self)` (method) — load_memory_provider returns a working provider instance.
- L429 `test_load_nonexistent_returns_none(self)` (method) — load_memory_provider returns None for unknown names.
- L435 `TestUserInstalledProviderDiscovery` (class) — Memory providers installed to $HERMES_HOME/plugins/ should be found.
- L443 `_make_user_memory_plugin(self, tmp_path, name='myprovider')` (method) — Create a minimal user memory provider plugin.
- L463 `test_discover_finds_user_plugins(self, tmp_path, monkeypatch)` (method) — discover_memory_providers() includes user-installed plugins.
- L476 `test_load_user_plugin(self, tmp_path, monkeypatch)` (method) — load_memory_provider() can load from $HERMES_HOME/plugins/.
- L489 `test_bundled_takes_precedence(self, tmp_path, monkeypatch)` (method) — Bundled provider wins when user plugin has the same name.
- L520 `test_non_memory_user_plugins_excluded(self, tmp_path, monkeypatch)` (method) — User plugins that don't reference MemoryProvider are skipped.
- L536 `test_load_user_plugin_with_relative_import(self, tmp_path, monkeypatch)` (method) — User plugins may import sibling modules with relative imports.
- L569 `test_load_user_plugin_with_nested_subpackage(self, tmp_path, monkeypatch)` (method) — User plugins may keep their implementation in a nested subpackage.
- L606 `TestUserInstalledProviderCli` (class) — CLI commands of user-installed providers must be discoverable.
- L615 `_make_plugin_with_cli(self, tmp_path, name)` (method)
- L640 `_activate(self, tmp_path, monkeypatch, name)` (method)
- L650 `test_cli_discovered_for_user_plugin_with_relative_import(self, tmp_path, monkeypatch)` (method) — discover_plugin_cli_commands() loads a user provider's cli.py.
- L662 `test_provider_load_after_cli_discovery(self, tmp_path, monkeypatch)` (method) — The provider still loads after CLI discovery ran first.
- L683 `TestSequentialDispatchRouting` (class) — Verify that memory provider tools are correctly routed through
- L698 `test_has_tool_returns_true_for_provider_tools(self)` (method) — has_tool returns True for tools registered by memory providers.
- L710 `test_has_tool_returns_false_for_builtin_tools(self)` (method) — has_tool returns False for agent-level tools (terminal, memory, etc.).
- L724 `test_handle_tool_call_routes_to_provider(self)` (method) — handle_tool_call dispatches to the correct provider's handler.
- L737 `test_handle_tool_call_unknown_returns_error(self)` (method) — handle_tool_call returns error for tools not in any provider.
- L748 `test_multiple_providers_route_to_correct_one(self)` (method) — Tools from different providers route to the right handler.
- L766 `test_tool_names_include_all_providers(self)` (method) — get_all_tool_names returns tools from all registered providers.
- L788 `TestSetupFieldFiltering` (class) — Test the 'when' clause and 'default_from' logic used by the
- L796 `_filter_fields(self, schema, provider_config)` (method) — Simulate the setup wizard's field filtering logic.
- L825 `test_when_clause_filters_fields(self)` (method) — Fields with 'when' are skipped if the condition doesn't match.
- L846 `test_when_clause_no_condition_always_shown(self)` (method) — Fields without 'when' are always included.
- L855 `test_default_from_resolves_dynamic_default(self)` (method) — default_from looks up the default from another field's value.
- L878 `test_default_from_falls_back_to_static_default(self)` (method) — default_from falls back to static default if provider not in map.
- L890 `test_default_from_with_no_ref_value(self)` (method) — default_from keeps static default if referenced field is not set.
- L902 `test_when_and_default_from_combined(self)` (method) — when clause and default_from work together correctly.
- L933 `TestMemoryContextFencing` (class) — Prefetch context must be wrapped in <memory-context> fence so the model
- L937 `test_build_memory_context_block_wraps_content(self)` (method)
- L947 `test_build_memory_context_block_empty_input(self)` (method)
- L952 `test_sanitize_context_strips_fence_escapes(self)` (method)
- L961 `test_sanitize_context_case_insensitive(self)` (method)
- L967 `test_fenced_block_separates_user_from_recall(self)` (method)
- L984 `_CommitRecorder` (class) — Provider that records on_session_end calls for assertions.
- L987 `__init__(self, name='recorder')` (method)
- L991 `on_session_end(self, messages)` (method)
- L995 `TestCommitMemorySessionRouting` (class)
- L996 `test_on_session_end_fans_out(self)` (method)
- L1009 `test_on_session_end_tolerates_failure(self)` (method)
- L1025 `TestOnMemoryWriteBridge` (class) — Verify that MemoryManager.on_memory_write is called when built-in
- L1033 `test_on_memory_write_add(self)` (method) — on_memory_write fires for 'add' actions.
- L1042 `test_on_memory_write_metadata_passed_to_opt_in_provider(self)` (method) — Providers that accept metadata receive structured write provenance.
- L1072 `test_on_memory_write_metadata_keeps_legacy_provider_compatible(self)` (method) — Old 3-arg providers keep working when the manager receives metadata.
- L1087 `test_on_memory_write_replace(self)` (method) — on_memory_write fires for 'replace' actions.
- L1096 `test_on_memory_write_remove_not_bridged(self)` (method) — The bridge intentionally skips 'remove' — only add/replace notify.
- L1109 `test_memory_manager_tool_injection_deduplicates(self)` (method) — Memory manager tools already in self.tools (from plugin registry)
- L1152 `test_on_memory_write_tolerates_provider_failure(self)` (method) — If a provider's on_memory_write raises, others still get notified.
- L1166 `TestHonchoCadenceTracking` (class) — Verify Honcho provider cadence gating depends on on_turn_start().
- L1175 `test_turn_count_updates_on_turn_start(self)` (method) — on_turn_start sets _turn_count, enabling cadence math.
- L1185 `test_queue_prefetch_respects_dialectic_cadence(self)` (method) — With dialecticCadence=3, dialectic should skip turns 2 and 3.
- L1216 `test_injection_frequency_first_turn_with_1indexed(self)` (method) — injection_frequency='first-turn' must inject on turn 1 (1-indexed).
- L1235 `TestMemoryToolToolsetGate` (class) — Issue #5544: memory provider tools must respect platform_toolsets.
- L1254 `_run_memory_injection(enabled_toolsets, memory_manager)` (method) — Simulate the gated memory-tool injection block from agent_init.py.
- L1278 `_mgr_with_tools(self, *tool_names)` (method) — Build a MemoryManager whose providers expose the named tool schemas.
- L1288 `test_none_toolsets_injects(self)` (method) — enabled_toolsets=None (no filter) injects memory tools — backward compat.
- L1295 `test_memory_in_toolsets_injects(self)` (method) — enabled_toolsets including 'memory' injects memory tools.
- L1301 `test_empty_toolsets_blocks_injection(self)` (method) — `platform_toolsets: telegram: []` must suppress memory tools. (#5544)
- L1308 `test_toolsets_without_memory_blocks_injection(self)` (method) — Toolset list that doesn't name 'memory' must suppress injection.
- L1315 `test_no_memory_manager_no_injection(self)` (method) — Gate is moot without a memory manager.
- L1320 `test_multiple_schemas_all_blocked_together(self)` (method) — When the gate is closed, no memory tools leak — not even partially.
- L1327 `test_multiple_schemas_all_injected_when_enabled(self)` (method) — When the gate is open, every memory tool schema is injected.
- L1334 `TestContextEngineToolsetGate` (class) — Issue #5544 (sibling): context engine tools follow the same gate.
- L1344 `_run_context_engine_injection(enabled_toolsets, compressor)` (method) — Simulate the gated context-engine injection block from agent_init.py.
- L1382 `_compressor_with(self, *tool_names)` (method)
- L1387 `test_none_toolsets_injects(self)` (method) — enabled_toolsets=None injects context-engine tools — backward compat.
- L1393 `test_context_engine_in_toolsets_injects(self)` (method) — enabled_toolsets including 'context_engine' injects the tools.
- L1401 `test_empty_toolsets_blocks_injection(self)` (method) — `platform_toolsets: telegram: []` must suppress context-engine tools.
- L1408 `test_toolsets_without_context_engine_blocks_injection(self)` (method) — A toolset list that doesn't name 'context_engine' suppresses injection.
- L1417 `test_no_compressor_no_injection(self)` (method) — Gate is moot without a context_compressor.
