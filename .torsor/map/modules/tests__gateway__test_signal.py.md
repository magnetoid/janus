---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/gateway/test_signal.py

Symbols in `tests/gateway/test_signal.py`.

- L13 `_reset_signal_scheduler()` (function) — The attachment scheduler is process-wide; drop it between tests
- L26 `_make_signal_adapter(monkeypatch, account='+15551234567', **extra)` (function) — Create a SignalAdapter with sensible test defaults.
- L40 `_stub_rpc(return_value)` (function) — Return an async mock for SignalAdapter._rpc that captures call params.
- L55 `TestSignalConfigLoading` (class)
- L56 `test_apply_env_overrides_signal(self, monkeypatch)` (method)
- L70 `test_signal_not_loaded_without_both_vars(self, monkeypatch)` (method)
- L84 `TestSignalAdapterInit` (class)
- L85 `test_init_parses_config(self, monkeypatch)` (method)
- L91 `test_init_empty_allowlist(self, monkeypatch)` (method)
- L95 `test_init_strips_trailing_slash(self, monkeypatch)` (method)
- L99 `test_self_message_filtering(self, monkeypatch)` (method)
- L104 `TestSignalConnectCleanup` (class) — Regression coverage for failed connect() cleanup.
- L108 `test_releases_lock_and_closes_client_on_healthcheck_failure(self, monkeypatch)` (method)
- L127 `TestSignalHelpers` (class)
- L128 `test_redact_phone_long(self)` (method)
- L132 `test_redact_phone_short(self)` (method)
- L136 `test_redact_phone_empty(self)` (method)
- L140 `test_parse_comma_list(self)` (method)
- L146 `test_guess_extension_png(self)` (method)
- L150 `test_guess_extension_jpeg(self)` (method)
- L154 `test_guess_extension_pdf(self)` (method)
- L158 `test_guess_extension_zip(self)` (method)
- L162 `test_guess_extension_mp4(self)` (method)
- L166 `test_guess_extension_unknown(self)` (method)
- L170 `test_is_image_ext(self)` (method)
- L177 `test_is_audio_ext(self)` (method)
- L183 `test_check_requirements(self, monkeypatch)` (method)
- L189 `test_render_mentions(self)` (method)
- L197 `test_render_mentions_no_mentions(self)` (method)
- L203 `test_check_requirements_missing(self, monkeypatch)` (method)
- L214 `TestSignalSSEUrlEncoding` (class) — Verify that phone numbers with + are URL-encoded in the SSE endpoint.
- L217 `test_sse_url_encodes_plus_in_account(self)` (method) — The + in E.164 phone numbers must be percent-encoded in the SSE query string.
- L222 `test_sse_url_encoding_preserves_digits(self)` (method) — Digits and country codes should pass through URL encoding unchanged.
- L231 `TestSignalAttachmentFetch` (class) — Verify that _fetch_attachment uses the correct RPC parameter name.
- L235 `test_fetch_attachment_uses_id_parameter(self, monkeypatch)` (method) — RPC getAttachment must use 'id', not 'attachmentId' (signal-cli requirement).
- L254 `test_fetch_attachment_returns_none_on_empty(self, monkeypatch)` (method)
- L262 `test_fetch_attachment_handles_dict_response(self, monkeypatch)` (method)
- L281 `TestSignalSessionSource` (class)
- L282 `test_session_source_alt_fields(self)` (method)
- L295 `test_session_source_roundtrip(self)` (method)
- L316 `TestSignalPhoneRedaction` (class)
- L318 `_ensure_redaction_enabled(self, monkeypatch)` (method)
- L327 `test_us_number(self)` (method)
- L334 `test_uk_number(self)` (method)
- L340 `test_multiple_numbers(self)` (method)
- L347 `test_short_number_not_matched(self)` (method)
- L358 `TestSignalAuthorization` (class)
- L359 `test_signal_in_allowlist_maps(self)` (method) — Signal should be in the platform auth maps.
- L387 `TestSignalSendImageFile` (class)
- L389 `test_send_image_file_sends_via_rpc(self, monkeypatch, tmp_path)` (method) — send_image_file should send image as attachment via signal-cli RPC.
- L414 `test_send_image_file_to_group(self, monkeypatch, tmp_path)` (method) — send_image_file should route group chats via groupId.
- L433 `test_send_image_file_missing(self, monkeypatch)` (method) — send_image_file should fail gracefully for nonexistent files.
- L444 `test_send_image_file_too_large(self, monkeypatch, tmp_path)` (method) — send_image_file should reject files over 100MB.
- L464 `test_send_image_file_rpc_failure(self, monkeypatch, tmp_path)` (method) — send_image_file should return error when RPC returns None.
- L480 `TestSignalRecipientResolution` (class)
- L482 `test_send_prefers_cached_uuid_for_direct_messages(self, monkeypatch)` (method)
- L502 `test_send_looks_up_uuid_via_list_contacts(self, monkeypatch)` (method)
- L531 `test_send_falls_back_to_phone_when_no_uuid_found(self, monkeypatch)` (method)
- L553 `test_send_typing_uses_cached_uuid(self, monkeypatch)` (method)
- L575 `TestSignalSendVoice` (class)
- L577 `test_send_voice_sends_via_rpc(self, monkeypatch, tmp_path)` (method) — send_voice should send audio as attachment via signal-cli RPC.
- L597 `test_send_voice_missing_file(self, monkeypatch)` (method) — send_voice should fail for nonexistent audio.
- L608 `test_send_voice_to_group(self, monkeypatch, tmp_path)` (method) — send_voice should route group chats correctly.
- L624 `test_send_voice_too_large(self, monkeypatch, tmp_path)` (method) — send_voice should reject files over 100MB.
- L644 `test_send_voice_rpc_failure(self, monkeypatch, tmp_path)` (method) — send_voice should return error when RPC returns None.
- L664 `TestSignalSendVideo` (class)
- L666 `test_send_video_sends_via_rpc(self, monkeypatch, tmp_path)` (method) — send_video should send video as attachment via signal-cli RPC.
- L686 `test_send_video_missing_file(self, monkeypatch)` (method) — send_video should fail for nonexistent video.
- L697 `test_send_video_too_large(self, monkeypatch, tmp_path)` (method) — send_video should reject files over 100MB.
- L717 `test_send_video_rpc_failure(self, monkeypatch, tmp_path)` (method) — send_video should return error when RPC returns None.
- L737 `TestSignalMediaExtraction` (class) — Verify the full pipeline: MEDIA: tag → extract → send_image_file/send_voice.
- L740 `test_extract_media_finds_image_tag(self)` (method) — BasePlatformAdapter.extract_media should find MEDIA: image paths.
- L750 `test_extract_media_finds_audio_tag(self)` (method) — BasePlatformAdapter.extract_media should find MEDIA: audio paths.
- L760 `test_signal_has_all_media_methods(self, monkeypatch)` (method) — SignalAdapter must override all media send methods used by gateway.
- L777 `TestSignalSendDocumentViaHelper` (class) — Verify send_document gained size check and path-in-error via _send_attachment.
- L781 `test_send_document_too_large(self, monkeypatch, tmp_path)` (method) — send_document should now reject files over 100MB (was previously missing).
- L801 `test_send_document_error_includes_path(self, monkeypatch)` (method) — send_document error message should include the file path.
- L816 `TestSignalStreamingCapabilities` (class) — Signal must opt out of edit-based streaming behavior.
- L819 `test_signal_declares_no_message_editing(self, monkeypatch)` (method)
- L825 `TestSignalSendReturnsMessageId` (class) — Signal send() should not pretend sent messages are editable.
- L829 `test_send_returns_none_message_id_even_with_timestamp(self, monkeypatch)` (method)
- L841 `test_send_returns_none_message_id_when_no_timestamp(self, monkeypatch)` (method)
- L853 `test_send_returns_none_message_id_for_non_dict(self, monkeypatch)` (method)
- L869 `TestSignalStopTyping` (class) — Signal must expose a public stop_typing() so base adapter's
- L874 `test_stop_typing_calls_private_method(self, monkeypatch)` (method)
- L887 `TestSignalTypingBackoff` (class) — When base.py's _keep_typing refresh loop calls send_typing every ~2s
- L900 `test_first_failure_logs_at_warning_subsequent_at_debug(self, monkeypatch)` (method)
- L920 `test_three_consecutive_failures_trigger_cooldown(self, monkeypatch)` (method)
- L946 `test_cooldown_is_per_chat_not_global(self, monkeypatch)` (method)
- L969 `test_success_resets_failure_counter_and_cooldown(self, monkeypatch)` (method)
- L999 `test_stop_typing_indicator_clears_backoff_state(self, monkeypatch)` (method)
- L1024 `TestSignalQuoteExtraction` (class) — Verify Signal reply quote fields are propagated to MessageEvent.
- L1028 `test_handle_envelope_sets_reply_context_from_quote(self, monkeypatch)` (method)
- L1060 `test_handle_envelope_without_quote_leaves_reply_fields_none(self, monkeypatch)` (method)
- L1087 `test_handle_envelope_quote_without_text_sets_only_reply_id(self, monkeypatch)` (method)
- L1120 `_FakeHttpResponse` (class) — Minimal stand-in for httpx.Response — only what _rpc touches.
- L1123 `__init__(self, json_data)` (method)
- L1126 `raise_for_status(self)` (method)
- L1129 `json(self)` (method)
- L1133 `_install_fake_client(adapter, json_data)` (function) — Replace adapter.client.post with an async fn returning json_data.
- L1143 `TestSignalRpcRateLimit` (class) — _rpc opt-in 429 detection and SignalRateLimitError propagation.
- L1147 `test_raises_on_429_when_opted_in(self, monkeypatch)` (method)
- L1159 `test_raises_on_rate_limit_exception_substring(self, monkeypatch)` (method) — Some signal-cli builds emit 'RateLimitException' without a literal [429].
- L1172 `test_default_swallows_rate_limit_returns_none(self, monkeypatch)` (method) — Without opt-in, 429 stays swallowed — preserves backwards compat.
- L1183 `test_non_rate_limit_error_does_not_raise_when_opted_in(self, monkeypatch)` (method) — Opt-in only escalates 429s; other errors still return None.
- L1194 `test_raises_with_retry_after_from_v0_14_3_payload(self, monkeypatch)` (method) — signal-cli ≥ v0.14.3 surfaces server Retry-After under
- L1224 `test_raises_with_retry_after_none_for_old_signal_cli(self, monkeypatch)` (method) — Older signal-cli builds emit only the substring; retry_after=None.
- L1239 `test_raises_on_retry_later_inside_attachment_invalid(self, monkeypatch)` (method) — Production case: 429 during attachment upload surfaces as
- L1271 `_make_image_files(tmp_path, count, prefix='img')` (function) — Materialize `count` tiny PNG files and return file:// URIs for them.
- L1281 `_stub_rpc_responses(responses)` (function) — Build an _rpc replacement that pops a response per call.
- L1305 `_patch_scheduler_sleep(monkeypatch, capture: list)` (function) — Capture sleeps inside the scheduler so tests don't actually wait.
- L1328 `TestSignalSendMultipleImages` (class)
- L1330 `test_empty_list_is_noop(self, monkeypatch)` (method)
- L1342 `test_all_bad_files_no_rpc(self, monkeypatch, tmp_path)` (method) — If every image is missing/invalid, no RPC fires.
- L1358 `test_single_batch_under_limit(self, monkeypatch, tmp_path)` (method)
- L1376 `test_skips_bad_images_in_mixed_batch(self, monkeypatch, tmp_path)` (method)
- L1392 `test_429_calibrates_scheduler_then_retries(self, monkeypatch, tmp_path)` (method) — Server says retry_after=27 per token. After feedback, the
- L1417 `test_429_without_retry_after_uses_default_rate(self, monkeypatch, tmp_path)` (method) — signal-cli < v0.14.3 doesn't surface Retry-After. The
- L1450 `test_rate_limit_exhaust_continues_to_next_batch(self, monkeypatch, tmp_path)` (method) — Both attempts on batch 0 fail; batch 1 still gets a chance.
- L1478 `test_full_batch_emits_pacing_notice_for_followup(self, monkeypatch, tmp_path)` (method) — Two full batches of 32. Batch 1 needs 14 more tokens than the
- L1517 `test_short_followup_wait_skips_pacing_notice(self, monkeypatch, tmp_path)` (method) — Batch 1 only needs 1 token but 18 remain after batch 0
- L1541 `test_single_batch_send_does_not_pace(self, monkeypatch, tmp_path)` (method) — A single-batch send (≤32 attachments) leaves the scheduler
- L1559 `TestSignalRateLimitDetection` (class) — Coverage for the typed-code + substring detection helpers.
- L1562 `test_detect_typed_code(self)` (method)
- L1570 `test_detect_substring_fallback(self)` (method)
- L1575 `test_detect_non_rate_limit(self)` (method)
- L1580 `test_extract_retry_after_from_results(self)` (method)
- L1597 `test_extract_retry_after_missing(self)` (method) — Old signal-cli builds don't expose retryAfterSeconds — return None.
- L1603 `test_detect_retry_later_exception_substring(self)` (method) — libsignal-net's RetryLaterException leaks through as
- L1618 `test_extract_retry_after_parses_message_string(self)` (method) — When the structured field is missing, parse the seconds out
- L1633 `TestSignalSendTimeout` (class) — Timeout scaling for batched attachment sends.
- L1636 `test_zero_attachments_uses_default(self)` (method)
- L1640 `test_floor_at_60s(self)` (method)
- L1646 `test_scales_with_batch_size(self)` (method)
- L1657 `TestSignalContentlessEnvelope` (class) — Verify that profile key updates and empty Signal messages are skipped.
- L1661 `test_skips_profile_key_update_no_message_field(self, monkeypatch)` (method) — Profile key updates may carry a dataMessage without 'message' field.
- L1689 `test_skips_empty_message(self, monkeypatch)` (method) — Empty text messages (message='') should be skipped.
- L1714 `test_skips_whitespace_only_message(self, monkeypatch)` (method) — Whitespace-only messages ('   ') should be skipped.
- L1739 `test_allows_message_with_attachment_no_text(self, monkeypatch)` (method) — Messages with attachments but no text should still be processed.
- L1772 `test_allows_normal_text_message(self, monkeypatch)` (method) — Normal text messages should still flow through.
