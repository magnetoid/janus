---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/gateway/test_slack.py

Symbols in `tests/gateway/test_slack.py`.

- L32 `_ensure_slack_mock()` (function) — Install mock slack modules so SlackAdapter can be imported.
- L73 `_pending_for_fake_task()` (function)
- L81 `_fake_create_task(coro)` (function) — Test helper: consume the real coroutine and return a real awaitable Task.
- L104 `adapter()` (function)
- L118 `_redirect_cache(tmp_path, monkeypatch)` (function) — Point document cache to tmp_path so tests don't touch ~/.hermes.
- L130 `TestSlashCommandSessionIsolation` (class)
- L132 `test_channel_slash_command_uses_group_session_semantics(self, adapter)` (method)
- L149 `test_dm_slash_command_keeps_dm_session_semantics(self, adapter)` (method)
- L171 `TestAppMentionHandler` (class) — Verify that the app_mention event handler is registered.
- L174 `test_app_mention_registered_on_connect(self)` (method) — connect() should register message + assistant lifecycle handlers.
- L256 `TestSlackConnectCleanup` (class) — Regression coverage for failed connect() cleanup.
- L260 `test_releases_platform_lock_when_auth_fails(self)` (method)
- L285 `test_reconnect_closes_previous_handler_to_prevent_zombie_socket(self)` (method) — Regression for #18980: calling connect() on an adapter that already has
- L353 `TestSlackSocketWatchdog` (class) — End-to-end behavioural coverage for the Socket Mode watchdog/reconnect.
- L361 `_make_fake_handler_factory(self)` (method) — Return ``(factory, instances)`` where each call records a handler.
- L388 `_patch_stack(self, fake_factory)` (method) — Return a list of patcher context managers to keep active for the test.
- L422 `_drain(self, iterations=10)` (method)
- L427 `test_watchdog_reconnects_when_socket_task_dies_unexpectedly(self)` (method)
- L456 `test_watchdog_reconnects_when_transport_reports_disconnected(self)` (method)
- L483 `test_disconnect_stops_watchdog_and_does_not_reconnect(self)` (method)
- L508 `test_watchdog_cancellation_does_not_respawn(self)` (method) — Cancellation is the intentional-shutdown signal — no respawn allowed.
- L538 `test_watchdog_unexpected_exit_respawns_via_done_callback(self)` (method) — A real exception out of the loop body must trigger a respawn.
- L580 `test_connect_replaces_prior_watchdog_atomically(self)` (method) — A reconnect must not leave the adapter without a watchdog.
- L606 `test_reconnect_refreshes_multi_workspace_state(self)` (method) — A reconnect that rotates the primary token must drop stale state.
- L634 `test_reconnect_lock_prevents_concurrent_reconnects(self)` (method)
- L666 `TestSlackProxyBehavior` (class)
- L667 `test_no_proxy_helper_matches_slack_hosts(self)` (method)
- L673 `test_resolve_slack_proxy_url_ignores_unsupported_proxy_schemes(self)` (method)
- L681 `test_resolve_slack_proxy_url_checks_all_slack_hosts(self)` (method)
- L704 `test_connect_uses_proxy_when_not_bypassed(self)` (method)
- L798 `test_connect_clears_proxy_when_no_proxy_matches_slack(self)` (method)
- L890 `TestSendDocument` (class)
- L892 `test_send_document_success(self, adapter, tmp_path)` (method)
- L913 `test_send_document_custom_name(self, adapter, tmp_path)` (method)
- L930 `test_send_document_missing_file(self, adapter)` (method)
- L940 `test_send_document_not_connected(self, adapter)` (method)
- L951 `test_send_document_api_error_falls_back(self, adapter, tmp_path)` (method)
- L969 `test_send_document_with_thread(self, adapter, tmp_path)` (method)
- L986 `test_send_document_thread_upload_marks_bot_participation(self, adapter, tmp_path)` (method)
- L1003 `test_send_document_retries_transient_upload_error(self, adapter, tmp_path)` (method)
- L1024 `TestSendPrivateNotice` (class)
- L1026 `test_send_private_notice_uses_ephemeral_api(self, adapter)` (method)
- L1053 `TestSendVideo` (class)
- L1055 `test_send_video_success(self, adapter, tmp_path)` (method)
- L1073 `test_send_video_missing_file(self, adapter)` (method)
- L1083 `test_send_video_not_connected(self, adapter)` (method)
- L1094 `test_send_video_api_error_falls_back(self, adapter, tmp_path)` (method)
- L1116 `TestBangPrefixCommands` (class) — ``!cmd`` is rewritten to ``/cmd`` so commands work inside Slack threads.
- L1126 `_make_event(self, text, thread_ts=None, channel_type='im', channel='D123')` (method)
- L1139 `test_bang_known_command_is_rewritten_to_slash(self, adapter)` (method) — ``!queue`` → ``/queue`` and tagged as COMMAND.
- L1149 `test_bang_command_with_args_preserved(self, adapter)` (method) — ``!model gpt-5.4`` → ``/model gpt-5.4``.
- L1158 `test_bang_works_inside_thread(self, adapter)` (method) — The whole point: ``!stop`` inside a thread reply dispatches.
- L1171 `test_bang_unknown_token_passes_through_unchanged(self, adapter)` (method) — ``!nice work`` is just a casual message — must NOT be rewritten.
- L1180 `test_bang_with_bot_suffix_resolves(self, adapter)` (method) — ``!stop@hermes`` matches the get_command() ``@suffix`` stripping.
- L1189 `test_plain_slash_still_works(self, adapter)` (method) — Sanity check — ``/queue`` (top-level channel/DM) still dispatches.
- L1203 `TestIncomingDocumentHandling` (class)
- L1204 `_make_event(self, files=None, text='hello', channel_type='im', blocks=None, attachments=None)` (method) — Build a mock Slack message event with file attachments.
- L1220 `test_pdf_document_cached(self, adapter)` (method) — A PDF attachment should be downloaded, cached, and set as DOCUMENT type.
- L1247 `test_txt_document_injects_content(self, adapter)` (method) — A .txt file under 100KB should have its content injected into event text.
- L1274 `test_md_document_injects_content(self, adapter)` (method) — A .md file under 100KB should have its content injected.
- L1299 `test_json_snippet_injects_content(self, adapter)` (method) — A .json snippet should be treated as a text document and injected.
- L1333 `test_large_txt_not_injected(self, adapter)` (method) — A .txt file over 100KB should be cached but NOT injected.
- L1359 `test_zip_file_cached(self, adapter)` (method) — A .zip file should be cached as a supported document.
- L1383 `test_oversized_document_skipped(self, adapter)` (method) — A document over 20MB should be skipped.
- L1401 `test_document_download_error_handled(self, adapter)` (method) — If document download fails, handler should not crash.
- L1423 `test_image_still_handled(self, adapter)` (method) — Image attachments should still go through the image path, not document.
- L1445 `test_download_failure_is_surfaced_in_message_text(self, adapter)` (method) — Attachment download failures (401/403/HTML-body/etc.) should be
- L1482 `test_rich_text_blocks_do_not_duplicate_plain_text(self, adapter)` (method) — Plain rich_text composer blocks match the plain text field exactly,
- L1508 `test_rich_text_quotes_and_lists_are_extracted(self, adapter)` (method) — Nested quote and list content should be surfaced from rich_text blocks.
- L1559 `test_attachments_unfurl_text_is_appended_even_when_url_is_in_message(self, adapter)` (method) — Shared URLs should still expose unfurl preview text to the agent.
- L1584 `test_message_unfurl_attachments_are_skipped(self, adapter)` (method) — Message unfurls should be skipped to avoid echoing Slack message copies.
- L1603 `test_channel_routing_ignores_bot_mentions_inside_block_text(self, adapter)` (method) — Block-extracted text with a bot mention must not satisfy mention
- L1640 `test_quoted_slash_command_text_does_not_change_message_type(self, adapter)` (method) — Quoted slash-like content should not convert a normal message into a command.
- L1678 `TestMessageRouting` (class)
- L1680 `test_dm_processed_without_mention(self, adapter)` (method) — DM messages should be processed without requiring a bot mention.
- L1693 `test_channel_message_requires_mention(self, adapter)` (method) — Channel messages without a bot mention should be ignored.
- L1706 `test_channel_mention_strips_bot_id(self, adapter)` (method) — When mentioned in a channel, the bot mention should be stripped.
- L1721 `test_bot_messages_ignored(self, adapter)` (method) — Messages from bots should be ignored.
- L1734 `test_message_edits_ignored(self, adapter)` (method) — Message edits should be ignored.
- L1753 `TestSendTyping` (class) — Test typing indicator via assistant.threads.setStatus.
- L1757 `test_sets_status_in_thread(self, adapter)` (method)
- L1767 `test_noop_without_thread(self, adapter)` (method)
- L1773 `test_handles_missing_scope_gracefully(self, adapter)` (method)
- L1781 `test_uses_thread_ts_fallback(self, adapter)` (method)
- L1791 `test_stop_typing_clears_tracked_thread(self, adapter)` (method)
- L1807 `test_stop_typing_noop_without_tracked_thread(self, adapter)` (method)
- L1815 `test_stop_typing_handles_api_error_gracefully(self, adapter)` (method)
- L1831 `test_send_clears_status_after_final_post(self, adapter)` (method)
- L1850 `test_streaming_final_edit_clears_status(self, adapter)` (method)
- L1876 `test_streaming_intermediate_edit_keeps_status(self, adapter)` (method)
- L1898 `TestFormatMessage` (class) — Test markdown to Slack mrkdwn conversion.
- L1901 `test_bold_conversion(self, adapter)` (method)
- L1904 `test_italic_asterisk_conversion(self, adapter)` (method)
- L1907 `test_italic_underscore_preserved(self, adapter)` (method)
- L1910 `test_header_to_bold(self, adapter)` (method)
- L1913 `test_header_with_bold_content(self, adapter)` (method)
- L1917 `test_link_conversion(self, adapter)` (method)
- L1921 `test_link_conversion_strips_markdown_angle_brackets(self, adapter)` (method)
- L1925 `test_escapes_control_characters(self, adapter)` (method)
- L1929 `test_preserves_existing_slack_entities(self, adapter)` (method)
- L1933 `test_strikethrough(self, adapter)` (method)
- L1936 `test_code_block_preserved(self, adapter)` (method)
- L1940 `test_inline_code_preserved(self, adapter)` (method)
- L1944 `test_mixed_content(self, adapter)` (method)
- L1951 `test_empty_string(self, adapter)` (method)
- L1954 `test_none_passthrough(self, adapter)` (method)
- L1957 `test_blockquote_preserved(self, adapter)` (method) — Single-line blockquote > marker is preserved.
- L1961 `test_multiline_blockquote(self, adapter)` (method) — Multi-line blockquote preserves > on each line.
- L1966 `test_blockquote_with_formatting(self, adapter)` (method) — Blockquote containing bold text.
- L1970 `test_nested_blockquote(self, adapter)` (method) — Multiple > characters for nested quotes.
- L1974 `test_blockquote_mixed_with_plain(self, adapter)` (method) — Blockquote lines interleaved with plain text.
- L1981 `test_non_prefix_gt_still_escaped(self, adapter)` (method) — Greater-than in mid-line is still escaped.
- L1985 `test_blockquote_with_code(self, adapter)` (method) — Blockquote containing inline code.
- L1991 `test_bold_italic_combined(self, adapter)` (method) — Triple-star ***text*** converts to Slack bold+italic *_text_*.
- L1995 `test_bold_italic_with_surrounding_text(self, adapter)` (method) — Bold+italic in a sentence.
- L2000 `test_bold_italic_does_not_break_plain_bold(self, adapter)` (method) — **bold** still works after adding ***bold italic*** support.
- L2004 `test_bold_italic_does_not_break_plain_italic(self, adapter)` (method) — *italic* still works after adding ***bold italic*** support.
- L2008 `test_bold_italic_mixed_with_bold(self, adapter)` (method) — Both ***bold italic*** and **bold** in the same message.
- L2014 `test_pre_escaped_ampersand_not_double_escaped(self, adapter)` (method) — Already-escaped &amp; must not become &amp;amp;.
- L2018 `test_pre_escaped_lt_not_double_escaped(self, adapter)` (method) — Already-escaped &lt; must not become &amp;lt;.
- L2022 `test_pre_escaped_gt_not_double_escaped(self, adapter)` (method) — Already-escaped &gt; in plain text must not become &amp;gt;.
- L2026 `test_mixed_raw_and_escaped_entities(self, adapter)` (method) — Raw & and pre-escaped &amp; coexist correctly.
- L2031 `test_link_with_parentheses_in_url(self, adapter)` (method) — Wikipedia-style URL with balanced parens is not truncated.
- L2038 `test_link_with_multiple_paren_pairs(self, adapter)` (method) — URL with multiple balanced paren pairs.
- L2043 `test_link_without_parens_still_works(self, adapter)` (method) — Normal URL without parens is unaffected by regex change.
- L2048 `test_link_with_angle_brackets_and_parens(self, adapter)` (method) — Angle-bracket URL with parens (CommonMark syntax).
- L2055 `test_escaping_is_idempotent(self, adapter)` (method) — Formatting already-formatted text produces the same result.
- L2064 `test_channel_mention_preserved(self, adapter)` (method) — <!channel> special mention passes through unchanged.
- L2068 `test_everyone_mention_preserved(self, adapter)` (method) — <!everyone> special mention passes through unchanged.
- L2072 `test_subteam_mention_preserved(self, adapter)` (method) — <!subteam^ID> user group mention passes through unchanged.
- L2079 `test_date_formatting_preserved(self, adapter)` (method) — <!date^...> formatting token passes through unchanged.
- L2084 `test_channel_link_preserved(self, adapter)` (method) — <#CHANNEL_ID> channel link passes through unchanged.
- L2090 `test_message_only_code_block(self, adapter)` (method) — Entire message is a fenced code block — no conversion.
- L2095 `test_multiline_mixed_formatting(self, adapter)` (method) — Multi-line message with headers, bold, links, code, and blockquotes.
- L2105 `test_markdown_unordered_list_with_asterisk(self, adapter)` (method) — Asterisk list items must not trigger italic conversion.
- L2112 `test_nested_bold_in_link(self, adapter)` (method) — Bold inside link label — label is stashed before bold pass.
- L2118 `test_url_with_query_string_and_ampersand(self, adapter)` (method) — Ampersand in URL query string must not be escaped.
- L2123 `test_markdown_image_does_not_create_broken_slack_link(self, adapter)` (method) — Markdown image syntax should not become '!<url|alt>' in Slack.
- L2128 `test_literal_asterisks_with_spaces_are_not_treated_as_italic(self, adapter)` (method) — Asterisks used as plain delimiters should stay literal.
- L2133 `test_emoji_shortcodes_passthrough(self, adapter)` (method) — Emoji shortcodes like :smile: pass through unchanged.
- L2143 `TestEditMessage` (class) — Verify that edit_message() applies mrkdwn formatting before sending.
- L2147 `test_edit_message_formats_bold(self, adapter)` (method) — edit_message converts **bold** to Slack *bold*.
- L2155 `test_edit_message_formats_links(self, adapter)` (method) — edit_message converts markdown links to Slack format.
- L2163 `test_edit_message_preserves_blockquotes(self, adapter)` (method) — edit_message preserves blockquote > markers.
- L2171 `test_edit_message_escapes_control_chars(self, adapter)` (method) — edit_message escapes & < > in plain text.
- L2184 `TestEditMessageStreamingPipeline` (class) — E2E: verify that sequential streaming edits all go through format_message.
- L2193 `test_edit_message_formats_streaming_updates(self, adapter)` (method) — Simulates streaming: multiple edits, each should be formatted.
- L2212 `test_edit_message_formats_code_and_bold(self, adapter)` (method) — Streaming update with code block and bold — code must be preserved.
- L2224 `test_edit_message_formats_blockquote_in_stream(self, adapter)` (method) — Streaming update with blockquote — '>' marker must survive.
- L2236 `test_edit_message_formats_progressive_accumulation(self, adapter)` (method) — Simulate real streaming: text grows with each edit, all formatted.
- L2259 `test_edit_message_formats_bold_italic(self, adapter)` (method) — Bold+italic ***text*** is formatted as *_text_* in edited messages.
- L2267 `test_edit_message_does_not_double_escape(self, adapter)` (method) — Pre-escaped entities in edited messages must not get double-escaped.
- L2278 `test_edit_message_formats_url_with_parens(self, adapter)` (method) — Wikipedia-style URL with parens survives edit pipeline.
- L2288 `test_edit_message_not_connected(self, adapter)` (method) — edit_message returns failure when adapter is not connected.
- L2301 `TestReactions` (class) — Test emoji reaction methods.
- L2305 `test_add_reaction_calls_api(self, adapter)` (method)
- L2314 `test_add_reaction_handles_error(self, adapter)` (method)
- L2322 `test_remove_reaction_calls_api(self, adapter)` (method)
- L2328 `test_reactions_in_message_flow(self, adapter)` (method) — Reactions should be bracketed around actual processing via hooks.
- L2386 `test_reactions_failure_outcome(self, adapter)` (method) — Failed processing should add :x: instead of :white_check_mark:.
- L2422 `test_reactions_skipped_for_non_dm_non_mention(self, adapter)` (method) — Non-DM, non-mention messages should not get reactions.
- L2445 `test_reactions_disabled_via_env(self, adapter, monkeypatch)` (method) — SLACK_REACTIONS=false should suppress all reaction lifecycle.
- L2496 `test_reactions_enabled_by_default(self, adapter)` (method) — SLACK_REACTIONS defaults to true (matches existing behavior).
- L2506 `TestThreadReplyHandling` (class) — Test thread reply processing without explicit bot mentions.
- L2510 `mock_session_store(self)` (method) — Create a mock session store with entries dict.
- L2520 `adapter_with_session_store(self, mock_session_store)` (method) — Create an adapter with a mock session store attached.
- L2534 `test_thread_reply_without_mention_no_session_ignored(self, adapter_with_session_store, mock_session_store)` (method) — Thread replies without mention should be ignored if no active session.
- L2553 `test_thread_reply_without_mention_with_session_processed(self, adapter_with_session_store, mock_session_store)` (method) — Thread replies without mention should be processed if there's an active session.
- L2578 `test_thread_reply_with_mention_strips_bot_id(self, adapter_with_session_store, mock_session_store)` (method) — Thread replies with @mention should still strip the bot ID.
- L2603 `test_top_level_message_requires_mention_even_with_session(self, adapter_with_session_store, mock_session_store)` (method) — Top-level channel messages should require mention even if session exists.
- L2624 `test_no_session_store_ignores_thread_replies(self, adapter)` (method) — If no session store is attached, thread replies without mention should be ignored.
- L2645 `TestAssistantThreadLifecycle` (class) — Slack Assistant lifecycle events should seed session/user context.
- L2649 `mock_session_store(self)` (method)
- L2659 `assistant_adapter(self, mock_session_store)` (method)
- L2672 `test_lifecycle_event_seeds_session_store(self, assistant_adapter, mock_session_store)` (method)
- L2701 `test_message_uses_cached_assistant_thread_identity(self, assistant_adapter)` (method)
- L2732 `test_assistant_threads_cache_eviction(self, assistant_adapter)` (method) — Cache should evict oldest entries when exceeding the size limit.
- L2764 `TestUserNameResolution` (class) — Test user identity resolution.
- L2768 `test_resolves_display_name(self, adapter)` (method)
- L2778 `test_falls_back_to_real_name(self, adapter)` (method)
- L2788 `test_caches_result(self, adapter)` (method)
- L2798 `test_handles_api_error(self, adapter)` (method)
- L2806 `test_user_name_in_message_source(self, adapter)` (method) — Message source should include resolved user name.
- L2833 `TestSlashCommands` (class) — Test slash command routing.
- L2837 `test_compact_maps_to_compress(self, adapter)` (method)
- L2844 `test_resume_command(self, adapter)` (method)
- L2851 `test_background_command(self, adapter)` (method)
- L2858 `test_usage_command(self, adapter)` (method)
- L2865 `test_reasoning_command(self, adapter)` (method)
- L2878 `test_native_btw_slash(self, adapter)` (method) — /btw with args must dispatch to /background, not /hermes btw.
- L2894 `test_native_stop_slash_no_args(self, adapter)` (method)
- L2906 `test_native_model_slash_with_args(self, adapter)` (method)
- L2918 `test_legacy_hermes_prefix_still_works(self, adapter)` (method) — Backward compat: /hermes btw foo must still route to /btw foo.
- L2936 `test_legacy_hermes_freeform_question(self, adapter)` (method) — /hermes <free-form text> must stay as the raw text (non-command).
- L2954 `TestMessageSplitting` (class) — Test that long messages are split before sending.
- L2958 `test_long_message_split_into_chunks(self, adapter)` (method) — Messages over MAX_MESSAGE_LENGTH should be split.
- L2967 `test_short_message_single_send(self, adapter)` (method) — Short messages should be sent in one call.
- L2974 `test_send_preserves_blockquote_formatting(self, adapter)` (method) — Blockquote '>' markers must survive format → chunk → send pipeline.
- L2984 `test_send_formats_bold_italic(self, adapter)` (method) — Bold+italic ***text*** is formatted as *_text_* in sent messages.
- L2992 `test_send_explicitly_enables_mrkdwn(self, adapter)` (method)
- L2999 `test_send_does_not_double_escape_entities(self, adapter)` (method) — Pre-escaped &amp; in sent messages must not become &amp;amp;.
- L3008 `test_send_formats_url_with_parens(self, adapter)` (method) — Wikipedia-style URL with parens survives send pipeline.
- L3021 `TestReplyBroadcast` (class) — Test reply_broadcast config option.
- L3025 `test_broadcast_disabled_by_default(self, adapter)` (method)
- L3032 `test_broadcast_enabled_via_config(self, adapter)` (method)
- L3045 `TestFallbackPreservesThreadContext` (class) — Bug fix: file upload fallbacks lost thread context (metadata) when
- L3051 `test_send_image_file_fallback_preserves_thread(self, adapter, tmp_path)` (method)
- L3072 `test_send_video_fallback_preserves_thread(self, adapter, tmp_path)` (method)
- L3092 `test_send_document_fallback_preserves_thread(self, adapter, tmp_path)` (method)
- L3113 `test_send_image_file_fallback_includes_caption(self, adapter, tmp_path)` (method)
- L3137 `TestSendImageSSRFGuards` (class) — send_image should reject redirects that land on private/internal hosts.
- L3141 `test_send_image_blocks_private_redirect_target(self, adapter)` (method)
- L3190 `test_send_image_fallback_preserves_thread_metadata(self, adapter)` (method)
- L3239 `TestProgressMessageThread` (class) — Verify that progress messages go to the correct thread.
- L3249 `test_dm_toplevel_progress_uses_message_ts_as_thread(self, adapter)` (method) — Progress messages for a top-level DM should go into the reply thread.
- L3308 `test_dm_toplevel_shares_session_when_disabled(self, adapter)` (method) — Opting out restores legacy single-session-per-DM-channel behavior.
- L3340 `test_channel_mention_progress_uses_thread_ts(self, adapter)` (method) — Progress messages for a channel @mention should go into the reply thread.
- L3374 `TestSlackReplyToText` (class) — Ensure MessageEvent.reply_to_text is populated on thread replies so
- L3380 `test_slack_reply_to_text_set_on_thread_reply(self, adapter)` (method) — When a thread reply arrives and the parent was posted by a bot
- L3426 `test_slack_reply_to_text_none_for_top_level_message(self, adapter)` (method) — Top-level messages (no thread_ts) must not set reply_to_text.
- L3454 `TestSlashEphemeralAck` (class) — Slash commands should produce an ephemeral ack and route replies ephemerally.
- L3458 `test_slash_command_stashes_response_url(self, adapter)` (method) — _handle_slash_command stashes response_url for later ephemeral routing.
- L3477 `test_slash_command_without_response_url_does_not_stash(self, adapter)` (method) — Commands without a response_url should not create a context.
- L3490 `test_pop_slash_context_returns_and_removes(self, adapter)` (method) — _pop_slash_context returns the context and removes it.
- L3506 `test_pop_slash_context_returns_none_for_no_match(self, adapter)` (method) — _pop_slash_context returns None when no context exists.
- L3512 `test_pop_slash_context_discards_stale_entries(self, adapter)` (method) — Stale contexts older than TTL are cleaned up.
- L3526 `test_send_uses_response_url_when_context_exists(self, adapter)` (method) — send() should POST to response_url for slash command replies.
- L3564 `test_send_falls_through_without_context(self, adapter)` (method) — send() should use normal chat_postMessage when no slash context exists.
- L3575 `test_send_slash_ephemeral_fallback_on_post_failure(self, adapter)` (method) — _send_slash_ephemeral returns success=True even if POST fails.
- L3604 `test_send_slash_ephemeral_fallback_on_exception(self, adapter)` (method) — _send_slash_ephemeral returns success=True even if aiohttp raises.
- L3626 `test_native_slash_stashes_context_and_dispatches(self, adapter)` (method) — Full flow: native /q slash → stash + handle_message dispatch.
- L3647 `test_legacy_hermes_slash_stashes_context(self, adapter)` (method) — Legacy /hermes <subcommand> also stashes context.
- L3662 `test_freeform_hermes_question_does_not_stash_context(self, adapter)` (method) — Free-form /hermes <question> must NOT route agent reply ephemeral.
- L3682 `test_concurrent_users_same_channel_isolates_contexts(self, adapter)` (method) — Two users slash on the same channel — each gets their own context.
- L3722 `test_no_contextvar_does_not_match_any_context(self, adapter)` (method) — send() without ContextVar (non-slash path) must not steal contexts.
