---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:43'
updated: '2026-06-08T00:38:43'
---

# tests/tools/test_browser_cleanup.py

Symbols in `tests/tools/test_browser_cleanup.py`.

- L6 `TestScreenshotPathRecovery` (class)
- L7 `test_extracts_standard_absolute_path(self)` (method)
- L15 `test_extracts_quoted_absolute_path(self)` (method)
- L26 `TestBrowserCleanup` (class)
- L27 `setup_method(self)` (method)
- L36 `teardown_method(self)` (method)
- L45 `test_cleanup_browser_clears_tracking_state(self)` (method)
- L68 `test_cleanup_camofox_managed_persistence_skips_close(self)` (method) — When camofox mode + managed persistence, soft_cleanup fires instead of close.
- L96 `test_cleanup_camofox_no_persistence_calls_close(self)` (method) — When camofox mode but managed persistence is off, camofox_close fires.
- L124 `test_emergency_cleanup_clears_all_tracking_state(self)` (method)
