---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:41'
updated: '2026-06-08T00:38:41'
---

# run_agent.py

Symbols in `run_agent.py`.

- L68 `_launch_cwd_for_session(source: str)` (function) — Working directory to stamp on a new session row, or None.
- L224 `_routermint_headers()` (function) — Return the User-Agent RouterMint needs to avoid Cloudflare 1010 blocks.
- L233 `_pool_may_recover_from_rate_limit(pool, *, provider: str | None=None, base_url: str | None=None)` (function) — Decide whether to wait for credential-pool rotation instead of falling back.
- L268 `_qwen_portal_headers()` (function) — Return default HTTP headers required by Qwen Portal API.
- L281 `_StreamErrorEvent` (class) — Synthesized provider error surfaced from a Responses ``error`` SSE frame.
- L295 `__init__(self, message: str, *, code: Optional[str]=None, param: Optional[str]=None, status_code: Optional[int]=None)` (method)
- L320 `AIAgent` (class) — AI Agent with tool calling capabilities.
- L334 `base_url(self)` (method)
- L338 `base_url(self, value: str)` (method)
- L343 `__init__(self, base_url: str=None, api_key: str=None, provider: str=None, api_mode: str=None, acp_command: str=None, acp_args: list[str] | None=None, command: str=None, args: list[str] | None=None, model: str='', max_iterations: int=90, tool_delay: float=1.0, enabled_toolsets: List[str]=None, disabled_toolsets: List[str]=None, save_trajectories: bool=False, verbose_logging: bool=False, quiet_mode: bool=False, ephemeral_system_prompt: str=None, log_prefix_chars: int=100, log_prefix: str='', providers_allowed: List[str]=None, providers_ignored: List[str]=None, providers_order: List[str]=None, provider_sort: str=None, provider_require_parameters: bool=False, provider_data_collection: str=None, openrouter_min_coding_score: Optional[float]=None, session_id: str=None, tool_progress_callback: callable=None, tool_start_callback: callable=None, tool_complete_callback: callable=None, thinking_callback: callable=None, reasoning_callback: callable=None, clarify_callback: callable=None, step_callback: callable=None, stream_delta_callback: callable=None, interim_assistant_callback: callable=None, tool_gen_callback: callable=None, status_callback: callable=None, notice_callback: callable=None, notice_clear_callback: callable=None, max_tokens: int=None, reasoning_config: Dict[str, Any]=None, service_tier: str=None, request_overrides: Dict[str, Any]=None, prefill_messages: List[Dict[str, Any]]=None, platform: str=None, user_id: str=None, user_id_alt: str=None, user_name: str=None, chat_id: str=None, chat_name: str=None, chat_type: str=None, thread_id: str=None, gateway_session_key: str=None, skip_context_files: bool=False, load_soul_identity: bool=False, skip_memory: bool=False, session_db=None, parent_session_id: str=None, iteration_budget: 'IterationBudget'=None, fallback_model: Dict[str, Any]=None, credential_pool=None, checkpoints_enabled: bool=False, checkpoint_max_snapshots: int=20, checkpoint_max_total_size_mb: int=500, checkpoint_max_file_size_mb: int=10, pass_session_id: bool=False)` (method) — Forwarder — see ``agent.agent_init.init_agent``.
- L486 `_get_session_db_for_recall(self)` (method) — Return a SessionDB for recall, lazily creating it if an entrypoint forgot.
- L505 `_ensure_db_session(self)` (method) — Create session DB row on first use. Disables _session_db on failure.
- L529 `_transition_context_engine_session(self, *, old_session_id: Optional[str]=None, new_session_id: Optional[str]=None, previous_messages: Optional[list]=None, carry_over_context: bool=False, reset_engine: bool=True, **extra_context)` (method) — Notify the active context engine about a host session transition.
- L598 `reset_session_state(self, previous_messages: Optional[list]=None, old_session_id: Optional[str]=None, carry_over_context: bool=False)` (method) — Reset all session-scoped token counters to 0 for a fresh session.
- L650 `_ensure_lmstudio_runtime_loaded(self, config_context_length: Optional[int]=None)` (method) — Preload the LM Studio model with at least Hermes' minimum context.
- L683 `switch_model(self, new_model, new_provider, api_key='', base_url='', api_mode='')` (method) — Forwarder — see ``agent.agent_runtime_helpers.switch_model``.
- L688 `_safe_print(self, *args, **kwargs)` (method) — Print that silently handles broken pipes / closed stdout.
- L706 `_vprint(self, *args, force: bool=False, **kwargs)` (method) — Verbose print — suppressed when actively streaming tokens.
- L733 `_should_start_quiet_spinner(self)` (method) — Return True when quiet-mode spinner output has a safe sink.
- L752 `_should_emit_quiet_tool_messages(self)` (method) — Return True when quiet-mode tool summaries should print directly.
- L766 `_emit_status(self, message: str)` (method) — Emit a lifecycle status message to both CLI and gateway channels.
- L786 `_emit_warning(self, message: str)` (method) — Emit a user-visible warning through the same status plumbing.
- L803 `_emit_notice(self, notice)` (method) — Fire a structured ``AgentNotice`` to the active driver (TUI / CLI).
- L816 `_emit_notice_clear(self, key: str)` (method) — Clear a previously-fired sticky notice by ``key`` (e.g. on recovery).
- L835 `_buffer_status(self, message: str)` (method) — Buffer a retry/fallback status message.
- L855 `_buffer_vprint(self, message: str)` (method) — Buffer a vprint(force=True) retry/fallback line.
- L866 `_clear_status_buffer(self)` (method) — Drop buffered retry messages — call on successful recovery.
- L875 `_flush_status_buffer(self)` (method) — Emit buffered retry messages — call on terminal failure.
- L901 `_disable_codex_reasoning_replay(self, messages: Optional[List[Dict[str, Any]]]=None)` (method) — Disable Responses encrypted reasoning replay and strip cached state.
- L939 `_stream_diag_init()` (method) — Forwarder — see ``agent.stream_diag.stream_diag_init``.
- L944 `_stream_diag_capture_response(self, diag: Dict[str, Any], http_response: Any)` (method) — Forwarder — see ``agent.stream_diag.stream_diag_capture_response``.
- L952 `_flatten_exception_chain(error: BaseException)` (method) — Forwarder — see ``agent.stream_diag.flatten_exception_chain``.
- L957 `_is_provider_stream_parse_error(self, error: BaseException)` (method) — Return True for malformed provider streaming data from SDK parsers.
- L975 `_log_stream_retry(self, *, kind: str, error: BaseException, attempt: int, max_attempts: int, mid_tool_call: bool, diag: Optional[Dict[str, Any]]=None)` (method) — Forwarder — see ``agent.stream_diag.log_stream_retry``.
- L992 `_emit_stream_drop(self, *, error: BaseException, attempt: int, max_attempts: int, mid_tool_call: bool, diag: Optional[Dict[str, Any]]=None)` (method) — Forwarder — see ``agent.stream_diag.emit_stream_drop``.
- L1008 `_emit_auxiliary_failure(self, task: str, exc: BaseException)` (method) — Surface a compact warning for failed auxiliary work.
- L1019 `_current_main_runtime(self)` (method) — Return the live main runtime for session-scoped auxiliary routing.
- L1029 `_check_compression_model_feasibility(self)` (method) — Forwarder — see ``agent.conversation_compression.check_compression_model_feasibility``.
- L1034 `_replay_compression_warning(self)` (method) — Forwarder — see ``agent.conversation_compression.replay_compression_warning``.
- L1039 `_is_direct_openai_url(self, base_url: str=None)` (method) — Return True when a base URL targets OpenAI's native API.
- L1049 `_is_azure_openai_url(self, base_url: str=None)` (method) — Return True when a base URL targets Azure OpenAI.
- L1065 `_is_github_copilot_url(self, base_url: str=None)` (method) — Return True when a base URL targets GitHub Copilot's OpenAI-compatible API.
- L1075 `_resolved_api_call_timeout(self)` (method) — Resolve the effective per-call request timeout in seconds.
- L1095 `_resolved_api_call_stale_timeout_base(self)` (method) — Resolve the base non-stream stale timeout and whether it is implicit.
- L1123 `_compute_non_stream_stale_timeout(self, api_payload: Any)` (method) — Compute the effective non-stream stale timeout for this request.
- L1144 `_codex_silent_hang_hint(self, model: Optional[str]=None)` (method) — Return an actionable hint when this request matches a known
- L1196 `_is_openrouter_url(self)` (method) — Return True when the base URL targets OpenRouter.
- L1200 `_anthropic_prompt_cache_policy(self, *, provider: Optional[str]=None, base_url: Optional[str]=None, api_mode: Optional[str]=None, model: Optional[str]=None)` (method) — Forwarder — see ``agent.agent_runtime_helpers.anthropic_prompt_cache_policy``.
- L1213 `_model_requires_responses_api(model: str)` (method) — Return True for models that require the Responses API path.
- L1228 `_provider_model_requires_responses_api(model: str, *, provider: Optional[str]=None)` (method) — Return True when this provider/model pair should use Responses API.
- L1249 `_max_tokens_param(self, value: int)` (method) — Return the correct max tokens kwarg for the current provider.
- L1263 `_requested_output_cap_from_api_kwargs(api_kwargs: Any)` (method) — Extract the outgoing response token cap from a prepared request.
- L1277 `_has_content_after_think_block(self, content: str)` (method) — Check if content has actual text after any reasoning/thinking blocks.
- L1300 `_strip_think_blocks(self, content: str)` (method) — Forwarder — see ``agent.agent_runtime_helpers.strip_think_blocks``.
- L1306 `_has_natural_response_ending(content: str)` (method) — Heuristic: does visible assistant text look intentionally finished?
- L1325 `_is_ollama_glm_backend(self)` (method) — Detect the narrow backend family affected by Ollama/GLM stop misreports.
- L1335 `_should_treat_stop_as_truncated(self, finish_reason: str, assistant_message, messages: Optional[list]=None)` (method) — Detect conservative stop->length misreports for Ollama-hosted GLM models.
- L1366 `_looks_like_codex_intermediate_ack(self, user_message: str, assistant_content: str, messages: List[Dict[str, Any]])` (method) — Forwarder — see ``agent.agent_runtime_helpers.looks_like_codex_intermediate_ack``.
- L1376 `_extract_reasoning(self, assistant_message)` (method) — Forwarder — see ``agent.agent_runtime_helpers.extract_reasoning``.
- L1381 `_cleanup_task_resources(self, task_id: str)` (method) — Forwarder — see ``agent.chat_completion_helpers.cleanup_task_resources``.
- L1396 `_summarize_background_review_actions(review_messages: List[Dict], prior_snapshot: List[Dict])` (method) — Forwarder — see ``agent.background_review.summarize_background_review_actions``.
- L1404 `_spawn_background_review(self, messages_snapshot: List[Dict], review_memory: bool=False, review_skills: bool=False)` (method) — Spawn the background memory/skill review thread.
- L1428 `_build_memory_write_metadata(self, *, write_origin: Optional[str]=None, execution_context: Optional[str]=None, task_id: Optional[str]=None, tool_call_id: Optional[str]=None)` (method) — Forwarder — see ``agent.background_review.build_memory_write_metadata``.
- L1446 `_apply_persist_user_message_override(self, messages: List[Dict])` (method) — Rewrite the current-turn user message before persistence/return.
- L1464 `_persist_session(self, messages: List[Dict], conversation_history: List[Dict]=None)` (method) — Save session state to both JSON log and SQLite on any exit path.
- L1475 `_drop_trailing_empty_response_scaffolding(self, messages: List[Dict])` (method) — Remove private empty-response retry/failure scaffolding from transcript tails.
- L1528 `_repair_message_sequence(self, messages: List[Dict])` (method) — Forwarder — see ``agent.agent_runtime_helpers.repair_message_sequence``.
- L1533 `_flush_messages_to_session_db(self, messages: List[Dict], conversation_history: List[Dict]=None)` (method) — Persist any un-flushed messages to the SQLite session store.
- L1592 `_get_messages_up_to_last_assistant(self, messages: List[Dict])` (method) — Get messages up to (but not including) the last assistant turn.
- L1623 `_format_tools_for_system_message(self)` (method) — Forwarder — see ``agent.system_prompt.format_tools_for_system_message``.
- L1628 `_convert_to_trajectory_format(self, messages: List[Dict[str, Any]], user_query: str, completed: bool)` (method) — Forwarder — see ``agent.agent_runtime_helpers.convert_to_trajectory_format``.
- L1633 `_save_trajectory(self, messages: List[Dict[str, Any]], user_query: str, completed: bool)` (method) — Save conversation trajectory to JSONL file.
- L1649 `_is_entitlement_failure(error_context: Optional[Dict[str, Any]], status_code: Optional[int])` (method) — Detect subscription/entitlement 403s that masquerade as auth failures.
- L1717 `_decorate_xai_entitlement_error(detail: str)` (method) — Append a neutral hint when xAI's OAuth surface returns the
- L1774 `_summarize_api_error(error: Exception)` (method) — Extract a human-readable one-liner from an API error.
- L1819 `_mask_api_key_for_logs(self, key: Any)` (method)
- L1830 `_clean_error_message(self, error_msg: str)` (method) — Clean up error messages for user display, removing HTML content and truncating.
- L1857 `_extract_api_error_context(error: Exception)` (method) — Forwarder — see ``agent.agent_runtime_helpers.extract_api_error_context``.
- L1862 `_usage_summary_for_api_request_hook(self, response: Any)` (method) — Token buckets for ``post_api_request`` plugins (no raw ``response`` object).
- L1879 `_hook_payload_max_chars()` (method)
- L1887 `_is_sensitive_hook_key(key: Any)` (method)
- L1901 `_hook_jsonable(cls, value: Any, *, depth: int=0, max_depth: int=8, max_string: int=8000, max_sequence: int=200)` (method)
- L2007 `_sanitize_hook_payload(cls, value: Any)` (method)
- L2029 `_api_request_payload_for_hook(self, api_kwargs: Optional[Dict[str, Any]])` (method)
- L2042 `_api_response_payload_for_hook(self, response: Any, assistant_message: Any, *, finish_reason: Optional[str])` (method)
- L2070 `_invoke_api_request_error_hook(self, *, task_id: str, turn_id: str, api_request_id: str, api_call_count: int, api_start_time: float, api_kwargs: Optional[Dict[str, Any]], error_type: str, error_message: str, status_code: Optional[int]=None, retry_count: Optional[int]=None, max_retries: Optional[int]=None, retryable: Optional[bool]=None, reason: Optional[str]=None)` (method)
- L2126 `_dump_api_request_debug(self, api_kwargs: Dict[str, Any], *, reason: str, error: Optional[Exception]=None)` (method) — Forwarder — see ``agent.agent_runtime_helpers.dump_api_request_debug``.
- L2138 `_clean_session_content(content: str)` (method) — Convert REASONING_SCRATCHPAD to think tags and clean up whitespace.
- L2148 `_redact_message_content(content)` (method) — Apply secret redaction to message content (str or list-of-parts).
- L2177 `_save_session_log(self, messages: List[Dict[str, Any]]=None)` (method) — Optional per-session JSON snapshot writer.
- L2263 `interrupt(self, message: str=None)` (method) — Request the agent to interrupt its current tool-calling loop.
- L2331 `clear_interrupt(self)` (method) — Clear any pending interrupt request and the per-thread tool interrupt signal.
- L2364 `steer(self, text: str)` (method) — Inject a user message into the next tool result without interrupting.
- L2400 `_drain_pending_steer(self)` (method) — Return the pending steer text (if any) and clear the slot.
- L2416 `_record_file_mutation_result(self, tool_name: str, args: Dict[str, Any], result: Any, is_error: bool)` (method) — Record a ``write_file`` / ``patch`` outcome for the turn-end verifier.
- L2455 `_file_mutation_verifier_enabled(self)` (method) — Check whether the per-turn file-mutation verifier footer is on.
- L2495 `_neutralize_footer_paths(cls, text: str)` (method) — Wrap bare file paths in backticks so they aren't auto-delivered.
- L2512 `_format_file_mutation_failure_footer(cls, failed: Dict[str, Dict[str, Any]])` (method) — Render the per-turn failed-mutation dict as a user-facing footer.
- L2552 `_turn_completion_explainer_enabled(self)` (method) — Check whether the end-of-turn completion explainer footer is on.
- L2580 `_format_turn_completion_explanation(turn_exit_reason: str)` (method) — Render a user-facing explanation for an abnormal turn ending.
- L2672 `_apply_pending_steer_to_tool_results(self, messages: list, num_tool_msgs: int)` (method) — Forwarder — see ``agent.agent_runtime_helpers.apply_pending_steer_to_tool_results``.
- L2677 `_touch_activity(self, desc: str)` (method) — Update the last-activity timestamp and description (thread-safe).
- L2699 `_capture_rate_limits(self, http_response: Any)` (method) — Parse x-ratelimit-* headers from an HTTP response and cache the state.
- L2718 `get_rate_limit_state(self)` (method) — Return the last captured RateLimitState, or None.
- L2722 `_capture_credits(self, http_response: Any)` (method) — Parse x-nous-credits-* headers, cache CreditsState, fire threshold notices.
- L2803 `_emit_credits_notices(self)` (method) — Run the threshold policy on the current credits state and emit notices.
- L2831 `get_credits_state(self)` (method) — Return the last captured CreditsState, or None.
- L2835 `get_credits_spent_micros(self)` (method) — Session-cumulative micros spent = first_seen_remaining - current_remaining. None if no data.
- L2841 `_check_openrouter_cache_status(self, http_response: Any)` (method) — Read X-OpenRouter-Cache-Status from response headers and log it.
- L2863 `get_activity_summary(self)` (method) — Return a snapshot of the agent's current activity for diagnostics.
- L2881 `shutdown_memory_provider(self, messages: list=None)` (method) — Shut down the memory provider and context engine — call at actual session boundaries.
- L2908 `commit_memory_session(self, messages: list=None)` (method) — Trigger end-of-session extraction without tearing providers down.
- L2933 `_sync_external_memory_for_turn(self, *, original_user_message: Any, final_response: Any, interrupted: bool, messages: list | None=None)` (method) — Mirror a completed turn into external memory providers.
- L2987 `release_clients(self)` (method) — Release LLM client resources WITHOUT tearing down session tool state.
- L3034 `close(self)` (method) — Release all resources held by this agent instance.
- L3090 `_hydrate_todo_store(self, history: List[Dict[str, Any]])` (method) — Recover todo state from conversation history.
- L3123 `is_interrupted(self)` (method) — Check if an interrupt has been requested.
- L3136 `_build_system_prompt_parts(self, system_message: str=None)` (method) — Forwarder — see ``agent.system_prompt.build_system_prompt_parts``.
- L3141 `_build_system_prompt(self, system_message: str=None)` (method) — Forwarder — see ``agent.system_prompt.build_system_prompt``.
- L3147 `_get_tool_call_id_static(tc)` (method) — Extract call ID from a tool_call entry (dict or object).
- L3154 `_get_tool_call_name_static(tc)` (method) — Extract function name from a tool_call entry (dict or object).
- L3173 `_sanitize_api_messages(messages: List[Dict[str, Any]])` (method) — Forwarder — see ``agent.agent_runtime_helpers.sanitize_api_messages``.
- L3179 `_is_thinking_only_assistant(msg: Dict[str, Any])` (method) — Return True if ``msg`` is an assistant turn whose only payload is reasoning.
- L3233 `_drop_thinking_only_and_merge_users(messages: List[Dict[str, Any]])` (method) — Forwarder — see ``agent.agent_runtime_helpers.drop_thinking_only_and_merge_users``.
- L3241 `_cap_delegate_task_calls(tool_calls: list)` (method) — Truncate excess delegate_task calls to max_concurrent_children.
- L3272 `_deduplicate_tool_calls(tool_calls: list)` (method) — Remove duplicate (tool_name, arguments) pairs within a single turn.
- L3289 `_repair_tool_call(self, tool_name: str)` (method) — Forwarder — see ``agent.agent_runtime_helpers.repair_tool_call``.
- L3294 `_invalidate_system_prompt(self)` (method) — Forwarder — see ``agent.system_prompt.invalidate_system_prompt``.
- L3300 `_deterministic_call_id(fn_name: str, arguments: str, index: int=0)` (method) — Generate a deterministic call_id from tool call content.
- L3310 `_split_responses_tool_id(raw_id: Any)` (method) — Split a stored tool id into (call_id, response_item_id).
- L3314 `_derive_responses_function_call_id(self, call_id: str, response_item_id: Optional[str]=None)` (method) — Build a valid Responses `function_call.id` (must start with `fc_`).
- L3322 `_thread_identity(self)` (method)
- L3326 `_client_log_context(self)` (method)
- L3335 `_openai_client_lock(self)` (method)
- L3343 `_is_openai_client_closed(client: Any)` (method) — Check if an OpenAI client is closed.
- L3373 `_build_keepalive_http_client(base_url: str='')` (method)
- L3397 `_create_openai_client(self, client_kwargs: dict, *, reason: str, shared: bool)` (method) — Forwarder — see ``agent.agent_runtime_helpers.create_openai_client``.
- L3403 `_force_close_tcp_sockets(client: Any)` (method) — Forwarder — see ``agent.agent_runtime_helpers.force_close_tcp_sockets``.
- L3408 `_close_openai_client(self, client: Any, *, reason: str, shared: bool)` (method)
- L3432 `_replace_primary_openai_client(self, *, reason: str)` (method)
- L3449 `_ensure_primary_openai_client(self, *, reason: str)` (method)
- L3465 `_cleanup_dead_connections(self)` (method) — Forwarder — see ``agent.agent_runtime_helpers.cleanup_dead_connections``.
- L3471 `_api_kwargs_have_image_parts(api_kwargs: dict)` (method) — Return True when the outbound request still contains native image parts.
- L3497 `_copilot_headers_for_request(self, *, is_vision: bool)` (method)
- L3502 `_create_request_openai_client(self, *, reason: str, api_kwargs: Optional[dict]=None)` (method)
- L3528 `_close_request_openai_client(self, client: Any, *, reason: str)` (method)
- L3531 `_abort_request_openai_client(self, client: Any, *, reason: str)` (method) — Cross-thread abort: shut sockets down without releasing FDs.
- L3564 `_run_codex_stream(self, api_kwargs: dict, client: Any=None, on_first_delta: callable=None)` (method) — Forwarder — see ``agent.codex_runtime.run_codex_stream``.
- L3569 `_run_codex_create_stream_fallback(self, api_kwargs: dict, client: Any=None)` (method) — Forwarder — see ``agent.codex_runtime.run_codex_create_stream_fallback``.
- L3574 `_try_refresh_codex_client_credentials(self, *, force: bool=True)` (method)
- L3648 `_try_refresh_nous_client_credentials(self, *, force: bool=True)` (method)
- L3686 `_try_refresh_copilot_client_credentials(self)` (method) — Refresh Copilot credentials and rebuild the shared OpenAI client.
- L3721 `_try_refresh_anthropic_client_credentials(self)` (method)
- L3772 `_apply_client_headers_for_base_url(self, base_url: str)` (method)
- L3817 `_apply_user_default_headers(self)` (method) — Merge user-configured request headers onto the OpenAI client.
- L3847 `_swap_credential(self, entry)` (method)
- L3877 `_recover_with_credential_pool(self, *, status_code: Optional[int], has_retried_429: bool, classified_reason: Optional[FailoverReason]=None, error_context: Optional[Dict[str, Any]]=None)` (method) — Forwarder — see ``agent.agent_runtime_helpers.recover_with_credential_pool``.
- L3889 `_credential_pool_may_recover_rate_limit(self)` (method) — Whether a rate-limit retry should wait for same-provider credentials.
- L3904 `_anthropic_messages_create(self, api_kwargs: dict)` (method)
- L3909 `_rebuild_anthropic_client(self)` (method) — Rebuild the Anthropic client after an interrupt or stale call.
- L3935 `_interruptible_api_call(self, api_kwargs: dict)` (method) — Forwarder — see ``agent.chat_completion_helpers.interruptible_api_call``.
- L3942 `_reset_stream_delivery_tracking(self)` (method) — Reset tracking for text delivered during the current model response.
- L3983 `_record_streamed_assistant_text(self, text: str)` (method) — Accumulate visible assistant text emitted through stream callbacks.
- L3991 `_normalize_interim_visible_text(text: str)` (method)
- L3996 `_interim_content_was_streamed(self, content: str)` (method)
- L4007 `_emit_interim_assistant_message(self, assistant_msg: Dict[str, Any])` (method) — Surface a real mid-turn assistant commentary message to the UI layer.
- L4022 `_fire_stream_delta(self, text: str)` (method) — Fire all registered stream delta callbacks (display + TTS).
- L4075 `_fire_reasoning_delta(self, text: str)` (method) — Fire reasoning callback if registered.
- L4084 `_fire_tool_gen_started(self, tool_name: str)` (method) — Notify display layer that the model is generating tool call arguments.
- L4099 `_has_stream_consumers(self)` (method) — Return True if any streaming consumer is registered.
- L4106 `_interruptible_streaming_api_call(self, api_kwargs: dict, *, on_first_delta: callable=None)` (method) — Forwarder — see ``agent.chat_completion_helpers.interruptible_streaming_api_call``.
- L4113 `_try_activate_fallback(self, reason: 'FailoverReason | None'=None)` (method) — Forwarder — see ``agent.chat_completion_helpers.try_activate_fallback``.
- L4118 `_has_pending_fallback(self)` (method) — Whether a fallback provider is actually available to switch to.
- L4132 `_restore_primary_runtime(self)` (method) — Forwarder — see ``agent.agent_runtime_helpers.restore_primary_runtime``.
- L4137 `_try_recover_primary_transport(self, api_error: Exception, *, retry_count: int, max_retries: int)` (method) — Forwarder — see ``agent.agent_runtime_helpers.try_recover_primary_transport``.
- L4145 `_content_has_image_parts(content: Any)` (method)
- L4154 `_materialize_data_url_for_vision(image_url: str)` (method)
- L4183 `_describe_image_for_anthropic_fallback(self, image_url: str, role: str)` (method)
- L4234 `_model_supports_vision(self)` (method) — Return True if the active provider+model reports native vision.
- L4258 `_preprocess_anthropic_content(self, content: Any, role: str)` (method)
- L4302 `_get_transport(self, api_mode: str=None)` (method) — Return the cached transport for the given (or current) api_mode.
- L4320 `_prepare_anthropic_messages_for_api(self, api_messages: list)` (method)
- L4348 `_prepare_messages_for_non_vision_model(self, api_messages: list)` (method) — Strip native image parts when the active model lacks vision.
- L4380 `_tool_result_content_for_active_model(self, tool_name: str, result: Any)` (method) — Return the tool message content that is safe for the active model.
- L4439 `_try_shrink_image_parts_in_messages(self, api_messages: list)` (method) — Forwarder — see ``agent.conversation_compression.try_shrink_image_parts_in_messages``.
- L4444 `_try_strip_image_parts_from_tool_messages(self, api_messages: list)` (method) — Downgrade list-type tool messages to text summaries in-place.
- L4518 `_anthropic_preserve_dots(self)` (method) — True when using an anthropic-compatible endpoint that preserves dots in model names.
- L4552 `_is_qwen_portal(self)` (method) — Return True when the base URL targets Qwen Portal.
- L4556 `_qwen_prepare_chat_messages(self, api_messages: list)` (method)
- L4589 `_qwen_prepare_chat_messages_inplace(self, messages: list)` (method) — In-place variant — mutates an already-copied message list.
- L4617 `_build_api_kwargs(self, api_messages: list)` (method) — Forwarder — see ``agent.chat_completion_helpers.build_api_kwargs``.
- L4622 `_supports_reasoning_extra_body(self)` (method) — Return True when reasoning extra_body is safe to send for this route/model.
- L4664 `_lmstudio_reasoning_options_cached(self)` (method) — Probe LM Studio's published reasoning ``allowed_options`` once per
- L4698 `_resolve_lmstudio_summary_reasoning_effort(self)` (method) — Resolve a safe top-level ``reasoning_effort`` for LM Studio.
- L4711 `_github_models_reasoning_extra_body(self)` (method) — Format reasoning payload for GitHub Models/OpenAI-compatible routes.
- L4743 `_build_assistant_message(self, assistant_message, finish_reason: str)` (method) — Forwarder — see ``agent.chat_completion_helpers.build_assistant_message``.
- L4748 `_needs_thinking_reasoning_pad(self)` (method) — Return True when the active provider enforces reasoning_content echo-back.
- L4775 `_needs_kimi_tool_reasoning(self)` (method) — Return True when the current provider is Kimi / Moonshot thinking mode.
- L4795 `_needs_deepseek_tool_reasoning(self)` (method) — Return True when the current provider is DeepSeek thinking mode.
- L4810 `_needs_mimo_tool_reasoning(self)` (method) — Return True when the current provider is Xiaomi MiMo thinking mode.
- L4826 `_copy_reasoning_content_for_api(self, source_msg: dict, api_msg: dict)` (method) — Forwarder — see ``agent.agent_runtime_helpers.copy_reasoning_content_for_api``.
- L4831 `_reapply_reasoning_echo_for_provider(self, api_messages: list)` (method) — Forwarder — see ``agent.agent_runtime_helpers.reapply_reasoning_echo_for_provider``.
- L4837 `_sanitize_tool_calls_for_strict_api(api_msg: dict, model: 'str | None'=None)` (method) — Strip Codex Responses API fields from tool_calls for strict providers.
- L4874 `_sanitize_tool_call_arguments(messages: list, *, logger=None, session_id: str=None)` (method) — Forwarder — see ``agent.agent_runtime_helpers.sanitize_tool_call_arguments``.
- L4884 `_should_sanitize_tool_calls(self)` (method) — Determine if tool_calls need sanitization for strict APIs.
- L4897 `_compress_context(self, messages: list, system_message: str, *, approx_tokens: int=None, task_id: str='default', focus_topic: str=None, force: bool=False)` (method) — Forwarder — see ``agent.conversation_compression.compress_context``.
- L4912 `_set_tool_guardrail_halt(self, decision: ToolGuardrailDecision)` (method) — Record the first guardrail decision that should stop this turn.
- L4917 `_toolguard_controlled_halt_response(self, decision: ToolGuardrailDecision)` (method)
- L4926 `_append_guardrail_observation(self, tool_name: str, function_args: dict, function_result: str, *, failed: bool)` (method)
- L4946 `_guardrail_block_result(self, decision: ToolGuardrailDecision)` (method)
- L4950 `_execute_tool_calls(self, assistant_message, messages: list, effective_task_id: str, api_call_count: int=0)` (method) — Execute tool calls from the assistant message and append results to messages.
- L4973 `_dispatch_delegate_task(self, function_args: dict)` (method) — Single call site for delegate_task dispatch.
- L4992 `_invoke_tool(self, function_name: str, function_args: dict, effective_task_id: str, tool_call_id: Optional[str]=None, messages: list=None, pre_tool_block_checked: bool=False, skip_tool_request_middleware: bool=False, tool_request_middleware_trace: Optional[list[dict[str, Any]]]=None)` (method) — Forwarder — see ``agent.agent_runtime_helpers.invoke_tool``.
- L5012 `_wrap_verbose(label: str, text: str, indent: str='     ')` (method) — Word-wrap verbose tool output to fit the terminal width.
- L5036 `_execute_tool_calls_concurrent(self, assistant_message, messages: list, effective_task_id: str, api_call_count: int=0)` (method) — Forwarder — see ``agent.tool_executor.execute_tool_calls_concurrent``.
- L5041 `_execute_tool_calls_sequential(self, assistant_message, messages: list, effective_task_id: str, api_call_count: int=0)` (method) — Forwarder — see ``agent.tool_executor.execute_tool_calls_sequential``.
- L5046 `_handle_max_iterations(self, messages: list, api_call_count: int)` (method) — Forwarder — see ``agent.chat_completion_helpers.handle_max_iterations``.
- L5051 `run_conversation(self, user_message: str, system_message: str=None, conversation_history: List[Dict[str, Any]]=None, task_id: str=None, stream_callback: Optional[callable]=None, persist_user_message: Optional[str]=None)` (method) — Forwarder — see ``agent.conversation_loop.run_conversation``.
- L5064 `chat(self, message: str, stream_callback: Optional[callable]=None)` (method) — Simple chat interface that returns just the final response.
- L5078 `_run_codex_app_server_turn(self, *, user_message: str, original_user_message: Any, messages: List[Dict[str, Any]], effective_task_id: str, should_review_memory: bool=False)` (method) — Forwarder — see ``agent.codex_runtime.run_codex_app_server_turn``.
- L5091 `main(query: str=None, model: str='', api_key: str=None, base_url: str='', max_turns: int=10, enabled_toolsets: str=None, disabled_toolsets: str=None, list_tools: bool=False, save_trajectories: bool=False, save_sample: bool=False, verbose: bool=False, log_prefix_chars: int=20)` (function) — Main function for running the agent directly.
