---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/gateway/test_dingtalk.py

Symbols in `tests/gateway/test_dingtalk.py`.

- L12 `_FakeDingTalkModel` (class)
- L13 `__init__(self, **kwargs)` (method)
- L17 `_FakeChatbotMessage` (class)
- L19 `from_dict(cls, data)` (method)
- L40 `_fake_dingtalk_optional_sdks(monkeypatch)` (function) — Keep DingTalk adapter tests hermetic when optional SDKs are absent.
- L91 `TestDingTalkRequirements` (class)
- L93 `test_returns_false_when_sdk_missing(self, monkeypatch)` (method)
- L102 `test_returns_false_when_env_vars_missing(self, monkeypatch)` (method)
- L112 `test_returns_true_when_all_available(self, monkeypatch)` (method)
- L128 `TestDingTalkAdapterInit` (class)
- L130 `test_reads_config_from_extra(self)` (method)
- L141 `test_falls_back_to_env_vars(self, monkeypatch)` (method)
- L156 `TestExtractText` (class)
- L158 `test_extracts_dict_text(self)` (method)
- L165 `test_extracts_string_text(self)` (method)
- L172 `test_falls_back_to_rich_text(self)` (method)
- L179 `test_returns_empty_for_no_content(self)` (method)
- L192 `TestDeduplication` (class)
- L194 `test_first_message_not_duplicate(self)` (method)
- L199 `test_second_same_message_is_duplicate(self)` (method)
- L205 `test_different_messages_not_duplicate(self)` (method)
- L211 `test_cache_cleanup_on_overflow(self)` (method)
- L227 `TestSend` (class)
- L230 `test_send_posts_to_webhook(self)` (method)
- L256 `test_send_fails_without_webhook(self)` (method)
- L266 `test_send_uses_cached_webhook(self)` (method)
- L282 `test_send_handles_http_error(self)` (method)
- L301 `test_send_image_renders_markdown_image(self)` (method)
- L326 `test_send_image_file_returns_explicit_unsupported_error(self)` (method)
- L336 `test_send_document_returns_explicit_unsupported_error(self)` (method)
- L351 `TestConnect` (class)
- L354 `test_disconnect_closes_session_websocket(self)` (method)
- L377 `test_connect_fails_without_sdk(self, monkeypatch)` (method)
- L387 `test_connect_fails_without_credentials(self)` (method)
- L396 `test_disconnect_cleans_up(self)` (method)
- L410 `test_disconnect_finalizes_open_streaming_cards(self)` (method) — Streaming cards must be finalized before HTTP client closes.
- L450 `TestWebhookDomainAllowlist` (class) — Guard the webhook origin allowlist against regression.
- L458 `test_api_domain_accepted(self)` (method)
- L464 `test_oapi_domain_accepted(self)` (method)
- L470 `test_http_rejected(self)` (method)
- L474 `test_suffix_attack_rejected(self)` (method)
- L480 `test_unsanctioned_subdomain_rejected(self)` (method)
- L486 `TestHandlerProcessIsAsync` (class) — dingtalk-stream >= 0.20 requires ``process`` to be a coroutine.
- L489 `test_process_is_coroutine_function(self)` (method)
- L494 `TestExtractText` (class) — _extract_text must handle both legacy and current SDK payload shapes.
- L503 `test_text_as_dict_legacy(self)` (method)
- L511 `test_text_as_textcontent_object(self)` (method) — SDK >= 0.20 shape: object with ``.content`` attribute.
- L529 `test_text_content_attr_with_empty_string(self)` (method)
- L541 `test_rich_text_content_new_shape(self)` (method) — SDK >= 0.20 exposes rich text as ``message.rich_text_content.rich_text_list``.
- L555 `test_rich_text_legacy_shape(self)` (method) — Legacy ``message.rich_text`` list remains supported.
- L565 `test_empty_message(self)` (method)
- L574 `TestExtractMedia` (class) — _extract_media must split native voice rich-text items (auto-STT)
- L578 `_msg_with_rich_text(self, items)` (method)
- L586 `test_voice_rich_text_item_classified_as_voice(self)` (method) — Native DingTalk voice notes (type=voice) must enter the auto-STT
- L602 `test_audio_rich_text_item_stays_audio(self)` (method) — Generic audio uploads (e.g. an mp3 the user attached) must NOT
- L631 `_make_gating_adapter(monkeypatch, *, extra=None, env=None)` (function) — Build a DingTalkAdapter with only the gating fields populated.
- L650 `TestAllowedUsersGate` (class)
- L652 `test_empty_allowlist_allows_everyone(self, monkeypatch)` (method)
- L656 `test_wildcard_allowlist_allows_everyone(self, monkeypatch)` (method)
- L660 `test_matches_sender_id_case_insensitive(self, monkeypatch)` (method)
- L666 `test_matches_staff_id(self, monkeypatch)` (method)
- L672 `test_rejects_unknown_user(self, monkeypatch)` (method)
- L678 `test_env_var_csv_populates_allowlist(self, monkeypatch)` (method)
- L686 `TestMentionPatterns` (class)
- L688 `test_empty_patterns_list(self, monkeypatch)` (method)
- L693 `test_pattern_matches_text(self, monkeypatch)` (method)
- L700 `test_pattern_is_case_insensitive(self, monkeypatch)` (method)
- L706 `test_invalid_regex_is_skipped_not_raised(self, monkeypatch)` (method)
- L715 `test_env_var_json_populates_patterns(self, monkeypatch)` (method)
- L723 `test_env_var_newline_fallback_when_not_json(self, monkeypatch)` (method)
- L731 `TestShouldProcessMessage` (class)
- L733 `test_dm_always_accepted(self, monkeypatch)` (method)
- L740 `test_group_rejected_when_require_mention_and_no_trigger(self, monkeypatch)` (method)
- L747 `test_group_accepted_when_require_mention_disabled(self, monkeypatch)` (method)
- L754 `test_group_accepted_when_bot_is_mentioned(self, monkeypatch)` (method)
- L761 `test_group_accepted_when_text_matches_wake_word(self, monkeypatch)` (method)
- L769 `test_group_accepted_when_chat_in_free_response_list(self, monkeypatch)` (method)
- L785 `TestIncomingHandlerProcess` (class) — Verify that _IncomingHandler.process correctly converts callback data
- L791 `test_process_extracts_session_webhook(self)` (method) — session_webhook must be populated from callback data.
- L822 `test_process_fallback_session_webhook_when_from_dict_misses_it(self)` (method) — If ChatbotMessage.from_dict does not map sessionWebhook (e.g. SDK
- L851 `test_process_returns_ack_immediately(self)` (method) — process() must not block on _on_message — it should return
- L890 `TestExtractTextMentions` (class)
- L892 `test_preserves_at_mentions_in_text(self)` (method) — @mentions are routing signals (via isInAtList), not text to strip.
- L915 `test_dingtalk_in_platform_enum(self)` (method)
- L927 `TestMessageContextIsolation` (class)
- L929 `test_contexts_keyed_by_chat_id(self)` (method) — Two concurrent chats must not clobber each other's context.
- L952 `TestCardLifecycle` (class)
- L955 `adapter_with_card(self)` (method)
- L981 `test_final_reply_finalizes_card(self, adapter_with_card)` (method) — send(reply_to=...) creates a closed card (final response path).
- L992 `test_intermediate_send_stays_streaming(self, adapter_with_card)` (method) — send() without reply_to creates an OPEN card (tool progress /
- L1005 `test_done_fires_only_when_reply_to_is_set(self, adapter_with_card)` (method) — reply_to distinguishes final response (base.py) from tool-progress
- L1021 `test_edit_message_finalize_fires_done(self, adapter_with_card)` (method) — Stream consumer's final edit_message(finalize=True) fires Done.
- L1040 `test_edit_message_finalize_false_tracks_sibling(self, adapter_with_card)` (method) — After edit_message(finalize=False), card is tracked as open.
- L1051 `test_next_send_auto_closes_sibling_streaming_cards(self, adapter_with_card)` (method) — Tool-progress card left open (send without reply_to + edits) must
- L1081 `test_edit_message_requires_message_id(self, adapter_with_card)` (method)
- L1089 `test_fire_done_reaction_is_idempotent(self, adapter_with_card)` (method)
- L1107 `TestDingTalkAdapterAICards` (class)
- L1109 `config(self)` (method)
- L1120 `mock_stream_client(self)` (method)
- L1126 `mock_http_client(self)` (method)
- L1130 `mock_message(self)` (method)
- L1146 `test_send_uses_ai_card_if_configured(self, config, mock_stream_client, mock_http_client, mock_message)` (method)
