---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:43'
updated: '2026-06-08T00:38:43'
---

# tests/tools/test_checkpoint_manager.py

Symbols in `tests/tools/test_checkpoint_manager.py`.

- L39 `work_dir(tmp_path)` (function)
- L48 `checkpoint_base(tmp_path)` (function) — Isolated checkpoint base — never writes to ~/.hermes/.
- L54 `fake_home(tmp_path, monkeypatch)` (function)
- L66 `mgr(work_dir, checkpoint_base, monkeypatch)` (function)
- L72 `disabled_mgr(checkpoint_base, monkeypatch)` (function)
- L81 `TestStorePath` (class)
- L82 `test_store_is_single_shared_path(self, work_dir, checkpoint_base, monkeypatch)` (method)
- L89 `test_project_hash_deterministic(self, work_dir)` (method)
- L92 `test_project_hash_differs_per_dir(self, tmp_path)` (method)
- L95 `test_tilde_and_expanded_home_share_project_hash(self, fake_home, checkpoint_base, monkeypatch)` (method)
- L109 `TestStoreInit` (class)
- L110 `test_creates_git_store(self, work_dir, checkpoint_base, monkeypatch)` (method)
- L120 `test_no_git_in_project_dir(self, work_dir, checkpoint_base, monkeypatch)` (method)
- L126 `test_init_idempotent(self, work_dir, checkpoint_base, monkeypatch)` (method)
- L132 `test_bc_init_shadow_repo_shim(self, work_dir, checkpoint_base, monkeypatch)` (method) — Backward-compatible helper still works for old callers/tests.
- L141 `test_legacy_migration_archives_prev2_repos(self, checkpoint_base, work_dir)` (method) — Pre-v2 per-project shadow repos get moved into legacy-<ts>/.
- L170 `TestDisabledManager` (class)
- L171 `test_ensure_checkpoint_returns_false(self, disabled_mgr, work_dir)` (method)
- L174 `test_new_turn_works(self, disabled_mgr)` (method)
- L182 `TestTakeCheckpoint` (class)
- L183 `test_first_checkpoint(self, mgr, work_dir)` (method)
- L187 `test_dedup_same_turn(self, mgr, work_dir)` (method)
- L193 `test_new_turn_resets_dedup(self, mgr, work_dir)` (method)
- L199 `test_no_changes_skips_commit(self, mgr, work_dir)` (method)
- L204 `test_skip_root_dir(self, mgr)` (method)
- L207 `test_skip_home_dir(self, mgr)` (method)
- L210 `test_multiple_projects_share_store(self, mgr, tmp_path)` (method) — Two projects commit to the SAME shared store (dedup wins).
- L236 `TestListCheckpoints` (class)
- L237 `test_empty_when_no_checkpoints(self, mgr, work_dir)` (method)
- L240 `test_list_after_take(self, mgr, work_dir)` (method)
- L249 `test_multiple_checkpoints_ordered(self, mgr, work_dir)` (method)
- L263 `test_list_isolated_per_project(self, mgr, tmp_path)` (method) — Listing one project doesn't leak checkpoints from another.
- L279 `test_tilde_path_lists_same_checkpoints(self, checkpoint_base, fake_home, monkeypatch)` (method)
- L295 `TestRealPruning` (class)
- L296 `test_max_snapshots_trims_history(self, work_dir, checkpoint_base, monkeypatch)` (method)
- L313 `test_max_file_size_mb_skips_large_files(self, tmp_path, checkpoint_base, monkeypatch)` (method)
- L341 `TestRestore` (class)
- L342 `test_restore_to_previous(self, mgr, work_dir)` (method)
- L356 `test_restore_invalid_hash(self, mgr, work_dir)` (method)
- L361 `test_restore_no_checkpoints(self, mgr, work_dir)` (method)
- L365 `test_restore_creates_pre_rollback_snapshot(self, mgr, work_dir)` (method)
- L378 `test_tilde_path_supports_diff_and_restore_flow(self, checkpoint_base, fake_home, monkeypatch)` (method)
- L407 `TestWorkingDirResolution` (class)
- L408 `test_resolves_git_project_root(self, tmp_path)` (method)
- L420 `test_resolves_pyproject_root(self, tmp_path)` (method)
- L429 `test_falls_back_to_parent(self, tmp_path, monkeypatch)` (method)
- L453 `test_resolves_tilde_path_to_project_root(self, fake_home)` (method)
- L472 `TestGitEnvIsolation` (class)
- L473 `test_sets_git_dir(self, tmp_path)` (method)
- L478 `test_sets_work_tree(self, tmp_path)` (method)
- L484 `test_clears_index_file(self, tmp_path, monkeypatch)` (method)
- L489 `test_sets_index_file_when_provided(self, tmp_path)` (method)
- L496 `test_expands_tilde_in_work_tree(self, fake_home, tmp_path)` (method)
- L507 `TestFormatCheckpointList` (class)
- L508 `test_empty_list(self)` (method)
- L511 `test_formats_entries(self)` (method)
- L531 `TestDirFileCount` (class)
- L532 `test_counts_files(self, work_dir)` (method)
- L535 `test_nonexistent_dir(self, tmp_path)` (method)
- L543 `TestErrorResilience` (class)
- L544 `test_no_git_installed(self, work_dir, checkpoint_base, monkeypatch)` (method)
- L551 `test_run_git_allows_expected_nonzero_without_error_log(self, tmp_path, caplog)` (method)
- L571 `test_run_git_invalid_working_dir_reports_path_error(self, tmp_path, caplog)` (method)
- L583 `test_run_git_missing_git_reports_git_not_found(self, tmp_path, monkeypatch, caplog)` (method)
- L603 `test_checkpoint_failure_does_not_raise(self, mgr, work_dir, monkeypatch)` (method)
- L610 `TestTouchProjectMalformedMeta` (class) — _touch_project must not raise when the project metadata file is corrupted.
- L627 `test_non_dict_meta_does_not_raise(self, tmp_path, payload)` (method)
- L651 `TestSecurity` (class)
- L652 `test_restore_rejects_argument_injection(self, mgr, work_dir)` (method)
- L663 `test_restore_rejects_invalid_hex_chars(self, mgr, work_dir)` (method)
- L673 `test_restore_rejects_path_traversal(self, mgr, work_dir)` (method)
- L686 `test_restore_accepts_valid_file_path(self, mgr, work_dir)` (method)
- L707 `TestGpgAndGlobalConfigIsolation` (class)
- L708 `test_git_env_isolates_global_and_system_config(self, tmp_path)` (method)
- L714 `test_init_sets_commit_gpgsign_false(self, work_dir, checkpoint_base, monkeypatch)` (method)
- L725 `test_init_sets_tag_gpgsign_false(self, work_dir, checkpoint_base, monkeypatch)` (method)
- L736 `test_checkpoint_works_with_global_gpgsign_and_broken_gpg(self, work_dir, checkpoint_base, monkeypatch, tmp_path)` (method)
- L761 `_seed_legacy_repo(base: Path, name: str, workdir: Path, mtime: float=None)` (function) — Create a minimal pre-v2 shadow repo directly under base.
- L776 `_seed_v2_project(base: Path, workdir: Path, last_touch: float=None)` (function) — Register a v2 project in the shared store (no commits, just metadata).
- L792 `TestPruneCheckpointsLegacy` (class) — Backwards-compat: prune still handles pre-v2 per-project shadow repos.
- L795 `test_deletes_orphan_when_workdir_missing(self, tmp_path)` (method)
- L810 `test_deletes_stale_by_mtime(self, tmp_path)` (method)
- L827 `test_delete_orphans_disabled_keeps_orphans(self, tmp_path)` (method)
- L837 `test_skips_non_shadow_dirs(self, tmp_path)` (method)
- L847 `test_base_missing_returns_empty_counts(self, tmp_path)` (method)
- L853 `TestPruneCheckpointsV2` (class) — v2 pruning walks the shared store's projects/ metadata.
- L856 `test_deletes_orphan_project_entry(self, tmp_path, monkeypatch)` (method)
- L886 `test_deletes_stale_project_by_last_touch(self, tmp_path, monkeypatch)` (method)
- L918 `test_legacy_archive_dirs_also_pruned(self, tmp_path, monkeypatch)` (method) — legacy-<ts>/ dirs older than retention_days get wiped.
- L937 `TestMaybeAutoPruneCheckpoints` (class)
- L938 `test_first_call_prunes_and_writes_marker(self, tmp_path)` (method)
- L947 `test_second_call_within_interval_skips(self, tmp_path)` (method)
- L963 `test_corrupt_marker_treated_as_no_prior_run(self, tmp_path)` (method)
- L973 `test_missing_base_no_raise(self, tmp_path)` (method)
- L985 `TestStoreStatus` (class)
- L986 `test_empty_base(self, tmp_path, monkeypatch)` (method)
- L993 `test_reports_projects_and_legacy(self, tmp_path, monkeypatch, work_dir)` (method)
- L1014 `TestClearFunctions` (class)
- L1015 `test_clear_all_wipes_base(self, tmp_path, monkeypatch, work_dir)` (method)
- L1027 `test_clear_legacy_only_removes_legacy_dirs(self, tmp_path, monkeypatch, work_dir)` (method)
- L1046 `test_clear_all_on_missing_base_is_noop(self, tmp_path, monkeypatch)` (method)
