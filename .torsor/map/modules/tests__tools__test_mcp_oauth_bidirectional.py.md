---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:43'
updated: '2026-06-08T00:38:43'
---

# tests/tools/test_mcp_oauth_bidirectional.py

Symbols in `tests/tools/test_mcp_oauth_bidirectional.py`.

- L38 `test_hermes_provider_forwards_asend_values(tmp_path, monkeypatch)` (function) — The wrapper MUST forward ``.asend(response)`` into the inner generator.
- L119 `test_hermes_provider_forwards_401_triggers_refresh(tmp_path, monkeypatch)` (function) — A 401 response MUST flow into the inner generator and trigger the
- L201 `_noop_redirect(_url: str)` (function) — Redirect handler that does nothing (won't be invoked in these tests).
- L206 `_noop_callback()` (function) — Callback handler that won't be invoked in these tests.
