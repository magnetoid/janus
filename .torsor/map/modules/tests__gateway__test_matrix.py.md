---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/gateway/test_matrix.py

Symbols in `tests/gateway/test_matrix.py`.

- L12 `_make_fake_mautrix()` (function) — Create a lightweight set of fake ``mautrix`` modules.
- L244 `TestMatrixConfigLoading` (class)
- L245 `test_apply_env_overrides_with_access_token(self, monkeypatch)` (method)
- L259 `test_apply_env_overrides_with_password(self, monkeypatch)` (method)
- L275 `test_matrix_not_loaded_without_creds(self, monkeypatch)` (method)
- L286 `test_matrix_encryption_flag(self, monkeypatch)` (method)
- L298 `test_matrix_encryption_default_off(self, monkeypatch)` (method)
- L310 `test_matrix_home_room(self, monkeypatch)` (method)
- L325 `test_matrix_user_id_stored_in_extra(self, monkeypatch)` (method)
- L342 `_make_adapter()` (function) — Create a MatrixAdapter with mocked config.
- L361 `TestMatrixTypingIndicator` (class)
- L362 `setup_method(self)` (method)
- L368 `test_stop_typing_clears_matrix_typing_state(self)` (method) — stop_typing() should send typing=false instead of waiting for timeout expiry.
- L380 `test_stop_typing_no_client_is_noop(self)` (method)
- L385 `test_stop_typing_suppresses_exceptions(self)` (method)
- L394 `TestMatrixMxcToHttp` (class)
- L395 `setup_method(self)` (method)
- L398 `test_basic_mxc_conversion(self)` (method) — mxc://server/media_id should become an authenticated HTTP URL.
- L404 `test_mxc_with_different_server(self)` (method) — mxc:// from a different server should still use our homeserver.
- L411 `test_non_mxc_url_passthrough(self)` (method) — Non-mxc URLs should be returned unchanged.
- L416 `test_mxc_uses_client_v1_endpoint(self)` (method) — Should use /_matrix/client/v1/media/download/ not the deprecated path.
- L428 `TestMatrixDmDetection` (class)
- L429 `setup_method(self)` (method)
- L432 `test_room_in_m_direct_is_dm(self)` (method) — A room listed in m.direct should be detected as DM.
- L443 `test_unknown_room_not_in_cache(self)` (method) — Unknown rooms should not be in the DM cache.
- L449 `test_refresh_dm_cache_with_m_direct(self)` (method) — _refresh_dm_cache should populate _dm_rooms from m.direct data.
- L473 `TestMatrixReplyFallbackStripping` (class) — Test that Matrix reply fallback lines ('> ' prefix) are stripped.
- L476 `setup_method(self)` (method)
- L483 `_strip_fallback(self, body: str, has_reply: bool=True)` (method) — Simulate the reply fallback stripping logic from _on_room_message.
- L502 `test_simple_reply_fallback(self)` (method)
- L507 `test_multiline_reply_fallback(self)` (method)
- L512 `test_no_reply_fallback_preserved(self)` (method)
- L517 `test_quote_without_reply_preserved(self)` (method) — '> ' lines without a reply_to context should be preserved.
- L523 `test_empty_fallback_separator(self)` (method) — The blank line between fallback and actual content should be stripped.
- L529 `test_multiline_response_after_fallback(self)` (method)
- L539 `TestMatrixBangCommandAlias` (class) — Matrix clients may reserve /commands, so Hermes supports !commands.
- L542 `setup_method(self)` (method)
- L549 `_dispatch_text(self, body: str, *, is_dm: bool=True)` (method)
- L570 `_dispatch_text_reply(self, body: str, *, is_dm: bool=True)` (method) — Dispatch a message that is a Matrix reply (m.in_reply_to set), so
- L594 `test_known_bang_command_normalizes_to_slash_command(self)` (method)
- L608 `test_unknown_bang_text_is_not_treated_as_command(self)` (method)
- L617 `test_bang_model_reaches_gateway_as_slash_command(self)` (method)
- L626 `test_bang_queue_preserves_arguments(self)` (method)
- L636 `test_unknown_bang_text_stays_normal_text(self)` (method)
- L645 `test_bang_command_bypasses_room_mention_requirement(self)` (method)
- L653 `test_slash_command_bypasses_room_mention_requirement(self)` (method)
- L661 `test_unknown_bang_text_does_not_bypass_room_mention_requirement(self)` (method)
- L666 `test_bang_alias_underscore_resolves_to_hyphen_form(self)` (method) — !set_home must emit a dispatchable token even though set_home is
- L679 `test_bang_skill_command_normalizes(self)` (method) — The get_skill_commands() branch normalizes installed skill
- L704 `test_bang_command_in_quoted_reply_normalizes(self)` (method) — A bang command that follows a Matrix reply-fallback quote is
- L717 `test_slash_command_in_quoted_reply_normalizes(self)` (method) — Sanity: the slash equivalent already works post-strip — the bang
- L733 `TestMatrixThreadDetection` (class)
- L734 `test_thread_id_from_m_relates_to(self)` (method) — m.relates_to with rel_type=m.thread should extract the event_id.
- L748 `test_no_thread_for_reply(self)` (method) — m.in_reply_to without m.thread should not set thread_id.
- L758 `test_no_thread_for_edit(self)` (method) — m.replace relation should not set thread_id.
- L769 `test_empty_relates_to(self)` (method) — Empty m.relates_to should not set thread_id.
- L782 `TestMatrixFormatMessage` (class)
- L783 `setup_method(self)` (method)
- L786 `test_image_markdown_stripped(self)` (method) — ![alt](url) should be converted to just the URL.
- L791 `test_regular_markdown_preserved(self)` (method) — Standard markdown should be preserved (Matrix supports it).
- L796 `test_plain_text_unchanged(self)` (method)
- L800 `test_multiple_images_stripped(self)` (method)
- L812 `TestMatrixMarkdownToHtml` (class)
- L813 `setup_method(self)` (method)
- L816 `test_bold_conversion(self)` (method) — **bold** should produce <strong> tags.
- L822 `test_italic_conversion(self)` (method) — *italic* should produce <em> tags.
- L827 `test_inline_code(self)` (method) — `code` should produce <code> tags.
- L832 `test_plain_text_returns_html(self)` (method) — Plain text should still be returned (possibly with <br> or <p>).
- L842 `TestMatrixDisplayName` (class)
- L843 `setup_method(self)` (method)
- L847 `test_get_display_name_from_state_store(self)` (method) — Should get display name from state_store.get_member().
- L863 `test_get_display_name_fallback_to_localpart(self)` (method) — Should extract localpart from @user:server format.
- L876 `test_get_display_name_no_client(self)` (method) — Should handle None client gracefully.
- L887 `TestMatrixModuleImport` (class)
- L888 `test_module_importable_without_mautrix(self)` (method) — gateway.platforms.matrix must be importable even when mautrix is
- L923 `TestMatrixRequirements` (class)
- L924 `test_check_requirements_with_token(self, monkeypatch)` (method)
- L936 `test_check_requirements_without_creds(self, monkeypatch)` (method)
- L943 `test_check_requirements_without_homeserver(self, monkeypatch)` (method)
- L949 `test_check_requirements_encryption_true_no_e2ee_deps(self, monkeypatch)` (method) — MATRIX_ENCRYPTION=true should fail if python-olm is not installed.
- L960 `test_check_requirements_encryption_false_no_e2ee_deps_ok(self, monkeypatch)` (method) — Without encryption, missing E2EE deps should not block startup.
- L976 `test_check_requirements_encryption_true_with_e2ee_deps(self, monkeypatch)` (method) — MATRIX_ENCRYPTION=true should pass if E2EE deps are available.
- L991 `test_check_e2ee_deps_requires_asyncpg(self, monkeypatch)` (method) — E2EE deps check must reject when asyncpg is missing — even if olm is present.
- L1013 `test_check_e2ee_deps_requires_aiosqlite(self)` (method) — E2EE deps check must reject when aiosqlite is missing.
- L1031 `test_check_requirements_runs_lazy_install_when_partial(self, monkeypatch)` (method) — When mautrix is installed but asyncpg/aiosqlite are missing,
- L1069 `TestMatrixAccessTokenAuth` (class)
- L1071 `test_connect_with_access_token_and_encryption(self)` (method) — connect() should call whoami, set user_id/device_id, set up crypto.
- L1140 `TestDeviceKeyReVerification` (class)
- L1142 `test_verify_fails_when_server_keys_mismatch_after_upload(self)` (method) — share_keys() succeeds but server still has old keys -> should return False.
- L1174 `TestMatrixE2EEHardFail` (class) — connect() must refuse to start when E2EE is requested but deps are missing.
- L1178 `test_connect_fails_when_encryption_true_but_no_e2ee_deps(self)` (method)
- L1215 `test_connect_fails_when_crypto_setup_raises(self)` (method) — Even if _check_e2ee_deps passes, if OlmMachine raises, hard-fail.
- L1253 `TestMatrixDeviceId` (class) — MATRIX_DEVICE_ID should be used for stable device identity.
- L1256 `test_device_id_from_config_extra(self)` (method)
- L1270 `test_device_id_from_env(self, monkeypatch)` (method)
- L1285 `test_device_id_config_takes_precedence_over_env(self, monkeypatch)` (method)
- L1302 `test_connect_uses_configured_device_id_over_whoami(self)` (method) — When MATRIX_DEVICE_ID is set, it should be used instead of whoami device_id.
- L1365 `TestMatrixPasswordLoginDeviceId` (class) — MATRIX_DEVICE_ID should be passed to mautrix Client even with password login.
- L1369 `test_password_login_uses_device_id(self)` (method)
- L1412 `TestMatrixDeviceIdConfig` (class) — MATRIX_DEVICE_ID should be plumbed through gateway config.
- L1415 `test_device_id_in_config_extra(self, monkeypatch)` (method)
- L1427 `test_device_id_not_set_when_env_empty(self, monkeypatch)` (method)
- L1440 `TestMatrixSyncLoop` (class)
- L1442 `test_sync_loop_dispatches_events_and_stores_token(self)` (method) — _sync_loop should call handle_sync() and persist next_batch.
- L1477 `test_sync_loop_reconciles_pending_invites(self)` (method) — Pending rooms.invite entries should be joined if callbacks were missed.
- L1511 `TestMatrixUploadAndSend` (class)
- L1513 `test_upload_unencrypted_room_uses_plain_url(self)` (method) — Unencrypted rooms should use plain 'url' key.
- L1535 `test_upload_encrypted_room_uses_file_payload(self)` (method) — Encrypted rooms should use 'file' key with crypto metadata.
- L1562 `TestMatrixEncryptedSendFallback` (class)
- L1564 `test_send_retries_after_e2ee_error(self)` (method) — send() should retry with crypto.share_keys() on E2EE errors.
- L1591 `TestJoinedRoomsReference` (class)
- L1592 `test_joined_rooms_reference_preserved_after_reassignment(self)` (method) — _CryptoStateStore must see updates after initial sync populates rooms.
- L1612 `TestMatrixEncryptedEventHandler` (class)
- L1614 `test_connect_registers_encrypted_event_handler_when_encryption_on(self)` (method)
- L1679 `test_connect_fails_on_stale_otk_conflict(self)` (method) — connect() must refuse E2EE when OTK upload hits 'already exists'.
- L1742 `TestMatrixDisconnect` (class)
- L1744 `test_disconnect_closes_api_session(self)` (method) — disconnect() should close client.api.session.
- L1765 `test_disconnect_handles_session_close_failure(self)` (method) — disconnect() should not raise if session close fails.
- L1785 `test_disconnect_without_client(self)` (method) — disconnect() should handle None client gracefully.
- L1799 `TestMatrixMarkdownHtmlSecurity` (class) — Tests for HTML injection prevention in _markdown_to_html_fallback.
- L1802 `setup_method(self)` (method)
- L1806 `test_script_injection_in_header(self)` (method)
- L1811 `test_script_injection_in_plain_text(self)` (method)
- L1815 `test_img_onerror_in_blockquote(self)` (method)
- L1819 `test_script_in_list_item(self)` (method)
- L1823 `test_script_in_ordered_list(self)` (method)
- L1827 `test_javascript_uri_blocked(self)` (method)
- L1831 `test_data_uri_blocked(self)` (method)
- L1835 `test_vbscript_uri_blocked(self)` (method)
- L1839 `test_link_text_html_injection(self)` (method)
- L1843 `test_link_href_attribute_breakout(self)` (method)
- L1847 `test_html_injection_in_bold(self)` (method)
- L1851 `test_html_injection_in_italic(self)` (method)
- L1860 `TestMatrixMarkdownHtmlFormatting` (class) — Tests for new formatting capabilities in _markdown_to_html_fallback.
- L1863 `setup_method(self)` (method)
- L1867 `test_fenced_code_block(self)` (method)
- L1872 `test_fenced_code_block_no_lang(self)` (method)
- L1876 `test_code_block_html_escaped(self)` (method)
- L1881 `test_headers(self)` (method)
- L1886 `test_unordered_list(self)` (method)
- L1891 `test_ordered_list(self)` (method)
- L1896 `test_blockquote(self)` (method)
- L1901 `test_horizontal_rule(self)` (method)
- L1905 `test_strikethrough(self)` (method)
- L1909 `test_links_preserved(self)` (method)
- L1913 `test_complex_mixed_document(self)` (method) — A realistic agent response with multiple formatting types.
- L1929 `TestMatrixLinkSanitization` (class)
- L1930 `test_safe_https_url(self)` (method)
- L1934 `test_javascript_blocked(self)` (method)
- L1938 `test_data_blocked(self)` (method)
- L1942 `test_vbscript_blocked(self)` (method)
- L1946 `test_quotes_escaped(self)` (method)
- L1957 `TestMatrixReactions` (class)
- L1958 `setup_method(self)` (method)
- L1962 `test_send_reaction(self)` (method) — _send_reaction should call send_message_event with m.reaction.
- L1978 `test_send_reaction_no_client(self)` (method)
- L1984 `test_on_processing_start_sends_eyes(self)` (method) — on_processing_start should send eyes reaction.
- L2005 `test_on_processing_complete_sends_check(self)` (method)
- L2034 `test_on_processing_complete_sends_cross_on_failure(self)` (method)
- L2063 `test_on_processing_complete_cancelled_sends_no_terminal_reaction(self)` (method)
- L2082 `test_on_processing_complete_no_pending_reaction(self)` (method) — on_processing_complete should skip redaction if no eyes reaction was tracked.
- L2105 `test_approval_reaction_cleanup_is_delayed(self)` (method) — Bot approval reaction redactions should not run inline.
- L2132 `test_reactions_disabled(self)` (method)
- L2155 `TestMatrixReadReceipts` (class)
- L2156 `setup_method(self)` (method)
- L2160 `test_accepted_message_schedules_read_receipt(self)` (method)
- L2180 `test_send_read_receipt(self)` (method) — send_read_receipt should call mautrix's real read-marker API.
- L2193 `test_send_read_receipt_falls_back_to_receipt_only(self)` (method) — send_read_receipt should still work with clients lacking read markers.
- L2204 `test_read_receipt_no_client(self)` (method)
- L2214 `TestMatrixImageOnlyMediaNormalization` (class)
- L2215 `setup_method(self)` (method)
- L2227 `test_image_only_filename_body_is_not_forwarded_as_text(self)` (method)
- L2259 `test_image_caption_text_is_preserved(self)` (method)
- L2289 `TestMatrixRedaction` (class)
- L2290 `setup_method(self)` (method)
- L2294 `test_redact_message(self)` (method) — redact_message should call client.redact().
- L2306 `test_redact_no_client(self)` (method)
- L2316 `TestMatrixRoomManagement` (class)
- L2317 `setup_method(self)` (method)
- L2321 `test_create_room(self)` (method) — create_room should call client.create_room() returning RoomID string.
- L2333 `test_invite_user(self)` (method) — invite_user should call client.invite_user().
- L2343 `test_create_room_no_client(self)` (method)
- L2353 `TestMatrixPresence` (class)
- L2354 `setup_method(self)` (method)
- L2358 `test_set_presence_valid(self)` (method)
- L2367 `test_set_presence_invalid_state(self)` (method)
- L2375 `test_set_presence_no_client(self)` (method)
- L2387 `TestMatrixSelfSenderFilter` (class)
- L2388 `setup_method(self)` (method)
- L2391 `test_exact_match_is_self(self)` (method)
- L2395 `test_case_insensitive_match_is_self(self)` (method)
- L2404 `test_whitespace_trimmed(self)` (method)
- L2408 `test_different_user_is_not_self(self)` (method)
- L2412 `test_empty_user_id_is_treated_as_self(self)` (method)
- L2421 `TestMatrixSystemBridgeFilter` (class)
- L2422 `setup_method(self)` (method)
- L2425 `test_appservice_underscore_prefix_is_bridge(self)` (method)
- L2437 `test_empty_localpart_is_system(self)` (method)
- L2440 `test_empty_sender_is_system(self)` (method)
- L2444 `test_regular_user_is_not_bridge(self)` (method)
- L2454 `test_bot_account_is_not_bridge(self)` (method)
- L2463 `TestMatrixOnRoomMessageFilter` (class) — End-to-end coverage of _on_room_message drop conditions.
- L2466 `setup_method(self)` (method)
- L2474 `_mk_event(sender, body='hi', msgtype='m.text', event_id=None, ts=None)` (method)
- L2487 `test_own_sender_case_insensitive_dropped(self)` (method)
- L2495 `test_bridge_sender_dropped_before_pairing(self)` (method)
- L2503 `test_empty_sender_dropped(self)` (method)
- L2509 `test_self_with_unresolved_user_id_dropped(self)` (method)
- L2518 `test_regular_user_reaches_text_handler(self)` (method)
- L2524 `TestMatrixClockSkewWarning` (class) — Clock-skew detector for #12614.
- L2534 `setup_method(self)` (method)
- L2541 `_mk_event(sender, ts_ms, event_id=None)` (method)
- L2552 `test_late_drops_emit_one_shot_clock_skew_warning(self, caplog)` (method)
- L2593 `test_initial_sync_drops_do_not_warn(self, caplog)` (method) — During the first 30s after startup, old events are normal backfill.
- L2620 `test_fewer_than_three_late_drops_do_not_warn(self, caplog)` (method) — A single delayed backfill event after 30s shouldn't trigger NTP advice.
- L2640 `test_varied_backfill_skews_do_not_warn(self, caplog)` (method) — Backfill from a freshly-invited room delivers events of varied age.
- L2674 `test_state_reset_allows_warning_to_fire_again(self, caplog)` (method) — After the reset block at top of connect() runs, the warning is rearmed.
- L2730 `TestMatrixDmAutoThread` (class)
- L2731 `setup_method(self)` (method)
- L2740 `test_dm_auto_thread_enabled_creates_thread(self)` (method) — When dm_auto_thread is True, DM messages get auto-threaded.
- L2758 `test_dm_auto_thread_disabled_no_thread(self)` (method) — When dm_auto_thread is False (default), DMs have no auto-thread.
- L2781 `TestMatrixProxyConfig` (class) — Verify that MatrixAdapter resolves and propagates proxy settings.
- L2784 `_make_adapter(self, monkeypatch, proxy_env=None)` (method)
- L2801 `test_no_proxy_by_default(self, monkeypatch)` (method)
- L2805 `test_matrix_proxy_env_var(self, monkeypatch)` (method)
- L2810 `test_generic_proxy_fallback(self, monkeypatch)` (method)
- L2815 `test_matrix_proxy_takes_priority(self, monkeypatch)` (method)
- L2822 `TestCreateMatrixSession` (class) — Verify _create_matrix_session applies proxy at the session level.
- L2826 `test_no_proxy_returns_trust_env_session(self)` (method)
- L2836 `test_http_proxy_sets_default_proxy(self)` (method)
- L2846 `test_socks_proxy_uses_connector(self)` (method)
