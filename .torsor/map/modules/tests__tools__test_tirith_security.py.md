---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:44'
updated: '2026-06-08T00:38:44'
---

# tests/tools/test_tirith_security.py

Symbols in `tests/tools/test_tirith_security.py`.

- L18 `_reset_resolved_path()` (function) — Pre-set cached path to skip auto-install in scan tests.
- L37 `_mock_run(returncode=0, stdout='', stderr='')` (function) — Build a mock subprocess.CompletedProcess.
- L46 `_json_stdout(findings=None, summary='')` (function)
- L54 `TestExitCodeMapping` (class)
- L57 `test_exit_0_allow(self, mock_cfg, mock_run)` (method)
- L67 `test_exit_1_block_with_findings(self, mock_cfg, mock_run)` (method)
- L79 `test_exit_2_warn_with_findings(self, mock_cfg, mock_run)` (method)
- L94 `TestJsonParseFailure` (class)
- L97 `test_exit_1_invalid_json_still_blocks(self, mock_cfg, mock_run)` (method)
- L107 `test_exit_2_invalid_json_still_warns(self, mock_cfg, mock_run)` (method)
- L117 `test_exit_0_invalid_json_allows(self, mock_cfg, mock_run)` (method)
- L129 `TestOSErrorFailOpen` (class)
- L132 `test_file_not_found_fail_open(self, mock_cfg, mock_run)` (method)
- L142 `test_permission_error_fail_open(self, mock_cfg, mock_run)` (method)
- L152 `test_os_error_fail_closed(self, mock_cfg, mock_run)` (method)
- L161 `TestTimeoutFailOpen` (class)
- L164 `test_timeout_fail_open(self, mock_cfg, mock_run)` (method)
- L174 `test_timeout_fail_closed(self, mock_cfg, mock_run)` (method)
- L183 `TestUnknownExitCode` (class)
- L186 `test_unknown_exit_code_fail_open(self, mock_cfg, mock_run)` (method)
- L196 `test_unknown_exit_code_fail_closed(self, mock_cfg, mock_run)` (method)
- L209 `TestDisabled` (class)
- L211 `test_disabled_returns_allow(self, mock_cfg)` (method)
- L218 `TestPathExpansion` (class)
- L219 `test_tilde_expanded_in_resolve(self)` (method) — _resolve_tirith_path should expand ~ in configured path.
- L233 `TestCaps` (class)
- L236 `test_findings_capped_at_50(self, mock_cfg, mock_run)` (method)
- L246 `test_summary_capped_at_500(self, mock_cfg, mock_run)` (method)
- L259 `TestProgrammingErrors` (class)
- L262 `test_attribute_error_propagates(self, mock_cfg, mock_run)` (method)
- L271 `test_type_error_propagates(self, mock_cfg, mock_run)` (method)
- L283 `TestEnsureInstalled` (class)
- L285 `test_disabled_returns_none(self, mock_cfg)` (method)
- L293 `test_found_on_path_returns_immediately(self, mock_cfg, mock_which)` (method)
- L304 `test_not_found_returns_none(self, mock_cfg)` (method)
- L321 `test_startup_prefetch_can_suppress_install_failure_logs(self, mock_cfg)` (method)
- L342 `TestUnsupportedPlatform` (class) — When _detect_target() returns None (no tirith binary for this OS+arch),
- L348 `test_is_platform_supported_true_on_linux_x86_64(self)` (method)
- L353 `test_is_platform_supported_true_on_darwin_arm64(self)` (method)
- L358 `test_is_platform_supported_false_on_windows(self)` (method)
- L363 `test_is_platform_supported_false_on_unknown_arch(self)` (method)
- L369 `test_ensure_installed_unsupported_returns_none_no_thread(self, mock_cfg)` (method) — Windows: don't start a background install thread, don't write a
- L388 `test_check_command_security_unsupported_allows_silently(self, mock_cfg)` (method) — Windows: skip the resolver and spawn entirely — return allow with
- L403 `test_resolve_path_unsupported_caches_failure_without_probing(self, mock_cfg)` (method) — The per-command resolver must also short-circuit on Windows so
- L419 `test_explicit_path_still_honored_on_unsupported_platform(self, mock_cfg)` (method) — If a user explicitly configured a tirith_path (e.g. they built it
- L439 `TestFailedDownloadCaching` (class)
- L444 `test_failed_install_cached_no_retry(self, mock_which, mock_install, mock_disk_check, mock_mark)` (method) — After a failed download, subsequent resolves must not retry.
- L468 `test_failed_install_scan_uses_fail_open(self, mock_cfg, mock_run, mock_which, mock_install, mock_disk_check, mock_mark)` (method) — After cached miss, check_command_security hits OSError → fail_open.
- L493 `TestExplicitPathNoAutoDownload` (class)
- L496 `test_explicit_path_missing_no_download(self, mock_which, mock_install)` (method) — An explicit tirith_path that doesn't exist must NOT trigger download.
- L511 `test_tilde_explicit_path_missing_no_download(self, mock_which, mock_install)` (method) — An explicit ~/path that doesn't exist must NOT trigger download.
- L527 `test_default_path_does_auto_download(self, mock_which, mock_install, mock_disk_check, mock_mark)` (method) — The default bare 'tirith' SHOULD trigger auto-download.
- L544 `TestCosignVerification` (class)
- L547 `test_cosign_pass(self, mock_which, mock_run)` (method) — cosign verify-blob exits 0 → returns True.
- L561 `test_cosign_identity_pinned_to_release_workflow(self, mock_which, mock_run)` (method) — Identity regexp must pin to the release workflow, not the whole repo.
- L576 `test_cosign_fail_aborts(self, mock_which, mock_run)` (method) — cosign verify-blob exits non-zero → returns False (abort install).
- L585 `test_cosign_not_found_returns_none(self, mock_which)` (method) — cosign not on PATH → returns None (proceed with SHA-256 only).
- L595 `test_cosign_timeout_returns_none(self, mock_which, mock_run)` (method) — cosign times out → returns None (proceed with SHA-256 only).
- L605 `test_cosign_os_error_returns_none(self, mock_which, mock_run)` (method) — cosign OSError → returns None (proceed with SHA-256 only).
- L616 `test_install_aborts_on_cosign_rejection(self, mock_target, mock_dl, mock_which, mock_cosign)` (method) — _install_tirith returns None when cosign rejects the signature.
- L629 `test_install_proceeds_without_cosign(self, mock_target, mock_dl, mock_which, mock_checksum, mock_tarfile)` (method) — _install_tirith proceeds with SHA-256 only when cosign is not on PATH.
- L652 `test_install_proceeds_when_cosign_exec_fails(self, mock_target, mock_dl, mock_which, mock_cosign, mock_checksum, mock_tarfile)` (method) — _install_tirith falls back to SHA-256 when cosign exists but fails to execute.
- L673 `test_install_proceeds_when_cosign_artifacts_missing(self, mock_target, mock_dl, mock_which, mock_checksum, mock_tarfile)` (method) — _install_tirith proceeds with SHA-256 when .sig/.pem downloads fail.
- L702 `test_install_proceeds_when_cosign_passes(self, mock_target, mock_dl, mock_which, mock_cosign, mock_checksum, mock_tarfile)` (method) — _install_tirith proceeds only when cosign explicitly passes (True).
- L721 `TestInstallArchiveMemberValidation` (class)
- L722 `_write_archive(self, tmp_path, member: tarfile.TarInfo, data: bytes | None=None)` (method)
- L736 `_download_side_effect(self, archive, checksums)` (method)
- L754 `test_install_extracts_regular_tirith_member(self, mock_target, mock_which, mock_checksum, tmp_path, monkeypatch)` (method) — A valid regular-file tirith member is installed as a plain file.
- L782 `test_install_rejects_non_regular_tirith_member(self, mock_target, mock_which, mock_checksum, tmp_path, monkeypatch)` (method) — Symlink or hardlink tar members must not be installed as tirith.
- L808 `TestBackgroundInstall` (class)
- L809 `test_ensure_installed_non_blocking(self)` (method) — ensure_installed must return immediately when download needed.
- L831 `test_ensure_installed_skips_on_disk_marker(self)` (method) — ensure_installed skips network attempt when disk marker exists.
- L850 `test_resolve_returns_default_when_thread_alive(self)` (method) — _resolve_tirith_path returns default while background thread runs.
- L866 `test_resolve_picks_up_background_result(self)` (method) — After background thread finishes, _resolve_tirith_path uses cached path.
- L882 `TestDiskFailureMarker` (class)
- L883 `test_mark_and_check(self)` (method) — Writing then reading the marker should work.
- L898 `test_expired_marker_ignored(self)` (method) — Marker older than TTL should be ignored.
- L911 `test_cosign_missing_marker_clears_when_cosign_appears(self)` (method) — Marker with 'cosign_missing' reason clears if cosign is now on PATH.
- L928 `test_cosign_missing_marker_stays_when_cosign_still_absent(self)` (method) — Marker with 'cosign_missing' reason stays if cosign is still missing.
- L939 `test_non_cosign_marker_not_affected_by_cosign_presence(self)` (method) — Markers with other reasons are NOT cleared by cosign appearing.
- L954 `test_sync_resolve_persists_failure(self, mock_which, mock_install, mock_disk_check, mock_mark)` (method) — Synchronous _resolve_tirith_path persists failure to disk.
- L969 `test_sync_resolve_clears_marker_on_success(self, mock_which, mock_install, mock_disk_check, mock_clear)` (method) — Successful install clears the disk failure marker.
- L981 `test_sync_resolve_skips_install_on_disk_marker(self)` (method) — _resolve_tirith_path skips download when disk marker is recent.
- L998 `test_install_failed_still_checks_local_paths(self)` (method) — After _INSTALL_FAILED, a manual install on PATH is picked up.
- L1012 `test_install_failed_recovers_from_hermes_bin(self)` (method) — After _INSTALL_FAILED, manual install in HERMES_HOME/bin is picked up.
- L1035 `test_install_failed_skips_network_when_local_absent(self)` (method) — After _INSTALL_FAILED, if local checks fail, network is NOT retried.
- L1049 `test_cosign_missing_disk_marker_allows_retry(self)` (method) — Disk marker with cosign_missing reason allows retry when cosign appears.
- L1066 `test_in_memory_cosign_missing_retries_when_cosign_appears(self)` (method) — In-memory _INSTALL_FAILED with cosign_missing retries when cosign appears.
- L1090 `test_in_memory_cosign_exec_failed_not_retried(self)` (method) — In-memory _INSTALL_FAILED with cosign_exec_failed is NOT retried.
- L1105 `test_in_memory_cosign_missing_stays_when_cosign_still_absent(self)` (method) — In-memory cosign_missing is NOT retried when cosign is still absent.
- L1120 `test_disk_marker_reason_preserved_in_memory(self)` (method) — Disk marker reason is loaded into _install_failure_reason, not a generic tag.
- L1158 `TestHermesHomeIsolation` (class)
- L1159 `test_hermes_bin_dir_respects_hermes_home(self)` (method) — _hermes_bin_dir must use HERMES_HOME, not hardcoded ~/.hermes.
- L1169 `test_failure_marker_respects_hermes_home(self)` (method) — _failure_marker_path must use HERMES_HOME, not hardcoded ~/.hermes.
- L1176 `test_conftest_isolation_prevents_real_home_writes(self)` (method) — The conftest autouse fixture sets HERMES_HOME; verify it's active.
- L1182 `test_get_hermes_home_fallback(self)` (method) — Without HERMES_HOME set, falls back to the active OS home.
- L1199 `TestSpawnWarningDedup` (class) — When tirith isn't installed yet (background install in flight, or
- L1209 `test_repeated_spawn_failure_logs_once(self, mock_cfg, mock_run, caplog)` (method)
- L1237 `test_distinct_exception_types_each_log_once(self, mock_cfg, mock_run, caplog)` (method) — ``FileNotFoundError`` and ``PermissionError`` are distinct
- L1266 `test_repeated_timeout_logs_once(self, mock_cfg, mock_run, caplog)` (method)
- L1286 `test_path_none_logs_once(self, mock_cfg, caplog)` (method) — ``_resolve_tirith_path`` returning ``None`` (explicit path set
- L1320 `TestAppTldSuppression` (class) — warn verdicts whose only finding is lookalike_tld/.app are downgraded to allow.
- L1325 `test_app_only_warn_downgraded_to_allow(self, mock_cfg, mock_run)` (method)
- L1337 `test_app_tld_in_description_field_also_suppressed(self, mock_cfg, mock_run)` (method)
- L1347 `test_mixed_findings_preserve_warn(self, mock_cfg, mock_run)` (method) — If .app finding is accompanied by another finding, warn is preserved.
- L1361 `test_non_app_lookalike_tld_preserved(self, mock_cfg, mock_run)` (method) — lookalike_tld for a non-.app TLD is not suppressed.
- L1373 `test_block_verdict_never_suppressed(self, mock_cfg, mock_run)` (method) — block exit code is never downgraded, even if finding looks like .app.
- L1383 `test_multiple_app_tld_findings_all_suppressed(self, mock_cfg, mock_run)` (method) — All findings being .app lookalike_tld → allow.
- L1395 `TestIsAppTldFinding` (class) — Unit tests for the _is_app_tld_finding helper.
- L1398 `setup_method(self)` (method)
- L1402 `test_matching_value_field(self)` (method)
- L1405 `test_matching_tld_field(self)` (method)
- L1408 `test_matching_description_field(self)` (method)
- L1412 `test_matching_message_field(self)` (method)
- L1416 `test_wrong_rule_id(self)` (method)
- L1419 `test_non_app_tld(self)` (method)
- L1422 `test_no_tld_value_fields(self)` (method)
- L1425 `test_non_dict_input(self)` (method)
- L1428 `test_case_insensitive_match(self)` (method)
