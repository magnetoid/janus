---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/hermes_cli/test_kanban_core_functionality.py

Symbols in `tests/hermes_cli/test_kanban_core_functionality.py`.

- L33 `kanban_home(tmp_path, monkeypatch)` (function)
- L58 `test_idempotency_key_returns_existing_task(kanban_home)` (function)
- L71 `test_idempotency_key_ignored_for_archived(kanban_home)` (function)
- L82 `test_no_idempotency_key_never_collides(kanban_home)` (function)
- L96 `test_spawn_failure_auto_blocks_after_limit(kanban_home, all_assignees_spawnable)` (function) — N consecutive spawn failures on the same task → auto_blocked.
- L123 `test_successful_spawn_does_not_reset_failure_counter(kanban_home, all_assignees_spawnable)` (function) — Under unified consecutive-failure counting, a successful spawn
- L158 `test_successful_completion_resets_failure_counter(kanban_home, all_assignees_spawnable)` (function) — A successful kb.complete_task wipes the counter — the task+profile
- L182 `test_reassign_resets_failure_counter_for_new_profile(kanban_home, all_assignees_spawnable)` (function) — Retry streaks are scoped to a task/profile pair; reassigning is a
- L203 `test_per_task_max_retries_overrides_dispatcher_limit(kanban_home, all_assignees_spawnable)` (function) — Per-task ``max_retries`` overrides both the caller-supplied
- L248 `test_per_task_max_retries_allows_more_than_default(kanban_home, all_assignees_spawnable)` (function) — A task with ``max_retries=5`` does NOT auto-block at the default
- L290 `test_max_retries_none_falls_through_to_dispatcher_limit(kanban_home, all_assignees_spawnable)` (function) — ``max_retries=None`` (the default) falls through to the caller-
- L334 `test_workspace_resolution_failure_also_counts(kanban_home, all_assignees_spawnable)` (function) — `dir:` workspace with no path should fail workspace resolution AND
- L369 `test_pid_alive_helper()` (function)
- L379 `test_pid_alive_detects_darwin_zombie(monkeypatch)` (function)
- L393 `test_detect_crashed_workers_reclaims(kanban_home)` (function) — A running task whose pid vanished gets dropped to ready with a
- L432 `test_daemon_runs_and_stops(kanban_home)` (function) — run_daemon should execute at least one tick and exit cleanly on
- L455 `test_daemon_keeps_going_after_tick_exception(kanban_home, monkeypatch)` (function) — A tick that raises shouldn't kill the loop.
- L485 `test_board_stats(kanban_home)` (function)
- L501 `test_task_age_helper(kanban_home)` (function)
- L518 `test_notify_sub_crud(kanban_home)` (function)
- L551 `test_notify_cursor_advances(kanban_home)` (function)
- L584 `test_notify_claim_is_single_owner_and_rewindable(kanban_home)` (function)
- L639 `test_gc_events_keeps_active_task_history(kanban_home)` (function) — gc_events should only prune rows for terminal (done/archived) tasks.
- L663 `test_gc_worker_logs_deletes_old_files(kanban_home)` (function)
- L683 `test_worker_log_rotation_keeps_one_generation(kanban_home, tmp_path)` (function)
- L693 `test_worker_log_rotation_keeps_configured_generations(kanban_home)` (function)
- L709 `test_worker_log_rotation_config_defaults_and_overrides()` (function)
- L720 `test_read_worker_log_tail(kanban_home)` (function)
- L740 `test_cli_complete_bulk(kanban_home)` (function)
- L758 `test_cli_archive_bulk(kanban_home)` (function)
- L775 `test_cli_archive_rm_deletes_archived_tasks(kanban_home)` (function)
- L791 `test_cli_archive_rm_rejects_live_tasks(kanban_home)` (function)
- L806 `test_cli_unblock_bulk(kanban_home)` (function)
- L819 `test_cli_block_bulk_via_ids_flag(kanban_home)` (function)
- L830 `test_cli_create_with_idempotency_key(kanban_home)` (function)
- L842 `test_cli_stats_json(kanban_home)` (function)
- L855 `test_cli_notify_subscribe_and_list(kanban_home)` (function)
- L871 `test_cli_log_missing_task(kanban_home)` (function)
- L878 `test_cli_gc_reports_counts(kanban_home)` (function)
- L893 `test_run_slash_every_verb_returns_sensible_output(kanban_home)` (function) — Smoke-test every verb with minimal args. None may raise, none may
- L938 `test_max_runtime_terminates_overrun_worker(kanban_home)` (function) — A running task whose elapsed time exceeds max_runtime_seconds gets
- L994 `test_repeated_timeouts_auto_block_at_default_limit(kanban_home)` (function) — Two timed_out outcomes on the same task/profile trip the retry guard.
- L1036 `test_max_runtime_none_means_no_cap(kanban_home)` (function) — A task with max_runtime_seconds=None is never timed out regardless
- L1058 `test_create_task_persists_max_runtime(kanban_home)` (function)
- L1068 `test_enforce_max_runtime_integrates_with_dispatch(kanban_home, monkeypatch)` (function) — enforce_max_runtime + dispatch_once integrate cleanly — a timed-out
- L1127 `test_heartbeat_on_running_task(kanban_home)` (function)
- L1144 `test_heartbeat_refused_when_not_running(kanban_home)` (function)
- L1156 `test_cli_heartbeat_verb(kanban_home)` (function)
- L1182 `test_recompute_ready_emits_promoted_not_ready(kanban_home)` (function)
- L1200 `test_spawn_failure_circuit_breaker_emits_gave_up(kanban_home, all_assignees_spawnable)` (function)
- L1216 `test_spawned_event_emitted_with_pid(kanban_home, all_assignees_spawnable)` (function) — Successful spawn must append a ``spawned`` event with the pid in
- L1233 `test_migration_renames_legacy_event_kinds(tmp_path, monkeypatch)` (function) — A DB created with the old vocab must have its event rows renamed
- L1274 `test_list_profiles_on_disk(tmp_path, monkeypatch)` (function) — list_profiles_on_disk returns the implicit default profile plus
- L1293 `test_list_profiles_on_disk_custom_root(tmp_path, monkeypatch)` (function) — list_profiles_on_disk respects a custom HERMES_HOME root.
- L1307 `test_known_assignees_merges_disk_and_board(tmp_path, monkeypatch)` (function) — known_assignees unions profiles on disk with currently-assigned
- L1341 `test_cli_assignees_json(kanban_home)` (function)
- L1357 `test_parse_duration_accepts_formats()` (function)
- L1369 `test_parse_duration_rejects_garbage()` (function)
- L1378 `test_cli_create_max_runtime_via_duration(kanban_home)` (function) — `hermes kanban create --max-runtime 2h` should persist 7200 seconds.
- L1391 `test_cli_create_max_runtime_bad_format_exits_nonzero(kanban_home)` (function)
- L1400 `test_run_created_on_claim(kanban_home)` (function) — claim_task opens a new task_runs row and points current_run_id at it.
- L1426 `test_run_closed_on_complete_with_summary(kanban_home)` (function) — complete_task ends the active run with outcome='completed' and
- L1457 `test_run_summary_falls_back_to_result(kanban_home)` (function) — If the caller doesn't pass summary, we fall back to result so
- L1471 `test_multiple_attempts_preserved_as_runs(kanban_home)` (function) — Crash / retry / complete flow produces one run per attempt, all
- L1516 `test_stale_run_cannot_complete_new_attempt(kanban_home, monkeypatch)` (function) — A worker from an earlier attempt cannot close a later retry.
- L1557 `test_stale_run_cannot_block_or_heartbeat_new_attempt(kanban_home, monkeypatch)` (function) — Stale retry attempts cannot mutate the active run lifecycle.
- L1589 `test_run_on_block_with_reason(kanban_home)` (function)
- L1605 `test_run_on_spawn_failure_records_failed_runs(kanban_home, all_assignees_spawnable)` (function) — Each spawn_failed event closes a run with outcome='spawn_failed',
- L1628 `test_event_rows_carry_run_id(kanban_home)` (function) — task_events.run_id is populated for run-scoped kinds and NULL for
- L1653 `test_build_worker_context_includes_prior_attempts(kanban_home)` (function) — A worker spawned after a prior attempt sees that attempt's outcome
- L1677 `test_build_worker_context_uses_parent_run_summary(kanban_home)` (function) — Downstream children read the parent's run.summary + metadata, not
- L1704 `test_migration_backfills_inflight_run_for_legacy_db(kanban_home)` (function) — An existing 'running' task from before task_runs existed should
- L1747 `test_forward_compat_columns_writable(kanban_home)` (function) — v2 will route by workflow_template_id + current_step_key. In v1
- L1768 `test_cli_runs_verb(kanban_home)` (function)
- L1782 `test_cli_runs_json(kanban_home)` (function)
- L1800 `test_cli_complete_with_summary_and_metadata(kanban_home)` (function)
- L1822 `test_cli_edit_backfills_result_on_done_task(kanban_home)` (function)
- L1852 `test_cli_edit_rejects_non_done_task(kanban_home)` (function)
- L1864 `test_cli_complete_bad_metadata_exits_nonzero(kanban_home)` (function)
- L1879 `test_archive_of_running_task_closes_run(kanban_home)` (function) — Archiving a claimed task must close the in-flight run with
- L1903 `test_archive_of_ready_task_does_not_create_spurious_run(kanban_home)` (function) — No active run → archive shouldn't synthesize one.
- L1916 `test_dashboard_direct_status_change_off_running_closes_run(kanban_home)` (function) — Dashboard drag-drop running->ready must close the active run.
- L1945 `test_dashboard_direct_status_change_within_same_state_is_noop_for_runs(kanban_home)` (function) — todo -> ready on an unclaimed task must not create any run rows.
- L1961 `test_cli_bulk_complete_with_summary_rejects(kanban_home)` (function)
- L1991 `test_cli_bulk_complete_without_summary_still_works(kanban_home)` (function) — Bulk close with no per-task handoff is allowed — the common case.
- L2005 `test_completed_event_payload_carries_summary(kanban_home)` (function) — The 'completed' event must embed the run summary so gateway
- L2023 `test_completed_event_payload_summary_none_when_missing(kanban_home)` (function) — If the caller passes no summary AND no result, payload.summary is None.
- L2041 `test_complete_never_claimed_task_synthesizes_run(kanban_home)` (function) — complete_task on a ready (never-claimed) task must persist the
- L2075 `test_block_never_claimed_task_synthesizes_run(kanban_home)` (function) — block_task on a ready task must persist --reason on a synthetic run.
- L2096 `test_complete_never_claimed_without_handoff_skips_synthesis(kanban_home)` (function) — If a bulk-complete passes no summary/metadata/result, don't spam
- L2109 `test_event_dataclass_carries_run_id(kanban_home)` (function) — list_events and the Event dataclass must expose run_id so
- L2133 `test_unseen_events_for_sub_includes_run_id(kanban_home)` (function) — Gateway notifier path must also surface run_id on events.
- L2157 `test_claim_task_recovers_from_invariant_leak(kanban_home)` (function) — Belt-and-suspenders: if a prior run somehow leaked (stranded
- L2195 `test_cli_create_on_fresh_home_auto_inits(tmp_path, monkeypatch)` (function) — First CLI action on an empty HERMES_HOME must not error with
- L2221 `test_connect_auto_inits_fresh_db(tmp_path, monkeypatch)` (function) — Calling connect() on a fresh HERMES_HOME must create the
- L2241 `test_cli_show_json_carries_runs(kanban_home)` (function) — hermes kanban show --json must include runs[] so scripts that
- L2277 `test_unblock_invariant_recovery(kanban_home)` (function) — unblock_task must leave current_run_id NULL even if some other
- L2309 `test_unblock_normal_path_no_spurious_run(kanban_home)` (function) — Happy path: claim -> block -> unblock. Unblock must be a no-op
- L2330 `test_migration_backfill_idempotent_under_re_run(tmp_path, monkeypatch)` (function) — init_db must be safe to re-run repeatedly. Each call should leave
- L2368 `test_build_worker_context_includes_role_history(kanban_home)` (function) — build_worker_context must surface recent completed runs for the
- L2398 `test_build_worker_context_role_history_skipped_when_no_assignee(kanban_home)` (function) — If task has no assignee, the role-history section is omitted.
- L2410 `test_build_worker_context_role_history_bounded_to_5(kanban_home)` (function) — Role history must be capped at 5 entries even when the assignee
- L2438 `test_pid_alive_detects_zombie(kanban_home)` (function) — _pid_alive must return False for a zombie process.
- L2472 `test_task_ids_dont_collide_at_scale(kanban_home)` (function) — ID generator must be wide enough that creating 10k tasks doesn't
- L2497 `test_cli_show_clamps_negative_elapsed(kanban_home)` (function) — When NTP jumps backward between claim and complete, started_at
- L2535 `test_resolve_workspace_rejects_relative_dir_path(kanban_home)` (function) — dir: workspace_path must be absolute. A relative path like
- L2556 `test_resolve_workspace_accepts_absolute_dir_path(kanban_home, tmp_path)` (function) — Legitimate absolute paths are accepted and created.
- L2574 `test_resolve_workspace_rejects_relative_worktree_path(kanban_home)` (function) — Worktree paths also must be absolute when explicitly set.
- L2589 `test_build_worker_context_caps_prior_attempts(kanban_home)` (function) — When a task has more than _CTX_MAX_PRIOR_ATTEMPTS runs, only
- L2632 `test_build_worker_context_renders_author_with_safe_framing(kanban_home)` (function) — Author rendering wraps the operator-controlled author in code fences
- L2653 `test_build_worker_context_caps_comments(kanban_home)` (function) — Same cap for comments — comment-storm tasks stay bounded.
- L2680 `test_build_worker_context_caps_huge_summary(kanban_home)` (function) — A 1 MB summary on a single prior run must not dominate the
- L2706 `test_default_spawn_auto_loads_kanban_worker_skill(kanban_home, monkeypatch)` (function) — The dispatcher's _default_spawn must include --skills kanban-worker
- L2761 `test_default_spawn_raises_terminal_timeout_to_task_runtime(kanban_home, monkeypatch)` (function) — A task runtime cap should raise the worker's terminal default.
- L2800 `test_default_spawn_preserves_longer_terminal_timeout(kanban_home, monkeypatch)` (function) — Kanban should never lower an explicitly larger terminal timeout.
- L2833 `test_default_spawn_leaves_terminal_timeout_without_runtime_cap(kanban_home, monkeypatch)` (function) — Uncapped tasks keep the existing terminal timeout behavior.
- L2861 `test_build_worker_context_includes_runtime_timeout_budget(kanban_home, monkeypatch)` (function)
- L2884 `test_create_task_persists_skills(kanban_home)` (function) — Task.skills round-trips through create -> get_task.
- L2901 `test_create_task_skills_none_stays_none(kanban_home)` (function) — Default behavior: no skills arg means Task.skills is None.
- L2913 `test_create_task_skills_deduplicates_and_strips(kanban_home)` (function) — Dup names collapse; whitespace is stripped; empties dropped.
- L2929 `test_create_task_skills_rejects_comma_embedded(kanban_home)` (function) — Comma in a skill name is rejected — force caller to pass a list.
- L2944 `test_create_task_skills_rejects_toolset_names(kanban_home)` (function) — Toolset names belong in profile config, not per-task skills.
- L2959 `test_create_task_skills_lists_all_toolset_typos(kanban_home)` (function) — When several toolset names are passed, the error names every one.
- L2986 `test_default_spawn_appends_per_task_skills(kanban_home, monkeypatch)` (function) — Dispatcher argv must carry one `--skills X` pair per task skill,
- L3037 `test_default_spawn_dedupes_kanban_worker_from_task_skills(kanban_home, monkeypatch)` (function) — If a task explicitly lists 'kanban-worker', we don't double-pass it.
- L3073 `test_cli_create_skill_flag_repeatable(kanban_home)` (function) — `hermes kanban create --skill a --skill b` persists the list.
- L3085 `test_cli_create_without_skill_flag_leaves_none(kanban_home)` (function) — No --skill on the CLI means Task.skills stays None (not []) —
- L3095 `test_cli_show_renders_skills(kanban_home)` (function) — `hermes kanban show <id>` prints a skills row when present.
- L3107 `test_legacy_db_without_skills_column_migrates(tmp_path)` (function) — _migrate_add_optional_columns is idempotent and adds skills
- L3163 `test_legacy_spawn_failure_columns_are_copied_not_renamed(tmp_path)` (function) — Legacy failure counters survive migration without fragile column renames.
- L3238 `test_legacy_migration_no_legacy_columns_at_all(tmp_path)` (function) — Scenario A: DB has neither spawn_failures nor consecutive_failures.
- L3297 `test_legacy_migration_both_columns_already_present(tmp_path)` (function) — Scenario D: DB already has both spawn_failures AND consecutive_failures.
- L3365 `test_config_default_dispatch_in_gateway_is_true()` (function) — Default config must enable gateway-embedded dispatch out of the box.
- L3381 `test_check_dispatcher_presence_silent_when_gateway_running(monkeypatch)` (function)
- L3394 `test_check_dispatcher_presence_warns_when_no_gateway(monkeypatch)` (function)
- L3406 `test_check_dispatcher_presence_warns_when_flag_off(monkeypatch)` (function) — Gateway is up but dispatch_in_gateway=false -> warning.
- L3419 `test_check_dispatcher_presence_silent_on_probe_error(monkeypatch)` (function) — If the probe itself errors, we stay silent.
- L3430 `_make_create_ns(**overrides)` (function) — Build a Namespace suitable for kb_cli._cmd_create().
- L3444 `test_cli_create_warns_when_no_gateway(kanban_home, monkeypatch, capsys)` (function) — ready+assigned task + no gateway -> warning on stderr.
- L3459 `test_cli_create_silent_when_gateway_up(kanban_home, monkeypatch, capsys)` (function) — gateway running + dispatch enabled -> no warning.
- L3473 `test_cli_create_no_warn_on_triage(kanban_home, monkeypatch, capsys)` (function) — Triage tasks can't be dispatched -> no warning.
- L3487 `test_cli_create_no_warn_unassigned(kanban_home, monkeypatch, capsys)` (function) — Unassigned tasks can't be dispatched -> no warning.
- L3501 `test_cli_daemon_without_force_prints_deprecation_exits_2(kanban_home, capsys)` (function) — `hermes kanban daemon` (no --force) is a deprecation stub.
- L3515 `test_cli_daemon_help_marks_deprecated()` (function) — The argparse help string on `daemon` mentions deprecation so users
- L3553 `test_gateway_dispatcher_watcher_respects_config_flag_off(monkeypatch)` (function) — dispatch_in_gateway=false -> watcher exits fast, no loop.
- L3574 `test_gateway_dispatcher_watcher_respects_env_override(monkeypatch)` (function) — HERMES_KANBAN_DISPATCH_IN_GATEWAY=0 disables without touching config.
- L3590 `test_gateway_dispatcher_watcher_env_truthy_uses_config(monkeypatch)` (function) — Truthy env value doesn't force-enable — config still decides.
- L3617 `test_gateway_dispatcher_disables_corrupt_board_without_traceback(monkeypatch, tmp_path, caplog, corrupt_exc)` (function) — Corrupt board DBs log one actionable error and stop retrying per tick.
- L3710 `test_gateway_dispatcher_retries_corrupt_board_after_quarantine(monkeypatch, tmp_path, caplog)` (function) — A corrupt-looking board is retried after the quarantine TTL expires.
- L3801 `test_complete_with_created_cards_all_verified_records_manifest(kanban_home)` (function) — A completion with created_cards that all exist + belong to this
- L3828 `test_complete_with_phantom_created_cards_raises_and_audits(kanban_home)` (function) — A completion claiming a card id that doesn't exist raises
- L3865 `test_complete_with_cross_worker_card_is_rejected(kanban_home)` (function) — A card that exists but was created by a different worker profile
- L3884 `test_complete_accepts_cross_worker_card_when_linked_as_child(kanban_home)` (function) — A card created by a different principal but explicitly linked as
- L3924 `test_complete_can_retry_after_phantom_rejection(kanban_home)` (function) — A worker that hits the hallucinated-card gate must be able to
- L3994 `test_complete_prose_scan_flags_nonexistent_ids(kanban_home)` (function) — Successful completion whose summary references a ``t_<hex>`` id
- L4023 `test_complete_prose_scan_ignores_existing_ids(kanban_home)` (function) — Summaries referencing real task ids don't emit a warning.
- L4049 `test_reclaim_task_resets_running_to_ready(kanban_home, monkeypatch)` (function) — Manual reclaim releases the claim, resets status, and emits a
- L4117 `test_reclaim_task_returns_false_for_already_ready(kanban_home)` (function) — Reclaiming a task that's not running returns False (no-op).
- L4127 `test_reassign_task_refuses_running_without_reclaim_first(kanban_home)` (function) — Without ``reclaim_first=True``, reassigning a running task is a
- L4149 `test_reassign_task_with_reclaim_first_switches_profile(kanban_home)` (function) — With ``reclaim_first=True``, a running task is reclaimed and
- L4193 `test_enforce_max_runtime_increments_consecutive_failures(kanban_home, monkeypatch)` (function) — A single timeout increments consecutive_failures by 1 (was the
- L4244 `test_repeated_timeouts_trip_the_circuit_breaker(kanban_home, monkeypatch)` (function) — N consecutive timeouts with the unified counter should eventually
- L4324 `test_detect_crashed_workers_increments_counter(kanban_home)` (function) — A single crash increments the consecutive_failures counter.
- L4341 `test_detect_crashed_workers_protocol_violation_auto_blocks(kanban_home)` (function) — A worker that exited rc=0 while its task was still ``running``
- L4400 `test_detect_crashed_workers_nonzero_exit_uses_default_limit(kanban_home)` (function) — A worker that exited non-zero (real error / crash) uses the
- L4435 `test_reclaim_task_clears_failure_counter(kanban_home)` (function) — Operator reclaim wipes the counter so the next retry gets a fresh
- L4473 `test_dispatch_once_integrates_stale_detection(kanban_home, monkeypatch)` (function) — dispatch_once with stale_timeout_seconds reclaims stale running tasks.
- L4504 `test_dispatch_once_stale_disabled_when_timeout_zero(kanban_home, monkeypatch)` (function) — dispatch_once with stale_timeout_seconds=0 skips stale detection.
