---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:44'
updated: '2026-06-08T00:38:44'
---

# tools/skills_hub.py

Symbols in `tools/skills_hub.py`.

- L70 `SkillMeta` (class) — Minimal metadata returned by search results.
- L84 `SkillBundle` (class) — A downloaded skill ready for quarantine/scanning/installation.
- L94 `_normalize_bundle_path(path_value: str, *, field_name: str, allow_nested: bool)` (function) — Normalize and validate bundle-controlled paths before touching disk.
- L119 `_validate_skill_name(name: str)` (function)
- L123 `_validate_install_parent_path(category: str)` (function)
- L127 `_normalize_lock_install_path(install_path: str, skill_name: str)` (function) — Validate a skill install path before it touches the lock file or disk.
- L154 `_is_path_redirect(path: Path)` (function) — True when ``path`` is a symlink or (on Windows) a directory junction.
- L164 `_resolve_lock_install_path(install_path: str, skill_name: str)` (function) — Resolve a lock-file install path without allowing escapes from ``SKILLS_DIR``.
- L192 `_guarded_http_get(url: str, *, timeout: int=20)` (function) — Fetch a URL with SSRF and redirect-target validation.
- L229 `_validate_bundle_rel_path(rel_path: str)` (function)
- L237 `GitHubAuth` (class) — GitHub API authentication. Tries methods in priority order:
- L246 `__init__(self)` (method)
- L251 `get_headers(self)` (method) — Return authorization headers for GitHub API requests.
- L259 `is_authenticated(self)` (method)
- L262 `auth_method(self)` (method) — Return which auth method is active: 'pat', 'gh-cli', 'github-app', or 'anonymous'.
- L267 `_resolve_token(self)` (method)
- L298 `_try_gh_cli(self)` (method) — Try to get a token from the gh CLI.
- L311 `_try_github_app(self)` (method) — Try GitHub App JWT authentication if credentials are configured.
- L360 `SkillSource` (class) — Abstract base for all skill registry adapters.
- L364 `search(self, query: str, limit: int=10)` (method) — Search for skills matching a query string.
- L369 `fetch(self, identifier: str)` (method) — Download a skill bundle by identifier.
- L374 `inspect(self, identifier: str)` (method) — Fetch metadata for a skill without downloading all files.
- L379 `source_id(self)` (method) — Unique identifier for this source (e.g. 'github', 'clawhub').
- L383 `trust_level_for(self, identifier: str)` (method) — Determine trust level for a skill from this source.
- L392 `GitHubSource` (class) — Fetch skills from GitHub repos via the Contents API.
- L415 `__init__(self, auth: GitHubAuth, extra_taps: Optional[List[Dict]]=None)` (method)
- L430 `source_id(self)` (method)
- L434 `is_rate_limited(self)` (method) — Whether GitHub API rate limit was hit during operations.
- L438 `trust_level_for(self, identifier: str)` (method)
- L447 `search(self, query: str, limit: int=10)` (method) — Search all taps for skills matching the query.
- L477 `fetch(self, identifier: str)` (method) — Download a skill from GitHub.
- L504 `inspect(self, identifier: str)` (method) — Fetch just the SKILL.md metadata for preview.
- L545 `_list_skills_in_repo(self, repo: str, path: str)` (method) — List skill directories in a GitHub repo path, using cached index.
- L590 `_get_repo_tree(self, repo: str)` (method) — Get cached or fresh repo tree.
- L640 `_check_rate_limit_response(self, resp: 'httpx.Response')` (method) — Flag the instance as rate-limited when GitHub returns 403 + exhausted quota.
- L651 `_download_directory(self, repo: str, path: str)` (method) — Recursively download all text files from a GitHub directory.
- L665 `_download_directory_via_tree(self, repo: str, path: str)` (method) — Download an entire directory using the Git Trees API (single request).
- L708 `_download_directory_recursive(self, repo: str, path: str)` (method) — Recursively download via Contents API (fallback).
- L742 `_find_skill_in_repo_tree(self, repo: str, skill_name: str)` (method) — Use the GitHub Trees API to find a skill directory anywhere in the repo.
- L768 `_fetch_file_content(self, repo: str, path: str)` (method) — Fetch a single file's content from GitHub.
- L784 `_get_skillsh_groupings(self, repo: str)` (method) — Fetch and parse the repo-root ``skills.sh.json`` grouping sidecar.
- L810 `_parse_skillsh_groupings(content: str)` (method) — Flatten a ``skills.sh.json`` document into ``{skill_name: title}``.
- L839 `_read_cache(self, key: str)` (method) — Read cached index if not expired.
- L852 `_write_cache(self, key: str, data: list)` (method) — Write index data to cache.
- L862 `_meta_to_dict(meta: SkillMeta)` (method)
- L876 `_parse_frontmatter_quick(content: str)` (method) — Parse YAML frontmatter from SKILL.md content.
- L895 `WellKnownSkillSource` (class) — Read skills from a domain exposing /.well-known/skills/index.json.
- L900 `source_id(self)` (method)
- L903 `trust_level_for(self, identifier: str)` (method)
- L906 `search(self, query: str, limit: int=10)` (method)
- L937 `inspect(self, identifier: str)` (method)
- L968 `fetch(self, identifier: str)` (method)
- L1022 `_query_to_index_url(self, query: str)` (method)
- L1033 `_parse_identifier(self, identifier: str)` (method)
- L1071 `_parse_index(self, index_url: str)` (method)
- L1097 `_index_entry(self, index_url: str, skill_name: str)` (method)
- L1107 `_fetch_text(url: str)` (method)
- L1114 `_wrap_identifier(base_url: str, skill_name: str)` (method)
- L1122 `UrlSource` (class) — Fetch a single-file SKILL.md skill directly from an HTTP(S) URL.
- L1135 `source_id(self)` (method)
- L1138 `trust_level_for(self, identifier: str)` (method)
- L1142 `search(self, query: str, limit: int=10)` (method)
- L1145 `_matches(self, identifier: str)` (method) — Return True iff this source should handle ``identifier``.
- L1168 `inspect(self, identifier: str)` (method)
- L1197 `fetch(self, identifier: str)` (method)
- L1231 `_fetch_text(url: str)` (method)
- L1243 `_is_valid_skill_name(cls, name: Optional[str])` (method)
- L1252 `_resolve_skill_name(cls, fm: dict, url: str)` (method) — Pick a skill name from frontmatter or URL.
- L1288 `SkillsShSource` (class) — Discover skills via skills.sh and fetch content from the underlying GitHub repo.
- L1320 `__init__(self, auth: GitHubAuth)` (method)
- L1324 `source_id(self)` (method)
- L1327 `trust_level_for(self, identifier: str)` (method)
- L1330 `search(self, query: str, limit: int=10)` (method)
- L1367 `fetch(self, identifier: str)` (method)
- L1388 `inspect(self, identifier: str)` (method)
- L1396 `_sitemap_catalog(self, limit: int)` (method) — Walk the skills.sh sitemap to enumerate the full catalog.
- L1487 `_featured_skills(self, limit: int)` (method)
- L1527 `_meta_from_search_item(self, item: dict)` (method)
- L1563 `_fetch_detail_page(self, identifier: str)` (method)
- L1581 `_parse_detail_page(self, identifier: str, html: str)` (method)
- L1618 `_discover_identifier(self, identifier: str, detail: Optional[dict]=None)` (method)
- L1687 `_resolve_github_meta(self, identifier: str, detail: Optional[dict]=None)` (method)
- L1698 `_finalize_inspect_meta(self, meta: SkillMeta, canonical: str, detail: Optional[dict])` (method)
- L1716 `_matches_skill_tokens(cls, meta: SkillMeta, skill_tokens: List[str])` (method)
- L1729 `_token_variants(value: Optional[str])` (method)
- L1760 `_extract_repo_slug(repo_value: str)` (method)
- L1771 `_extract_first_match(pattern: re.Pattern, text: str)` (method)
- L1780 `_detail_to_metadata(self, canonical: str, detail: Optional[dict])` (method)
- L1796 `_extract_weekly_installs(html: str)` (method)
- L1803 `_extract_security_audits(html: str, identifier: str)` (method)
- L1816 `_strip_html(value: str)` (method)
- L1820 `_normalize_identifier(identifier: str)` (method)
- L1833 `_candidate_identifiers(identifier: str)` (method)
- L1856 `_wrap_identifier(identifier: str)` (method)
- L1864 `ClawHubSource` (class) — Fetch skills from ClawHub (clawhub.ai) via their HTTP API.
- L1873 `source_id(self)` (method)
- L1876 `trust_level_for(self, identifier: str)` (method)
- L1880 `_normalize_tags(tags: Any)` (method)
- L1888 `_coerce_skill_payload(data: Any)` (method)
- L1901 `_query_terms(query: str)` (method)
- L1905 `_search_score(cls, query: str, meta: SkillMeta)` (method)
- L1954 `_dedupe_results(results: List[SkillMeta])` (method)
- L1965 `_exact_slug_meta(self, query: str)` (method)
- L1998 `_finalize_search_results(self, query: str, results: List[SkillMeta], limit: int)` (method)
- L2026 `search(self, query: str, limit: int=10)` (method)
- L2097 `fetch(self, identifier: str)` (method)
- L2139 `inspect(self, identifier: str)` (method)
- L2156 `_search_catalog(self, query: str, limit: int=10)` (method)
- L2170 `_load_catalog_index(self)` (method)
- L2227 `_get_json(self, url: str, timeout: int=20)` (method)
- L2236 `_resolve_latest_version(self, slug: str, skill_data: Dict[str, Any])` (method)
- L2258 `_extract_files(self, version_data: Dict[str, Any])` (method)
- L2289 `_download_zip(self, slug: str, version: str)` (method) — Download skill as a ZIP bundle from the /download endpoint and extract text files.
- L2352 `_fetch_text(self, url: str)` (method)
- L2363 `ClaudeMarketplaceSource` (class) — Discover skills from Claude Code marketplace repos.
- L2374 `__init__(self, auth: GitHubAuth)` (method)
- L2377 `source_id(self)` (method)
- L2380 `trust_level_for(self, identifier: str)` (method)
- L2388 `search(self, query: str, limit: int=10)` (method)
- L2416 `fetch(self, identifier: str)` (method)
- L2424 `inspect(self, identifier: str)` (method)
- L2432 `_fetch_marketplace_index(self, repo: str)` (method) — Fetch and parse .claude-plugin/marketplace.json from a repo.
- L2461 `LobeHubSource` (class) — Fetch skills from LobeHub's agent marketplace (14,500+ agents).
- L2470 `source_id(self)` (method)
- L2473 `trust_level_for(self, identifier: str)` (method)
- L2476 `search(self, query: str, limit: int=10)` (method)
- L2511 `fetch(self, identifier: str)` (method)
- L2528 `inspect(self, identifier: str)` (method)
- L2551 `_fetch_index(self)` (method) — Fetch the LobeHub agent index (cached for 1 hour).
- L2569 `_fetch_agent(self, agent_id: str)` (method) — Fetch a single agent's JSON file.
- L2581 `_convert_to_skill_md(agent_data: dict)` (method) — Convert a LobeHub agent JSON into SKILL.md format.
- L2621 `BrowseShSource` (class) — Discover and install site-specific browser automation skills from browse.sh.
- L2637 `source_id(self)` (method)
- L2640 `trust_level_for(self, identifier: str)` (method)
- L2643 `_fetch_catalog(self)` (method)
- L2659 `_item_to_meta(self, item: Dict)` (method)
- L2686 `search(self, query: str, limit: int=10)` (method)
- L2707 `inspect(self, identifier: str)` (method)
- L2717 `fetch(self, identifier: str)` (method)
- L2757 `_resolve_skill_md_url(self, slug: str, item: Dict)` (method) — Resolve the SKILL.md content URL for a slug.
- L2784 `_slug_from_identifier(self, identifier: str)` (method) — Extract slug from identifier like 'browse-sh/airbnb.com/search-listings-abc'.
- L2795 `OptionalSkillSource` (class) — Fetch skills from the optional-skills/ directory shipped with the repo.
- L2805 `__init__(self)` (method)
- L2812 `source_id(self)` (method)
- L2815 `trust_level_for(self, identifier: str)` (method)
- L2820 `search(self, query: str, limit: int=10)` (method)
- L2835 `fetch(self, identifier: str)` (method)
- L2887 `inspect(self, identifier: str)` (method)
- L2898 `_find_skill_dir(self, name: str)` (method) — Find a skill directory by name anywhere in optional-skills/.
- L2909 `_scan_all(self)` (method) — Enumerate all optional skills with metadata.
- L2950 `_parse_frontmatter(content: str)` (method) — Parse YAML frontmatter from SKILL.md content.
- L2969 `_read_index_cache(key: str)` (function) — Read cached data if not expired.
- L2983 `_write_index_cache(key: str, data: Any)` (function) — Write data to cache.
- L3002 `_skill_meta_to_dict(meta: SkillMeta)` (function) — Convert a SkillMeta to a dict for caching.
- L3021 `HubLockFile` (class) — Manages skills/.hub/lock.json — tracks provenance of installed hub skills.
- L3024 `__init__(self, path: Path=LOCK_FILE)` (method)
- L3027 `load(self)` (method)
- L3035 `save(self, data: dict)` (method)
- L3039 `record_install(self, name: str, source: str, identifier: str, trust_level: str, scan_verdict: str, skill_hash: str, install_path: str, files: List[str], metadata: Optional[Dict[str, Any]]=None)` (method)
- L3072 `record_uninstall(self, name: str)` (method)
- L3077 `get_installed(self, name: str)` (method)
- L3081 `list_installed(self)` (method)
- L3093 `TapsManager` (class) — Manages the taps.json file — custom GitHub repo sources.
- L3096 `__init__(self, path: Path=TAPS_FILE)` (method)
- L3099 `load(self)` (method)
- L3108 `save(self, taps: List[dict])` (method)
- L3112 `add(self, repo: str, path: str='skills/')` (method) — Add a tap. Returns False if already exists.
- L3121 `remove(self, repo: str)` (method) — Remove a tap by repo name. Returns False if not found.
- L3130 `list_taps(self)` (method)
- L3138 `append_audit_log(action: str, skill_name: str, source: str, trust_level: str, verdict: str, extra: str='')` (function) — Append a line to the audit log.
- L3158 `ensure_hub_dirs()` (function) — Create the .hub directory structure if it doesn't exist.
- L3171 `quarantine_bundle(bundle: SkillBundle)` (function) — Write a skill bundle to the quarantine directory for scanning.
- L3196 `install_from_quarantine(quarantine_path: Path, skill_name: str, category: str, bundle: SkillBundle, scan_result: ScanResult)` (function) — Move a scanned skill from quarantine into the skills directory.
- L3281 `uninstall_skill(skill_name: str)` (function) — Remove a hub-installed skill. Refuses to remove builtins.
- L3311 `bundle_content_hash(bundle: SkillBundle)` (function) — Compute a deterministic hash for an in-memory skill bundle.
- L3327 `_source_matches(source: SkillSource, source_name: str)` (function)
- L3335 `check_for_skill_updates(name: Optional[str]=None, *, lock: Optional[HubLockFile]=None, sources: Optional[List[SkillSource]]=None, auth: Optional[GitHubAuth]=None)` (function) — Check installed hub skills for upstream changes.
- L3400 `_load_hermes_index()` (function) — Fetch the centralized skills index, with local cache.
- L3441 `_load_stale_index_cache()` (function) — Fall back to stale cache when the network fetch fails.
- L3451 `HermesIndexSource` (class) — Skill source backed by the centralized Hermes Skills Index.
- L3463 `__init__(self, auth: GitHubAuth)` (method)
- L3471 `_ensure_loaded(self)` (method)
- L3477 `_get_github(self)` (method)
- L3482 `source_id(self)` (method)
- L3486 `is_available(self)` (method) — Whether the index is loaded and has skills.
- L3491 `trust_level_for(self, identifier: str)` (method)
- L3498 `search(self, query: str, limit: int=10)` (method) — Search the cached index.  Zero API calls.
- L3519 `fetch(self, identifier: str)` (method) — Fetch a skill using the resolved path from the index.
- L3554 `inspect(self, identifier: str)` (method) — Return metadata from the index.  Zero API calls.
- L3562 `_find_entry(self, identifier: str, index: dict)` (method) — Look up a skill in the index by identifier or name.
- L3593 `_to_meta(entry: dict)` (method)
- L3607 `create_source_router(auth: Optional[GitHubAuth]=None)` (function) — Create all configured source adapters.
- L3634 `_search_one_source(src: SkillSource, query: str, limit: int)` (function) — Search a single source.  Runs in a thread for parallelism.
- L3645 `parallel_search_sources(sources: List[SkillSource], query: str='', per_source_limits: Optional[Dict[str, int]]=None, source_filter: str='all', overall_timeout: float=30, on_source_done: Optional[Any]=None)` (function) — Search all sources in parallel with per-source timeout.
- L3725 `unified_search(query: str, sources: List[SkillSource], source_filter: str='all', limit: int=10)` (function) — Search all sources (in parallel) and merge results.
