---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:43'
updated: '2026-06-08T00:38:43'
---

# tests/run_agent/test_run_agent.py

Symbols in `tests/run_agent/test_run_agent.py`.

- L34 `_make_tool_defs(*names: str)` (function) — Build minimal tool definition list accepted by AIAgent.__init__.
- L49 `test_is_destructive_command_treats_cp_as_mutating()` (function)
- L53 `test_is_destructive_command_treats_install_as_mutating()` (function)
- L58 `agent()` (function) — Minimal AIAgent with mocked OpenAI client and tool loading.
- L79 `agent_with_memory_tool()` (function) — Agent whose valid_tool_names includes 'memory'.
- L100 `test_aiagent_reuses_existing_errors_log_handler()` (function) — Repeated AIAgent init should not accumulate duplicate errors.log handlers.
- L156 `TestProviderModelNormalization` (class)
- L157 `test_aiagent_strips_matching_native_provider_prefix(self)` (method)
- L177 `test_aiagent_keeps_aggregator_vendor_slug(self)` (method)
- L203 `_mock_assistant_msg(content='Hello', tool_calls=None, reasoning=None, reasoning_content=None, reasoning_details=None)` (function) — Return a SimpleNamespace mimicking an OpenAI ChatCompletionMessage.
- L221 `_mock_tool_call(name='web_search', arguments='{}', call_id=None)` (function) — Return a SimpleNamespace mimicking a tool call object.
- L230 `_mock_response(content='Hello', finish_reason='stop', tool_calls=None, reasoning=None, reasoning_content=None, reasoning_details=None, usage=None)` (function) — Return a SimpleNamespace mimicking an OpenAI ChatCompletion response.
- L261 `TestHasContentAfterThinkBlock` (class)
- L262 `test_none_returns_false(self, agent)` (method)
- L265 `test_empty_returns_false(self, agent)` (method)
- L268 `test_only_think_block_returns_false(self, agent)` (method)
- L271 `test_content_after_think_returns_true(self, agent)` (method)
- L277 `test_no_think_block_returns_true(self, agent)` (method)
- L281 `TestStripThinkBlocks` (class)
- L282 `test_none_returns_empty(self, agent)` (method)
- L285 `test_no_blocks_unchanged(self, agent)` (method)
- L288 `test_single_block_removed(self, agent)` (method)
- L293 `test_multiline_block_removed(self, agent)` (method)
- L299 `test_orphaned_closing_think_tag(self, agent)` (method)
- L304 `test_orphaned_closing_thinking_tag(self, agent)` (method)
- L309 `test_orphaned_opening_think_tag(self, agent)` (method)
- L313 `test_mixed_orphaned_and_paired_tags(self, agent)` (method)
- L320 `test_thought_block_removed(self, agent)` (method) — Gemma 4 uses <thought> tags for inline reasoning.
- L327 `test_orphaned_thought_tag(self, agent)` (method)
- L337 `test_unterminated_think_block_content_stripped(self, agent)` (method) — Content after unterminated <think> is fully stripped.
- L343 `test_unterminated_thought_block_content_stripped(self, agent)` (method) — Gemma-style <thought> with no close is fully stripped.
- L349 `test_unterminated_multiline_block_stripped(self, agent)` (method) — Multi-line unterminated blocks are stripped in full.
- L357 `test_unterminated_block_after_answer_preserves_prefix(self, agent)` (method) — Visible answer before a line-starting unterminated tag is kept.
- L365 `test_inline_think_mention_in_prose_not_over_stripped(self, agent)` (method) — Mid-line `<think>` mentioned in prose must not swallow the rest
- L374 `test_mixed_case_closed_pair_stripped(self, agent)` (method) — Mixed-case variants <THINK>…</THINK>, <Thinking>…</Thinking> are
- L391 `test_tool_call_block_stripped(self, agent)` (method)
- L398 `test_function_calls_block_stripped(self, agent)` (method)
- L404 `test_gemma_function_name_block_stripped(self, agent)` (method) — Gemma-style: <function name="read"><parameter>...</parameter></function>.
- L417 `test_gemma_function_multiline_payload_stripped(self, agent)` (method)
- L430 `test_function_mention_in_prose_preserved(self, agent)` (method) — 'Use <function> in JavaScript.' — no name attr, not at block boundary
- L438 `test_function_with_attr_in_middle_of_sentence_preserved(self, agent)` (method) — Docs example: 'Use <function name="x">...</function> in docs.'
- L448 `test_stray_function_close_tag_removed(self, agent)` (method)
- L455 `test_dangling_function_open_tag_preserved(self, agent)` (method) — A streamed-but-truncated <function name="..."> block with no close
- L463 `test_mixed_reasoning_and_tool_call_both_stripped(self, agent)` (method)
- L471 `TestExtractReasoning` (class)
- L472 `test_reasoning_field(self, agent)` (method)
- L476 `test_reasoning_content_field(self, agent)` (method)
- L480 `test_reasoning_details_array(self, agent)` (method)
- L486 `test_no_reasoning_returns_none(self, agent)` (method)
- L490 `test_combined_reasoning(self, agent)` (method)
- L499 `test_deduplication(self, agent)` (method)
- L518 `test_inline_reasoning_blocks_fallback(self, agent, content, expected)` (method)
- L522 `test_content_list_thinking_blocks_extracted(self, agent)` (method) — DeepSeek V4 Pro returns content as a typed-block list (issue #21944).
- L537 `test_content_list_non_thinking_blocks_ignored(self, agent)` (method) — Non-thinking blocks in a content list must not be treated as reasoning.
- L546 `test_content_list_thinking_prefers_structured_field(self, agent)` (method) — Structured ``reasoning`` field wins over content-list thinking blocks.
- L559 `TestSessionJsonSnapshotOptIn` (class) — Regression: per-session JSON snapshot writer is opt-in via config.
- L569 `test_session_json_disabled_by_default(self, agent)` (method)
- L575 `test_save_session_log_noops_when_disabled(self, agent, tmp_path)` (method)
- L585 `test_save_session_log_writes_when_enabled(self, agent, tmp_path)` (method)
- L597 `test_logs_dir_retained_for_request_dumps(self, agent)` (method)
- L605 `TestSaveSessionLogRedactsSecrets` (class) — Regression: session_*.json must not contain plaintext credentials (#19798, #19845).
- L609 `_ensure_redaction_enabled(self, monkeypatch)` (method) — Force redaction on regardless of host HERMES_REDACT_SECRETS state.
- L617 `test_redacts_api_key_in_tool_content(self, agent, tmp_path)` (method)
- L632 `test_redacts_api_key_in_user_message(self, agent, tmp_path)` (method)
- L643 `test_redacts_system_prompt_credentials(self, agent, tmp_path)` (method)
- L652 `test_redacts_list_type_multimodal_content(self, agent, tmp_path)` (method) — OpenAI/Anthropic multimodal shape: content = list of {type, text|image_url} parts.
- L675 `TestGetMessagesUpToLastAssistant` (class)
- L676 `test_empty_list(self, agent)` (method)
- L679 `test_no_assistant_returns_copy(self, agent)` (method)
- L685 `test_single_assistant(self, agent)` (method)
- L694 `test_multiple_assistants_returns_up_to_last(self, agent)` (method)
- L705 `test_assistant_then_tool_messages(self, agent)` (method)
- L717 `TestMaskApiKey` (class)
- L718 `test_none_returns_none(self, agent)` (method)
- L721 `test_short_key_returns_stars(self, agent)` (method)
- L724 `test_long_key_masked(self, agent)` (method)
- L737 `TestInit` (class)
- L738 `test_anthropic_base_url_accepted(self)` (method) — Anthropic base URLs should route to native Anthropic client.
- L755 `test_prompt_caching_claude_openrouter(self)` (method) — Claude model via OpenRouter should enable prompt caching.
- L772 `test_prompt_caching_non_claude(self)` (method) — Non-Claude model should disable prompt caching.
- L789 `test_prompt_caching_non_openrouter(self)` (method) — Custom base_url (not OpenRouter) should disable prompt caching.
- L806 `test_prompt_caching_native_anthropic(self)` (method) — Native Anthropic provider should enable prompt caching.
- L823 `test_prompt_caching_cache_ttl_defaults_without_config(self)` (method) — cache_ttl stays 5m when prompt_caching is absent from config.
- L841 `test_prompt_caching_cache_ttl_custom_1h(self)` (method) — prompt_caching.cache_ttl 1h is applied when present in config.
- L862 `test_model_max_tokens_from_config(self)` (method) — model.max_tokens config populates the chat-completions request cap.
- L888 `test_constructor_max_tokens_wins_over_config(self)` (method) — Explicit constructor max_tokens keeps programmatic callers stable.
- L912 `test_prompt_caching_cache_ttl_invalid_falls_back(self)` (method) — Non-Anthropic TTL values keep default 5m without raising.
- L933 `test_valid_tool_names_populated(self)` (method) — valid_tool_names should contain names from loaded tools.
- L950 `test_session_id_auto_generated(self)` (method) — Session ID should be auto-generated in YYYYMMDD_HHMMSS_<hex6> format.
- L970 `TestInterrupt` (class)
- L971 `test_interrupt_sets_flag(self, agent)` (method)
- L976 `test_interrupt_with_message(self, agent)` (method)
- L981 `test_clear_interrupt(self, agent)` (method)
- L988 `test_is_interrupted_property(self, agent)` (method)
- L995 `TestHydrateTodoStore` (class)
- L996 `test_no_todo_in_history(self, agent)` (method)
- L1005 `test_recovers_from_history(self, agent)` (method)
- L1020 `test_skips_non_todo_tools(self, agent)` (method)
- L1032 `test_invalid_json_skipped(self, agent)` (method)
- L1045 `TestBuildSystemPrompt` (class)
- L1046 `test_always_has_identity(self, agent)` (method)
- L1050 `test_can_use_soul_identity_even_when_context_files_are_skipped(self)` (method)
- L1070 `test_includes_system_message(self, agent)` (method)
- L1074 `test_memory_guidance_when_memory_tool_loaded(self, agent_with_memory_tool)` (method)
- L1080 `test_no_memory_guidance_without_tool(self, agent)` (method)
- L1086 `test_includes_datetime(self, agent)` (method)
- L1091 `test_datetime_is_date_only_not_minute_precision(self, agent)` (method) — Timestamp must be date-only (no HH:MM) so the system prompt
- L1113 `test_includes_nous_subscription_prompt(self, agent, monkeypatch)` (method)
- L1118 `test_skills_prompt_derives_available_toolsets_from_loaded_tools(self)` (method)
- L1152 `TestToolUseEnforcementConfig` (class) — Tests for the agent.tool_use_enforcement config option.
- L1155 `_make_agent(self, model='openai/gpt-4.1', tool_use_enforcement='auto')` (method) — Create an agent with tools and a specific enforcement config.
- L1180 `test_auto_injects_for_gpt(self)` (method)
- L1186 `test_auto_injects_for_codex(self)` (method)
- L1192 `test_auto_skips_for_claude(self)` (method)
- L1198 `test_auto_injects_for_grok(self)` (method) — xAI Grok / xai-oauth models hit the same enforcement path as GPT.
- L1205 `test_auto_injects_for_qwen(self)` (method) — Qwen models default to chatty/hallucinatory tool use without enforcement.
- L1212 `test_auto_injects_for_deepseek(self)` (method) — DeepSeek models default to chatty/hallucinatory tool use without enforcement.
- L1219 `test_auto_injects_execution_guidance_for_grok(self)` (method) — Grok also gets OPENAI_MODEL_EXECUTION_GUIDANCE (verification,
- L1230 `test_auto_injects_execution_guidance_for_xai_oauth_model(self)` (method) — xai-oauth bare model names (no slash) also match the grok pattern.
- L1237 `test_auto_does_not_inject_execution_guidance_for_claude(self)` (method) — Sanity: execution guidance stays off for non-targeted families.
- L1246 `test_true_forces_for_all_models(self)` (method)
- L1252 `test_string_true_forces_for_all_models(self)` (method)
- L1258 `test_always_forces_for_all_models(self)` (method)
- L1264 `test_false_disables_for_gpt(self)` (method)
- L1270 `test_string_false_disables(self)` (method)
- L1276 `test_custom_list_matches(self)` (method)
- L1285 `test_custom_list_no_match(self)` (method)
- L1294 `test_custom_list_case_insensitive(self)` (method)
- L1303 `test_no_tools_never_injects(self)` (method) — Even with enforcement=true, no injection when agent has no tools.
- L1328 `TestTaskCompletionGuidance` (class) — Tests for the universal task-completion / no-fabrication guidance
- L1336 `_make_agent(self, model='anthropic/claude-opus-4.8', task_completion_guidance=True, **extra_cfg)` (method)
- L1363 `test_default_injects_for_claude(self)` (method) — The block must reach Claude by default — that's the
- L1371 `test_default_injects_for_deepseek(self)` (method) — And for DeepSeek — the other model that failed the Sarasota
- L1379 `test_default_injects_for_gpt(self)` (method) — Also reaches model families that already get enforcement —
- L1387 `test_false_disables(self)` (method)
- L1395 `test_no_tools_no_injection(self)` (method) — Same gate as tool_use_enforcement — no tools means no guidance.
- L1421 `TestEnvironmentProbeIntegration` (class) — Tests for the local Python toolchain probe wiring (config.yaml
- L1427 `_make_agent(self, model='anthropic/claude-opus-4.8', environment_probe=True)` (method)
- L1452 `test_probe_appears_when_problem_detected(self, monkeypatch)` (method) — When the probe finds something off, the line lands in the prompt.
- L1469 `test_probe_silent_on_clean_env(self, monkeypatch)` (method) — Clean environment → probe emits nothing → no line in prompt.
- L1484 `test_probe_disabled_by_config(self, monkeypatch)` (method) — Even with detectable problems, the probe stays out when disabled.
- L1500 `TestInvalidateSystemPrompt` (class)
- L1501 `test_clears_cache(self, agent)` (method)
- L1506 `test_reloads_memory_store(self, agent)` (method)
- L1514 `TestBuildApiKwargs` (class)
- L1515 `test_basic_kwargs(self, agent)` (method)
- L1522 `test_public_moonshot_kimi_k2_5_omits_temperature(self, agent)` (method) — Kimi models should NOT have client-side temperature overrides.
- L1536 `test_public_moonshot_cn_kimi_k2_5_omits_temperature(self, agent)` (method)
- L1546 `test_kimi_coding_endpoint_omits_temperature(self, agent)` (method)
- L1557 `test_kimi_coding_endpoint_sends_max_tokens_and_reasoning(self, agent)` (method) — Kimi endpoint sends max_tokens=32000. With no reasoning_config it
- L1573 `test_kimi_coding_endpoint_respects_custom_effort(self, agent)` (method) — reasoning_effort should reflect reasoning_config.effort when set.
- L1586 `test_kimi_coding_endpoint_sends_thinking_extra_body(self, agent)` (method) — Kimi endpoint should send extra_body.thinking={"type":"enabled"}
- L1599 `test_kimi_coding_endpoint_disables_thinking(self, agent)` (method) — When reasoning_config.enabled=False, thinking should be disabled
- L1615 `test_moonshot_endpoint_sends_max_tokens_and_reasoning(self, agent)` (method) — api.moonshot.ai should get the same Kimi-compatible params.
- L1629 `test_moonshot_cn_endpoint_sends_max_tokens_and_reasoning(self, agent)` (method) — api.moonshot.cn (China endpoint) should get the same params.
- L1643 `test_provider_preferences_injected(self, agent)` (method)
- L1651 `test_reasoning_config_default_openrouter(self, agent)` (method) — Default reasoning config for OpenRouter should be medium.
- L1662 `test_reasoning_config_custom(self, agent)` (method)
- L1671 `test_reasoning_not_sent_for_unsupported_openrouter_model(self, agent)` (method)
- L1678 `test_reasoning_sent_for_supported_openrouter_model(self, agent)` (method)
- L1686 `test_reasoning_sent_for_nous_route(self, agent)` (method)
- L1694 `test_reasoning_sent_for_copilot_gpt5(self, agent)` (method) — Copilot/GitHub Models: GPT-5 reasoning goes in extra_body.reasoning.
- L1711 `test_reasoning_xhigh_normalized_for_copilot(self, agent)` (method) — xhigh effort should normalize to high for Copilot GitHub Models.
- L1729 `test_reasoning_omitted_for_non_reasoning_copilot_model(self, agent)` (method)
- L1736 `test_max_tokens_injected(self, agent)` (method)
- L1743 `test_qwen_portal_formats_messages_and_metadata(self, agent)` (method)
- L1760 `test_qwen_portal_normalizes_bare_string_content_parts(self, agent)` (method)
- L1773 `test_qwen_portal_no_system_message(self, agent)` (method)
- L1783 `test_qwen_portal_sends_explicit_max_tokens(self, agent)` (method) — When the user explicitly sets max_tokens, it should be sent to Qwen Portal.
- L1792 `test_qwen_portal_default_max_tokens(self, agent)` (method) — When max_tokens is None, Qwen Portal gets a default of 65536
- L1803 `test_ollama_think_false_on_effort_none(self, agent)` (method) — Custom (Ollama) provider with effort=none should inject think=false.
- L1813 `test_ollama_think_false_on_enabled_false(self, agent)` (method) — Custom (Ollama) provider with enabled=false should inject think=false.
- L1823 `test_ollama_no_think_param_when_reasoning_enabled(self, agent)` (method) — Custom provider with reasoning enabled should NOT inject think=false.
- L1833 `test_non_custom_provider_unaffected(self, agent)` (method) — OpenRouter provider with effort=none should NOT inject think=false.
- L1844 `TestBuildAssistantMessage` (class)
- L1845 `test_basic_message(self, agent)` (method)
- L1852 `test_with_reasoning(self, agent)` (method)
- L1857 `test_reasoning_content_preserved_separately(self, agent)` (method)
- L1866 `test_with_tool_calls(self, agent)` (method)
- L1873 `test_with_reasoning_details(self, agent)` (method)
- L1880 `test_empty_content(self, agent)` (method)
- L1885 `test_streaming_only_reasoning_promoted_to_reasoning_content(self, agent)` (method) — Refs #16844 / #16884. Streaming-only providers (glm, MiniMax,
- L1913 `test_sdk_reasoning_content_still_wins_over_fallback(self, agent)` (method) — Additive fallback must not override SDK-supplied reasoning_content.
- L1928 `test_no_reasoning_text_leaves_field_absent(self, agent)` (method) — Non-thinking turns with no reasoning leave reasoning_content absent.
- L1941 `test_tool_call_extra_content_preserved(self, agent)` (method) — Gemini thinking models attach extra_content with thought_signature
- L1954 `test_tool_call_without_extra_content(self, agent)` (method) — Standard tool calls (no thinking model) should not have extra_content.
- L1961 `test_think_blocks_stripped_from_content(self, agent)` (method) — Inline <think> blocks are stripped from stored content (#8878, #9568).
- L1979 `test_think_blocks_stripped_preserves_normal_content(self, agent)` (method) — Content without reasoning tags passes through unchanged.
- L1985 `test_memory_context_in_stored_content_is_preserved(self, agent)` (method) — `_build_assistant_message` must not silently mutate model output
- L2003 `test_unterminated_think_block_stripped(self, agent)` (method) — Unterminated <think> block (MiniMax / NIM dropped close tag) is
- L2015 `TestFormatToolsForSystemMessage` (class)
- L2016 `test_no_tools_returns_empty_array(self, agent)` (method)
- L2020 `test_formats_single_tool(self, agent)` (method)
- L2027 `test_formats_multiple_tools(self, agent)` (method)
- L2041 `TestExecuteToolCalls` (class)
- L2042 `test_single_tool_executed(self, agent)` (method)
- L2058 `test_keyboard_interrupt_emits_cancelled_post_tool_hook(self, agent, monkeypatch)` (method)
- L2092 `test_interrupt_skips_remaining(self, agent)` (method)
- L2109 `test_invalid_json_args_defaults_empty(self, agent)` (method)
- L2125 `test_result_truncation_over_100k(self, agent, tmp_path, monkeypatch)` (method)
- L2138 `test_quiet_tool_output_suppressed_when_progress_callback_present(self, agent)` (method)
- L2152 `test_quiet_tool_output_prints_without_progress_callback(self, agent)` (method)
- L2168 `test_quiet_tool_output_suppressed_without_progress_callback_for_non_cli_agent(self, agent)` (method)
- L2183 `test_vprint_suppressed_in_parseable_quiet_mode(self, agent)` (method)
- L2192 `test_run_conversation_suppresses_retry_noise_in_parseable_quiet_mode(self, agent)` (method)
- L2225 `TestConcurrentToolExecution` (class) — Tests for _execute_tool_calls_concurrent and dispatch logic.
- L2228 `test_single_tool_uses_sequential_path(self, agent)` (method) — Single tool call should use sequential path, not concurrent.
- L2239 `test_clarify_forces_sequential(self, agent)` (method) — Batch containing clarify should use sequential path.
- L2251 `test_multiple_tools_uses_concurrent_path(self, agent)` (method) — Multiple read-only tools should use concurrent path.
- L2263 `test_terminal_batch_forces_sequential(self, agent)` (method) — Stateful tools should not share the concurrent execution path.
- L2275 `test_write_batch_forces_sequential(self, agent)` (method) — File mutations should stay ordered within a turn.
- L2287 `test_disjoint_write_batch_uses_concurrent_path(self, agent)` (method) — Independent file writes should still run concurrently.
- L2307 `test_overlapping_write_batch_forces_sequential(self, agent)` (method) — Writes to the same file must stay ordered.
- L2327 `test_malformed_json_args_forces_sequential(self, agent)` (method) — Unparseable tool arguments should fall back to sequential.
- L2339 `test_non_dict_args_forces_sequential(self, agent)` (method) — Tool arguments that parse to a non-dict type should fall back to sequential.
- L2351 `test_concurrent_executes_all_tools(self, agent)` (method) — Concurrent path should execute all tools and append results in order.
- L2380 `test_concurrent_preserves_order_despite_timing(self, agent)` (method) — Even if tools finish in different order, messages should be in original order.
- L2403 `test_concurrent_handles_tool_error(self, agent)` (method) — If one tool raises, others should still complete.
- L2426 `test_concurrent_interrupt_before_start(self, agent)` (method) — If interrupt is requested before concurrent execution, all tools are skipped.
- L2441 `test_concurrent_truncates_large_results(self, agent, tmp_path, monkeypatch)` (method) — Concurrent path should save oversized results to file.
- L2459 `test_invoke_tool_dispatches_to_handle_function_call(self, agent)` (method) — _invoke_tool should route regular tools through handle_function_call.
- L2478 `test_sequential_tool_callbacks_fire_in_order(self, agent)` (method)
- L2493 `test_concurrent_tool_callbacks_fire_for_each_tool(self, agent)` (method)
- L2514 `test_invoke_tool_handles_agent_level_tools(self, agent)` (method) — _invoke_tool should handle todo tool directly.
- L2521 `test_invoke_tool_agent_level_tool_emits_terminal_post_tool_hook(self, agent, monkeypatch)` (method) — Agent-owned tool paths should close observer tool spans.
- L2546 `test_invoke_tool_blocked_returns_error_and_skips_execution(self, agent, monkeypatch)` (method) — _invoke_tool should return error JSON when a plugin blocks the tool.
- L2558 `test_invoke_tool_blocked_skips_handle_function_call(self, agent, monkeypatch)` (method) — Blocked registry tools should not reach handle_function_call.
- L2569 `test_sequential_blocked_tool_skips_checkpoints_and_callbacks(self, agent, monkeypatch)` (method) — Sequential path: blocked tool should not trigger checkpoints or start callbacks.
- L2598 `test_sequential_blocked_tool_emits_terminal_post_tool_hook(self, agent, monkeypatch)` (method) — Blocked pre_tool_call decisions still terminate observer tool spans.
- L2627 `test_sequential_agent_level_tool_emits_terminal_post_tool_hook(self, agent, monkeypatch)` (method) — Sequential built-in tool paths should also close observer tool spans.
- L2654 `test_sequential_agent_level_tool_execution_middleware_wraps_inline_dispatch(self, agent, monkeypatch)` (method) — Sequential built-in tool paths should expose the adaptive execution boundary.
- L2701 `test_concurrent_agent_level_tool_preserves_request_middleware_trace(self, agent, monkeypatch)` (method)
- L2737 `test_agent_runtime_post_hook_ownership_predicate_covers_agent_tools(self, agent)` (method) — Sequential and concurrent agent-level paths share post-hook ownership.
- L2751 `test_blocked_memory_tool_does_not_reset_counter(self, agent, monkeypatch)` (method) — Blocked memory tool should not reset the nudge counter.
- L2766 `test_concurrent_blocked_write_skips_checkpoint(self, agent, monkeypatch)` (method) — Concurrent path: blocked write_file should not trigger checkpoint.
- L2793 `test_concurrent_blocked_patch_skips_checkpoint(self, agent, monkeypatch)` (method) — Concurrent path: blocked patch should not trigger checkpoint.
- L2820 `test_concurrent_blocked_terminal_skips_checkpoint(self, agent, monkeypatch)` (method) — Concurrent path: blocked terminal should not trigger checkpoint.
- L2848 `test_concurrent_blocked_write_does_not_steal_slot_from_allowed_write(self, agent, monkeypatch)` (method) — When write_file is blocked, its dedup slot must not be consumed,
- L2883 `TestAgentRuntimePostHookOwnershipSync` (class) — Pin the inline-dispatch tool list against the post-hook ownership set.
- L2902 `_is_dispatch_anchor(cls, test_node)` (method)
- L2914 `_function_name_literal(test_node)` (method) — Return the string literal X for `function_name == "X"`, else None.
- L2928 `_extract_dispatch_chain_names(cls, func)` (method) — Find the if/elif chain anchored on `_block_msg is not None`, return its
- L2952 `_extract_invoke_tool_names(cls, func)` (method) — invoke_tool uses a flat if/elif on function_name directly; walk every
- L2965 `test_frozenset_matches_inline_dispatch_chain(self)` (method)
- L2989 `test_invoke_tool_dispatch_matches_inline_dispatch_chain(self)` (method) — invoke_tool (concurrent path) and the inline dispatcher (sequential
- L3012 `TestPathsOverlap` (class) — Unit tests for the _paths_overlap helper.
- L3015 `test_same_path_overlaps(self)` (method)
- L3019 `test_siblings_do_not_overlap(self)` (method)
- L3023 `test_parent_child_overlap(self)` (method)
- L3027 `test_different_roots_do_not_overlap(self)` (method)
- L3031 `test_nested_vs_flat_do_not_overlap(self)` (method)
- L3035 `test_empty_paths_do_not_overlap(self)` (method)
- L3039 `test_one_empty_path_does_not_overlap(self)` (method)
- L3045 `TestParallelScopePathNormalization` (class)
- L3046 `test_extract_parallel_scope_path_normalizes_relative_to_cwd(self, tmp_path, monkeypatch)` (method)
- L3055 `test_extract_parallel_scope_path_treats_relative_and_absolute_same_file_as_same_scope(self, tmp_path, monkeypatch)` (method)
- L3067 `test_should_parallelize_tool_batch_rejects_same_file_with_mixed_path_spellings(self, tmp_path, monkeypatch)` (method)
- L3077 `TestMcpParallelToolBatch` (class) — Integration test: _should_parallelize_tool_batch respects MCP parallel flag.
- L3080 `test_mcp_tools_default_sequential(self)` (method) — MCP tools without supports_parallel_tool_calls are sequential.
- L3087 `test_mcp_tools_parallel_when_server_opted_in(self)` (method) — MCP tools from a parallel-safe server can run concurrently.
- L3105 `test_mixed_mcp_and_builtin_parallel(self)` (method) — MCP parallel tools mixed with built-in parallel-safe tools.
- L3121 `test_mixed_parallel_and_serial_mcp_servers(self)` (method) — One parallel MCP server + one non-parallel MCP server = sequential.
- L3141 `TestHandleMaxIterations` (class)
- L3142 `test_returns_summary(self, agent)` (method)
- L3152 `test_api_failure_returns_error(self, agent)` (method)
- L3161 `test_summary_skips_reasoning_for_unsupported_openrouter_model(self, agent)` (method)
- L3175 `test_summary_request_removes_orphan_tool_result(self, agent)` (method) — Regression: max-iterations summary request must NOT contain
- L3201 `test_summary_request_inserts_stub_for_missing_tool_result(self, agent)` (method) — If an assistant tool_call has no matching tool result in the
- L3224 `test_summary_strips_strict_schema_foreign_fields(self, agent)` (method) — Regression: the max-iterations summary request must NOT carry
- L3258 `test_summary_omits_provider_preferences_for_non_openrouter(self, agent)` (method)
- L3272 `test_summary_keeps_provider_preferences_for_openrouter(self, agent)` (method)
- L3286 `test_codex_summary_sanitizes_orphan_tool_results(self, agent)` (method)
- L3329 `test_api_sanitizer_matches_responses_call_id_when_id_differs(self, agent)` (method)
- L3354 `TestRunConversation` (class) — Tests for the main run_conversation method.
- L3361 `_setup_agent(self, agent)` (method) — Common setup for run_conversation tests.
- L3369 `test_stop_finish_reason_returns_response(self, agent)` (method)
- L3382 `test_ollama_small_runtime_context_fails_before_api_call(self, agent, caplog)` (method)
- L3407 `test_tool_calls_then_stop(self, agent)` (method)
- L3425 `test_request_scoped_api_hooks_fire_for_each_api_call(self, agent)` (method)
- L3469 `test_api_request_error_hook_skips_payload_work_without_listener(self, agent, monkeypatch)` (method)
- L3501 `test_request_scoped_api_hooks_skip_payload_work_without_listeners(self, agent, monkeypatch)` (method)
- L3539 `test_content_with_tool_calls_stays_silent_for_non_cli_quiet_mode(self, agent)` (method)
- L3563 `test_interrupt_breaks_loop(self, agent)` (method)
- L3582 `test_invalid_tool_name_retry(self, agent)` (method) — Model hallucinates an invalid tool name, agent retries and succeeds.
- L3601 `test_reasoning_only_local_resumed_no_compression_triggered(self, agent)` (method) — Reasoning-only responses no longer trigger compression — prefill then accepted.
- L3635 `test_reasoning_only_response_prefill_then_empty(self, agent)` (method) — Structured reasoning-only triggers prefill (2), then retries (3), then (empty).
- L3657 `test_reasoning_only_prefill_succeeds_on_continuation(self, agent)` (method) — When prefill continuation produces content, it becomes the final response.
- L3685 `test_truly_empty_response_retries_3_times_then_empty(self, agent)` (method) — Truly empty response (no content, no reasoning) retries 3 times then falls through to (empty).
- L3706 `test_truly_empty_response_succeeds_on_nudge(self, agent)` (method) — Model produces content after being nudged for empty response.
- L3727 `test_empty_response_triggers_fallback_provider(self, agent)` (method) — After 3 empty retries, fallback provider is activated and produces content.
- L3766 `test_empty_response_fallback_also_empty_returns_empty(self, agent)` (method) — If fallback also returns empty, final response is (empty).
- L3803 `test_empty_response_emits_status_for_gateway(self, agent)` (method) — _emit_status is called during empty retries so gateway users see feedback.
- L3837 `test_partial_stream_recovery_uses_streamed_content(self, agent)` (method) — When streaming fails after partial delivery, recovered partial content becomes final response.
- L3859 `test_partial_stream_recovery_on_empty_stub(self, agent)` (method) — When stub response has no content but text was streamed, use streamed text.
- L3895 `test_partial_stream_recovery_preempts_prior_turn_fallback(self, agent)` (method) — Partial streamed content takes priority over _last_content_with_tools fallback.
- L3919 `test_nous_401_refreshes_after_remint_and_retries(self, agent)` (method)
- L3960 `test_context_compression_triggered(self, agent)` (method) — When compressor says should_compress, compression runs.
- L3990 `test_glm_prompt_exceeds_max_length_triggers_compression(self, agent)` (method) — GLM/Z.AI uses 'Prompt exceeds max length' for context overflow.
- L4021 `test_minimax_delta_overflow_keeps_known_context_length(self, agent)` (method) — MiniMax reports overflow deltas like 'limit (2013)' without the real window.
- L4066 `test_non_minimax_overflow_without_provider_limit_keeps_context(self, agent)` (method) — Generic overflow without a provider-reported max must NOT probe-step down.
- L4112 `test_length_finish_reason_requests_continuation(self, agent)` (method) — Normal truncation (partial real content) triggers continuation.
- L4134 `test_length_continuation_preserves_large_provider_default_output_cap(self, agent)` (method) — Continuation retries must not shrink a higher provider default cap.
- L4164 `test_ollama_glm_stop_after_tools_without_terminal_boundary_requests_continuation(self, agent)` (method) — Ollama-hosted GLM responses can misreport truncated output as stop.
- L4209 `test_ollama_glm_stop_with_terminal_boundary_does_not_continue(self, agent)` (method) — Complete Ollama/GLM responses should not be reclassified as truncated.
- L4242 `test_non_ollama_stop_without_terminal_boundary_does_not_continue(self, agent)` (method) — The stop->length workaround should stay scoped to Ollama/GLM backends.
- L4272 `test_length_thinking_exhausted_skips_continuation(self, agent)` (method) — When finish_reason='length' but content is only thinking, skip retries.
- L4298 `test_length_empty_content_without_think_tags_retries_normally(self, agent)` (method) — When finish_reason='length' and content is None but no think tags,
- L4317 `test_length_with_tool_calls_returns_partial_without_executing_tools(self, agent)` (method)
- L4340 `test_truncated_tool_call_retries_once_before_refusing(self, agent)` (method) — When tool call args are truncated, the agent retries the API call
- L4380 `test_stub_stall_mid_tool_call_recovers_within_3_retries(self, agent)` (method) — A network stream stall mid tool-call (PARTIAL_STREAM_STUB_ID) must
- L4422 `test_truncated_tool_args_detected_when_finish_reason_not_length(self, agent)` (method) — When a router rewrites finish_reason from 'length' to 'tool_calls',
- L4451 `test_kanban_block_called_on_iteration_exhaustion(self, agent, monkeypatch)` (method) — Regression: kanban worker must signal the dispatcher when its
- L4514 `test_no_kanban_block_when_not_in_kanban_mode(self, agent, monkeypatch)` (method) — The exhaustion bridge must NOT fire when HERMES_KANBAN_TASK
- L4550 `TestHookPayloadSanitizesSimpleNamespace` (class) — Regression: ``_hook_jsonable`` referenced ``SimpleNamespace`` without
- L4565 `test_hook_jsonable_normalizes_simplenamespace(self)` (method)
- L4570 `test_api_response_payload_for_hook_normalizes_simplenamespace_tool_calls(self, agent)` (method)
- L4596 `TestRetryExhaustion` (class) — Regression: retry_count > max_retries was dead code (off-by-one).
- L4604 `_setup_agent(self, agent)` (method)
- L4612 `_make_fast_time_mock()` (method) — Return a mock time module where sleep loops exit instantly.
- L4626 `test_invalid_response_returns_error_not_crash(self, agent)` (method) — Exhausted retries on invalid (empty choices) response must not IndexError.
- L4656 `test_api_error_returns_gracefully_after_retries(self, agent)` (method) — Exhausted retries on API errors must return error result, not crash.
- L4675 `test_build_api_kwargs_error_no_unbound_local(self, agent)` (method) — When _build_api_kwargs raises, except handler must not crash with UnboundLocalError.
- L4703 `TestConversationHistoryNotMutated` (class) — run_conversation must not mutate the caller's conversation_history list.
- L4706 `test_caller_list_unchanged_after_run(self, agent)` (method) — Passing conversation_history should not modify the original list.
- L4739 `TestNousCredentialRefresh` (class) — Verify Nous credential refresh rebuilds the runtime client.
- L4742 `test_try_refresh_nous_client_credentials_rebuilds_client(self, agent, monkeypatch)` (method)
- L4789 `TestCredentialPoolRecovery` (class)
- L4790 `test_recover_with_pool_rotates_on_402(self, agent)` (method)
- L4815 `test_recover_with_pool_rotates_on_billing_reason_even_with_http_400(self, agent)` (method)
- L4838 `test_recover_with_pool_retries_first_429_then_rotates(self, agent)` (method)
- L4870 `test_recover_with_pool_refreshes_on_401(self, agent)` (method) — 401 with successful refresh should swap to refreshed credential.
- L4889 `test_recover_with_pool_rotates_on_401_when_refresh_fails(self, agent)` (method) — 401 with failed refresh should rotate to next credential.
- L4914 `test_recover_with_pool_401_refresh_fails_no_more_credentials(self, agent)` (method) — 401 with failed refresh and no other credentials returns not recovered.
- L4936 `test_extract_api_error_context_uses_reset_timestamp_and_reason(self, agent)` (method)
- L4955 `test_extract_api_error_context_uses_type_as_reason(self, agent)` (method)
- L4971 `test_extract_api_error_context_parses_resets_in_hours_and_minutes(self, agent, monkeypatch)` (method)
- L4990 `test_recover_with_pool_passes_error_context_on_rotated_429(self, agent)` (method)
- L5018 `TestMaxTokensParam` (class) — Verify _max_tokens_param returns the correct key for each provider.
- L5021 `test_returns_max_completion_tokens_for_direct_openai(self, agent)` (method)
- L5026 `test_returns_max_tokens_for_openrouter(self, agent)` (method)
- L5031 `test_returns_max_tokens_for_local(self, agent)` (method)
- L5036 `test_not_tricked_by_openai_in_openrouter_url(self, agent)` (method)
- L5041 `test_returns_max_completion_tokens_for_azure(self, agent)` (method) — Azure OpenAI requires max_completion_tokens for gpt-5.x models.
- L5047 `test_returns_max_completion_tokens_for_github_copilot(self, agent)` (method) — GitHub Copilot's OpenAI-compatible API rejects max_tokens for newer models.
- L5053 `test_returns_max_completion_tokens_for_github_copilot_path(self, agent)` (method) — Detect Copilot by hostname even when the configured URL includes a path.
- L5060 `TestGpt5ApiModeRouting` (class) — Verify provider-specific GPT-5 API-mode routing.
- L5063 `test_azure_gpt5_stays_on_chat_completions(self, agent)` (method) — Azure serves gpt-5.x on /chat/completions — must not upgrade to codex_responses.
- L5082 `test_non_azure_gpt5_upgrades_to_codex_responses(self, agent)` (method) — On api.openai.com, gpt-5.x must still upgrade to codex_responses.
- L5100 `test_nous_gpt5_stays_on_chat_completions(self, agent)` (method) — Nous serves gpt-5.x on /chat/completions — must not upgrade to codex_responses.
- L5119 `test_is_azure_openai_url_detection(self, agent)` (method)
- L5132 `TestSystemPromptStability` (class) — Verify that the system prompt stays stable across turns for cache hits.
- L5135 `test_stored_prompt_reused_for_continuing_session(self, agent)` (method) — When conversation_history is non-empty and session DB has a stored
- L5174 `test_fresh_build_when_no_history(self, agent)` (method) — On the first turn (no history), system prompt should be built fresh.
- L5200 `test_fresh_build_when_db_has_no_prompt(self, agent)` (method) — If the session DB has no stored prompt, build fresh even with history.
- L5227 `TestBudgetPressure` (class) — Budget exhaustion grace call system.
- L5230 `test_grace_call_flags_initialized(self, agent)` (method) — Agent should have budget grace call flags.
- L5236 `TestSafeWriter` (class) — Verify _SafeWriter guards stdout against OSError (broken pipes).
- L5239 `test_write_delegates_normally(self)` (method) — When stdout is healthy, _SafeWriter is transparent.
- L5248 `test_write_catches_oserror(self)` (method) — OSError on write is silently caught, returns len(data).
- L5258 `test_flush_catches_oserror(self)` (method) — OSError on flush is silently caught.
- L5267 `test_print_survives_broken_stdout(self, monkeypatch)` (method) — print() through _SafeWriter doesn't crash on broken pipe.
- L5281 `test_installed_in_run_conversation(self, agent)` (method) — run_conversation installs _SafeWriter on stdio.
- L5305 `test_double_wrap_prevented(self)` (method) — Wrapping an already-wrapped stream doesn't add layers.
- L5328 `TestBuildApiKwargsAnthropicMaxTokens` (class) — Bug fix: max_tokens was always None for Anthropic mode, ignoring user config.
- L5331 `test_max_tokens_passed_to_anthropic(self, agent)` (method)
- L5347 `test_max_tokens_none_when_unset(self, agent)` (method)
- L5363 `TestAnthropicImageFallback` (class)
- L5364 `test_build_api_kwargs_converts_multimodal_user_image_to_text(self, agent)` (method)
- L5393 `test_build_api_kwargs_reuses_cached_image_analysis_for_duplicate_images(self, agent)` (method)
- L5426 `TestFallbackAnthropicProvider` (class) — Bug fix: _try_activate_fallback had no case for anthropic provider.
- L5429 `test_fallback_to_anthropic_sets_api_mode(self, agent)` (method)
- L5452 `test_fallback_to_anthropic_enables_prompt_caching(self, agent)` (method)
- L5471 `test_fallback_to_openrouter_uses_openai_client(self, agent)` (method)
- L5489 `test_aiagent_uses_copilot_acp_client()` (function)
- L5519 `test_quiet_spinner_allowed_with_explicit_print_fn(agent)` (function)
- L5525 `test_quiet_spinner_allowed_on_real_tty(agent)` (function)
- L5531 `test_quiet_spinner_suppressed_on_non_tty_without_print_fn(agent)` (function)
- L5537 `test_is_openai_client_closed_honors_custom_client_flag()` (function)
- L5542 `test_is_openai_client_closed_handles_method_form()` (function) — Fix for issue #4377: is_closed as method (openai SDK) vs property (httpx).
- L5569 `test_is_openai_client_closed_falls_back_to_http_client()` (function) — Verify fallback to _client.is_closed when top-level is_closed is None.
- L5582 `TestAnthropicBaseUrlPassthrough` (class) — Bug fix: base_url was filtered with 'anthropic in base_url', blocking proxies.
- L5585 `test_custom_proxy_base_url_passed_through(self)` (method)
- L5604 `test_none_base_url_passed_as_none(self)` (method)
- L5624 `TestAnthropicCredentialRefresh` (class)
- L5625 `test_try_refresh_anthropic_client_credentials_rebuilds_client(self)` (method)
- L5661 `test_try_refresh_anthropic_client_credentials_returns_false_when_token_unchanged(self)` (method)
- L5689 `test_anthropic_messages_create_preflights_refresh(self)` (method)
- L5720 `_make_chunk(content=None, tool_calls=None, finish_reason=None, model='test/model')` (function) — Build a SimpleNamespace mimicking an OpenAI streaming chunk.
- L5727 `_make_tc_delta(index=0, tc_id=None, name=None, arguments=None)` (function) — Build a SimpleNamespace mimicking a streaming tool_call delta.
- L5733 `TestStreamingApiCall` (class) — Tests for _streaming_api_call — voice TTS streaming pipeline.
- L5736 `test_content_assembly(self, agent)` (method)
- L5756 `test_tool_call_accumulation(self, agent)` (method)
- L5776 `test_multiple_tool_calls(self, agent)` (method)
- L5791 `test_truncated_tool_call_args_upgrade_finish_reason_to_length(self, agent)` (method)
- L5805 `test_ollama_reused_index_separate_tool_calls(self, agent)` (method) — Ollama sends every tool call at index 0 with different ids.
- L5829 `test_ollama_reused_index_streamed_args(self, agent)` (method) — Ollama with streamed arguments across multiple chunks at same index.
- L5849 `test_content_and_tool_calls_together(self, agent)` (method)
- L5862 `test_empty_content_returns_none(self, agent)` (method)
- L5871 `test_callback_exception_swallowed(self, agent)` (method)
- L5884 `test_model_name_captured(self, agent)` (method)
- L5895 `test_stream_kwarg_injected(self, agent)` (method)
- L5904 `test_api_exception_propagates_no_non_streaming_fallback(self, agent)` (method) — When streaming fails before any deltas, error propagates to the main retry loop.
- L5913 `test_response_has_uuid_id(self, agent)` (method)
- L5922 `test_empty_choices_chunk_skipped(self, agent)` (method)
- L5942 `TestInterruptVprintForceTrue` (class) — All interrupt _vprint calls must use force=True so they are always visible.
- L5945 `test_all_interrupt_vprint_have_force_true(self)` (method) — Scan source for _vprint calls containing 'Interrupt' — each must have force=True.
- L5967 `TestAnthropicInterruptHandler` (class) — _interruptible_api_call must handle Anthropic mode when interrupted.
- L5970 `test_interruptible_has_anthropic_branch(self)` (method) — The interrupt handler must check api_mode == 'anthropic_messages'.
- L5978 `test_interruptible_rebuilds_anthropic_client(self)` (method) — After interrupting, the Anthropic client should be rebuilt.
- L5986 `test_streaming_has_anthropic_branch(self)` (method) — _streaming_api_call must also handle Anthropic interrupt.
- L6000 `TestStreamCallbackNonStreamingProvider` (class) — When api_mode != chat_completions, stream_callback must still receive
- L6004 `test_callback_receives_chat_completions_response(self, agent)` (method) — For chat_completions-shaped responses, callback gets content.
- L6052 `test_callback_receives_anthropic_content(self, agent)` (method) — For Anthropic responses, text blocks are extracted and forwarded.
- L6088 `TestPersistUserMessageOverride` (class) — Synthetic API-only user prefixes should never leak into transcripts.
- L6091 `test_persist_session_rewrites_current_turn_user_message(self, agent)` (method)
- L6115 `TestReasoningReplayForStrictProviders` (class) — Assistant replay must preserve provider-native reasoning fields.
- L6118 `_setup_agent(self, agent)` (method)
- L6125 `test_kimi_tool_replay_includes_space_reasoning_content(self, agent)` (method)
- L6164 `test_explicit_reasoning_content_beats_normalized_reasoning_on_replay(self, agent)` (method)
- L6204 `TestVprintForceOnErrors` (class) — Error/warning messages must be visible during streaming TTS.
- L6207 `test_forced_message_shown_during_tts(self, agent)` (method)
- L6214 `test_non_forced_suppressed_during_tts(self, agent)` (method)
- L6221 `test_all_shown_without_tts(self, agent)` (method)
- L6230 `TestNormalizeCodexDictArguments` (class) — _normalize_codex_response must produce valid JSON strings for tool
- L6234 `_make_codex_response(self, item_type, arguments, item_status='completed')` (method) — Build a minimal Responses API response with a single tool call.
- L6255 `test_function_call_dict_arguments_produce_valid_json(self, agent)` (method) — dict arguments from function_call must be serialised with
- L6265 `test_custom_tool_call_dict_arguments_produce_valid_json(self, agent)` (method) — dict arguments from custom_tool_call must also use json.dumps.
- L6274 `test_string_arguments_unchanged(self, agent)` (method) — String arguments must pass through without modification.
- L6288 `TestOAuthFlagAfterCredentialRefresh` (class) — _is_anthropic_oauth must update when token type changes during refresh.
- L6291 `test_oauth_flag_updates_api_key_to_oauth(self, agent)` (method) — Refreshing from API key to OAuth token must set flag to True.
- L6310 `test_oauth_flag_updates_oauth_to_api_key(self, agent)` (method) — Refreshing from OAuth to API key must set flag to False.
- L6330 `TestFallbackSetsOAuthFlag` (class) — _try_activate_fallback must set _is_anthropic_oauth for Anthropic fallbacks.
- L6333 `test_fallback_to_anthropic_oauth_sets_flag(self, agent)` (method)
- L6356 `test_fallback_to_anthropic_api_key_clears_flag(self, agent)` (method)
- L6380 `TestMemoryNudgeCounterPersistence` (class) — _turns_since_memory must persist across run_conversation calls.
- L6383 `test_counters_initialized_in_init(self)` (method) — Counters must exist on the agent after __init__.
- L6395 `test_counters_not_reset_in_preamble(self)` (method) — The run_conversation preamble must not zero the nudge counters.
- L6414 `TestDeadRetryCode` (class) — Unreachable retry_count >= max_retries after raise must not exist.
- L6417 `test_no_unreachable_max_retries_after_backoff(self)` (method)
- L6428 `TestSupportsReasoningExtraBody` (class)
- L6429 `_make_agent(self)` (method)
- L6437 `test_xiaomi_models_are_treated_as_reasoning_capable(self)` (method)
- L6450 `TestMemoryContextSanitization` (class) — sanitize_context() helper correctness — used at provider boundaries.
- L6453 `test_user_message_is_not_mutated_by_run_conversation(self)` (method) — User input must reach run_conversation untouched — if a user types
- L6463 `test_sanitize_context_strips_full_block(self)` (method) — Helper-level: a string with an embedded memory-context block is
- L6484 `TestMemoryProviderTurnStart` (class) — run_conversation() must call memory_manager.on_turn_start() before prefetch_all().
- L6492 `test_on_turn_start_called_before_prefetch(self)` (method) — Source-level check: on_turn_start appears before prefetch_all in run_conversation.
- L6505 `test_on_turn_start_uses_user_turn_count(self)` (method) — Source-level check: on_turn_start receives the user_turn_count.
