---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:43'
updated: '2026-06-08T00:38:43'
---

# tests/honcho_plugin/test_client.py

Symbols in `tests/honcho_plugin/test_client.py`.

- L24 `TestHonchoClientConfigDefaults` (class)
- L25 `test_default_values(self)` (method)
- L40 `TestFromEnv` (class)
- L41 `test_reads_api_key_from_env(self)` (method)
- L47 `test_reads_environment_from_env(self)` (method)
- L55 `test_defaults_without_env(self)` (method)
- L64 `test_custom_workspace(self)` (method)
- L68 `test_reads_base_url_from_env(self)` (method)
- L74 `test_enabled_without_api_key_when_base_url_set(self)` (method) — base_url alone (no API key) is sufficient to enable a local instance.
- L83 `test_reads_timeout_from_env(self)` (method)
- L89 `TestFromGlobalConfig` (class)
- L90 `test_missing_config_falls_back_to_env(self, tmp_path)` (method)
- L99 `test_reads_full_config(self, tmp_path, monkeypatch)` (method)
- L135 `test_host_block_overrides_root(self, tmp_path)` (method)
- L153 `test_root_fields_used_when_no_host_block(self, tmp_path)` (method)
- L165 `test_session_strategy_default_from_global_config(self, tmp_path)` (method) — from_global_config with no sessionStrategy should match dataclass default.
- L172 `test_context_tokens_default_is_none(self, tmp_path)` (method) — Default context_tokens should be None (uncapped) unless explicitly set.
- L179 `test_context_tokens_explicit_sets_cap(self, tmp_path)` (method) — Explicit contextTokens in config sets the cap.
- L186 `test_context_tokens_explicit_overrides_default(self, tmp_path)` (method) — Explicit contextTokens in config should override the default.
- L193 `test_context_tokens_host_block_wins(self, tmp_path)` (method) — Host block contextTokens should override root.
- L204 `test_recall_mode_from_config(self, tmp_path)` (method) — recallMode is read from config, host block wins.
- L215 `test_recall_mode_default(self, tmp_path)` (method)
- L221 `test_corrupt_config_falls_back_to_env(self, tmp_path)` (method)
- L229 `test_api_key_env_fallback(self, tmp_path)` (method)
- L237 `test_base_url_env_fallback(self, tmp_path)` (method) — HONCHO_BASE_URL env var is used when no baseUrl in config JSON.
- L247 `test_base_url_from_config_root(self, tmp_path)` (method) — baseUrl in config root is read and takes precedence over env var.
- L256 `test_base_url_not_read_from_host_block(self, tmp_path)` (method) — baseUrl is a root-level connection setting, not overridable per-host (consistent with apiKey).
- L267 `test_timeout_from_config_root(self, tmp_path)` (method)
- L274 `test_request_timeout_alias_from_config_root(self, tmp_path)` (method)
- L282 `TestResolveSessionName` (class)
- L283 `test_manual_override(self)` (method)
- L287 `test_derive_from_dirname(self)` (method)
- L292 `test_peer_prefix(self)` (method)
- L297 `test_no_peer_prefix_when_no_peer_name(self)` (method)
- L302 `test_default_cwd(self)` (method)
- L308 `test_per_repo_uses_git_root(self)` (method)
- L316 `test_per_repo_with_peer_prefix(self)` (method)
- L326 `test_per_repo_falls_back_to_dirname_outside_git(self)` (method)
- L334 `test_per_repo_manual_override_still_wins(self)` (method)
- L343 `TestResolveConfigPath` (class)
- L344 `test_prefers_hermes_home_when_exists(self, tmp_path)` (method)
- L354 `test_falls_back_to_default_profile_when_no_local(self, tmp_path, monkeypatch)` (method)
- L374 `test_falls_back_to_global_without_hermes_home_env(self, tmp_path)` (method)
- L384 `test_global_fallback_uses_home_at_call_time(self, tmp_path)` (method)
- L395 `test_from_global_config_uses_default_profile_fallback(self, tmp_path, monkeypatch)` (method)
- L417 `test_from_global_config_uses_local_path(self, tmp_path)` (method)
- L433 `TestResolveActiveHost` (class)
- L434 `test_profile_host_key_uses_honcho_safe_separator(self)` (method)
- L438 `test_default_returns_hermes(self)` (method)
- L444 `test_explicit_env_var_wins(self)` (method)
- L448 `test_profile_name_derives_host(self)` (method)
- L454 `test_default_profile_returns_hermes(self)` (method)
- L460 `test_custom_profile_returns_hermes(self)` (method)
- L466 `test_profiles_import_failure_falls_back(self)` (method)
- L482 `TestProfileScopedConfig` (class)
- L483 `test_from_env_uses_profile_host(self)` (method)
- L490 `test_from_env_default_workspace_preserved_for_default_host(self)` (method)
- L496 `test_from_global_config_reads_profile_host_block(self, tmp_path)` (method)
- L517 `test_from_global_config_auto_resolves_host(self, tmp_path)` (method)
- L530 `test_from_global_config_reads_legacy_dot_profile_host_block(self, tmp_path)` (method)
- L547 `TestObservationModeMigration` (class) — Existing configs without explicit observationMode keep 'unified' default.
- L550 `test_existing_config_defaults_to_unified(self, tmp_path)` (method) — Config with host block but no observationMode → 'unified' (old default).
- L560 `test_new_config_defaults_to_directional(self, tmp_path)` (method) — Config with no host block and no credentials → 'directional' (new default).
- L567 `test_explicit_directional_respected(self, tmp_path)` (method) — Existing config with explicit observationMode → uses what's set.
- L577 `test_explicit_unified_respected(self, tmp_path)` (method) — Existing config with explicit observationMode unified → stays unified.
- L588 `test_granular_observation_overrides_preset(self, tmp_path)` (method) — Explicit observation object overrides both preset and migration default.
- L610 `TestGetHonchoClient` (class)
- L611 `teardown_method(self)` (method)
- L618 `test_passes_timeout_from_config(self)` (method)
- L638 `test_hermes_config_timeout_override_used_when_config_timeout_missing(self)` (method)
- L658 `test_defaults_to_30s_when_no_timeout_configured(self)` (method)
- L680 `test_hermes_request_timeout_alias_used(self)` (method)
- L697 `TestResolveSessionNameGatewayKey` (class) — Regression tests for gateway_session_key priority in resolve_session_name.
- L705 `test_gateway_key_overrides_per_session_strategy(self)` (method) — gateway_session_key must win over per-session session_id.
- L714 `test_session_title_still_wins_over_gateway_key(self)` (method) — Explicit /title remap takes priority over gateway_session_key.
- L724 `test_per_session_fallback_without_gateway_key(self)` (method) — Without gateway_session_key, per-session returns session_id (CLI path).
- L733 `test_gateway_key_sanitizes_special_chars(self)` (method) — Colons and other non-alphanumeric chars are replaced with hyphens.
- L743 `TestResolveSessionNameLengthLimit` (class) — Regression tests for Honcho's 100-char session ID limit (issue #13868).
- L754 `test_short_gateway_key_unchanged(self)` (method) — Short keys must not get a hash suffix appended.
- L764 `test_key_at_exact_limit_unchanged(self)` (method) — A sanitized key that is exactly 100 chars must be returned as-is.
- L772 `test_long_gateway_key_truncated_to_limit(self)` (method) — An over-limit sanitized key must truncate to exactly 100 chars.
- L780 `test_truncation_is_deterministic(self)` (method) — Same long key must always produce the same truncated session ID.
- L788 `test_truncated_result_respects_char_allowlist(self)` (method) — Truncated result must still match Honcho's [a-zA-Z0-9_-] allowlist.
- L797 `test_distinct_long_keys_do_not_collide(self)` (method) — Two long keys sharing a prefix must produce different truncated IDs.
- L809 `test_truncated_result_has_hash_suffix(self)` (method) — Truncated IDs must end with '-<8 hex chars>' for collision resistance.
- L819 `TestResetHonchoClient` (class)
- L820 `test_reset_clears_singleton(self)` (method)
- L828 `TestDialecticDepthParsing` (class) — Tests for _parse_dialectic_depth and _parse_dialectic_depth_levels.
- L831 `test_default_depth_is_1(self, tmp_path)` (method) — Default dialecticDepth should be 1.
- L838 `test_depth_from_root(self, tmp_path)` (method)
- L844 `test_depth_host_block_wins(self, tmp_path)` (method)
- L854 `test_depth_clamped_high(self, tmp_path)` (method)
- L860 `test_depth_clamped_low(self, tmp_path)` (method)
- L866 `test_depth_levels_default_none(self, tmp_path)` (method)
- L872 `test_depth_levels_from_config(self, tmp_path)` (method)
- L882 `test_depth_levels_padded_if_short(self, tmp_path)` (method) — Array shorter than depth gets padded with 'low'.
- L893 `test_depth_levels_truncated_if_long(self, tmp_path)` (method) — Array longer than depth gets truncated.
- L904 `test_depth_levels_invalid_values_default_to_low(self, tmp_path)` (method) — Invalid reasoning levels in the array fall back to 'low'.
- L916 `TestGetHonchoClientBaseUrlDoublePrefixFix` (class) — Regression tests for #20688 — Honcho SDK double-prefixing of /v3 for
- L920 `teardown_method(self)` (method)
- L927 `test_local_base_url_with_v3_suffix_stripped(self)` (method) — base_url 'http://localhost:38000/v3' must become 'http://localhost:38000'
- L952 `test_local_base_url_without_version_unchanged(self)` (method) — base_url 'http://localhost:38000' (no version) must be passed unchanged.
- L976 `test_cloud_base_url_without_version_unchanged(self)` (method) — A cloud base_url with no version segment must pass through untouched.
- L1000 `test_cloud_base_url_with_version_stripped(self)` (method) — A version segment double-prefixes regardless of host, so a cloud
- L1042 `test_self_hosted_base_url_version_stripped(self, raw_url, expected)` (method) — Non-loopback self-hosted instances (LAN IPs, Tailscale, custom
- L1068 `test_local_base_url_with_trailing_slash_stripped(self)` (method) — base_url 'http://127.0.0.1:38000/v3/' must also be cleaned up.
