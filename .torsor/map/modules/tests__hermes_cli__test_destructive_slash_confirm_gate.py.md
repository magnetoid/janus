---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/hermes_cli/test_destructive_slash_confirm_gate.py

Symbols in `tests/hermes_cli/test_destructive_slash_confirm_gate.py`.

- L17 `TestDestructiveSlashConfirmDefault` (class)
- L18 `test_default_config_has_the_key(self)` (method)
- L23 `test_default_is_true(self)` (method)
- L28 `test_shape_matches_other_approval_keys(self)` (method)
- L35 `TestUserConfigMerge` (class) — If a user has a pre-existing config without this key, load_config
- L40 `test_existing_user_config_without_key_gets_default(self, tmp_path, monkeypatch)` (method)
- L59 `test_existing_user_config_with_false_key_survives_merge(self, tmp_path, monkeypatch)` (method) — A user who clicked "Always Approve" (key=false) must keep that
