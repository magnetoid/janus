---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/gateway/test_goal_max_turns_config.py

Symbols in `tests/gateway/test_goal_max_turns_config.py`.

- L10 `_FakeSessionEntry` (class)
- L14 `_FakeSessionStore` (class)
- L15 `__init__(self)` (method)
- L18 `get_or_create_session(self, source)` (method)
- L21 `_generate_session_key(self, source)` (method)
- L26 `test_gateway_goal_uses_goals_max_turns_from_full_config(tmp_path, monkeypatch)` (function) — Gateway /goal should honor top-level goals.max_turns from config.yaml.
