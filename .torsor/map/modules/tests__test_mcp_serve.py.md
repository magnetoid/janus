---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:43'
updated: '2026-06-08T00:38:43'
---

# tests/test_mcp_serve.py

Symbols in `tests/test_mcp_serve.py`.

- L28 `_isolate_hermes_home(tmp_path, monkeypatch)` (function) — Redirect HERMES_HOME to a temp directory.
- L40 `sessions_dir(tmp_path)` (function)
- L47 `sample_sessions()` (function)
- L119 `populated_sessions_dir(sessions_dir, sample_sessions)` (function)
- L124 `_create_test_db(db_path, session_id, messages)` (function) — Create a minimal SQLite DB mimicking hermes_state schema.
- L171 `mock_session_db(tmp_path, populated_sessions_dir)` (function) — Create a real SQLite DB with test messages and wire it up.
- L210 `_FakeTool` (class)
- L211 `__init__(self, fn)` (method)
- L217 `_FakeToolManager` (class)
- L218 `__init__(self)` (method)
- L221 `add_tool(self, fn)` (method)
- L224 `call_tool(self, name, args=None)` (method)
- L227 `list_tools(self)` (method)
- L231 `_FakeFastMCP` (class)
- L232 `__init__(self, *args, **kwargs)` (method)
- L235 `tool(self)` (method)
- L244 `fake_mcp_server(populated_sessions_dir, mock_session_db, monkeypatch)` (function)
- L262 `TestImports` (class)
- L263 `test_import_module(self)` (method)
- L269 `test_mcp_available_flag(self)` (method)
- L274 `TestHelpers` (class)
- L275 `test_get_sessions_dir(self, tmp_path)` (method)
- L280 `test_coerce_int_handles_invalid_and_out_of_range_values(self)` (method)
- L289 `test_load_sessions_index_empty(self, sessions_dir, monkeypatch)` (method)
- L294 `test_load_sessions_index_with_data(self, populated_sessions_dir, monkeypatch)` (method)
- L300 `test_load_sessions_index_corrupt(self, sessions_dir, monkeypatch)` (method)
- L307 `TestContentExtraction` (class)
- L308 `test_text(self)` (method)
- L312 `test_multipart(self)` (method)
- L321 `test_empty(self)` (method)
- L328 `TestAttachmentExtraction` (class)
- L329 `test_image_url_block(self)` (method)
- L338 `test_media_tag_in_text(self)` (method)
- L345 `test_multiple_media_tags(self)` (method)
- L350 `test_no_attachments(self)` (method)
- L354 `test_image_content_block(self)` (method)
- L365 `TestEventBridge` (class)
- L366 `test_create(self)` (method)
- L372 `test_enqueue_and_poll(self)` (method)
- L382 `test_cursor_filter(self)` (method)
- L391 `test_session_filter(self)` (method)
- L400 `test_poll_empty(self)` (method)
- L406 `test_poll_limit(self)` (method)
- L414 `test_wait_immediate(self)` (method)
- L423 `test_wait_timeout(self)` (method)
- L430 `test_wait_wakes_on_enqueue(self)` (method)
- L446 `test_queue_limit(self)` (method)
- L453 `test_concurrent_enqueue(self)` (method)
- L475 `test_approvals_lifecycle(self)` (method)
- L488 `test_respond_nonexistent(self)` (method)
- L499 `mcp_server_e2e(populated_sessions_dir, mock_session_db, monkeypatch)` (function) — Create a fully wired MCP server for E2E testing.
- L512 `_run_tool(server, name, args=None)` (function) — Call an MCP tool through FastMCP's tool manager and return parsed JSON.
- L521 `_event_loop()` (function) — Ensure an event loop exists for sync tests calling async tools.
- L529 `TestE2EConversationsList` (class)
- L530 `test_list_all(self, mcp_server_e2e, _event_loop)` (method)
- L537 `test_list_sorted_by_updated(self, mcp_server_e2e, _event_loop)` (method)
- L546 `test_filter_by_platform(self, mcp_server_e2e, _event_loop)` (method)
- L552 `test_filter_by_platform_case_insensitive(self, mcp_server_e2e, _event_loop)` (method)
- L557 `test_search_by_name(self, mcp_server_e2e, _event_loop)` (method)
- L563 `test_search_no_match(self, mcp_server_e2e, _event_loop)` (method)
- L568 `test_limit(self, mcp_server_e2e, _event_loop)` (method)
- L574 `TestE2EConversationGet` (class)
- L575 `test_get_existing(self, mcp_server_e2e, _event_loop)` (method)
- L584 `test_get_nonexistent(self, mcp_server_e2e, _event_loop)` (method)
- L591 `TestE2EMessagesRead` (class)
- L592 `test_read_messages(self, mcp_server_e2e, _event_loop)` (method)
- L603 `test_read_messages_content(self, mcp_server_e2e, _event_loop)` (method)
- L611 `test_read_messages_have_ids(self, mcp_server_e2e, _event_loop)` (method)
- L619 `test_read_with_limit(self, mcp_server_e2e, _event_loop)` (method)
- L626 `test_read_nonexistent_session(self, mcp_server_e2e, _event_loop)` (method)
- L633 `TestE2EAttachmentsFetch` (class)
- L634 `test_fetch_media_from_message(self, mcp_server_e2e, _event_loop)` (method)
- L655 `test_fetch_from_nonexistent_message(self, mcp_server_e2e, _event_loop)` (method)
- L663 `test_fetch_from_nonexistent_session(self, mcp_server_e2e, _event_loop)` (method)
- L672 `TestE2EEventsPoll` (class)
- L673 `test_poll_empty(self, mcp_server_e2e, _event_loop)` (method)
- L679 `test_poll_with_events(self, mcp_server_e2e, _event_loop)` (method)
- L695 `test_poll_cursor_pagination(self, mcp_server_e2e, _event_loop)` (method)
- L711 `test_poll_session_filter(self, mcp_server_e2e, _event_loop)` (method)
- L723 `TestE2EEventsWait` (class)
- L724 `test_wait_timeout(self, mcp_server_e2e, _event_loop)` (method)
- L730 `test_wait_with_existing_event(self, mcp_server_e2e, _event_loop)` (method)
- L740 `test_wait_caps_timeout(self, mcp_server_e2e, _event_loop)` (method) — Timeout should be capped at 300000ms (5 min).
- L749 `TestMCPToolParameterCoercion` (class)
- L750 `test_conversations_list_coerces_string_limit(self, fake_mcp_server, _event_loop)` (method)
- L755 `test_messages_read_coerces_string_limit(self, fake_mcp_server, _event_loop)` (method)
- L764 `test_events_poll_coerces_string_cursor_and_limit(self, fake_mcp_server, _event_loop)` (method)
- L775 `test_events_wait_coerces_invalid_timeout(self, fake_mcp_server, _event_loop)` (method)
- L793 `TestE2EMessagesSend` (class)
- L794 `test_send_missing_args(self, mcp_server_e2e, _event_loop)` (method)
- L799 `test_send_delegates_to_tool(self, mcp_server_e2e, _event_loop, monkeypatch)` (method)
- L813 `TestE2EChannelsList` (class)
- L814 `test_channels_from_sessions(self, mcp_server_e2e, _event_loop)` (method)
- L823 `test_channels_platform_filter(self, mcp_server_e2e, _event_loop)` (method)
- L829 `test_channels_with_directory(self, mcp_server_e2e, _event_loop, monkeypatch)` (method) — Populated channel_directory.json should be unwrapped via the 'platforms' key.
- L855 `test_channels_with_directory_platform_filter(self, mcp_server_e2e, _event_loop, monkeypatch)` (method) — Platform filter should work against the wrapped 'platforms' payload.
- L871 `TestE2EPermissions` (class)
- L872 `test_list_empty(self, mcp_server_e2e, _event_loop)` (method)
- L878 `test_list_with_approvals(self, mcp_server_e2e, _event_loop)` (method)
- L890 `test_respond_allow(self, mcp_server_e2e, _event_loop)` (method)
- L901 `test_respond_deny(self, mcp_server_e2e, _event_loop)` (method)
- L908 `test_respond_invalid_decision(self, mcp_server_e2e, _event_loop)` (method)
- L915 `test_respond_nonexistent(self, mcp_server_e2e, _event_loop)` (method)
- L926 `TestToolRegistration` (class)
- L927 `test_all_tools_registered(self, mcp_server_e2e, _event_loop)` (method)
- L940 `test_tools_have_descriptions(self, mcp_server_e2e, _event_loop)` (method)
- L950 `TestServerCreation` (class)
- L951 `test_create_server(self, populated_sessions_dir, monkeypatch)` (method)
- L957 `test_create_with_bridge(self, populated_sessions_dir, monkeypatch)` (method)
- L964 `test_create_without_mcp_sdk(self, monkeypatch)` (method)
- L971 `TestRunMcpServer` (class)
- L972 `test_run_without_mcp_exits(self, monkeypatch)` (method)
- L980 `TestCliIntegration` (class)
- L981 `test_parse_serve(self)` (method)
- L994 `test_parse_serve_verbose(self)` (method)
- L1006 `test_dispatcher_routes_serve(self, monkeypatch, tmp_path)` (method)
- L1022 `TestEdgeCases` (class)
- L1023 `test_empty_sessions_json(self, sessions_dir, monkeypatch)` (method)
- L1029 `test_sessions_without_origin(self, sessions_dir, monkeypatch)` (method)
- L1042 `test_bridge_start_stop(self)` (method)
- L1050 `test_truncation(self)` (method)
- L1058 `TestEventBridgePollE2E` (class) — End-to-end tests for the EventBridge polling loop with real files.
- L1061 `test_poll_detects_new_messages(self, tmp_path, monkeypatch)` (method) — Write to SQLite + sessions.json, verify EventBridge picks it up.
- L1119 `test_poll_skips_when_unchanged(self, tmp_path, monkeypatch)` (method) — Second poll with no file changes should be a no-op.
- L1171 `test_poll_detects_new_message_after_db_write(self, tmp_path, monkeypatch)` (method) — Write a new message to the DB after first poll, verify it's detected.
- L1235 `test_poll_interval_is_200ms(self)` (method) — Verify the poll interval constant.
