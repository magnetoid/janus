---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:41'
updated: '2026-06-08T00:38:41'
---

# tests/gateway/test_api_server.py

Symbols in `tests/gateway/test_api_server.py`.

- L44 `TestCheckRequirements` (class)
- L45 `test_returns_true_when_aiohttp_available(self)` (method)
- L49 `test_returns_false_without_aiohttp(self)` (method)
- L58 `TestResponseStore` (class)
- L59 `test_put_and_get(self)` (method)
- L64 `test_get_missing_returns_none(self)` (method)
- L68 `test_lru_eviction(self)` (method)
- L79 `test_access_refreshes_lru(self)` (method)
- L91 `test_update_existing_key(self)` (method)
- L98 `test_delete_existing(self)` (method)
- L105 `test_delete_missing(self)` (method)
- L109 `test_delete_clears_conversation_mapping(self)` (method) — Deleting a response also removes conversation mappings that reference it.
- L118 `test_eviction_clears_conversation_mapping(self)` (method) — LRU eviction also removes conversation mappings for evicted responses.
- L133 `test_file_store_created_owner_only_under_permissive_umask(self, tmp_path)` (method) — response_store.db must be 0o600 on creation even under umask 022.
- L169 `TestIdempotencyCache` (class)
- L171 `test_concurrent_same_key_and_fingerprint_runs_once(self)` (method)
- L196 `test_different_fingerprint_does_not_reuse_inflight_task(self)` (method)
- L223 `test_cancelled_waiter_does_not_drop_shared_inflight_task(self)` (method)
- L258 `TestAdapterInit` (class)
- L259 `test_default_config(self)` (method)
- L267 `test_custom_config_from_extra(self)` (method)
- L283 `test_config_from_env(self, monkeypatch)` (method)
- L298 `test_invalid_port_from_env_falls_back_to_default(self, monkeypatch)` (method)
- L304 `test_create_agent_forwards_config_reasoning_effort(self, monkeypatch)` (method)
- L346 `TestAuth` (class)
- L347 `test_no_key_configured_allows_all(self)` (method)
- L354 `test_valid_key_passes(self)` (method)
- L361 `test_invalid_key_returns_401(self)` (method)
- L370 `test_missing_auth_header_returns_401(self)` (method)
- L379 `test_malformed_auth_header_returns_401(self)` (method)
- L394 `_make_adapter(api_key: str='', cors_origins=None)` (function) — Create an adapter with optional API key.
- L405 `_create_app(adapter: APIServerAdapter)` (function) — Create the aiohttp app from the adapter (without starting the full server).
- L425 `adapter()` (function)
- L430 `auth_adapter()` (function)
- L439 `TestAgentExecution` (class)
- L441 `test_run_agent_uses_session_id_as_task_id(self, adapter)` (method)
- L474 `TestHealthEndpoint` (class)
- L476 `test_security_headers_present(self, adapter)` (method) — Responses should include basic security headers.
- L491 `test_health_returns_ok(self, adapter)` (method)
- L501 `test_v1_health_alias_returns_ok(self, adapter)` (method) — GET /v1/health should return the same response as /health.
- L517 `TestHealthDetailedEndpoint` (class)
- L519 `test_health_detailed_returns_ok(self, adapter)` (method) — GET /health/detailed returns status, platform, and runtime fields.
- L542 `test_health_detailed_no_runtime_status(self, adapter)` (method) — When gateway_state.json is missing, fields are None.
- L555 `test_health_detailed_does_not_require_auth(self, auth_adapter)` (method) — Health detailed endpoint should be accessible without auth, like /health.
- L569 `TestModelsEndpoint` (class)
- L571 `test_models_returns_hermes_agent(self, adapter)` (method)
- L583 `test_models_returns_profile_name(self)` (method) — When running under a named profile, /v1/models advertises the profile name.
- L596 `test_models_returns_explicit_model_name(self)` (method) — Explicit model_name in config overrides profile name.
- L603 `test_resolve_model_name_explicit(self)` (method)
- L606 `test_resolve_model_name_default_profile(self)` (method) — Default profile falls back to 'hermes-agent'.
- L611 `test_resolve_model_name_named_profile(self)` (method) — Named profile uses the profile name as model name.
- L617 `test_models_requires_auth(self, auth_adapter)` (method)
- L624 `test_models_with_valid_auth(self, auth_adapter)` (method)
- L639 `TestCapabilitiesEndpoint` (class)
- L641 `test_capabilities_advertises_plugin_safe_contract(self, adapter)` (method)
- L665 `test_capabilities_requires_auth_when_key_configured(self, auth_adapter)` (method)
- L685 `TestSkillsEndpoint` (class)
- L687 `test_skills_returns_list_envelope(self, adapter)` (method)
- L708 `test_skills_handles_enumeration_failure(self, adapter)` (method)
- L721 `test_skills_requires_auth_when_key_configured(self, auth_adapter)` (method)
- L735 `TestToolsetsEndpoint` (class)
- L737 `test_toolsets_returns_resolved_tools(self, adapter)` (method)
- L773 `test_toolsets_handles_resolution_failure_per_toolset(self, adapter)` (method) — If one toolset fails to resolve, others still appear with empty tools.
- L808 `test_toolsets_requires_auth_when_key_configured(self, auth_adapter)` (method)
- L833 `TestChatCompletionsEndpoint` (class)
- L835 `test_invalid_json_returns_400(self, adapter)` (method)
- L848 `test_missing_messages_returns_400(self, adapter)` (method)
- L857 `test_empty_messages_returns_400(self, adapter)` (method)
- L864 `test_stream_true_returns_sse(self, adapter)` (method) — stream=true returns SSE format with the full response.
- L897 `test_stream_string_false_returns_json_completion(self, adapter)` (method) — Quoted false must not route chat completions into SSE mode.
- L928 `test_stream_task_done_callback_enqueues_eos_for_chat_completions(self, adapter)` (method) — Regression guard for #24451: completion callback must signal SSE EOS.
- L978 `test_stream_sends_keepalive_during_quiet_tool_gap(self, adapter)` (method) — Idle SSE streams should send keepalive comments while tools run silently.
- L1016 `test_stream_survives_tool_call_none_sentinel(self, adapter)` (method) — stream_delta_callback(None) mid-stream (tool calls) must NOT kill the SSE stream.
- L1064 `test_stream_includes_tool_progress(self, adapter)` (method) — tool_start_callback fires → progress appears as custom SSE event, not in delta.content.
- L1123 `test_stream_tool_progress_skips_internal_events(self, adapter)` (method) — Internal tool calls (name starting with ``_``) are not streamed.
- L1168 `test_stream_emits_tool_lifecycle_with_call_id(self, adapter)` (method) — Regression for #16588.
- L1245 `test_stream_tool_lifecycle_skips_internal_and_orphan_completes(self, adapter)` (method) — Internal tools (``_thinking``-style) and ``completed`` events
- L1294 `test_no_user_message_returns_400(self, adapter)` (method)
- L1307 `test_successful_completion(self, adapter)` (method) — Test a successful chat completion with mocked agent.
- L1339 `test_system_prompt_extracted(self, adapter)` (method) — System messages from the client are passed as ephemeral_system_prompt.
- L1369 `test_conversation_history_passed(self, adapter)` (method) — Previous user/assistant messages become conversation_history.
- L1397 `test_agent_error_returns_500(self, adapter)` (method) — Agent exception returns 500.
- L1416 `test_stable_session_id_across_turns(self, adapter)` (method) — Same conversation (same first user message) produces the same session_id.
- L1455 `test_different_conversations_get_different_session_ids(self, adapter)` (method) — Different first messages produce different session_ids.
- L1482 `TestDeriveChatSessionId` (class)
- L1483 `test_deterministic(self)` (method) — Same inputs always produce the same session ID.
- L1489 `test_prefix(self)` (method)
- L1492 `test_different_system_prompt(self)` (method)
- L1497 `test_different_first_message(self)` (method)
- L1502 `test_none_system_prompt(self)` (method) — None system prompt doesn't crash.
- L1513 `TestResponsesEndpoint` (class)
- L1515 `test_missing_input_returns_400(self, adapter)` (method)
- L1524 `test_invalid_json_returns_400(self, adapter)` (method)
- L1535 `test_successful_response_with_string_input(self, adapter)` (method) — String input is wrapped in a user message.
- L1566 `test_successful_response_with_array_input(self, adapter)` (method) — Array input with role/content objects.
- L1592 `test_instructions_as_ephemeral_prompt(self, adapter)` (method) — The instructions field maps to ephemeral_system_prompt.
- L1614 `test_previous_response_id_chaining(self, adapter)` (method) — Test that responses can be chained via previous_response_id.
- L1661 `test_previous_response_id_stores_full_agent_transcript_once(self, adapter)` (method) — Chained Responses storage must not append result["messages"] twice.
- L1720 `test_previous_response_id_outputs_only_current_turn_items(self, adapter)` (method) — Response output must not replay previous tool artifacts.
- L1801 `test_previous_response_id_preserves_session(self, adapter)` (method) — Chained responses via previous_response_id reuse the same session_id.
- L1842 `test_invalid_previous_response_id_returns_404(self, adapter)` (method)
- L1856 `test_store_false_does_not_store(self, adapter)` (method) — When store=false, the response is NOT stored.
- L1879 `test_store_string_false_does_not_store(self, adapter)` (method) — Quoted false must preserve ephemeral store=false semantics.
- L1904 `test_instructions_inherited_from_previous(self, adapter)` (method) — If no instructions provided, carry forward from previous response.
- L1942 `test_agent_error_returns_500(self, adapter)` (method)
- L1955 `test_invalid_input_type_returns_400(self, adapter)` (method)
- L1965 `TestResponsesStreaming` (class)
- L1967 `test_stream_true_returns_responses_sse(self, adapter)` (method)
- L1998 `test_stream_string_false_returns_json_response(self, adapter)` (method) — Quoted false must not route Responses API requests into SSE mode.
- L2029 `test_stream_task_done_callback_enqueues_eos_for_responses(self, adapter)` (method) — Regression guard for #24451 on /v1/responses streaming path.
- L2075 `test_stream_emits_function_call_and_output_items(self, adapter)` (method)
- L2132 `test_streamed_response_is_stored_for_get(self, adapter)` (method)
- L2170 `test_streamed_previous_response_id_stores_full_agent_transcript_once(self, adapter)` (method)
- L2235 `test_stream_cancelled_persists_incomplete_snapshot(self, adapter)` (method) — Server-side asyncio.CancelledError (shutdown, request timeout) must
- L2310 `test_stream_client_disconnect_persists_incomplete_snapshot(self, adapter)` (method) — Client disconnect (ConnectionResetError) during streaming must
- L2373 `TestEndpointAuth` (class)
- L2375 `test_chat_completions_requires_auth(self, auth_adapter)` (method)
- L2385 `test_responses_requires_auth(self, auth_adapter)` (method)
- L2395 `test_models_requires_auth(self, auth_adapter)` (method)
- L2402 `test_health_does_not_require_auth(self, auth_adapter)` (method)
- L2414 `TestConfigIntegration` (class)
- L2415 `test_platform_enum_has_api_server(self)` (method)
- L2418 `test_env_override_enables_api_server(self, monkeypatch)` (method)
- L2425 `test_env_override_with_key(self, monkeypatch)` (method)
- L2432 `test_env_override_port_and_host(self, monkeypatch)` (method)
- L2441 `test_env_override_cors_origins(self, monkeypatch)` (method)
- L2454 `test_api_server_in_connected_platforms(self)` (method)
- L2460 `test_api_server_not_in_connected_when_disabled(self)` (method)
- L2472 `TestMultipleSystemMessages` (class)
- L2474 `test_multiple_system_messages_concatenated(self, adapter)` (method)
- L2505 `TestSendMethod` (class)
- L2507 `test_send_returns_not_supported(self)` (method)
- L2520 `TestGetResponse` (class)
- L2522 `test_get_stored_response(self, adapter)` (method) — GET returns a previously stored response.
- L2549 `test_get_not_found(self, adapter)` (method)
- L2556 `test_get_requires_auth(self, auth_adapter)` (method)
- L2568 `TestDeleteResponse` (class)
- L2570 `test_delete_stored_response(self, adapter)` (method) — DELETE removes a stored response and returns confirmation.
- L2599 `test_delete_not_found(self, adapter)` (method)
- L2606 `test_delete_requires_auth(self, auth_adapter)` (method)
- L2618 `TestToolCallsInOutput` (class)
- L2620 `test_tool_calls_in_output(self, adapter)` (method) — When agent returns tool calls, they appear as function_call items.
- L2677 `test_no_tool_calls_still_works(self, adapter)` (method) — Without tool calls, output is just a message.
- L2701 `TestUsageCounting` (class)
- L2703 `test_responses_usage(self, adapter)` (method) — Responses API returns real token counts.
- L2724 `test_chat_completions_usage(self, adapter)` (method) — Chat completions returns real token counts.
- L2753 `TestTruncation` (class)
- L2755 `test_truncation_auto_limits_history(self, adapter)` (method) — With truncation=auto, history over 100 messages is trimmed.
- L2787 `test_no_truncation_keeps_full_history(self, adapter)` (method) — Without truncation=auto, long history is passed as-is.
- L2821 `TestChatCompletionsAgentIncomplete` (class) — When the agent run yields a partial / failed result, the API server
- L2828 `test_truncation_with_partial_text_uses_length_finish_reason(self, adapter)` (method) — Partial text + truncation marker → finish_reason='length', 200 OK,
- L2858 `test_failure_with_no_text_returns_502_error_envelope(self, adapter)` (method) — No usable assistant text + failure → 502 with OpenAI error envelope.
- L2892 `test_normal_completion_unchanged(self, adapter)` (method) — Sanity: a completed-True result still returns finish_reason='stop'
- L2924 `TestCORS` (class)
- L2925 `test_origin_allowed_for_non_browser_client(self, adapter)` (method)
- L2928 `test_origin_rejected_by_default(self, adapter)` (method)
- L2931 `test_origin_allowed_for_allowlist_match(self)` (method)
- L2935 `test_cors_headers_for_origin_disabled_by_default(self, adapter)` (method)
- L2938 `test_cors_headers_for_origin_matches_allowlist(self)` (method)
- L2945 `test_cors_headers_for_origin_rejects_unknown_origin(self)` (method)
- L2950 `test_cors_headers_not_present_by_default(self, adapter)` (method) — CORS is disabled unless explicitly configured.
- L2959 `test_browser_origin_rejected_by_default(self, adapter)` (method) — Browser-originated requests are rejected unless explicitly allowed.
- L2968 `test_cors_options_preflight_rejected_by_default(self, adapter)` (method) — Browser preflight is rejected unless CORS is explicitly configured.
- L2983 `test_cors_headers_present_for_allowed_origin(self)` (method) — Allowed origins receive explicit CORS headers.
- L2995 `test_cors_allows_idempotency_key_header(self)` (method)
- L3011 `test_cors_sets_vary_origin_header(self)` (method)
- L3020 `test_cors_options_preflight_allowed_for_configured_origin(self)` (method) — Configured origins can complete browser preflight.
- L3039 `test_cors_preflight_sets_max_age(self)` (method)
- L3058 `TestConversationParameter` (class)
- L3060 `test_conversation_creates_new(self, adapter)` (method) — First request with a conversation name works (new conversation).
- L3080 `test_conversation_chains_automatically(self, adapter)` (method) — Second request with same conversation name chains to first.
- L3118 `test_conversation_and_previous_response_id_conflict(self, adapter)` (method) — Cannot use both conversation and previous_response_id.
- L3132 `test_separate_conversations_are_isolated(self, adapter)` (method) — Different conversation names have independent histories.
- L3154 `test_conversation_store_false_no_mapping(self, adapter)` (method) — If store=false, conversation mapping is not updated.
- L3173 `test_conversation_reuse_after_eviction_no_404(self, adapter)` (method) — After eviction clears a conversation mapping, reusing that name starts fresh (no 404).
- L3217 `TestSessionIdHeader` (class)
- L3219 `test_new_session_response_includes_session_id_header(self, adapter)` (method) — Without X-Hermes-Session-Id, a new session is created and returned in the header.
- L3234 `test_provided_session_id_is_used_and_echoed(self, auth_adapter)` (method) — When X-Hermes-Session-Id is provided, it's passed to the agent and echoed in the response.
- L3260 `test_provided_session_id_loads_history_from_db(self, auth_adapter)` (method) — When X-Hermes-Session-Id is provided, history comes from SessionDB not request body.
- L3296 `test_db_failure_falls_back_to_empty_history(self, auth_adapter)` (method) — If SessionDB raises, history falls back to empty and request still succeeds.
- L3324 `TestSessionKeyHeader` (class) — The session key is a stable per-channel identifier that scopes
- L3333 `test_session_key_passed_to_agent_and_echoed(self, auth_adapter)` (method) — X-Hermes-Session-Key reaches _run_agent as gateway_session_key and is echoed back.
- L3354 `test_session_key_independent_of_session_id(self, auth_adapter)` (method) — Both headers coexist: key scopes memory, id scopes transcript.
- L3381 `test_session_key_absent_yields_none(self, auth_adapter)` (method) — Omitting the header passes gateway_session_key=None and doesn't echo.
- L3399 `test_session_key_rejected_without_api_key(self, adapter)` (method) — Without API_SERVER_KEY, accepting a caller-supplied memory scope is unsafe — reject with 403.
- L3411 `test_session_key_rejects_control_chars(self, auth_adapter)` (method) — Header injection via \r\n must be rejected by the server-side validator.
- L3428 `test_session_key_rejects_oversized(self, auth_adapter)` (method) — Session keys longer than the cap are rejected.
- L3440 `test_session_key_threads_into_create_agent(self, auth_adapter)` (method) — End-to-end: verify AIAgent(gateway_session_key=...) receives the key via _create_agent.
- L3469 `test_responses_endpoint_accepts_session_key(self, auth_adapter)` (method) — Responses API honors the same X-Hermes-Session-Key contract.
- L3490 `test_capabilities_advertises_session_key_header(self, adapter)` (method) — GET /v1/capabilities should advertise the new header so clients can feature-detect.
