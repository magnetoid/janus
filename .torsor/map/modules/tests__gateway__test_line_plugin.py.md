---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:42'
updated: '2026-06-08T00:38:42'
---

# tests/gateway/test_line_plugin.py

Symbols in `tests/gateway/test_line_plugin.py`.

- L54 `TestSignature` (class)
- L56 `_sign(self, body: bytes, secret: str)` (method)
- L60 `test_valid_signature_passes(self)` (method)
- L65 `test_tampered_body_rejected(self)` (method)
- L70 `test_wrong_secret_rejected(self)` (method)
- L75 `test_empty_signature_rejected(self)` (method)
- L78 `test_empty_secret_rejected(self)` (method)
- L81 `test_garbage_signature_rejected(self)` (method)
- L89 `TestSourceResolution` (class)
- L91 `test_user_source(self)` (method)
- L96 `test_group_source(self)` (method)
- L101 `test_room_source(self)` (method)
- L106 `test_unknown_source_falls_back_to_dm(self)` (method)
- L111 `test_empty_source(self)` (method)
- L121 `TestAllowlist` (class)
- L123 `test_allow_all_short_circuits(self)` (method)
- L131 `test_user_in_allowlist_passes(self)` (method)
- L135 `test_user_not_in_allowlist_rejected(self)` (method)
- L139 `test_group_uses_group_list_not_user_list(self)` (method)
- L144 `test_room_uses_room_list(self)` (method)
- L149 `test_unknown_type_rejected(self)` (method)
- L158 `TestDedup` (class)
- L160 `test_first_event_not_duplicate(self)` (method)
- L164 `test_repeat_event_marked_duplicate(self)` (method)
- L169 `test_blank_id_not_treated_as_duplicate(self)` (method)
- L175 `test_lru_eviction_under_pressure(self)` (method)
- L189 `TestRequestCache` (class)
- L191 `test_register_pending_is_pending(self)` (method)
- L197 `test_set_ready_transitions(self)` (method)
- L204 `test_set_error_transitions(self)` (method)
- L211 `test_mark_delivered_from_ready(self)` (method)
- L218 `test_mark_delivered_from_error(self)` (method)
- L225 `test_set_ready_on_delivered_is_noop(self)` (method)
- L235 `test_find_pending_for_chat(self)` (method)
- L251 `TestMarkdownAndChunking` (class)
- L253 `test_bold_stripped(self)` (method)
- L256 `test_italic_stripped(self)` (method)
- L259 `test_inline_code_unfenced(self)` (method)
- L262 `test_link_preserved_with_url(self)` (method)
- L267 `test_heading_prefix_stripped(self)` (method)
- L271 `test_bullet_marker_replaced(self)` (method)
- L275 `test_code_fence_content_kept(self)` (method)
- L283 `test_split_short_returns_single_chunk(self)` (method)
- L286 `test_split_long_chunks_at_paragraph_boundary(self)` (method)
- L292 `test_split_caps_at_five_chunks(self)` (method)
- L303 `TestSendRouting` (class)
- L306 `adapter(self, monkeypatch)` (method)
- L320 `test_system_bypass_recognized(self)` (method)
- L327 `test_send_uses_reply_when_token_present(self, adapter)` (method)
- L337 `test_send_falls_back_to_push_when_no_token(self, adapter)` (method)
- L343 `test_send_falls_back_to_push_when_reply_fails(self, adapter)` (method)
- L352 `test_send_returns_failure_when_push_fails(self, adapter)` (method)
- L358 `test_send_pending_button_caches_response(self, adapter)` (method)
- L370 `test_send_system_bypass_skips_postback_cache(self, adapter)` (method)
- L381 `test_send_caps_messages_per_call_at_five(self, adapter)` (method)
- L396 `test_format_message_strips_markdown(self, adapter)` (method)
- L406 `TestRegister` (class)
- L415 `test_register_calls_register_platform(self)` (method)
- L422 `test_register_advertises_required_env(self)` (method)
- L430 `test_register_wires_allowlist_envs(self)` (method)
- L436 `test_register_wires_cron_home_channel(self)` (method)
- L441 `test_register_provides_standalone_sender(self)` (method)
- L446 `test_register_provides_env_enablement(self)` (method)
- L451 `test_register_factory_yields_line_adapter(self)` (method)
- L462 `test_max_message_length_below_line_per_bubble_limit(self)` (method)
- L469 `TestEnvEnablement` (class)
- L471 `test_returns_none_without_credentials(self, monkeypatch)` (method)
- L476 `test_returns_dict_with_credentials(self, monkeypatch)` (method)
- L481 `test_seeds_port_from_env(self, monkeypatch)` (method)
- L487 `test_seeds_public_url(self, monkeypatch)` (method)
- L495 `TestStandaloneSend` (class)
- L497 `test_missing_token_returns_error(self, monkeypatch)` (method)
- L504 `test_missing_chat_id_returns_error(self, monkeypatch)` (method)
- L511 `test_pushes_via_client_when_credentials_present(self, monkeypatch)` (method)
- L536 `TestPostbackButtonShape` (class)
- L538 `test_template_buttons_structure(self)` (method)
- L549 `test_text_truncated_to_160(self)` (method)
- L554 `test_alt_text_truncated_to_400(self)` (method)
- L560 `TestCheckRequirements` (class)
- L562 `test_rejects_without_token(self, monkeypatch)` (method)
- L567 `test_rejects_without_secret(self, monkeypatch)` (method)
- L573 `TestValidateConfig` (class)
- L575 `test_validates_from_extra(self)` (method)
- L583 `test_rejects_empty_config(self, monkeypatch)` (method)
- L591 `TestAdapterInit` (class)
- L593 `test_init_from_config_extra(self, monkeypatch)` (method)
- L614 `test_env_overrides_extra(self, monkeypatch)` (method)
- L626 `test_csv_allowlist_parsed(self, monkeypatch)` (method)
- L636 `test_get_chat_info_infers_type_from_prefix(self, monkeypatch)` (method)
- L650 `TestMessageTypeMapping` (class) — LINE webhook message types must map to the right normalized
- L657 `test_image_event_not_attributeerror_regression(self)` (method)
- L662 `test_every_line_type_maps_to_correct_enum(self)` (method)
- L674 `test_unknown_type_falls_back_to_text(self)` (method)
