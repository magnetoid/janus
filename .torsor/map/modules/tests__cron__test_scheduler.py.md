---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:41'
updated: '2026-06-08T00:38:41'
---

# tests/cron/test_scheduler.py

Symbols in `tests/cron/test_scheduler.py`.

- L15 `TestResolveOrigin` (class)
- L16 `test_full_origin(self)` (method)
- L33 `test_no_origin(self)` (method)
- L37 `test_missing_platform(self)` (method)
- L41 `test_missing_chat_id(self)` (method)
- L45 `test_empty_origin(self)` (method)
- L59 `test_non_dict_origin_returns_none_instead_of_crashing(self, non_dict_origin)` (method) — Non-dict origins (provenance strings from hand-edited or migrated
- L73 `TestResolveDeliveryTarget` (class)
- L74 `test_origin_delivery_preserves_thread_id(self)` (method)
- L105 `test_origin_delivery_without_origin_falls_back_to_supported_home_channels(self, monkeypatch, platform, env_var, chat_id)` (method)
- L134 `test_bare_matrix_delivery_uses_matrix_home_room(self, monkeypatch)` (method)
- L144 `test_bare_platform_delivery_preserves_home_thread_id(self, monkeypatch)` (method)
- L154 `test_telegram_cron_thread_id_overrides_home_thread_id(self, monkeypatch)` (method) — TELEGRAM_CRON_THREAD_ID wins over TELEGRAM_HOME_CHANNEL_THREAD_ID for cron (#24409).
- L166 `test_telegram_cron_thread_id_sets_thread_when_home_thread_unset(self, monkeypatch)` (method) — TELEGRAM_CRON_THREAD_ID supplies a thread when no home thread is configured.
- L178 `test_telegram_cron_thread_id_does_not_leak_to_other_platforms(self, monkeypatch)` (method) — TELEGRAM_CRON_THREAD_ID is Telegram-only; other platforms keep their own thread resolution.
- L190 `test_explicit_telegram_topic_target_overrides_cron_thread_id(self, monkeypatch)` (method) — Explicit ``telegram:chat:thread`` targets bypass TELEGRAM_CRON_THREAD_ID.
- L201 `test_explicit_telegram_topic_target_with_thread_id(self)` (method) — deliver: 'telegram:chat_id:thread_id' parses correctly.
- L212 `test_explicit_telegram_topic_thread_survives_bare_directory_match(self)` (method) — Exact channel-directory matches must not erase an explicit topic id.
- L228 `test_explicit_telegram_chat_id_without_thread_id(self)` (method) — deliver: 'telegram:chat_id' sets thread_id to None.
- L239 `test_human_friendly_label_resolved_via_channel_directory(self)` (method) — deliver: 'whatsapp:Alice (dm)' resolves to the real JID.
- L254 `test_human_friendly_label_without_suffix_resolved(self)` (method) — deliver: 'telegram:My Group' resolves without display suffix.
- L268 `test_human_friendly_topic_label_preserves_thread_id(self)` (method) — Resolved Telegram topic labels should split chat_id and thread_id.
- L282 `test_raw_id_not_mangled_when_directory_returns_none(self)` (method) — deliver: 'whatsapp:12345@lid' passes through when directory has no match.
- L296 `test_bare_platform_uses_matching_origin_chat(self)` (method)
- L312 `test_bare_platform_falls_back_to_home_channel(self, monkeypatch)` (method)
- L328 `test_explicit_discord_topic_target_with_thread_id(self)` (method) — deliver: 'discord:chat_id:thread_id' parses correctly.
- L339 `test_explicit_discord_chat_id_without_thread_id(self)` (method) — deliver: 'discord:chat_id' sets thread_id to None.
- L350 `test_explicit_discord_channel_without_thread(self)` (method) — deliver: 'discord:1001234567890' resolves via explicit platform:chat_id path.
- L362 `test_list_form_deliver_is_normalized(self, monkeypatch)` (method) — deliver=['telegram'] (Python list) should resolve like 'telegram' string.
- L382 `test_list_form_multiple_platforms_normalized(self, monkeypatch)` (method) — deliver=['telegram', 'discord'] resolves to multiple targets.
- L394 `test_empty_list_form_deliver_resolves_to_local(self)` (method) — deliver=[] is treated as local (no delivery).
- L401 `TestRoutingIntents` (class) — ``all`` routing intent expands at fire time.
- L404 `test_all_expands_to_every_connected_home_channel(self, monkeypatch)` (method) — deliver='all' fans out to every platform with a configured home channel.
- L424 `test_all_combines_with_explicit_target_and_dedups(self, monkeypatch)` (method) — 'telegram:-999,all' yields every home channel + the explicit target without dupes.
- L443 `test_all_with_no_connected_channels_returns_empty(self, monkeypatch)` (method) — deliver='all' with nothing connected returns [] — delivery is recorded as failed upstream.
- L456 `test_origin_comma_all_preserves_origin_first(self, monkeypatch)` (method) — 'origin,all' delivers to the origin platform plus every other home channel.
- L477 `test_all_token_case_insensitive(self, monkeypatch)` (method) — 'ALL' / 'All' / 'all' are all recognized.
- L490 `TestDeliverResultWrapping` (class) — Verify that cron deliveries are wrapped with header/footer and no longer mirrored.
- L493 `_safe_media_path(self, tmp_path, monkeypatch, name, data=b'media')` (method)
- L504 `test_delivery_wraps_content_with_header_and_footer(self)` (method) — Delivered content should include task name header and agent-invisible note.
- L531 `test_delivery_uses_job_id_when_no_name(self)` (method) — When a job has no name, the wrapper should fall back to job id.
- L552 `test_delivery_skips_wrapping_when_config_disabled(self)` (method) — When cron.wrap_response is false, deliver raw content without header/footer.
- L578 `test_delivery_extracts_media_tags_before_send(self, tmp_path, monkeypatch)` (method) — Cron delivery should pass MEDIA attachments separately to the send helper.
- L606 `test_live_adapter_sends_media_as_attachments(self, tmp_path, monkeypatch)` (method) — When a live adapter is available, MEDIA files should be sent as native
- L660 `test_live_adapter_routes_image_to_send_image_file(self, tmp_path, monkeypatch)` (method) — Image MEDIA files should be routed to send_image_file, not send_voice.
- L704 `test_live_adapter_media_only_no_text(self, tmp_path, monkeypatch)` (method) — When content is ONLY a MEDIA tag with no text, media should still be sent.
- L748 `test_live_adapter_sends_cleaned_text_not_raw(self)` (method) — The live adapter path must send cleaned text (MEDIA tags stripped),
- L791 `test_no_mirror_to_session_call(self)` (method) — Cron deliveries should NOT mirror into the gateway session.
- L812 `test_origin_delivery_preserves_thread_id(self)` (method) — Origin delivery should forward thread_id to the send helper.
- L840 `TestDeliverResultErrorReturns` (class) — Verify _deliver_result returns error strings on failure, None on success.
- L843 `test_returns_error_when_platform_disabled(self)` (method)
- L861 `test_returns_error_for_unresolved_target(self, monkeypatch)` (method) — Non-local delivery with no resolvable target should return an error.
- L870 `TestRunJobSessionPersistence` (class)
- L871 `test_run_job_passes_session_db_and_cron_platform(self, tmp_path)` (method)
- L915 `test_run_job_titles_cron_session_from_job_not_important_hint(self, tmp_path)` (method)
- L952 `test_run_job_closes_agent_on_failure_to_prevent_fd_leak(self, tmp_path)` (method)
- L988 `test_run_job_reaps_stale_auxiliary_clients_per_tick(self, tmp_path)` (method)
- L1024 `_make_run_job_patches(self, tmp_path)` (method) — Common patches for run_job tests.
- L1043 `test_run_job_passes_enabled_toolsets_to_agent(self, tmp_path)` (method)
- L1061 `test_run_job_disabled_toolsets_layer_user_config_on_baseline(self, tmp_path)` (method) — agent.disabled_toolsets must be honoured in cron — issue #25752.
- L1097 `test_run_job_enabled_toolsets_resolves_from_platform_config_when_not_set(self, tmp_path)` (method) — When a job has no explicit enabled_toolsets, the scheduler now
- L1128 `test_run_job_per_job_toolsets_win_over_platform_config(self, tmp_path)` (method) — Per-job enabled_toolsets (via cronjob tool) always take precedence
- L1154 `test_run_job_empty_response_returns_empty_not_placeholder(self, tmp_path)` (method) — Empty final_response should stay empty for delivery logic (issue #2234).
- L1226 `test_run_job_treats_agent_failure_flag_as_failure(self, tmp_path, agent_result, expected_err_substring)` (method) — Issue #17855: run_conversation returns ``failed=True``/``completed=False``
- L1271 `test_run_job_completed_true_without_failed_flag_succeeds(self, tmp_path)` (method) — Regression guard: a normal success result (``completed=True``,
- L1309 `test_tick_marks_empty_response_as_error(self, tmp_path)` (method) — When run_job returns success=True but final_response is empty,
- L1345 `test_run_job_sets_auto_delivery_env_from_dotenv_home_channel(self, tmp_path, monkeypatch)` (method)
- L1400 `test_run_job_clears_stale_auto_delivery_thread_id_between_jobs(self, tmp_path, monkeypatch)` (method)
- L1475 `TestRunJobConfigLogging` (class) — Verify that config.yaml parse failures are logged, not silently swallowed.
- L1478 `test_bad_config_yaml_is_logged(self, caplog, tmp_path)` (method) — When config.yaml is malformed, a warning should be logged.
- L1513 `test_bad_prefill_messages_is_logged(self, caplog, tmp_path)` (method) — When the prefill messages file contains invalid JSON, a warning should be logged.
- L1548 `TestRunJobConfigEnvVarExpansion` (class) — Verify that ${VAR} references in config.yaml are expanded when running cron jobs.
- L1558 `test_model_env_ref_in_config_yaml_is_expanded(self, tmp_path, monkeypatch)` (method) — ${VAR} in config.yaml model: is expanded using env after .env is loaded.
- L1586 `test_legacy_agent_prefill_messages_file_is_loaded(self, tmp_path, monkeypatch)` (method) — Cron accepts the legacy agent.prefill_messages_file fallback.
- L1616 `test_fallback_model_env_ref_in_config_yaml_is_expanded(self, tmp_path, monkeypatch)` (method) — ${VAR} in config.yaml fallback_providers model: is expanded.
- L1649 `test_unexpanded_ref_passthrough_when_var_unset(self, tmp_path, monkeypatch)` (method) — When the env var is not set, the literal ${VAR} is kept verbatim (not crashed).
- L1675 `TestRunJobSkillBacked` (class)
- L1676 `test_run_job_preserves_skill_env_passthrough_into_worker_thread(self, tmp_path)` (method)
- L1727 `test_run_job_preserves_credential_file_passthrough_into_worker_thread(self, tmp_path)` (method) — copy_context() also propagates credential_files ContextVar.
- L1787 `test_run_job_loads_skill_and_disables_recursive_cron_tools(self, tmp_path)` (method)
- L1830 `test_run_job_loads_multiple_skills_in_order(self, tmp_path)` (method)
- L1877 `TestSilentDelivery` (class) — Verify that [SILENT] responses suppress delivery while still saving output.
- L1880 `_make_job(self)` (method)
- L1888 `test_silent_response_suppresses_delivery(self, caplog)` (method)
- L1900 `test_silent_with_note_suppresses_delivery(self)` (method)
- L1910 `test_silent_trailing_suppresses_delivery(self)` (method) — Agent appended [SILENT] after explanation text — must still suppress.
- L1922 `test_silent_is_case_insensitive(self)` (method)
- L1932 `test_failed_job_always_delivers(self)` (method) — Failed jobs deliver regardless of [SILENT] in output.
- L1943 `test_output_saved_even_when_delivery_suppressed(self)` (method)
- L1955 `test_whitespace_only_response_is_marked_failed_not_delivered(self)` (method) — Whitespace-only final responses should behave like empty responses.
- L1974 `TestBuildJobPromptSilentHint` (class) — Verify _build_job_prompt always injects [SILENT] guidance.
- L1977 `test_hint_always_present(self)` (method)
- L1983 `test_hint_present_even_without_prompt(self)` (method)
- L1988 `test_hint_present_when_legacy_prompt_is_null(self)` (method)
- L1993 `test_delivery_guidance_present(self)` (method) — Cron hint tells agents their final response is auto-delivered.
- L2000 `test_delivery_guidance_precedes_user_prompt(self)` (method) — System guidance appears before the user's prompt text.
- L2009 `TestParseWakeGate` (class) — Unit tests for _parse_wake_gate — pure function, no side effects.
- L2012 `test_empty_output_wakes(self)` (method)
- L2017 `test_whitespace_only_wakes(self)` (method)
- L2021 `test_non_json_last_line_wakes(self)` (method)
- L2026 `test_json_non_dict_wakes(self)` (method) — Bare arrays, numbers, strings must not be interpreted as a gate.
- L2033 `test_wake_gate_false_skips(self)` (method)
- L2037 `test_wake_gate_true_wakes(self)` (method)
- L2041 `test_wake_gate_missing_wakes(self)` (method) — A JSON dict without a wakeAgent key defaults to waking.
- L2046 `test_non_boolean_false_still_wakes(self)` (method) — Only strict ``False`` skips — truthy/falsy shortcuts are too risky.
- L2053 `test_only_last_non_empty_line_parsed(self)` (method)
- L2058 `test_trailing_blank_lines_ignored(self)` (method)
- L2063 `test_non_last_json_line_does_not_gate(self)` (method) — A JSON gate on an earlier line with plain text after it does NOT trigger.
- L2070 `TestRunJobWakeGate` (class) — Integration tests for run_job wake-gate short-circuit.
- L2074 `_stub_runtime_provider(self)` (method) — Stub ``resolve_runtime_provider`` for wake-gate tests.
- L2097 `_make_job(self, name='wake-gate-test', script='check.py')` (method) — Minimal valid cron job dict for run_job.
- L2107 `test_wake_false_skips_agent_and_returns_silent(self, caplog)` (method) — When _run_job_script output ends with {wakeAgent: false}, the agent
- L2125 `test_wake_true_runs_agent_with_injected_output(self)` (method) — When the script returns {wakeAgent: true, data: ...}, the agent is
- L2149 `test_script_runs_only_once_on_wake(self)` (method) — Wake-true path must not re-run the script inside _build_job_prompt
- L2171 `test_script_failure_does_not_trigger_gate(self)` (method) — If _run_job_script returns success=False, the gate is NOT evaluated
- L2189 `test_no_script_path_runs_agent_normally(self)` (method) — Regression: jobs without a script still work.
- L2207 `TestBuildJobPromptMissingSkill` (class) — Verify that a missing skill logs a warning and does not crash the job.
- L2210 `_missing_skill_view(self, name: str)` (method)
- L2213 `test_missing_skill_does_not_raise(self)` (method) — Job should run even when a referenced skill is not installed.
- L2220 `test_missing_skill_injects_user_notice_into_prompt(self)` (method) — A system notice about the missing skill is injected into the prompt.
- L2227 `test_missing_skill_logs_warning(self, caplog)` (method) — A warning is logged when a skill cannot be found.
- L2234 `test_valid_skill_loaded_alongside_missing(self)` (method) — A valid skill is still loaded when another skill in the list is missing.
- L2248 `TestBuildJobPromptBumpUse` (class) — Verify that cron jobs bump skill usage counters so the curator sees them as active.
- L2251 `test_bump_use_called_for_loaded_skill(self)` (method) — bump_use is called for each successfully loaded skill.
- L2266 `test_bump_use_not_called_for_missing_skill(self)` (method) — bump_use is NOT called when a skill fails to load.
- L2278 `test_bump_failure_does_not_break_prompt(self, caplog)` (method) — If bump_use raises, the prompt still builds — error is logged at DEBUG.
- L2296 `TestSendMediaViaAdapter` (class) — Unit tests for _send_media_via_adapter — routes files to typed adapter methods.
- L2299 `_safe_media_path(self, tmp_path, monkeypatch, name, data=b'media')` (method)
- L2311 `_run_with_loop(adapter, chat_id, media_files, metadata, job)` (method) — Helper: run _send_media_via_adapter with immediate scheduling.
- L2324 `test_video_dispatched_to_send_video(self, tmp_path, monkeypatch)` (method)
- L2333 `test_unknown_ext_dispatched_to_send_document(self, tmp_path, monkeypatch)` (method)
- L2342 `test_multiple_media_files_all_delivered(self, tmp_path, monkeypatch)` (method)
- L2354 `TestParallelTick` (class) — Verify that tick() runs due jobs concurrently and isolates ContextVars.
- L2358 `_isolate_tick_lock(self, tmp_path)` (method) — Point the tick file lock at a per-test temp dir to avoid xdist contention.
- L2366 `test_parallel_jobs_run_concurrently(self)` (method) — Two jobs launched in the same tick should overlap in time.
- L2402 `test_parallel_jobs_isolated_contextvars(self)` (method) — Each job's ContextVars must be isolated — no cross-contamination.
- L2442 `test_max_parallel_env_var(self, monkeypatch)` (method) — HERMES_CRON_MAX_PARALLEL=1 should restore serial behaviour.
- L2475 `TestDeliverResultTimeoutCancelsFuture` (class) — When future.result(timeout=60) raises TimeoutError in the live
- L2481 `test_live_adapter_timeout_cancels_future_and_falls_back(self)` (method) — End-to-end: live adapter hangs past the 60s budget, _deliver_result
- L2543 `test_live_adapter_thread_fallback_records_delivery_error(self)` (method) — A cron target with an explicit topic must not be marked clean if
- L2603 `TestSendMediaTimeoutCancelsFuture` (class) — Same orphan-coroutine guarantee for _send_media_via_adapter's
- L2609 `test_media_send_timeout_cancels_future_and_continues(self, tmp_path, monkeypatch)` (method) — End-to-end: _send_media_via_adapter with a future whose .result()
- L2669 `TestCronDeliveryTargets` (class) — ``cron_delivery_targets`` powers the dashboard delivery dropdown.
- L2677 `_patch_connected(self, monkeypatch, names)` (method)
- L2692 `test_lists_configured_platforms_flagging_missing_home_channel(self, monkeypatch)` (method)
- L2707 `test_home_channel_set_marks_target_ready(self, monkeypatch)` (method)
- L2717 `test_unconfigured_platforms_excluded(self, monkeypatch)` (method)
- L2729 `test_no_gateway_config_returns_empty(self, monkeypatch)` (method)
