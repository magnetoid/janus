---
type: map
status: derived
tags:
- map
links: []
created: '2026-06-08T00:38:40'
updated: '2026-06-08T00:38:40'
---

# gateway/platforms/telegram.py

Symbols in `gateway/platforms/telegram.py`.

- L111 `check_telegram_requirements()` (function) — Check if Telegram dependencies are available.
- L171 `_escape_mdv2(text: str)` (function) — Escape Telegram MarkdownV2 special characters with a preceding backslash.
- L176 `_strip_mdv2(text: str)` (function) — Strip MarkdownV2 escape backslashes to produce clean plain text.
- L213 `_is_table_row(line: str)` (function) — Return True if *line* could plausibly be a table data row.
- L219 `_split_markdown_table_row(line: str)` (function) — Split a simple GFM table row into stripped cell values.
- L229 `_render_table_block_for_telegram(table_block: list[str])` (function) — Render a detected GFM table as Telegram-friendly row groups.
- L281 `_wrap_markdown_tables(text: str)` (function) — Rewrite GFM-style pipe tables into Telegram-friendly bullet groups.
- L334 `TelegramAdapter` (class) — Telegram bot adapter.
- L372 `_env_float_clamped(name: str, default: float, *, min_value: Optional[float]=None, max_value: Optional[float]=None)` (method) — Read a float env var, reject non-finite values, and clamp to bounds.
- L400 `message_len_fn(self)` (method) — Telegram measures message length in UTF-16 code units.
- L404 `__init__(self, config: PlatformConfig)` (method)
- L495 `_notification_kwargs(self, metadata: Optional[Dict[str, Any]])` (method) — Return disable_notification kwargs when the adapter is in silent mode.
- L510 `_is_callback_user_authorized(self, user_id: str, *, chat_id: Optional[str]=None, chat_type: Optional[str]=None, thread_id: Optional[str]=None, user_name: Optional[str]=None)` (method) — Return whether a Telegram inline-button caller may perform gated actions.
- L563 `_metadata_thread_id(cls, metadata: Optional[Dict[str, Any]])` (method)
- L570 `_metadata_direct_messages_topic_id(cls, metadata: Optional[Dict[str, Any]])` (method)
- L577 `_metadata_reply_to_message_id(cls, metadata: Optional[Dict[str, Any]])` (method)
- L584 `_looks_like_private_chat_id(chat_id: str)` (method)
- L591 `_is_private_dm_topic_send(cls, chat_id: str, thread_id: Optional[str], metadata: Optional[Dict[str, Any]])` (method)
- L614 `_dm_topic_missing_anchor_error()` (method)
- L618 `_reply_to_message_id_for_send(cls, reply_to: Optional[str], metadata: Optional[Dict[str, Any]]=None, reply_to_mode: Optional[str]=None)` (method)
- L633 `_thread_kwargs_for_send(cls, chat_id: str, thread_id: Optional[str], metadata: Optional[Dict[str, Any]]=None, reply_to_message_id: Optional[int]=None, reply_to_mode: Optional[str]=None)` (method) — Return Telegram send kwargs for forum and direct-message topic routing.
- L678 `_message_thread_id_for_send(cls, thread_id: Optional[str])` (method)
- L684 `_message_thread_id_for_typing(cls, thread_id: Optional[str])` (method)
- L697 `_is_thread_not_found_error(error: Exception)` (method)
- L701 `_is_bad_request_error(error: Exception)` (method)
- L712 `_should_retry_without_dm_topic_reply_anchor(cls, error: Exception, metadata: Optional[Dict[str, Any]], reply_to_message_id: Optional[int])` (method) — True when a DM-topic send should be retried with routing stripped.
- L755 `_send_with_dm_topic_reply_anchor_retry(self, send_fn: Any, send_kwargs: Dict[str, Any], metadata: Optional[Dict[str, Any]], reply_to_message_id: Optional[int], media_label: str, reset_media: Optional[Any]=None)` (method) — Retry stale private-topic media replies once without the topic anchor.
- L789 `_fallback_ips(self)` (method) — Return validated fallback IPs from config (populated by _apply_env_overrides).
- L797 `_looks_like_polling_conflict(error: Exception)` (method)
- L806 `_looks_like_network_error(error: Exception)` (method) — Return True for transient network errors that warrant a reconnect attempt.
- L820 `_looks_like_connect_timeout(error: Exception)` (method) — Return True when a Telegram TimedOut wraps a connect-timeout.
- L848 `_looks_like_pool_timeout(error: Exception)` (method) — Return True when a Telegram TimedOut wraps an httpx pool timeout.
- L882 `_coerce_bool_extra(self, key: str, default: bool=False)` (method)
- L895 `_link_preview_kwargs(self)` (method)
- L902 `_drain_polling_connections(self)` (method) — Reset the httpx connection pool used for getUpdates polling.
- L945 `_handle_polling_network_error(self, error: Exception)` (method) — Reconnect polling after a transient network interruption.
- L1026 `_verify_polling_after_reconnect(self)` (method) — Heartbeat probe scheduled after a successful reconnect.
- L1071 `_handle_polling_conflict(self, error: Exception)` (method)
- L1177 `_create_dm_topic(self, chat_id: int, name: str, icon_color: Optional[int]=None, icon_custom_emoji_id: Optional[str]=None)` (method) — Create a forum topic in a private (DM) chat.
- L1228 `create_handoff_thread(self, parent_chat_id: str, name: str)` (method) — Create a forum topic for a session handoff.
- L1246 `ensure_dm_topic(self, chat_id: str, topic_name: str, force_create: bool=False)` (method) — Return a private DM topic thread id, creating and persisting it if needed.
- L1299 `rename_dm_topic(self, chat_id: int, thread_id: int, name: str)` (method) — Rename a forum topic in a private (DM) chat.
- L1322 `_persist_dm_topic_thread_id(self, chat_id: int, topic_name: str, thread_id: int, replace_existing: bool=False)` (method) — Save a newly created thread_id back into config.yaml so it persists across restarts.
- L1405 `_setup_dm_topics(self)` (method) — Load or create configured DM topics for specified chats.
- L1489 `connect(self)` (method) — Connect to Telegram via polling or webhook.
- L1790 `disconnect(self)` (method) — Stop polling/webhook, cancel pending album flushes, and disconnect.
- L1823 `_should_thread_reply(self, reply_to: Optional[str], chunk_index: int)` (method) — Determine if this message chunk should thread to the original message.
- L1843 `send(self, chat_id: str, content: str, reply_to: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None)` (method) — Send a message to a Telegram chat.
- L2120 `send_or_update_status(self, chat_id: str, status_key: str, content: str, *, metadata: Optional[Dict[str, Any]]=None)` (method) — Send a status message, or edit the previous one with the same key.
- L2154 `edit_message(self, chat_id: str, message_id: str, content: str, *, finalize: bool=False, metadata: Optional[Dict[str, Any]]=None)` (method) — Edit a previously sent Telegram message.
- L2289 `_edit_overflow_split(self, chat_id: str, message_id: str, content: str, *, finalize: bool, metadata: Optional[Dict[str, Any]]=None)` (method) — Split an oversized edit across the existing message + continuations.
- L2454 `delete_message(self, chat_id: str, message_id: str)` (method) — Delete a previously sent Telegram message.
- L2479 `supports_draft_streaming(self, chat_type: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None)` (method) — Telegram supports sendMessageDraft for private chats only.
- L2498 `send_draft(self, chat_id: str, draft_id: int, content: str, metadata: Optional[Dict[str, Any]]=None)` (method) — Stream a partial message via Telegram's native sendMessageDraft.
- L2574 `_send_message_with_thread_fallback(self, **kwargs)` (method) — Send a Telegram message, retrying once without message_thread_id
- L2606 `send_update_prompt(self, chat_id: str, prompt: str, default: str='', session_key: str='', metadata: Optional[Dict[str, Any]]=None)` (method) — Send an inline-keyboard update prompt (Yes / No buttons).
- L2649 `send_exec_approval(self, chat_id: str, command: str, session_key: str, description: str='dangerous command', metadata: Optional[Dict[str, Any]]=None)` (method) — Send an inline-keyboard approval prompt with interactive buttons.
- L2721 `send_slash_confirm(self, chat_id: str, title: str, message: str, session_key: str, confirm_id: str, metadata: Optional[Dict[str, Any]]=None)` (method) — Render a three-button slash-command confirmation prompt.
- L2769 `send_clarify(self, chat_id: str, question: str, choices: Optional[list], clarify_id: str, session_key: str, metadata: Optional[Dict[str, Any]]=None)` (method) — Render a clarify prompt with one inline button per choice.
- L2851 `send_model_picker(self, chat_id: str, providers: list, current_model: str, current_provider: str, session_key: str, on_model_selected, metadata: Optional[Dict[str, Any]]=None)` (method) — Send an interactive inline-keyboard model picker.
- L2924 `_build_provider_keyboard(self, providers: list)` (method) — Build the top-level provider keyboard, folding provider groups.
- L2974 `_build_model_keyboard(self, models: list, page: int)` (method) — Build paginated model buttons. Returns (keyboard, page_info_text).
- L3015 `_handle_model_picker_callback(self, query, data: str, chat_id: str)` (method) — Handle model picker inline keyboard callbacks (mp:/mm:/mb:/mx:/mg:).
- L3232 `_handle_callback_query(self, update: 'Update', context: 'ContextTypes.DEFAULT_TYPE')` (method) — Handle inline keyboard button clicks.
- L3600 `_handle_gmail_triage_callback(self, query, data: str, *, query_chat_id, query_chat_type, query_thread_id, query_user_name)` (method) — Dispatch a gmail-triage inline-button callback (gt:verb:arg).
- L3694 `_missing_media_path_error(self, label: str, path: str)` (method) — Build an actionable file-not-found error for gateway MEDIA delivery.
- L3709 `send_voice(self, chat_id: str, audio_path: str, caption: Optional[str]=None, reply_to: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None, **kwargs)` (method) — Send audio as a native Telegram voice message or audio file.
- L3800 `send_multiple_images(self, chat_id: str, images: List[tuple], metadata: Optional[Dict[str, Any]]=None, human_delay: float=0.0)` (method) — Send a batch of images natively via Telegram's media group API.
- L3935 `send_image_file(self, chat_id: str, image_path: str, caption: Optional[str]=None, reply_to: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None, **kwargs)` (method) — Send a local image file natively as a Telegram photo.
- L4028 `send_document(self, chat_id: str, file_path: str, caption: Optional[str]=None, file_name: Optional[str]=None, reply_to: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None, **kwargs)` (method) — Send a document/file natively as a Telegram file attachment.
- L4079 `send_video(self, chat_id: str, video_path: str, caption: Optional[str]=None, reply_to: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None, **kwargs)` (method) — Send a video natively as a Telegram video message.
- L4126 `send_image(self, chat_id: str, image_url: str, caption: Optional[str]=None, reply_to: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None)` (method) — Send an image natively as a Telegram photo.
- L4220 `send_animation(self, chat_id: str, animation_url: str, caption: Optional[str]=None, reply_to: Optional[str]=None, metadata: Optional[Dict[str, Any]]=None)` (method) — Send an animated GIF natively as a Telegram animation (auto-plays inline).
- L4267 `send_typing(self, chat_id: str, metadata: Optional[Dict[str, Any]]=None)` (method) — Send typing indicator.
- L4302 `get_chat_info(self, chat_id: str)` (method) — Get information about a Telegram chat.
- L4336 `format_message(self, content: str)` (method) — Convert standard markdown to Telegram MarkdownV2 format.
- L4510 `_telegram_require_mention(self)` (method) — Return whether group chats should require an explicit bot trigger.
- L4519 `_telegram_observe_unmentioned_group_messages(self)` (method) — Return whether skipped unmentioned group messages are stored as context.
- L4536 `_telegram_guest_mode(self)` (method) — Return whether non-allowlisted groups may trigger via direct @mention.
- L4545 `_telegram_exclusive_bot_mentions(self)` (method) — Return whether explicit @...bot mentions exclusively route group messages.
- L4554 `_telegram_free_response_chats(self)` (method)
- L4562 `_telegram_allowed_chats(self)` (method) — Return the whitelist of group/supergroup chat IDs the bot will respond in.
- L4577 `_telegram_group_allowed_chats(self)` (method) — Return Telegram chats authorized at group scope.
- L4586 `_telegram_observe_allowed_chats(self)` (method) — Chats where observed group context may use a shared source.
- L4601 `_telegram_allowed_topics(self)` (method) — Return the whitelist of Telegram forum topic IDs this bot handles.
- L4616 `_telegram_ignored_threads(self)` (method)
- L4637 `_compile_mention_patterns(self)` (method) — Compile optional regex wake-word patterns for group triggers.
- L4675 `_is_group_chat(self, message: Message)` (method)
- L4682 `_is_reply_to_bot(self, message: Message)` (method)
- L4689 `_extract_bot_mention_usernames(message: Message)` (method) — Extract explicit Telegram bot usernames mentioned in text/captions.
- L4742 `_message_mentions_bot(self, message: Message)` (method)
- L4798 `_explicit_bot_mentions_exclude_self(self, message: Message)` (method) — Return True when explicit bot handles target other bots, not this one.
- L4822 `_message_matches_mention_patterns(self, message: Message)` (method)
- L4833 `_is_guest_mention(self, message: Message)` (method) — Return True for the narrow guest-mode bypass: explicit bot mention.
- L4841 `_clean_bot_trigger_text(self, text: Optional[str])` (method)
- L4848 `_should_observe_unmentioned_group_message(self, message: Message)` (method) — Return True when a group message should be stored but not dispatched.
- L4897 `_telegram_group_observe_shared_source(self, source)` (method) — Return a chat/topic-scoped source for observed Telegram group context.
- L4901 `_telegram_group_observe_attributed_text(self, event: MessageEvent)` (method)
- L4906 `_telegram_group_observe_channel_prompt(self)` (method)
- L4918 `_apply_telegram_group_observe_attribution(self, event: MessageEvent)` (method) — Align triggered group turns with observed-history attribution.
- L4945 `_media_message_type(self, msg: Message)` (method) — Classify a Telegram media message into a MessageType.
- L4959 `_cache_observed_media(self, msg: Message, event: MessageEvent)` (method) — Cache an unmentioned group attachment and annotate the observed text.
- L5012 `_observed_media_source(self, msg: Message)` (method) — Return (telegram_file_source, filename, mime, default_kind) or Nones.
- L5028 `_append_observed_note(existing: Optional[str], note: str)` (method)
- L5035 `_observe_unmentioned_group_message(self, message: Message, msg_type: MessageType, update_id: Optional[int]=None, event: Optional[MessageEvent]=None)` (method) — Append skipped group chatter to the target session without dispatching.
- L5070 `_should_process_message(self, message: Message, *, is_command: bool=False)` (method) — Apply Telegram group trigger rules.
- L5148 `_ensure_forum_commands(self, message)` (method) — Lazy-register bot commands for forum supergroups.
- L5173 `_effective_update_message(self, update: Update)` (method) — Return the message-like payload for normal messages and channel posts.
- L5183 `_handle_text_message(self, update: Update, context: ContextTypes.DEFAULT_TYPE)` (method) — Handle incoming text messages.
- L5204 `_handle_command(self, update: Update, context: ContextTypes.DEFAULT_TYPE)` (method) — Handle incoming command messages.
- L5218 `_handle_location_message(self, update: Update, context: ContextTypes.DEFAULT_TYPE)` (method) — Handle incoming location/venue pin messages.
- L5262 `_text_batch_key(self, event: MessageEvent)` (method) — Session-scoped key for text message batching.
- L5277 `_enqueue_text_event(self, event: MessageEvent)` (method) — Buffer a text event and reset the flush timer.
- L5309 `_flush_text_batch(self, key: str)` (method) — Wait for the quiet period then dispatch the aggregated text.
- L5357 `_photo_batch_key(self, event: MessageEvent, msg: Message)` (method) — Return a batching key for Telegram photos/albums.
- L5370 `_flush_photo_batch(self, batch_key: str)` (method) — Send a buffered photo burst/album as a single MessageEvent.
- L5384 `_enqueue_photo_event(self, batch_key: str, event: MessageEvent)` (method) — Merge photo events into a pending batch and schedule flush.
- L5401 `_handle_media_message(self, update: Update, context: ContextTypes.DEFAULT_TYPE)` (method) — Handle incoming media messages, downloading images to local cache.
- L5653 `_queue_media_group_event(self, media_group_id: str, event: MessageEvent)` (method) — Buffer Telegram media-group items so albums arrive as one logical event.
- L5678 `_flush_media_group_event(self, media_group_id: str)` (method)
- L5689 `_handle_sticker(self, msg: Message, event: 'MessageEvent')` (method) — Describe a Telegram sticker via vision analysis, with caching.
- L5754 `_reload_dm_topics_from_config(self)` (method) — Re-read dm_topics from config.yaml and load any new thread_ids into cache.
- L5806 `_get_dm_topic_info(self, chat_id: str, thread_id: Optional[str])` (method) — Look up DM topic config by chat_id and thread_id.
- L5845 `_cache_dm_topic_from_message(self, chat_id: str, thread_id: str, topic_name: str)` (method) — Cache a thread_id -> topic_name mapping discovered from an incoming message.
- L5855 `_build_message_event(self, message: Message, msg_type: MessageType, update_id: Optional[int]=None)` (method) — Build a MessageEvent from a Telegram message.
- L6006 `_reactions_enabled(self)` (method) — Check if message reactions are enabled via config/env.
- L6010 `_set_reaction(self, chat_id: str, message_id: str, emoji: str)` (method) — Set a single emoji reaction on a Telegram message.
- L6025 `_clear_reactions(self, chat_id: str, message_id: str)` (method) — Clear all reactions from a Telegram message.
- L6046 `on_processing_start(self, event: MessageEvent)` (method) — Add an in-progress reaction when message processing begins.
- L6055 `on_processing_complete(self, event: MessageEvent, outcome: ProcessingOutcome)` (method) — Swap the in-progress reaction for a final success/failure reaction.
